# Known defects and constraints

**Register date**: 2026-09-01 · **Describes**: runward 0.37.1 · **Maintained by**: the maintainer, alone.

This register lists defects the maintainer knows of and considers useful to someone adopting runward. Information is not available for all defects, known or unknown. It is published because the schemes in [regulated-adoption.md](regulated-adoption.md) section 8 ask for exactly this artifact, and because a supplier who holds an unfavourable finding and does not publish it is worth less to an assessment than one who does.

**This register is not `GATE_NON_SCOPE`, and neither replaces the other.** The non scope states what the gate deliberately does not do, by decision; it is printed in every emitted pack — the three readiness drafts and the OSCAL component-definition — and readable through `runward rules --json`, with a blocking test holding that fact ("every pack carries the declared non-scope", `test/unit/compliance.test.js`; an earlier edition of this register said three packs did not carry it, which had been true and had stopped being true — caught by the 2026-08-14 audit). This register states what the gate did wrong without meaning to. An assessment that reads one and not the other has read half the picture.

**This register IS the "conformance-gate incident log".** Eight ADRs (0001-0005, 0019, 0020, 0021) name an incident log of rule-conformance failures as their reevaluation watch mechanism, and the 2026-08-14 audit found no file by that name — a watch mechanism eight decisions depend on, existing nowhere. It existed, under a better name: this register, dated, classed, with the fix and the workaround per entry. The citation is resolved here rather than by creating a second, competing log (`test/unit/regulated-posture.test.js` asserts the register exists and that the ADRs citing it are present, so the reference cannot go dangling again). An incident of that class lands here, with its `class`, `effect` and `fixed-in`.

**Every entry says what FOUND it** (`found-by`, a closed vocabulary guarded by
`test/unit/known-defects-register.test.js`). The field was added on 2026-08-27 and back-filled, and
the reason is worth stating: until then the register recorded what a defect WAS and what now guards
it, never what caught it. Fifty-eight of eighty-two entries said nothing about discovery in their own
prose; what existed lived in section headings, and headings flatten — forty-one entries had come to
sit under one of them. Back-filled values come from those headings or from first-hand knowledge of
the run that produced them; two entries are `not-recorded` because nobody wrote it down and guessing
would be the fabrication this register exists to refuse.

Read across the whole register, the mix is **59 `adversarial-audit`,
15 `mutation-instruction`, 8 `while-reproducing`,
4 `existing-guard`, 1 `ci-os-leg`, 1
`conformance-corpus`**, plus 3 declared limitations and one measurement. That is not the shape most people expect, and it is the point of recording it: the gate
does not find these. Deliberate adversarial measurement does, and what runward contributes is that
the measurement is required, its results are filed, and a stale filing refuses. The full derivation — the mix, three worked examples, and how to verify it —
is [what-found-them.md](what-found-them.md).

**Both directions are listed.** A register that only publishes false greens describes half a campaign and is falsifiable in one command against this project's own `CHANGELOG.md`, which records five adversarial audits of which two asked the opposite question: where does the gate cry on a mission that is telling the truth. Four of the nine hardening classes written on 2026-08-04 cried on the honest case before they shipped. Undue refusals are listed below alongside undue passes, because a gate that reds on correct work gets switched off, and a switched-off gate protects nothing.

**How to read the classes.**

| Class | Meaning |
|---|---|
| `wrong-verdict` | The gate returned the wrong exit code. |
| `unguarded-mechanism` | The behaviour was correct in every shipped release; no test protected it against regression. |
| `machine-surface` | Injecting a fault would corrupt what the tool prints or emits while the exit code stays correct. No shipped release did this; nothing detected it until 2026-08-05. |
| `measurement` | A measured property of the project, published with its perimeter. |

`effect: exit-code` means the item can move the 0/1/2 the gate returns. `effect: text-only` means it cannot.

---

## Wrong verdicts, found 2026-08-04, closed in 0.32.0

Three adversarial self-audits were run against the shipped binary, with every case executed rather than reasoned. They proved **22 false positives** grouped into the **9 classes** below. The full account is [ADR-0045](../adr/ADR-0045-the-gate-cannot-be-satisfied-by-paperwork.md).

**On the count.** The mapping from the 22 proven cases to these 9 classes is not published per class, and this register does not invent one. What is reproducible is the class: each entry below names a case in `test/audit-corpus.js`, which anyone can run against the real CLI (`node test/audit-corpus.js`, 14 cases, 10 refusals and 4 acceptances, with a sanity guard asserting the reference mission is green before any case runs). That count was wrong here until 2026-08-09: it read 13 and 9, from before the corpus divergence case was added, and the command the sentence gives falsifies it in one run. A register of known defects that carries an unregistered defect, on the very artifact it offers as reproducible, cancels itself.

All nine share: `affected-from` = 0.31.x and earlier, `fixed-in` = 0.32.0, `effect` = `exit-code`, `status` = `closed-by-fix`, `class` = `wrong-verdict`.

| id | Defect | How you detect it in your repo | Workaround before 0.32.0 |
|---|---|---|---|
| RWD-2026-0001 | The rule corpus was not checked against `scaffold-lock.json` by `check`. A rule edited, removed, or never written by runward was seen and never raised, so fabrication, substitution and signature stripping all passed. `update` read the lock; `check` did not. **This entry was written as fully closed in 0.32.0. It was not: see RWD-2026-0021, which reopens the same vector by deleting one file.** `found-by` = `adversarial-audit`. | Run `runward check --strict` on 0.33.1 or later. | Review `runward/rules/` by hand against the shipped templates. |
| RWD-2026-0002 | Circular evidence was accepted. `file:<manifest>#<slug>` was a universal green key, because the slug is column 1 of every row, so it always resolved and always matched. Pointing into `runward/rules/` is the same move once removed. `found-by` = `adversarial-audit`. | On 0.32.0, such a pointer is refused with a named reason. | Grep manifests for pointers into the manifest itself or into `runward/rules/`. |
| RWD-2026-0003 | The coverage counter printed only when `applied > 0`. Answering `n/a` to every rule removed the only vacuity signal the product had. This is the aggravating form: the emptiest missions produced the most reassuring output. `found-by` = `adversarial-audit`. | The counter now prints unconditionally: `N applied · N deviated · N n/a`. | Count `n/a` rows by hand. |
| RWD-2026-0004 | The ADR layer accepted an empty file, the `ADR-0000` template, a `rejected` or `superseded` decision, and an unratified one, as a ratified decision. The evidence layer had always refused an empty file. `found-by` = `adversarial-audit`. | `adr:NNNN` pointers now refuse those states. | Open each cited ADR. |
| RWD-2026-0005 | `resolveFile` was purely lexical, so a symlink escaped containment and the seal became an arbitrary file read oracle. `found-by` = `adversarial-audit`. | Containment now resolves the real path. | Refuse symlinks under evidence bases. |
| RWD-2026-0006 | Silent no-op pointers: `#`, `#""`, `::`, and one-character symbols. A pointer that looked precise and verified nothing. `found-by` = `adversarial-audit`. | Those forms are now refused. | Read each pointer's anchor. |
| RWD-2026-0007 | The manifest grammar was rewritten before parsing: quotes, a delimiting apostrophe, only the first pointer read, malformed `adr:`, duplicate sections, fenced tables, and a row without a trailing pipe. `found-by` = `adversarial-audit`. | The parser is fence aware, refuses duplicate sections, and parses every pointer. | None reliable. |
| RWD-2026-0008 | The seal covered the cited files, not the claim made about them. 31 files sealed, every manifest row rewritten to `n/a`, and the seal still read intact. Sealing zero files was accepted, and a lock declaring an unknown version was consumed silently. `found-by` = `adversarial-audit`. | On 0.32.0 a lock sealing zero files and a lock of unknown version are both refused. | Re-seal after any manifest edit and diff the manifest by hand. |
| RWD-2026-0009 | `unsafeSignature` did not scan non-capturing groups, so a rule signature could backtrack catastrophically: over 20 seconds on 38 characters. In CI that means no verdict rendered at all, which is an `exit-code` effect by absence of verdict. `found-by` = `adversarial-audit`. | The screen now scans non-capturing groups and reduces nested groups. | Review operator-authored `signature:` fields. |

---

## Wrong verdict, found 2026-08-06, closed in 0.33.1

One entry, and it matters more than its count: **it reopens a class this register called closed.**

| id | Defect | How you detect it | Workaround |
|---|---|---|---|
| RWD-2026-0021 | `corpusDivergence` answers `unrecorded` when a mission keeps its own rule copy and carries no `scaffold-lock.json`. That state printed a warning and contributed **nothing** to the verdict. Since the lock lives in the audited repository, "this mission predates the lock" is indistinguishable from "someone deleted the lock". Measured against the published 0.33.0: 64 rule files reduced to the word `ok` exit **1** with the lock present and **0** with the lock removed. `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.32.0 (when the corpus check was introduced) through 0.33.0, `fixed-in` = 0.33.1. `found-by` = `adversarial-audit`. | `node test/audit-corpus.js`, case *"deleted lock: the corpus is fabricated AND the lock removed"*. On 0.33.1 an unrecorded corpus is a named line of the verdict. | Assert `runward/scaffold-lock.json` is present in CI, or delete `runward/rules/` so the gate judges against the installed package. |

**How it was found, because that is part of the record.** Not by an audit of the gate. By an investigation into an unrelated product question, where an analyst was asked to build the cheapest mission that would earn a hypothetical attestation and did. The lesson is the one ADR-0045 already states and this register has to keep restating: a compatibility state controlled by the audited party is an attack surface, and a signal that lives in the printed text and not in the exit code is not a signal.

---

## Unguarded mechanisms, found 2026-08-05, closed by tests

**None of these three produced an observed defect in any shipped release.** All three were regression exposure: the behaviour was correct, and no test would have reddened if it stopped being. They are listed separately from the nine above on purpose. Merging them would over-declare three entries and under-declare twenty two.

Found by a full mutation pass; the account is [ADR-0046](../adr/ADR-0046-mutation-testing-is-an-instrument-not-a-gate.md). All three: `class` = `unguarded-mechanism`, `status` = `closed-by-test`, `fixed-in` = **0.33.0**, the first release to carry the tests that pin them. Between 2026-08-05, when they were found, and that release, they were open on every published version, and this register said so rather than back-dating them.

| id | Mechanism | Effect | Evidence |
|---|---|---|---|
| RWD-2026-0010 | Seal tamper detection, `verifyEvidenceLock`. One field returned false on the unparseable-lock path makes a sealed, tampered mission go from exit 1 to exit 0, because `src/commands/check.ts` gates the whole seal section on `if (seal.present)`: the violations are neither printed nor counted. `found-by` = `adversarial-audit`. | `exit-code`, **demonstrated** on a real mission sealed with `check --freeze`. | `test/unit/evidence-lock.test.js` |
| RWD-2026-0011 | The ReDoS screen of [ADR-0020](../adr/ADR-0020-rule-evidence-signatures.md). The loop collapsing nested groups was entered by no fixture and could be deleted with the suite still green. `found-by` = `adversarial-audit`. | `exit-code` potential, not demonstrated. | `test/unit/evidence-signature.test.js` |
| RWD-2026-0012 | Pointer containment, `resolvePointer`. The repository fallback was dead code under test: every containment test ran in a bare temp directory, where no repository marker exists above the base. `found-by` = `adversarial-audit`. | `exit-code` potential, not demonstrated. | `test/unit/evidence-resolve.test.js` |

---

## Undue refusals, found 2026-08-04, closed in 0.32.0

The same week's audits asked the opposite question twice: where does the gate red on a mission that is telling the truth. Four of the nine hardening classes written that morning cried on the honest case before they shipped, and the items below had shipped in 0.31.x. They are listed with the same weight as the undue passes, because a gate that reds on correct work gets switched off, and a switched-off gate protects nothing.

All: `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.31.x and earlier, `fixed-in` = 0.32.0, `status` = `closed-by-fix`.

| id | Defect | Verifiable at |
|---|---|---|
| RWD-2026-0016 | A Windows checkout turned the corpus into a fabrication. `core.autocrlf` rewrites every file and the frontmatter pattern was `/^---\n/`, so no rule parsed. `found-by` = `adversarial-audit`. | `git show v0.31.0:src/lib/rules.ts` line 49 against `src/lib/rules.ts` today (`/^---\r?\n/`) |
| RWD-2026-0017 | npm and pnpm workspaces broke under the containment hardening: `packages/api/src/shared -> ../../shared` stopped resolving and no spelling worked. Containment now accepts a target inside the enclosing repository, found by a marker on disk and never by reading git configuration. `found-by` = `adversarial-audit`. | `repoRootAbove` in `src/lib/evidence.ts`; `test/unit/evidence-reporoot.test.js` |
| RWD-2026-0018 | The mission search gave up after twelve parent directories and then asserted no mission existed, which is false and unfalsifiable from the operator's seat. `found-by` = `adversarial-audit`. | `git show v0.31.0:src/lib/mission.ts` line 65 (`i < 12`) against `src/lib/mission.ts` today (`i < 128`) |
| RWD-2026-0019 | An unreadable file was a crash rather than a verdict, and `--json` stopped being JSON, so a CI consuming the machine surface got neither an answer nor a parseable error. `found-by` = `adversarial-audit`. | `src/lib/evidence.ts`, try/catch on every evidence read |
| RWD-2026-0020 | The gate punished precision: a path outside the project passed as prose and failed as a typed pointer, so writing the more precise form was worse than writing the vaguer one. `found-by` = `adversarial-audit`. | `resolvePointer` in `src/lib/evidence.ts`; `test/unit/evidence-resolve.test.js` |

---

## Machine surface, guarded only since 2026-08-05

Neither entry below is a defect of any shipped release. Both describe what a **mutation** of the code would do without any test reddening, which is regression exposure on the machine surface rather than on the exit code. They are listed because the schemes in section 8 of [regulated-adoption.md](regulated-adoption.md) ask for failures that can be injected into a tool's output, and these are exactly that.

| id | Defect | Effect | Status |
|---|---|---|---|
| RWD-2026-0013 | **Not a defect of any shipped release, and listed here only to keep the record straight.** The mutation pass showed that if `artifactState` were changed to report an ADR directory as `filled` when it holds only the scaffolded template, the whole net would stay green: unit suite, self-gate, smoke and audit corpus. The shipped binary does not do that. Verified 2026-08-06 on 0.32.0: an `adr/` holding only `ADR-0000-template.md` prints `○ Decision journal (≥1 ADR) (runward/adr) — raw template`, because `isRealAdr` (`src/lib/mission.ts:89`) excludes the template by name. What was missing was the test, not the behaviour. `found-by` = `not-recorded`. | none observed | `closed-by-test` (`test/unit/artifact-state.test.js`) |
| RWD-2026-0014 | Faults injected into the territory scanner corrupt `runward rules --for --json` while `check --strict` stays at exit 0. Measured on 2026-08-05: of 42 mutants applied one at a time to a green mission, 4 corrupted the machine output and 0 moved the verdict. The failure mode is the dangerous one for an agent consuming that contract: it does not answer "I could not read it", it answers a wrong list, plausibly. `check.ts` imports neither the territory module nor characterize, so the partition protecting the verdict is real; it is half a partition, because the machine contract is not behind it. `found-by` = `not-recorded`. | none on the verdict, **machine surface** for an agent | `closed-by-test` (`test/unit/territory-*.test.js`) |

---

## Declared, unverifiable by construction

Neither entry is a defect to fix. Both are properties of where the data lives, and both were being
presented as though the gate had established them. They are listed because a reader of the output
could otherwise write a sentence the output never supported.

| id | What it is | Why it cannot be verified |
|---|---|---|
| RWD-2026-0022 | **The seal date is declared, not observed.** `runward/evidence-lock.json` carries `sealedAt`, and nothing signs the lock. Editing the field by hand yields `✓ seal intact — sealed 1999-12-31` with exit 0, verified 2026-08-08. The `RUNWARD_NOW` environment variable reaches it too, but that is a detail: the date is the operator's word either way. `found-by` = `declared`. | The lock is a JSON file inside the audited repository. An unsigned file cannot testify about itself. What the seal does prove is unchanged: the cited files still hash to what they hashed when it was written. From 0.33.2 the printed line and the machine surface say the date is declared. |
| RWD-2026-0023 | **The gate demands 31 of the 45 CRITICAL/HIGH rules.** The other 14 are mapped to no gated phase and are never asked about, five of them CRITICAL, including `checklist-pre-production-security` and `checklist-pre-production-resilience`. `found-by` = `declared`. | Not a defect: a rule with `phases: []` is documentation the operator may apply without the gate asking, and gating it would red every honest mission. What was wrong is that the conformance section printed "12 rule(s) accounted for" and stopped, so the only honest sentence — *the 31 mapped rules each have a row* — could not be written by a reader. From 0.33.2 the count and the list are printed and carried in `check --strict --json` as `criticalScope`. |

---

## Measurements

| id | Measurement | Status |
|---|---|---|
| RWD-2026-0015 | Mutation score **60.78 per cent** on 2026-08-05, 380 mutants surviving the entire net. Inseparable from its perimeter: **measured** on seven library modules the verdict is computed from; **not measured** on the ten commands, on five `lib/` modules nothing reaches even transitively, nor on the nine other modules outside the core, which include `src/lib/compliance.ts`, the module that emits the OSCAL pack. Of the 380, 246 have been instructed and 181 now die. The largest absence of that pass is closed since 2026-08-06: the verdict itself, which was assembled in `src/commands/check.ts` at 8.70 per cent line and zero function coverage, now lives in `src/lib/verdict.ts` at 97.79 per cent line and 100 per cent function coverage, inside the measured perimeter ([ADR-0047](../adr/ADR-0047-the-verdict-is-computed-where-a-test-can-reach-it.md)). The next pass will report a different number for that reason, and this line says so rather than letting the improvement look like a measurement error. `found-by` = `measurement`. | `open` |

This is one entry, not 380. A surviving mutant is not a defect: [ADR-0046](../adr/ADR-0046-mutation-testing-is-an-instrument-not-a-gate.md) records why, and records the case that proves it, where a mutant survived every net and the mission was still correctly refused.

## Wrong verdicts, found 2026-08-14 and 2026-08-17, closed in 0.35.0 and after

> **Identifier correction, 2026-08-26.** The two rows below were filed on 2026-08-17 as
> RWD-2026-0022 and RWD-2026-0023, numbers already taken on 2026-08-08 by the two declared
> limitations under *Declared, unverifiable by construction* (the seal date, and the 31-of-45 floor).
> Two numbering series had been continued from the same point. The FIRST assignment keeps its
> number, so the rows below are now **RWD-2026-0056** (the JUnit adapter) and **RWD-2026-0057**
> (`spec-check` verifying the path only). Anyone holding the old numbers: `RWD-2026-0022` means the
> seal date, cited by TOR-028 and regulated-adoption §5.4; `RWD-2026-0023` means the 31-of-45 floor.
> [ADR-0056](../adr/ADR-0056-the-evidence-layer-widens.md) cited the defect pair and was updated.
> A uniqueness guard now makes this impossible to reintroduce silently.


Found by the pre-announcement full-repo audit (thirteen agents, nine readers and three adversarial
refuters) and by the first `windows-latest` CI leg. All three are the same class as the nine of
2026-08-04 — a false green in the evidence layer — and all three were found and closed before the
release that would have carried them to an operator, which is the only reason they read as narrow.

| id | Class | Defect | Fixed in | How to check |
|---|---|---|---|---|
| RWD-2026-0056 | `wrong-verdict` / `exit-code` `found-by` = `adversarial-audit`. | The JUnit adapter stopped at the FIRST `<testcase name="X">`. With two homonymous cases in two suites — routine in parameterized runs and shared base classes — a RED case behind a green one was invisible, and `test:report.xml::X` read `pass`. A false green inside the adapter that exists to refuse red tests. `affected-from` 0.34.0 (when the adapter shipped, unpublished at the time of the finding). | 0.35.0 | `test/unit/tool-adapters.test.js`, case *"HOMONYMS — every occurrence is scanned"*. Pin a case with `CLASS::NAME` when homonyms are legitimately different tests. |
| RWD-2026-0057 | `wrong-verdict` / `exit-code` `found-by` = `adversarial-audit`. | `spec-check` verified the pointer's PATH only and silently dropped the `#SYMBOL` / `::NAME` the author declared: a criterion linked to an absent symbol or a red test read `linked`. The exact silent degradation `symbolDeclared` was created to refuse, reproduced in the newest feature. `affected-from` 0.34.0 (unpublished at the time of the finding). | 0.35.0 | `test/unit/spec-conformance.test.js`, the five *audit 2026-08-14* cases. |
| RWD-2026-0024 | `wrong-verdict` / `exit-code` `found-by` = `adversarial-audit`. | On Windows the case-forgiving-filesystem check silently skipped: the segment walk started on the bare drive letter (`D:` is DRIVE-RELATIVE, not the root) and `realpathSync` plain does not canonicalise case, so 8.3 short names defeated the fallback. A mis-spelled pointer resolved with no violation on the exact platform the check exists for — and no CI leg exercised it. `affected-from` 0.28.0 (when the check shipped) through 0.35.0. | after 0.35.0 | `test/unit/pointer-grammar.test.js` under the `windows-latest` CI leg (`.github/workflows/ci.yml`), which is what found it. |

---

## Wrong verdicts, found 2026-08-21 by instructing mutation survivors

Three false greens in the evidence layer, all live in the published 0.36.0, and none of them needed
a mutant to reach. They were found while INSTRUCTING the 215 mutants that survived both the unit
suite and the whole net on `evidence.js` — building a mission that would exercise each function is
what exposed them. That is the argument for ADR-0046 decision 3 in one line: the instruction is
worth more than the score, because it makes someone build the mission nobody had built.

| id | Defect | How you detect it | Workaround |
|---|---|---|---|
| RWD-2026-0025 | `textOutsideManifest` took the FIRST `## Rule conformance` heading, fenced or not, when removing the manifest table before looking for a self-cited symbol. An illustration of the format inside a ```` ``` ```` fence above the real table made the excluded slice run from the illustration to the real heading, leaving the real table in the text that is searched — so `file:<self>#<the rule's own slug>` matched again. That is RWD-2026-0002, the universal green key, reopened by a code fence. `readManifest` had already fixed this exact shape for row parsing; this function had not. Measured: the same row exits **1** without the illustration and **0** with it. `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.32.0 (when circular evidence was first refused) through 0.36.0. `found-by` = `mutation-instruction`. | `test/unit/evidence-false-greens.test.js`, case *"a self-citation is refused even when a fenced example of the table sits above it"*. | Keep no fenced example of the conformance table in a deliverable that also carries one. |
| RWD-2026-0026 | A single invisible line terminator swallowed a typed pointer in silence. `POINTER_PREFIX` ends in `$`, and `.` never matches a line terminator in JavaScript, so one CR, U+2028 or U+2029 after the prefix made `$` unreachable: the pointer was dropped, the row still read as typed, and the cited file was never opened. A paste leaves such characters behind and nobody can see them. Measured on a row citing a file that does not exist: exit **1** with an ordinary cell, exit **0** with a U+2028 in it. This is RWD-2026-0006 — the pointer that looks precise and verifies nothing — by a route that family did not cover. `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.32.0 through 0.36.0. `found-by` = `mutation-instruction`. | `test/unit/evidence-false-greens.test.js`, the three *"a pointer followed by …"* cases. | Pipe manifests through a filter that strips U+2028/U+2029/CR, or diff them with a tool that renders invisibles. |
| RWD-2026-0027 | An absent evidence seal printed **nothing at all**, so the output could not distinguish "never sealed" from "seal deleted". Measured: seal a mission, tamper with a sealed evidence file, `check --strict` exits 1; delete `runward/evidence-lock.json` and the same tampered tree exits **0**, silently. The verdict is deliberately unchanged (see the entry below); what was fixed is the silence. `class` = `machine-surface`, `effect` = `text`, `affected-from` = 0.32.0 through 0.36.0. `found-by` = `mutation-instruction`. | `test/unit/evidence-false-greens.test.js`, case *"the strict run names the absence of a seal instead of printing nothing"*. | Assert `runward/evidence-lock.json` is present in CI for any mission that seals. |

## Wrong verdicts, found 2026-08-25 while closing mutation holes

Two more false greens in the spelling ladder, both live in 0.36.1, and neither found by reading the
code: the first by a test that failed and turned out to be right, the second by the windows-latest
leg answering a question the author could not answer from a laptop.

| id | Defect | How you detect it | Workaround |
| --- | --- | --- | --- |
| RWD-2026-0029 | A directory that is traversable but **not listable** silently cleared the case check for everything beneath it. `onDiskSpelling` returns `null` for "the spelling already matches", and its `catch` returned `null` too, so "I could not check" and "it is fine" were the same answer. Measured on 0.36.1: `file:./src/Guard.TS` citing a file spelled `guard.ts` is refused with the directory at 0755 and **passes** at 0111, on a filesystem where it resolves only because the filesystem is forgiving. `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.32.0 through 0.36.1. `found-by` = `mutation-instruction`. | `test/unit/evidence-spelling-unmasked.test.js`, case *"an unlistable directory does not silently clear a mis-spelled pointer"* — it probes whether this process can actually be denied a listing and skips when it cannot (root ignores mode bits). | Keep evidence in directories the gate can list. |
| RWD-2026-0030 | **On Windows**, a case-divergent pointer written with a redundant `./` was accepted. There `onDiskSpelling` is already defeated by 8.3 short names (`RUNNER~1`), so `spellingViaRealpath` is the only rung left — and it compared the operator's raw cell against a canonical suffix: `.\src\Guard.TS` versus `src\guard.ts`, failing on the prefix rather than on the case, and returning "no difference". Measured on the windows-latest leg 2026-08-25: `file:src/Guard.TS` refused, `file:./src/Guard.TS` accepted, same tree. macOS caught both, which is why nothing before the Windows leg could see it. `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.34.0 (when the realpath rung was added) through 0.36.1. `found-by` = `mutation-instruction`. | `test/unit/evidence-spelling-unmasked.test.js` runs both pointer forms, so the Windows leg names which one breaks. | Write pointers without a leading `./`. |

**Why both took a failing test to find.** Neither is reachable by reading `evidence.ts`: the first needs
a permission state nobody creates by accident, the second needs a filesystem the author does not have.
The mutation register said 24 mutants survived in these two functions; closing them meant building the
states that make them observable, and the states themselves were the finding. That is ADR-0046
decision 3 in practice — the instruction is worth more than the score.

## Wrong verdict, found 2026-08-26 by measuring the ladder on a runner instead of a laptop

A third false green in the same ladder, and the one underneath the other two: the comparison was
weaker than the filesystem's own case fold.

| id | Defect | How you detect it | Workaround |
| --- | --- | --- | --- |
| RWD-2026-0031 | APFS and HFS+ apply **full Unicode case folding**, and the ladder compared with `toLowerCase()` and `normalize("NFC")`, which do not. A pointer citing `sguard.ts` as `\u017Fguard.ts` (LATIN SMALL LETTER LONG S) OPENS the file, no rung matched, and `if (!hit) return null` answered "the spelling already matches". Measured 2026-08-26 on the same tree: `check --strict --json` returns **exit 0, verdict `clean`, 0 conformance gaps** before the fix and **exit 1, verdict `gaps`, 2 gaps** after. A case-sensitive runner refuses the same pointer, so this is the RWD-2026-0016 family one level below case. Four other folds behave identically (measured): U+03C2 onto sigma, U+03D1 onto theta, U+212A onto k, U+00DF onto `ss`; of these `toLowerCase()` catches only U+212A, `NFC` catches none, `NFKC` catches two. `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.32.0 through 0.36.2. `found-by` = `ci-os-leg`. | `test/unit/evidence-spelling-ladder.test.js`, the five `the ladder sees a fold the filesystem performs` cases. | Write pointers in the spelling `ls` prints. |

**Why three passes missed it.** The two rungs were pinned only THROUGH the gate, and through the gate
this function is unreachable on a case-sensitive filesystem: `file:src/Guard.TS` does not resolve
there, so `resolvePointer` refuses the pointer for another reason and never calls the ladder. The
tests stayed green on Linux for a reason unrelated to the code they name. The chunked CI run of
2026-08-25 made it visible as a number: `onDiskSpelling` **22 %** and `spellingViaRealpath` **26 %**
on ubuntu-latest, against 50-100 % for every other function in the module, while the same mutants die
on macOS. The survivor register was describing the code **plus the filesystem**.

The fix is therefore two things, not one: a fold at least as strong as the filesystem's, and a test
that calls the ladder DIRECTLY so it is pinned on every filesystem rather than on the author's.

## Wrong verdict, found 2026-08-26 by instructing the fence-awareness of textOutsideManifest

RWD-2026-0002's universal green key, re-armed by an illustration.

| id | Defect | How you detect it | Workaround |
| --- | --- | --- | --- |
| RWD-2026-0032 | `circularEvidence` refuses `file:<self>#<the rule's own slug>`, and allows a documentary rule to cite the passage of the deliverable that states its fact. What counted as "outside the manifest" was decided by `textOutsideManifest`, which KEEPS fenced text on purpose so a code sample can be honest evidence — so a **fenced illustration of a manifest row** became a valid self-citation target, one fence removed. Any document explaining the manifest format carries such a block, which is exactly the artefact RWD-2026-0002 was about. Measured 2026-08-26 on the shipped example, same tree: bare self-citation `exit 1` with 1 conformance gap; add a fenced `\| hexa-architecture \| applied \| … \|` above the section and it returns **exit 0, verdict `clean`, 0 gaps**. The **unfenced** variant is the same hole and was not reported by the instruction that found the first: a bare conformance row outside the section is not read by `readManifest` and was kept here — measured the same day, also `exit 0`. `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.34.0 (when the documentary escape hatch was added) through 0.36.2. `found-by` = `mutation-instruction`. | `test/unit/evidence-circular-rows.test.js` — seven vector shapes and six shapes that must keep passing. Eight of its fifteen cases redden against the unfixed build. | Do not paste manifest rows into a deliverable that cites itself. |

**What the fix tests, and what it deliberately does not.** The exclusion is on the row's SHAPE — three
cells or more whose second is one of the three decisions a row may carry — not on the fence and not on
the rule under test. A row declaring conformance is what `circularEvidence`'s own sentence excludes:
*cite the section that states the fact, not the row that declares it.* An ordinary documentation table
(`\| rule \| where it lives \|`) has no status cell and is untouched; prose, a heading, and a fenced code
sample all keep clearing the citation, which is asserted rather than assumed — a gate that refuses
honest evidence is the one that gets switched off.

## Undue refusals, found 2026-08-26 by instructing the realpath rung

Two refusals that are wrong rather than merely unhelpful. Both come from the same place: the walk
answered `null` for two opposite facts, and the fallback that reads `null` cannot tell them apart.

| id | Defect | How you detect it | Workaround |
| --- | --- | --- | --- |
| RWD-2026-0033 | **On a case-sensitive filesystem**, a pointer traversing a symlink whose own NAME differs from its target only by case (`SRC -> src`) is REFUSED, with a message false in both halves: it names a case-insensitive filesystem that is not one, and prescribes rewriting a path that is already correct and that a Linux runner resolves. `onDiskSpelling` walks it, finds every segment listed verbatim including `SRC`, and returns `null` — which meant BOTH "verified, no divergence" and "a segment matched nothing, I have no opinion". `resolvePointer` consults `spellingViaRealpath` on `null`, and that rung compares a canonical suffix against what was written: `probe/src/guard.ts` versus `probe/SRC/guard.ts`, lowercase-equal and unequal, reported as a case divergence that is really a symlink traversal. `class` = `undue-refusal`, `effect` = `exit-code`, `affected-from` = 0.34.0 (when the realpath rung was added) through 0.36.2. `found-by` = `mutation-instruction`. | `test/unit/evidence-spelling-ladder.test.js`, case *"a verified match and a walk that broke off are DIFFERENT answers"*. The walk now returns `SPELLING_VERIFIED` for a reading it actually performed, and the fallback speaks only where the walk has none. | Do not name a symlink as a case-variant of its target. |
| RWD-2026-0034 | The refusal above, and any refusal the realpath rung raises, prescribed a spelling **the gate itself then rejects**. The spelling comes from an absolute canonical path and was relativised against `resolve(dirname(missionDir))` — and that rung exists precisely for the case where the mission is ADDRESSED differently from how the filesystem spells it (Windows 8.3: `RUNNER~1` in the mission path, the long name in the canonical one). There the two are not prefixes of one another, `relative()` climbs OUT of the mission, and pasting the prescribed path into the cell answers `resolves outside the project this mission audits (ADR-0019)`. The operator's only offered remedy is one the gate refuses. `class` = `undue-refusal`, `effect` = `message`, `affected-from` = 0.34.0 through 0.36.2. `found-by` = `mutation-instruction`. | `test/unit/evidence-spelling-ladder.test.js`, cases *"a spelling that climbs OUT of the project is refused, never prescribed"*. | Fix the case by reading `ls`, not by copying the gate's suggestion. |

**Why the render is tested and not the gate.** On POSIX, once the walk answers, `spellingViaRealpath`
is unreachable: every segment of a path that RESOLVES is listed by its parent, so the walk either
verifies the spelling or names the divergence, and never hands over. The rung is Windows-only in
practice — which is exactly why it was unguarded. Driving the render helper directly is what makes a
Windows-only defect testable on any machine, and it is the same move as exporting the ladder itself.

## Wrong verdicts, found 2026-08-26 by instructing the fixes of the same day

Three defects in code written that morning, none of them reachable by reading, all three found by a
fleet instructing the survivors the fixes themselves created. Two reopen entries above.

| id | Defect | How you detect it | Workaround |
| --- | --- | --- | --- |
| RWD-2026-0035 | **Reopens RWD-2026-0031.** The case fold routed through `toUpperCase()` first, and `toUpperCase()` is a no-op on a character that is ALREADY upper case. Measured: `"ß".toUpperCase()` is `"SS"`, so the lower-case sharp s folded correctly, while `"ẞ".toUpperCase()` is `"ẞ"` and lower-cases to `"ß"`, never to `"ss"`. APFS opens `ssharp.ts` under both spellings, so `file:…/ẞharp.ts` was declared correctly spelled: `check --strict` exit 0 / verdict clean, and `--freeze` SEALED that key into the lock — a name no case-sensitive filesystem holds. The corpus asserted in the test carried the lower-case half of the pair and not its upper-case twin, which is how a fold that is not idempotent survives its own subsumption test. Fixed by folding lower-then-upper-then-lower; swept over the whole BMP, that merges exactly ONE pair the old fold separated (ß ~ ẞ) and separates nothing it merged. `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.32.0 through 0.36.2. `found-by` = `mutation-instruction`. | `test/unit/evidence-spelling-ladder.test.js`, case *U+1E9E, CAPITAL sharp s onto ss*. | Write pointers in the spelling `ls` prints. |
| RWD-2026-0036 | **Reopens RWD-2026-0032.** The row-shape test required a LEADING pipe, so the same conformance row written without outer pipes — which a markdown renderer still lays out as a table — was kept as text outside the manifest and re-armed RWD-2026-0002's universal green key a second time. Measured on the shipped example: bare self-citation exit 1 with one gap; add `slug \| applied \| file:…` above the section and it returns exit 0, verdict clean, with `--freeze` sealing it. A trailing-pipe-only variant does the same. The obvious repair is wrong and was measured to be: dropping the pipe test refuses twelve honest cases (a fenced shell pipeline, a fenced code comment carrying a pipe, pipe-bearing prose, a list item, a blockquote). What separates them is the FIRST cell — a rule id carries no whitespace. `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.34.0 through 0.36.2. `found-by` = `mutation-instruction`. | `test/unit/evidence-circular-rows.test.js`, five vector shapes and five pipe-bearing honest cases. Four of them redden against the unfixed build. | Do not paste manifest rows into a deliverable that cites itself. |
| RWD-2026-0037 | **Reopens RWD-2026-0034 from the other side.** `resolvePointer` admits evidence anywhere under `repoRootAbove(base)` — the npm/pnpm workspace allowance of RWD-2026-0017 — while the render was handed only the project root. The two definitions of "the project" disagreed, so any pointer admitted through that allowance produced a spelling `relative()` reads as a climb-out and the operator was told the gate could not express the correct spelling. Measured on macOS, no Windows and no 8.3 name: the mission was reached under an ordinary name, the gate COULD express the spelling, and a working remedy existed. The first repair was also wrong and the test says so: expressing the remedy against the root that CONTAINS the file yields `shared/guard.ts`, which the gate answers `typed pointer does not resolve` when pasted. A cell resolves against the PROJECT root, so the remedy is expressed against that root and is allowed to climb. `class` = `undue-refusal`, `effect` = `message`, `affected-from` = 0.34.0 through 0.36.2. `found-by` = `mutation-instruction`. | `test/unit/evidence-spelling-ladder.test.js`, case *a spelling outside the project but inside the repository is expressed so it can be pasted* — which also names the wrong answer so a future edit cannot quietly return to it. | Fix the case by reading `ls`, not by copying the gate's suggestion. |

**What these three have in common.** Each was introduced by a fix for the entry it reopens, and each
was found by mutating that fix rather than by reviewing it. The pattern is the same every time: a
corpus that carries one half of a pair, a guard whose property is held up by code in another
function, or two definitions of the same word in two places. None is visible to a reader who already
believes the fix is correct, which is what the instrument is for.

## Wrong verdicts, found 2026-08-26 by the third adversarial audit

The adoption path itself. `update --corpus` is the gesture [ADR-0057](../adr/ADR-0057-the-shared-corpus-is-pinned-without-a-registry.md)
exists for, and it was broken in both directions at once.

| id | Defect | How you detect it | Workaround |
| --- | --- | --- | --- |
| RWD-2026-0038 | **`update --corpus` bricked a green mission and reported success.** `nextFiles` was built from `readdirSync(srcDir)`, and with `--corpus` that source is the org's directory, so every rule runward wrote and the corpus does not carry fell out of the lock while its FILE stayed exactly where it was. `scaffold-lock.ts` derives `known` from the lock, so each one became `extra`. Measured on the shipped binary: vendoring a one-rule house corpus onto a green example mission dropped the lock from 64 entries to 1, and `check --strict` then refused **31 rules runward had scaffolded thirty seconds earlier**, telling the operator they were rules runward never wrote — while `update` exited 0 with a summary of green ticks and nothing connecting the red to the `--corpus` run. `class` = `undue-refusal`, `effect` = `exit-code`, `affected-from` = 0.36.0 (when `--corpus` shipped) through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/update-corpus.test.js`, case *a one-rule org corpus does not erase the record of every rule runward wrote*. | Re-run plain `runward update` — it restores the records, at the cost of dropping the vendored rule from the lock in turn. |
| RWD-2026-0039 | **A same-slug replacement of a rule runward ships was labelled with the word runward uses for its own refreshes.** `classify` sees the mission copy as pristine and the source as moved, so the verdict is `upstream` and the line reads `(changed upstream)` — no `--force`, no distinct count. Measured: vendoring a fork of runward's own corpus with **one `signature:` line deleted** takes a mission from `check --strict` exit 1 to exit 0, and the only word the operator sees is the one that means "runward moved". The gate going green is the organisation's right; the silence was not. `class` = `unproven-claim`, `effect` = `message`, `affected-from` = 0.36.0 through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/update-corpus.test.js`, case *replacing a shipped rule from an org corpus is named a replacement, never an upstream change*, with a mirror case asserting an ordinary update still says `upstream`. | Diff the vendored corpus against `templates/rules/` before adopting it. |

**Why the entry path was the least reliable part of the product.** Nobody adopts a 64-rule corpus
written by someone else without touching it, so `--corpus` is not a peripheral flag: it is how an
organisation with its own standards gets in. It had never been exercised adversarially, and the two
defects are mirror images — one refuses what it should accept, the other accepts silently what it
should announce.

## Wrong verdicts in the artifacts the gate hands to a machine, found 2026-08-26

The third adversarial audit's own sentence for this family: *the gate itself was honest in every
fixture I built; it is the artifacts the gate hands to a machine that overstate it.* Five findings,
none of them a defect in the verdict logic, all of them a surface asserting more than the verdict
established. Three were corroborated independently by two or three of the five auditors.

| id | Defect | How you detect it | Workaround |
| --- | --- | --- | --- |
| RWD-2026-0040 | **A red gate emitted a SARIF byte-identical to a green one** whenever the gap was the evidence seal, the rule corpus, an unratified decision or a failed hook. `buildSarif` read `verdict.deliverables` and `verdict.gated` and nothing else, so four of the five strict terms never reached the log — same sha256 as green, `results: []`, `rules: []`. The README calls that document *the verdict as annotations* and this project documents it as the artifact that CLEARS a forge's stale alerts, so a mission whose sealed evidence was tampered with uploaded the exact document that wipes the warning. The seal is the worst of the four: its entire purpose is to detect that the evidence moved. `class` = `wrong-verdict`, `effect` = `message`, `affected-from` = 0.35.0 through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/sarif-verdict-terms.test.js` — one case per term, each asserting the log differs from the same fixture's green log. | Branch on `check --strict --json`, never on the SARIF alone. |
| RWD-2026-0041 | **Half of every SARIF used a mission-relative uri** (`floor.md`) where the other half used a repository-relative one (`runward/floor.md`), so the deliverable annotations pointed at paths no checkout holds. The comment on the defective line asserted the opposite and the CHANGELOG claims *repository-relative URIs*; the existing guard matched `/floor\.md$/`, a suffix satisfied by both bases, which is why it shipped. `class` = `wrong-verdict`, `effect` = `message`, `affected-from` = 0.35.0 through 0.36.2. `found-by` = `adversarial-audit`. | Same file, case *a deliverable annotation points at a path the checkout actually holds* — it resolves every emitted uri against the checkout root. | None; the violation half was always correct. |
| RWD-2026-0042 | **`runward verify` re-derived two predicate fields of fifteen.** `verdict` and `exitCode` were compared; everything else was unbound free text. Measured: rewrite `evidence` to 36 typed / 0 prose, `seal` to present with 4242 files on a mission that has none, `criticalScope` to 45 of 45, and replace `gateNonScope` with *"runward proves the code is correct"* — `verified: true`, exit 0. The attestation is unsigned by design, so its bytes are attacker-controllable until the operator checks a signature runward explicitly does not check, and README:93 and interop.md §5 both promise a tampered predicate fails loud. `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.34.0 through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/verify-predicate.test.js`. `verify --json` now carries `predicate: {matches, differing, notReDerived}`. | Bundle the attestation (`runward bundle`) — the bundle binds it by raw sha256. |
| RWD-2026-0043 | **`verify` took its strictness from the untrusted predicate and never reported it.** One boolean flipped `strict: true → false` re-derives a lenient gate on a tree that fails `check --strict`, and neither output contained the word *strict* — while the VSA carries that distinction in `verifiedLevels` precisely so a policy cannot lose it. `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.34.0 through 0.36.2. `found-by` = `adversarial-audit`. | Same file. `verify --json` now carries `strict` and `level`, and the human output names which gate re-derived. | Read `predicate.strict` yourself before trusting the exit code. |
| RWD-2026-0044 | **`--vsa` emitted `verifiedLevels: ["RUNWARD_GATE_STRICT"]` on a `verificationResult: "FAILED"`** — the level was computed from `strict`/`through` alone and the pass/fail reached only the result. docs/interop.md §4 tells the consumer *"Read the level, not just the result"*, so a Kyverno or OPA rule written to that instruction admitted an artifact whose gate had refused it. The SLSA VSA v1 spec carries `FAILED` for exactly this and it is not an `SLSA_`-prefixed value. `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.35.0 through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/vsa-failed-level.test.js`. | Read `verificationResult`, not the level. |
| RWD-2026-0045 | **The OSCAL pack was byte-identical between a green gate and a gate red on eighteen unresolvable pointers**, declared controls `implemented` from the manifest status column alone, and recorded the verdict nowhere — while its own remarks assert the evidence *resolves*. `compliance` never called `computeVerdict`. This is the artifact that leaves the building for a third-party GRC tool, where no prose follows it. `class` = `wrong-verdict`, `effect` = `message`, `affected-from` = 0.33.0 through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/compliance.test.js`, cases *`implemented` requires the gate to have accepted, in both directions* and *the declared non-scope and the verdict travel on EVERY requirement*. | Ship `check --strict --json` beside the pack. |
| RWD-2026-0046 | **Deleting five characters from a pointer turned a refusal into a pass.** Circularity was tested on the POINTER, not on the TARGET: `circularEvidence` ran only in the typed-pointer loop, so `file:runward/rules/<slug>.md` was refused as *"the rule's own text"* while the same file written `runward/rules/<slug>.md` fell into the bare-path loop, which resolved it, banked it in `resolvedFiles` unexamined, and let it satisfy the rule's own `signature:`. Measured on the shipped 0.36.2 binary and again on this tree, four states of one cell on a CRITICAL signed rule (`config-secrets-boundary`): prose → exit 1, unrelated real file → exit 1, typed self-pointer → exit 1, **bare self-path → exit 0**. That is ADR-0019's inverted incentive a second time (RWD-2026-0020, *"the gate punished precision"*): the vague spelling was the one that passed. `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.31.0 (when `circularEvidence` shipped, checking typed pointers only) through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/gate-false-greens.test.js`, case *circularity is a property of the target, not of the pointer's spelling* — measured red against the unfixed build, green against the fixed one. Also `test/audit-corpus.js`, attack *self-citation: the same target, five characters shorter*, which the unfixed build fails at exit 0. | Write the typed pointer. It was the spelling the gate checked. |
| RWD-2026-0047 | **The row that declares a rule discharged that rule's signature.** The ADR-0020 signature was tested against the whole file, conformance table included, so column 1 of the very row making the claim satisfied the rule it declared. It is not a corner case: **7 of the 9 signed rules runward ships carry a signature their own slug matches**, three of them CRITICAL (`config-secrets-boundary` /secret\|vault/, `security-code-execution-sandbox` /sand[-\s]?box/, `security-mcp-server-pinning` /pinn(ed\|ing)\|sha256\|digest/). Measured: with `| config-secrets-boundary `found-by` = `adversarial-audit`. | applied | file:runward/floor.md#of |`, the ONLY line of `floor.md` matching /secret\|vault/ was that row, and the gate answered verdict `clean`, exit 0, 0 violations. The signature now reads `textOutsideManifest` — the same cut `circularEvidence` already makes one branch above, for the same reason. `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.31.0 through 0.36.2. | `test/unit/gate-false-greens.test.js`, case *a conformance row DECLARES a fact and never states one*, plus the sensitivity control *the signature still reads the rest of the file* which stays green on BOTH builds so the fix cannot be a blanket refusal. | Point signed rules at code or tests, never at a deliverable. |
| RWD-2026-0048 | **A zero-byte file read `✓ Decision journal (≥1 ADR)`.** `isRealAdr` was a filename test alone, so `printf '' > runward/adr/ADR-0001-empty.md` produced `✓ Decision journal (≥1 ADR)`, `Current gate all gates passed`, `ADRs 1`, `1 decision(s) traced` in `status`, and `1 ratified ADR(s)` in the ISO 42001 pack — while the evidence layer printed *"an empty file is not a decision"* four lines below the tick, in the same pass. RWD-2026-0004 was closed in one layer and never asked of the other, and `adrDecision`'s own comment claimed *"the two layers now hold the same line"*. A **third** definition sat in `compliance.ts`: `readAdrs` accepted any `.md` that was not the template, and called the result *ratified* without ever reading a status. All three now share one predicate and one threshold (`ADR_MIN_CHARS`), and `dir` is a required argument so no call site can silently keep the name-only test. `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.14.0 (`isRealAdr`) and 0.10.0 (`readAdrs`) through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/artifact-state.test.js`, case *an ADR that is a name and nothing else does not fill adr/* — three rungs: zero bytes, a title-and-status stub, and a real decision, so neither a constant nor a name test passes. | Read the evidence layer's lines, not the presence ticks. |
| RWD-2026-0049 | **The evidence seal covered no `adr:` target.** `collectSealableEvidence` walked `applied` rows only, and an `adr:` pointer carries no `path`, so it contributed nothing even there. Measured 2026-08-26 on the shipped example: 0 of the 18 lock keys sat under `runward/adr/`, and replacing every ADR body with filler left `✓ seal intact`, `all gates passed`, exit 0. Of the three pointer kinds the grammar announces, `adr:` was the only one whose target could never be frozen — while a deviation's evidence IS its ADR. The seal now freezes the ADR behind any `adr:` pointer, from any status, resolved by `adrDecision`'s own lookup rather than a second one. `class` = `unproven-claim`, `effect` = `exit-code`, `affected-from` = 0.33.0 (when the seal shipped) through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/gate-missing-nets.test.js`, case *the seal freezes `adr:` targets*, with the opposite direction asserted: an ADR nobody cites is not evidence and the seal does not invent it. | Commit the ADRs and read the diff; the seal was not watching them. |
| RWD-2026-0050 | **The duplicate-cell census read `applied` rows only, and skipped the two columns where copying is free.** The `byEvidence` accumulation sat below the applied-only guard, so 36 rows of `| <slug> `found-by` = `adversarial-audit`. | deviated | ADR-0001 |` citing one unrelated ADR returned `evidence.duplicated` EMPTY, while the same cells under `applied` produced one entry. Nobody has to justify a deviation twice, which is exactly why that column is where a cell gets recopied. The census now runs for every status and reports which column was copied (`duplicated[].rules[].status`, an additive field on the ADR-0030 contract). Measured on an honest mission: the shipped example has zero repeated cells in any status, so widening cost no noise. `class` = `unproven-claim`, `effect` = `message`, `affected-from` = 0.32.0 through 0.36.2. | `test/unit/gate-missing-nets.test.js`, case *the duplicate-cell census reads EVERY status*, plus the opposite direction (two genuinely different reasons are not a duplicate). | Read the manifest column-wise before trusting a green. |
| RWD-2026-0051 | **The ReDoS screen had a cliff, and past the cliff it answered "safe".** The group reducer collapsed ONE group per pass under a flat budget of 20 and then fell through to `return false`. Measured 2026-08-26: depths 18-21 caught, **22 and beyond accepted**, and 25 nesting levels killed `check --strict` at 25 s with no verdict at all — in CI, a gate that renders nothing. A second class was invisible to both group scans: the same atom quantified twice in a row, `a*a*`. Measured against a 40-character subject, 4 repeats cost 13 ms, 6 cost 713 ms, and **8 exceeded 20 s** — `a*a*a*a*a*a*a*a*X` is a seventeen-character signature. The audit reached the wall with twenty; the real cliff is at eight. The reduction is now global (one whole level per pass), the budget comes from the input, an exhausted screen REFUSES instead of approving, and adjacent repetition of one atom is caught. The realistic carrier is not the operator attacking their own gate: it is `update --corpus <path>` (ADR-0057), which vendors a third party's rules. `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.32.0 through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/evidence-signature.test.js`, cases *the ReDoS screen has no depth at which it starts accepting* and *the same atom quantified twice in a row is refused*, the second asserting all nine shipped signatures stay legal. The pair that pinned the old bound is replaced by its inverse, on the record. | Read a vendored corpus's `signature:` lines before adopting it. |
| RWD-2026-0052 | **The `n/a` reason floor was a floor on keystrokes.** `trivialReason` refused under 8 characters, so `xxxxxxxx` cleared it and `xxxxxxx` did not: a manifest whose every `n/a` said `xxxxxxxx` passed, which is the cheapest green mission the audit assembled. The obvious repairs are wrong and were measured to be: both a word-count floor and a longer length floor refuse `no queue`, a complete reason, and redden an honest mission — the existing test pinned that edge precisely to stop this. The discriminator is lexical degeneracy, so the floor is three distinct characters, below every real reason (the shipped example runs 18 to 28) and above every degenerate one. **Recorded as a limitation, not closed**: an `n/a` cell is prose, the gate does not read prose (GATE_NON_SCOPE), and `abcdefgh` still clears this. The only real check on an `n/a` is a human who knows the system. `class` = `unproven-claim`, `effect` = `exit-code`, `affected-from` = 0.32.0 through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/conformance-adr-gate.test.js`, case *a reason that is one character repeated is a placeholder, however long*, with the `no queue` edge asserted in the same test so the fix cannot become a stricter length in disguise. | Read the `n/a` column yourself. The gate never claimed to. |
| RWD-2026-0053 | **Nothing verified that a `test:` target was a test.** Measured 2026-08-26 on an UNSIGNED rule, so no other check could account for the result: `| provider-llm-auto-detection `found-by` = `adversarial-audit`. | applied | test:runward/framing.md::of |` — a prose deliverable of the mission declared as the test that proves a rule — returned **exit 0**. The substring check found `of` in the document and the pointer was counted as behavioural evidence. `check` is not a runtime and says so; what it can state is that no test runner executes a document. The refusal is on the EXTENSION alone and deliberately not on a name convention: `*test*`/`*spec*` would refuse Rust `#[cfg(test)]` blocks and Go table tests living in ordinary source files, which are real tests in real projects. **"Is this a test?" is not decidable from a path** — the form that proves a test RAN is `test:` at a committed JUnit report (ADR-0056), where the case's result is read structurally. `class` = `unproven-claim`, `effect` = `exit-code`, `affected-from` = 0.32.0 through 0.36.2. | `test/unit/gate-missing-nets.test.js`, case *a `test:` pointer at a prose document is refused*, with both opposite directions: a real test file still clears, and the same document is still legitimate evidence under `file:`. | Cite a JUnit report, not a source file, when the claim is that a test passed. |
| RWD-2026-0054 | **`spec-check` answered *every criterion is linked* on six ordinary spec shapes.** With two criteria pointing at files that do not exist, `specConformance` returned exit 0 on: two acceptance sections (only the first read), a `### Sub-heading` inside the section (which ended it), criteria written as a markdown table (total 0), criteria written as prose (total 0), an EMPTY acceptance section (verdict `linked`, total 0), and a decoy heading such as *"Notes on acceptance criteria"* placed above the real one (which was then never read). The positive control moves: the same broken pointers as the first list item of the first section return exit 1 with the right reason, so the pointer layer is genuinely repaired and the SECTION WALK was not. The sting is that `readManifest`, in this same codebase, carries *"a second section above the real one hid it entirely"* and *"a `### Sub-heading` after the table did not end the section"* in its comments as bugs an adversarial audit already found — the lesson was learned in one parser and never carried to its neighbour, which is RWD-2026-0048's shape exactly. Now: every acceptance heading is read; only a heading of the same or higher level ends a section; a criterion is a list item OR any line carrying a `file:`/`test:` pointer, so tables and prose count while separator and header rows stay out by shape; and a section naming nothing is refused rather than reported linked — the vacuity RWD-2026-0003 named. `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.35.0 (when `spec-check` shipped) through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/spec-check-shapes.test.js`, one case per shape with the control asserted first, plus *every honest shape still passes* over six honest specs so the fix cannot be a stricter parser in disguise. | Put every criterion as a list item in one acceptance section, and read `total` — a spec-check that checked nothing reports 0. |
| RWD-2026-0055 | **A pointer written the way markdown is written was refused.** `- login works \`file:src/auth.ts#login\`` parsed its symbol as ``login` `` and the gate answered *symbol "login`" not found (identifier-boundary match)* — an undue refusal on a pointer that is correct, sending the operator to look for a defect in their own code. `clean()` stripped `)`, `.`, `,` and `:` from the end of a token and not the backtick, and the symbol is sliced after that. It surfaced only where the pointer ENDS THE LINE: in a manifest cell something usually follows it, which is why the manifest surface passed the same input. No path segment and no identifier ends in a backtick. `class` = `undue-refusal`, `effect` = `exit-code`, `affected-from` = 0.32.0 through 0.36.2. `found-by` = `while-reproducing`. | `test/unit/spec-check-shapes.test.js`, case *a pointer written the way markdown is written is not refused*. | Write the pointer without backticks. |
| RWD-2026-0058 | **The OSCAL pack declared a control `implemented` when its only evidence was a sentence.** A manifest row reads `applied` whether the gate OPENED its evidence or accepted prose on the operator's judgment. Measured 2026-08-26: rewriting one row to *"we discussed contracts at length and everyone agreed"* left `check --strict` at exit 0 — correctly, ADR-0004 allows prose — and the terminal printed `! 4 row(s) are prose: accepted on your judgment, never verified`. The pack then reported **asi-07 `implemented`**, and the word *prose* appeared **zero** times in the whole readiness document: the typed/prose distinction the verdict surface carries was dropped at the boundary where no prose follows the artifact. `implemented` is OSCAL's word for *the control is implemented*. A control resting on a sentence nobody checked is `partial`. Every requirement now also carries `runward-evidence-depth` in four states, naming which rules rest on prose. `class` = `unproven-claim`, `effect` = `message`, `affected-from` = 0.33.0 through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/compliance.test.js`, case *a control whose evidence is PROSE is not `implemented`*, with the depth asserted in both directions so the fix cannot be a blanket downgrade. The first draft of the depth prop said *"evidence opened and checked"* on a FRESH mission where nothing had been opened — the same overstatement, caught by the golden fixture before it shipped. | Read `check --strict`'s prose count beside the pack. |
| RWD-2026-0059 | **Two public documents said runward runs nothing of yours, in a build where `check --hooks` runs your shell commands.** Measured 2026-08-26: a hook of `sh -c "echo HOOK-RAN > /tmp/proof.txt"` writes that file **outside the audited repository** and `check --strict --hooks` exits 0. README:164 read *"runs nothing of yours"*; runward's own `port-contract.md` read *"no operation ever requires approval because none acts on the world"* — with `--hooks` listed in the signature nine lines above it, in the artifact TOR §8.4 offers to a DO-330 evaluator. The CODE was precise throughout (`hooks.ts`: *"Opt-in only: runward's own gate never runs these ... so a clone cannot run anything by surprise"*) and the run prints `Hooks · before — N ok`. Only the prose overstated. `class` = `unproven-claim`, `effect` = `message`, `affected-from` = 0.30.0 (when the hook seam shipped) through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/hooks-claim.test.js`. The guard derives from the CODE — while `check.ts` calls `runHooks`, every "runs nothing" sentence must name the exception or be explicitly scoped away from it in a paragraph that does; delete the seam and the guard retires itself. A second case asserts the seam stays opt-in and that no other command reaches it. | Do not pass `--hooks`. Without it nothing of yours runs. |
| RWD-2026-0060 | **The register that records this product's defects had two of its own.** `RWD-2026-0022` and `RWD-2026-0023` were each **defined twice**: on 2026-08-08 for a declared limitation (the seal date; the 31-of-45 floor) and again on 2026-08-17 for a wrong verdict (the JUnit adapter; `spec-check` verifying the path only). Two numbering series had been continued from the same point. Both meanings are cited elsewhere BY NUMBER — TOR-028 and regulated-adoption §5.4 mean the seal date, ADR-0056 meant the JUnit adapter — so the collision made a citation ambiguous rather than merely untidy. The header also still read `Describes: runward 0.34.0` while carrying entries for 0.36.0, 0.36.1 and 0.36.2. The first assignment keeps its number; the 2026-08-17 pair became RWD-2026-0056 and RWD-2026-0057, ADR-0056's citation was updated, and a redirect note stands in the register for anyone holding the old numbers. The TOR register has carried a uniqueness-and-contiguity guard from the start; this one had none. `class` = `unguarded`, `effect` = `message`, `affected-from` = 2026-08-17 through 2026-08-26. `found-by` = `while-reproducing`. | `test/unit/known-defects-register.test.js`: unique, contiguous from 0001, every RWD id cited anywhere under `docs/` is defined here, and the header names the package version. Three of the four are measured red against the pre-fix register. | Resolve an ambiguous citation by its subject, not its number. |
| RWD-2026-0061 | **`compliance` called a governance file *present* in the same pass where `check` called it a raw template.** `gatherComplianceInputs` decided by `existsSync`; `check` decides by `artifactState`. Measured 2026-08-26 on a mission fresh out of `init`, where nothing had been written: `check` printed `○ Threat model — raw template` and `○ Evaluation rubric — raw template`, and the compliance summary printed `Governance  threat model present, eval rubric present` — two of its four head lines green on a mission with no work in it. Both layers now ask the same function the same question, and answer in the SAME WORDS: two vocabularies for one fact is how a reader concludes the surfaces disagree when they do not. `class` = `unproven-claim`, `effect` = `message`, `affected-from` = 0.10.0 through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/compliance.test.js` fixtures cover the filled direction; the fresh-mission direction is exercised end to end by the golden test's `init` path. | Read `check` before the pack. |
| RWD-2026-0062 | **The README said *six phases, gated*; the gate gates five.** `GATED_DELIVERABLES` holds `architecture.md`, `execution-topology.md`, `floor.md`, `governance/threat-model.md` and `handover.md`. Phase 4 (`iterate`) has no gated deliverable, and that is a **decision**: [ADR-0033](../adr/ADR-0033-status-reports-real-lifecycle-position-state-and-reopenings.md) rejects adding it because *"`iterate` has no deliverable that is 'filled once and done'"* — a posture, not a milestone. The reasoning was traced in the ADR and never carried to the document a reader meets first, so the product's headline claim overstated its own gate by one phase. `class` = `unproven-claim`, `effect` = `message`, `affected-from` = 0.1.0 through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/conformance.test.js`, case *the README's phase claim matches the number of phases the gate actually gates* — derived from `GATED_DELIVERABLES`, so the day a sixth phase IS gated the guard is what names the stale sentence. It also asserts every gated deliverable is named beside the claim, so the count cannot be right by luck while the list is wrong. | Read `GATED_DELIVERABLES`, not the heading. |
| RWD-2026-0063 | **The summary misnamed every failure that was not a missing deliverable.** `strictGaps` counts four different things — rule-conformance violations, corpus divergence, a broken evidence seal, unratified decisions — and the summary rendered the total as *"N floor rule-conformance gap(s)"* whatever it counted. Measured 2026-08-26: change one sealed file and `check --strict` printed `Current gate all gates passed`, `! 1 floor rule-conformance gap(s)`, and then `Fill the deliverable(s) named above` — with **no deliverable named and every one of them filled**. The operator is sent to the wrong file, and a summary that misnames the failure costs more than no summary. The verdict now carries `strictBreakdown` (conformance/corpus/seal/unratified), the summary names each class, the `Next` gesture matches what actually failed, and the trailing clause about artifacts and rules only prints when one of those is what broke. `class` = `unproven-claim`, `effect` = `message`, `affected-from` = 0.33.0 through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/verdict.test.js`, case *strictBreakdown sums to strictGaps, on a clean mission and on each failure class* — the parts must account for the whole, so a new contributor to the total cannot land unnamed, and the seal case asserts `conformance === 0`, which is the defect itself. | Read the ✗ lines, not the summary. |
| RWD-2026-0064 | **The corpus-drift remedy prescribed a command that does nothing for two of its three cases.** One line was printed under all of them: *"Run `runward update` to restore it, or `runward update --force` to take the package version."* Measured on the shipped example, each case run end to end: `update` clears **missing** (exit 1 → 0); for **edited** it is a no-op and the gate stays red at exit 1, only `--force` clears it; for **extra** NEITHER clears it — the file has to be removed, or the corpus vendored deliberately with `update --corpus <path>`, after which the lock records the rule and the gate correctly asks for it in the manifest instead. A prescription that does nothing is worse than none: the operator runs it, sees the same red, and concludes the gate is broken. Each line now carries the gesture measured to clear it. `class` = `unproven-claim`, `effect` = `message`, `affected-from` = 0.32.0 through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/verdict.test.js`, case *each corpus-drift case prescribes the command that actually clears it* — one assertion per case, including that the edited line says why the obvious command does nothing. | Use `--force` for an edited rule; remove or vendor an extra one. |
| RWD-2026-0065 | **The two envelopes designed for a consumer who reads nothing else about runward were the two that carried no caveat.** `GATE_NON_SCOPE` reaches the terminal, the readiness documents, the OSCAL requirements, the SARIF rules and the in-toto verdict predicate — and not the SLSA VSA, nor the bundle predicate. Those are exactly the two a policy engine ingests without a human in the loop. `sarif.ts` states the discipline in a comment (*"so a consumer that keeps the findings and drops the non-scope has to drop it deliberately"*) and `compliance.ts` states the principle (*"A caveat that stays home is a caveat that was not made"*); neither was applied here. The VSA now carries it in `policy.annotations`, which is the in-toto ResourceDescriptor's own field for metadata, so it rides INSIDE the SLSA VSA v1 shape rather than beside it and a strict validator still accepts the document (`test/intoto-schema.js` passes). The bundle predicate is runward's own type and carries it directly. `class` = `unproven-claim`, `effect` = `message`, `affected-from` = 0.35.0 through 0.36.2. `found-by` = `adversarial-audit`. | `test/intoto-schema.js` for the shape; the field's presence is asserted end to end by emitting both envelopes. | Read `docs/interop.md` §4 before writing a policy on either envelope. |
| RWD-2026-0066 | **The OSCAL mapping spec offered a three-state scale of which one state never occurs.** `planned` requires an ASI category with no rule mapped to it. Measured 2026-08-26: the 64 rules runward ships map between them to **all ten** categories (the thinnest are ASI05 and ASI07, one rule each), so the first row of the derivation table never fires on a mission using the shipped corpus — and reaching it by deleting a rule instead makes `check --strict` refuse the mission for corpus divergence. An assessor reading three states as three outcomes this pack will show them is reading one more than exists. The spec now says so, names the case where `planned` IS reachable (an organisation vendoring its own corpus under ADR-0057 with a category uncovered), and the same edit brings the `implemented` row in line with what the code now requires (gate accepted, and no contributing row resting on prose). `class` = `unproven-claim`, `effect` = `message`, `affected-from` = 0.33.0 through 0.36.2. `found-by` = `adversarial-audit`. | Measured by enumerating `asi:` across `templates/rules/`; no category is unmapped. | Treat `partial` as the pack's floor, not its middle. |
| RWD-2026-0067 | **Two emission flags on one command silently produced one document.** `--json`, `--sarif`, `--vsa` and `--attest` each write a different document to stdout, and the resolution was an undocumented precedence chain (`vsa` → `sarif` → `attest`). Measured 2026-08-26: `check --strict --json --sarif` emitted a SARIF, abandoned the ADR-0030 contract, and exited 0 — a CI that asked for both got one and could not tell which. Second half: outside a mission, ANY emission flag printed `{"verdict":"no-mission"}` on stdout, so a consumer that asked for SARIF received a JSON that parses and is not SARIF. Both are now refused — combined flags are misuse (exit 2, named), and a document-typed request with no mission writes NOTHING to stdout and says so on stderr, which every parser fails on loudly. `--json` keeps the no-mission object: that shape is its contract. `class` = `unproven-claim`, `effect` = `message`, `affected-from` = 0.35.0 through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/check-contract.test.js` covers `optionFault`; the combination is refused there, purely, without spawning a process. | Run `runward check` once per document. |
| RWD-2026-0068 | **`check --strict --freeze --json` reported `seal: {present: false, count: 0}` on the pass that wrote the seal.** The machine payload is built from a verdict computed BEFORE the freeze block runs, so the same run printed `no evidence seal` and, seven lines below, `✓ sealed 29 evidence file(s)` — and the archived attestation recorded the mission as unsealed. It under-declares rather than over-declares, so it fabricates no green; what it does is make a CI branching on that field conclude no seal exists immediately after creating one. The verdict's seal state is now re-read after the write. `class` = `unproven-claim`, `effect` = `message`, `affected-from` = 0.33.0 through 0.36.2. `found-by` = `adversarial-audit`. | Measured end to end: the sealing pass and the pass after it now report the same `seal` object. | Re-run `check --json` after `--freeze`. |
| RWD-2026-0069 | **`SOURCE_DATE_EPOCH` reached only the VSA, and `docs/interop.md` was wrong about it twice in one sentence.** It read: *"The VSA is the **one** runward emission that is not byte-idempotent by default … Set `SOURCE_DATE_EPOCH` and the emission is byte-identical again."* Measured 2026-08-26: the evidence lock (`sealedAt`) and the OSCAL pack (`metadata.last-modified`) also carry a wall clock and both **ignored** `SOURCE_DATE_EPOCH` — they were pinned by `RUNWARD_NOW`, a variable that document never names. So the set was larger by two, and the convention named did not reach them. `generationDate()` now honours `SOURCE_DATE_EPOCH` for every clock runward writes, with `RUNWARD_NOW` keeping precedence as the explicit per-run override, and interop.md lists the three emissions that carry a clock and the two variables that pin them. `class` = `unproven-claim`, `effect` = `message`, `affected-from` = 0.35.0 through 0.36.2. `found-by` = `adversarial-audit`. | Measured in both directions: `SOURCE_DATE_EPOCH=1000000000` now yields `sealedAt: 2001-09-09` and `last-modified: 2001-09-09T00:00:00Z`, and `RUNWARD_NOW` still overrides it. | Set `RUNWARD_NOW`; it always worked. |
| RWD-2026-0070 | **The seal covered a cited symlink's TARGET and never the link, so re-pointing the link left `✓ seal intact`.** Keys are computed from the real path (correctly — a mixed real/logical pair once produced `../../../private/var/...` as a lock key), which meant a cited `evidence-alias.ts -> src/.../guard.ts` put `guard.ts` in the lock and nothing else. Measured 2026-08-26: after `--freeze`, re-pointing the link at a decoy file left `check --strict` at **exit 0** with the seal reported intact, while the pointer now resolved to a different file entirely. The link's own path is now sealed alongside its target — the hash follows the link, so a re-point changes it, and both entries stand so moving either is caught. Honest nuance from the auditor: the symlink has to be committed, so a reviewed commit would show it. `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.33.0 through 0.36.2. `found-by` = `adversarial-audit`. | Measured in both directions: the lock now carries link and target, re-pointing fails the seal by name, and an untouched mission still seals and passes. | Review the diff when a symlink enters `runward/` evidence. |
| RWD-2026-0071 | **`runward bundle` bound a file outside the project into a document about that project.** The subject name was `relative(cwd, abs)` with nothing stopping a climb out, so `runward bundle ../outside.txt` emitted a subject literally named `../outside.txt` — neither project-relative nor portable, and the comment on the line asserted it was both. ADR-0019's containment governs evidence; a bundle subject is the same claim one envelope out, and now holds the same rule. `class` = `unproven-claim`, `effect` = `message`, `affected-from` = 0.35.0 through 0.36.2. `found-by` = `adversarial-audit`. | Measured in both directions: an out-of-project artifact is refused at exit 2 with the boundary named, an in-project one still emits its relative subject. | Bundle paths inside the mission's project. |
| RWD-2026-0072 | **Nothing checked the SARIF runward emits.** `test/fixtures/` vendors an in-toto schema and an OSCAL schema and validates both emissions against them; the SARIF had no net at all. The auditor ran the missing one by hand — 22 mission states against the official OASIS schema, all valid — so the emitter is well formed today and nothing would have said if it stopped being. `test/sarif-shape.js` now runs on four mission states (clean, broken pointer, seal drift, `--through`) and checks every structural invariant a consumer relies on: version, `$schema`, one run, driver identity, unique rule ids, every result naming a declared rule, every level in the SARIF enum, every location carrying a relative uri that resolves in the checkout (RWD-2026-0041's defect), and byte-identical repeat runs. It began as structure only, with schema validation left as an open decision. **Closed 2026-08-27 by [ADR-0062](../adr/ADR-0062-the-sarif-schema-is-validated-against-the-official-one.md)**: the official OASIS schema is vendored (`test/fixtures/sarif_schema.v2.1.0.json`, provenance and sha256 recorded) and every one of the four mission states is validated against it, driven through `ajv-draft-04` — the same `ajv-validator` organisation as `ajv` and `ajv-formats`, both already dev dependencies, dev-only and never shipped. Verified to DETECT before adoption: a wrong `version`, a missing `runs` and a `level` outside the enumeration are each refused. The structural half stays and is not redundant — a schema cannot say that a uri RESOLVES in the checkout, and every uri of RWD-2026-0041 was schema-valid. `class` = `unguarded`, `effect` = `message`, `affected-from` = 0.35.0 through 0.36.2. `found-by` = `adversarial-audit`. | `test/sarif-shape.js`, wired into `npm test`. | None; the emitter was well formed. |
| RWD-2026-0073 | **The ADR-0054 boundary test was blind on the path that owns the exit code.** Five mutations on a copy of the built tree, measured 2026-08-26: a spawner imported into `dist/commands/check.js`, `await import("node:https")` in `verdict.js`, `createRequire(...)("net")` in `verdict.js`, and a `baseRef` in `src/lib/verdict.ts` — **all four left the four boundary tests green**; only a static `import __net from "node:net"` was caught. Four causes, all in the instrument: the closure started at `dist/lib/verdict.js` and `check.js` imports it and not the reverse, so the module deciding the exit code was never walked; the import regex required whitespace, so a dynamic import was invisible; the blindness guard matched `require(` but not `createRequire(`; and the base-ref grep covered `src/cli.ts` and `src/commands` and never `src/lib`, where the verdict lives. **This is verbatim the reevaluation trigger ADR-0054 wrote for itself** — *"the boundary test starts passing vacuously … e.g. a dynamic import"* — and the condition was met on the day it was written. The boundary itself never moved: the five enumerated crossings are unchanged. The instrument now walks a second, wider ring from `check.js` (18 modules) allowing exactly `node:child_process` from exactly `hooks.js`. `class` = `unguarded`, `effect` = `exit-code`, `affected-from` = 0.35.0 through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/runtime-boundary.test.js`. All five mutations re-run after the fix: every one now fails the suite, and the unmutated tree is green — the control this instrument had never been given. | None; no crossing had actually been introduced. |
| RWD-2026-0074 | **A directory the operator does not own decided the verdict.** `onDiskSpelling` split the ABSOLUTE path and listed every segment from `/`, so the case check read every ancestor of the project. Measured 2026-08-26 on a byte-identical project tree: `chmod 0755` on the project's PARENT gives exit 0; `chmod 0111` on the same parent gives **exit 1 with 21 pointers refused**, each telling the operator the gate cannot list a directory and to fix a path outside their project; `chmod 0755` again returns exit 0. That is the class that gets a gate switched off. Segments above the project are not part of what a pointer says and are not the operator's to fix, so the walk is now bounded to the project root. The bound is optional on the function: omitting it walks from the filesystem root as before, which checks MORE segments and never fewer — the safe direction for a caller that forgets. `class` = `undue-refusal`, `effect` = `exit-code`, `affected-from` = 0.32.0 through 0.36.2. `found-by` = `adversarial-audit`. | Measured in both directions: the ancestor permission no longer changes the verdict, and a genuine case error inside the project (`Guard.TS` for `guard.ts`) is still refused with the spelling to copy. The spelling-ladder suite stays green. | Move the repository, or fix the parent's permissions. |
| RWD-2026-0075 | **"Same working tree, same verdict" left out the second input, in four documents.** The rule corpus the gate judges against is the INSTALLED PACKAGE's, deliberately (`scaffold-lock.ts`: *"THE LOCK IS NOT THE AUTHORITY … the authority is the INSTALLED PACKAGE"*). So the installed version is a real input. Measured 2026-08-26 with the mission tree untouched: add one rule file to the installed package's corpus and `check --strict` goes exit 0 → **exit 1**; remove it and it returns to exit 0. The design is right and assumed; the CLAIM was not, and it read as a whole-truth in README, `docs/interop.md` (§ heading and intro), `runward/framing.md` and `runward/architecture.md`. All four now say the tree AND the version, and architecture.md names why: upgrading runward can change a verdict on an untouched tree, which is exactly why `verify` compares the version the attestation names against the one re-deriving it. `class` = `unproven-claim`, `effect` = `message`, `affected-from` = 0.32.0 through 0.36.2. `found-by` = `adversarial-audit`. | Measured end to end, both directions, on the shipped example. | Pin the runward version beside the tree. |
| RWD-2026-0076 | **Five crossings enumerated by ADR-0054, two with no test, and one test proving the wrong one.** `runtime-boundary.test.js` carried four tests: crossing 1 (twice), crossing 4, and one labelled *crossing 3* that actually proves crossing 5 (advisory-LLM purity). Crossing 2 (a long-lived process between invocations — daemon, watcher, auto-installer) and crossing 3 (runward holding a key, an identity or state the operator does not) had **no test at all**. Both held structurally; nothing would have reddened if they stopped. The mislabelled test is renamed and both missing crossings now have grep-level guards with their own positive controls. Worth recording: the crossing-2 guard reddened honest code TWICE before it was right — a bare `npm install` matched an instruction `init.ts` prints for the operator, and `\bwatch\s*\(` matched the prose *"Reopening watch (ADR-0033"* in a comment. Every alternative now names a call site. `class` = `unguarded`, `effect` = `message`, `affected-from` = 0.35.0 through 0.36.2. `found-by` = `adversarial-audit`. | `test/unit/runtime-boundary.test.js`, cases *crossing 2* and *crossing 3*, each asserting its own pattern fires on a real call and does NOT fire on prose. | None; neither crossing had been made. |
| RWD-2026-0077 | **The boundary test described `--hooks` as running "outside computeVerdict". It is an argument to it.** `hookFailed` is passed into `computeVerdict`, and `verdictFrom` reads `hookFailed === 0`, so an operator hook reaches the exit code. Measured 2026-08-26: a hook of `curl https://this-host-does-not-exist.invalid/` leaves `check --strict` at exit 0 (the opt-in guarantee holds) and takes `check --strict --hooks` to exit 1 with `gaps.hooks: 1` — a network call reaching the exit code. The mitigations are real and are now written down rather than implied: opt-in behind an explicit flag, documented by ADR-0008, carried in the threat model, and disclosed in `--json` and the attestation. Recorded alongside: the import closure stops at PACKAGE boundaries (it follows relative imports only, and `commander` already imports `child_process`), so what that file proves is that runward's OWN modules on the verdict path carry no crossing — the dependency surface belongs to the SBOM and the network-cut CI run. Both limits are stated at the top of the file. `class` = `unproven-claim`, `effect` = `message`, `affected-from` = 0.35.0 through 0.36.2. `found-by` = `adversarial-audit`. | The public wording is guarded by `test/unit/hooks-claim.test.js` (RWD-2026-0059); this entry covers the internal description. | Do not pass `--hooks` if the exit code must depend on the tree alone. |
| RWD-2026-0078 | **The `--corpus` boundary sentence was shown only to an operator who had already dropped the version.** `update --corpus` refuses a registry coordinate on purpose (resolving one would make runward a registry client — ADR-0054 crossing 1), and the refusal explains the boundary. Its guard was `/^@?[\w.-]+\/[\w.-]+$/`, which `[\w.-]+` stops at the second `@`: `@acme/rules` got the explanation and `@acme/rules@1.2.3` — the spelling npm prints and an operator actually types — fell through to a generic *"path not found"*. Measured 2026-08-26. Version and range suffixes are now part of the shape. `class` = `undue-refusal`, `effect` = `message`, `affected-from` = 0.36.0 (when `--corpus` shipped) through 0.36.2. `found-by` = `adversarial-audit`. | Measured across four spellings: `@acme/rules`, `@acme/rules@1.2.3` and `acme/rules@^1.2` all get the boundary sentence; a real path still gets *path not found*. | Drop the version when typing `--corpus`. |
| RWD-2026-0079 | **A mission citing no code read `100%` while the honest one read 87%.** Measured 2026-08-26 on the tree with the day's thirty-three fixes already in it: delete `code/`, point every `applied` row at one of the mission's own deliverables, and `check --strict` returns exit 0, verdict `clean`, `22 of 22 applied row(s) … (100%)`, `--freeze` seals six files and `compliance iso-42001` assembles the pack — on a mission citing no code, no test, no ADR. No check is wrong: every row does carry a pointer the gate opened. The gate tells the truth and the reader draws a conclusion it never made, which is RWD-2026-0003's phenomenon one level up (*"the emptiest missions produced the most reassuring output"*). Resolved by [ADR-0060](../adr/ADR-0060-a-vacuous-green-is-disclosed-not-refused.md) as a **disclaimer, not a refusal**: ADR-0054 makes this a documentary gate, so a documentation-only mission is legitimate and failing it would contradict the declared boundary. The prior art splits — pytest exits 5 on no tests, Jest fails unless `--passWithNoTests`, and ISA 705 issues a disclaimer of opinion — and the third is the one that fits a documentary gate. `evidenceFiles: {total, external}` is counted, said, and carried in the ADR-0030 contract; the coverage line now names where the pointers land. `class` = `unproven-claim`, `effect` = `message`, `affected-from` = 0.32.0 through 0.37.0-dev. `found-by` = `adversarial-audit`. | `test/unit/gate-missing-nets.test.js`, case *a mission that cites only its own documents is green AND says so*, both directions; `test/unit/check-contract.test.js` for the contract. Measured: 11 of 12 external on the shipped example, 0 of 1 on the code-free mission. | Read `evidence.evidenceFiles.external` beside the percentage. |
| RWD-2026-0080 | **`verify` compared predicate objects with `JSON.stringify`, which made KEY ORDER meaning.** Found 2026-08-27 while adding `evidenceFiles`: the field was inserted before `duplicated` in the emitted payload and after it in the re-derivation, and an honest attestation with byte-identical CONTENTS reported `differing: ["evidence"]`, `verified: false`. Aligning the two lists would have worked and hidden the trap until the next reorder — any future change to the payload's key order silently breaks `verify` with a message naming the wrong cause. The comparison now canonicalises object keys before stringifying. **Arrays keep their order**: there, order IS meaning (`duplicated` is sorted deterministically, so a different order is a different tree). `class` = `undue-refusal`, `effect` = `exit-code`, `affected-from` = 0.34.0 through 0.37.0-dev. `found-by` = `while-reproducing`. | Measured in both directions on a real attestation: an honest one verifies with `differing: []`, and flipping `evidence.typed` to 999 still gives `verified: false` with `evidence` named. | Keep the two key lists in the same order. |
| RWD-2026-0081 | **The bound added the previous day was passed in the wrong namespace, so it never engaged.** RWD-2026-0074 bounded the case walk to the project root; the bound was handed `baseAbs`, which is CANONICAL (`realpathOr(resolve(b))`), while the path being walked is LOGICAL (`resolve(b, p)`). On macOS `/var` is `/private/var`, so the prefix test failed on every temporary path and `onDiskSpelling` silently restarted at the filesystem root — the exact false red RWD-2026-0074 was filed to close, alive again one commit later. **The verification that accepted the fix could not have shown it**: it used a path already under `/private/tmp`, where the logical and canonical forms coincide. Found on the FIRST run of the conformance corpus ([ADR-0061](../adr/ADR-0061-the-spelling-brick-is-specified-by-a-corpus-not-by-its-implementation.md)), which builds its fixtures under `tmpdir()` and therefore asks the question on a path where the two namespaces differ. `class` = `undue-refusal`, `effect` = `exit-code`, `affected-from` = 0.37.0-dev (introduced 2026-08-26, never released). `found-by` = `conformance-corpus`. | `test/spelling-conformance.js`, case *ancestor-permissions-do-not-change-the-verdict* [RWD-2026-0074]. | Move the repository out of a symlinked temporary directory. |
| RWD-2026-0082 | **An ESCAPED parenthesis was refused as a catastrophic group.** `\(` is a literal, not a group, but the reduction cannot reduce it, so the fixpoint kept a parenthesis, the leftover check read that as *"I have no opinion"*, and RWD-2026-0051's fix — an exhausted screen REFUSES rather than approves — refused it. Measured 2026-08-27 on the shipped build: `sand\(box` and `a\)b`, ordinary signatures for matching a literal parenthesis, both answered `unsafe signature regex`. A false RED on correct work, which is the class RWD-2026-0074 is filed under: it is how a gate gets switched off. What kept it quiet is that a BALANCED escaped pair happens to reduce — `config\(secret\)` was accepted — so it fired only on the unbalanced case. **Found by an agent instructing the survivors of the very run that measured that fix**, while arguing why a mutant of the leftover-paren branch was a hole: the branch was both untested and wrong. Escapes are now neutralised into a non-parenthesis token before every structural question, which leaves the adjacent-atom scan (which reads `source` and treats `\\.` as one atom) untouched. `class` = `undue-refusal`, `effect` = `exit-code`, `affected-from` = 0.37.0-dev (introduced 2026-08-26, never released). `found-by` = `mutation-instruction`. | `test/unit/evidence-signature.test.js`, case *an ESCAPED parenthesis is a literal, not a group* — six escaped forms accepted, five dangerous shapes still refused in the same test, plus `sand\((a+)+box` to prove an escape does not launder a real group. | Write the signature without escaping the parenthesis, if the rule allows it. |
| RWD-2026-0083 | **The pack kept the pre-RWD-2026-0016 frontmatter delimiter, so a Windows checkout reports zero agentic-risk coverage.** RWD-2026-0016 taught the corpus readers to accept CRLF. `rules.ts` and `conformance.ts` carry `/^---\r?\n([\s\S]*?)\r?\n---/`; `compliance.ts` kept `/^---\n([\s\S]*?)\n---/`, and the constant is named `FRONTMATTER` in all three, so the divergence is invisible to a reader comparing them by name. Measured 2026-08-27 on a mission whose 65 rule files were rewritten CRLF, which is what `core.autocrlf=true` does on Windows: `runward rules --json` read 65 rules, 29 carrying an `asi:`, covering **10/10** categories, while `runward compliance iso-42001` wrote a readiness draft whose ten coverage rows all read `**no rule mapped — gap to assess**` and an OSCAL pack whose every requirement is `planned` — **0/10** on the same tree, in the same pass, at exit 0. The pack understates a mission the gate accepts, and the artefact leaves the building. Nothing could see it: this repository's `.gitattributes` pins `* text=auto eol=lf` (added 2026-08-17 for the first windows-latest leg), so runward's own tree checks out LF on every OS and its CI cannot produce the input, while a user's mission lives in the user's repository under the user's git config; and the smoke assertion that looks like it guards ASI coverage reads the rule files with its own unanchored `/asi:\s*\[([^\]]+)\]/` and never parses frontmatter at all, so it cannot see this parser disagree with the other two. The fix that made the project's own measurements honest is what hid this defect from them. `found-by` = `mutation-instruction`. | `test/unit/frontmatter-crlf.test.js` — two cases, both failing on the unfixed parser: every `FRONTMATTER` delimiter found in `src/lib` is compiled and run against a CRLF document (a property, so a copy of the old pattern under a new name still fails), and a CRLF rule must reach `gatherComplianceInputs`'s `asiCoverage`. | Add `* text=auto eol=lf` to the mission repository's `.gitattributes` and re-checkout `runward/rules/`, or clone with `core.autocrlf=input`. |
| RWD-2026-0084 | **One ADR status line, four readers, and they did not agree.** `readAdrs` printed the journal's Status cell from `/^\*\*status\*\*\s*:\s*(.+)$/im`; `adrStatusWord` decided ratification from `/^\*\*Status\*\*:\s*(.+)$/mi`; `readReopeningTriggers` decided which decisions are in force from `/^\*\*Status\*\*:\s*(.+)$/m`, narrow **and** without the `i` flag; and `resolvedSlugs` decided a draft was rejected from `/^\*\*Status\*\*:\s*rejected\b/im`. One line, four patterns, differing by one `\s*`. Measured 2026-08-27 on `**Status** : accepted`: the ADR journal renders `accepted` in all three readiness drafts while the same run's terminal counts the decision **not ratified**, and the reopening watch of ADR-0033 skips it, so a decision in force has its reevaluation triggers silently unwatched. On `**Status** : rejected` the fourth reader does not see the rejection, and ADR-0038's promise never to resurrect a resolved draft goes with it. The shape is not exotic: French typography *requires* a space before a colon, so it arrives from ordinary operators writing ordinary prose rather than from fuzzing. All 66 status lines in this repository and in the packaged example are written `**Status**:`, which is why no verdict in this tree ever flipped and why nothing saw it. Fixed by collapsing all four onto one reader, `adrStatusLine` in `mission.ts`, which already owns what an ADR file is; the direction is deliberate, the wider pattern honours a status the operator declared instead of dropping it. `found-by` = `mutation-instruction`. | `test/unit/adr-status-one-reader.test.js` — the invariant over eight spellings (whatever the pack prints in the cell, the ratified word is that cell's first token), the French case on its own, the end-to-end check that `gatherComplianceInputs` returns `status: "accepted"` and `ratified: true` together, the reopening watch seeing an ADR ratified with the French space, and a recurrence guard asserting that `mission.ts` is the only module in `src/lib` that spells the pattern at all. | Write the status as `**Status**: accepted`, with no space before the colon. |
| RWD-2026-0085 | **The register generator declared every survivor a survivor of `evidence`.** `scripts/mutation-register.mjs` wrote `## Module: evidence` as a literal — true for as long as evidence was the only module measured, and on 2026-08-27, the day `compliance` entered the register with 300 filed survivors, it filed all of them under the wrong module: the trust artefact stated something false about more than half its rows. The falsehood never reached a commit — the suite's own guards reddened in the same working tree (see RWD-2026-0087) before anything was pushed. The shape is RWD-2026-0083 one layer up: a constant encoding "there is one X" survived the day there were two, and nothing between the constant and the artefact re-derived the fact. Fixed by reading the module off each verdict's `stableKey` — the key names its module precisely so nothing has to assume it — grouping one `## Module:` section per module found, and refusing a verdicts file whose keys span two modules. `found-by` = `existing-guard`. | `node scripts/mutation-register.mjs` prints a per-module summary; `test/unit/mutation-key.test.js` requires every committed key to name a module the stryker perimeter actually covers. | None needed — repository apparatus; nothing of it ships in the npm package. |
| RWD-2026-0086 | **The guard on the register's key shape could not fail.** `test/unit/mutation-key.test.js` split committed keys on `String.fromCharCode(31)` while the separator `scripts/mutation-key.mjs` exports is `\x01`, so every key split into one part, the expected shape — computed through the same wrong split — was also one, and the assertion compared 1 to 1 over every committed verdict. A false green in the apparatus, the family the 2026-08-14 audit named on JUnit and spec-check, here guarding the register itself. Pointed at the real separator it failed at once, and for a second reason: its statement was wrong too — a key has six components, plus an ordinal from the second textually identical sibling on a line on. Instrument and spec both wrong, composition green. Fixed by importing `SEP` and stating the property (six parts, or seven where the seventh matches `#n`). Effect on prior results examined, per the defective-instrument rule: no committed key ever changed shape while the guard was blind — the corrected assertion passes over all 568 committed keys. `found-by` = `while-reproducing`. | The corrected shape assertion in `test/unit/mutation-key.test.js`, measured red against the pre-fix pair (wrong separator, wrong statement) and green on the committed register. | None needed — repository apparatus; nothing of it ships in the npm package. |
| RWD-2026-0087 | **The register's row parser assumed a function name is one token, so module top-level code broke its neighbour's count.** `test/unit/mutation-register.test.js` matched headings with `### (\S+) — N survivor`; the section `### (top level) — 1 survivor(s)` did not match, its row was appended to the previous function's table, and the guard fired as `symbolPresent: heading says 2, table holds 3` — loud and mis-attributed rather than silent, one function up from the cause. It fired on the first `(top level)` section the register ever carried (2026-08-27, the compliance filing): the declared-count-vs-rows guard did exactly what it was built for, and the defect sat in the parser beside it. Fixed with a lazy `(.+?)`. `found-by` = `existing-guard`. | The same declared-count-vs-rows guard, now over a register whose committed state carries a `(top level)` section. | None needed — repository apparatus; nothing of it ships in the npm package. |
| RWD-2026-0088 | **The register asserted it described the tree, for a module the same commit had changed.** `c17b622` (2026-08-27) fixed `unsafeSignature` — it inserted the escape-neutralising `.replace(/\\./g, "E")` that RWD-2026-0074 needed — and regenerated the mutation register in the same commit, under the title *the register describes the tree again*. No `evidence` ratchet ran afterwards: measured 2026-08-28 across every mutation-ratchet run since that commit, not one carried an `evidence` leg. When one finally did, it refused: six filed verdicts described a source line that no longer exists, and five survivors the new line produces were carried by nothing. The freshness gate of ADR-0059 exists precisely to catch this, and it did — a day late, because it had not been asked. The shape is worth naming: the register was regenerated from verdict FILES, which is a formatting step, while the claim it makes is about a MEASUREMENT, and nothing tied the two together at commit time. Cleared by re-measuring the module, sweeping the six, and instructing the five by measurement — a deterministic differential fuzz of 60000 generated signatures plus targeted probes, which filed three `hole` (one of them a false negative on the catastrophic-backtracking screen: with the alternation scan narrowed, `(a|bc)+` reads SAFE) and proved two `equivalent` against a control in the same expression. `found-by` = `existing-guard`. | The per-module ratchet, run for `evidence`, `conformance` and `check-contract` and green on all three; the same run is what refused before the sweep. | None needed — repository apparatus; nothing of it ships in the npm package. |
| RWD-2026-0089 | **A module collected its neighbour's chunks, because a module name is not a prefix.** `territory` and `territory-map` are both in the mutation perimeter, and both the workflow's artifact pattern (`chunk-territory-*`) and the merge's file filter (`startsWith("territory-")`) accept `territory-map-…`. Measured 2026-08-29 on a whole-perimeter run: the `territory` leg reported *10 of 7 chunk(s) came back* — its own seven plus territory-map's three — and refused. The direction is safe, not a false green: `mutation-ratchet.mjs` selects its file by `/territory.`, so it could never have compared the wrong module; what it does is report a COMPLETE measurement as incomplete, which blocks the gate. The reason it took this long to see is the reason it is worth recording: the same message is in the release-triggered run of 0.37.0 on 2026-08-27, where it sat among fifteen red legs that were all dismissed as the expected backlog on uninstructed modules. A workflow whose failures are routinely expected is a workflow whose real failure is invisible — which is also why `mode: measure` was added the same day, so that a refusal asked for on purpose stops being filed as an incident. A THIRD site surfaced on 2026-08-29, and it was the one that would have been permanent: `mutation-ratchet.mjs` selected a module's filed verdicts with `stableKey.startsWith(moduleName)`, so the day `territory` was first instructed its ratchet pulled in all 45 of `territory-map`'s verdicts and reported them as survivors the tree no longer produces. The workflow and the merge fail loudly on a chunk count; this one would simply have compared the wrong register against the right measurement, every run. Separated by the KEY SEPARATOR, a control character that cannot occur inside a module name. Fixed on all three sides: artifacts are named `chunk-<module>--<offset>` so the pattern cannot cross a name boundary, and the merge matches `<module>-<digits>` exactly, accepting both shapes the tree produces (a line range locally, a counter in the workflow) and rejecting a neighbour, because `m` is not a digit. `class` = `unguarded`, `effect` = `exit-code`, `affected-from` = 2026-08-24 through 2026-08-29. `found-by` = `existing-guard`. | `test/unit/mutation-chunk-merge.test.js` — the key-selection case (a fixture that reproduces the collision, then asserts the separator refuses it and the module still selects its own), plus a chunk directory holding both modules in both naming shapes: `territory` must merge to one file, and `territory-map` must still find its own, so a filter tightened until it matches nothing fails too. Measured red against the pre-fix filter. | None needed — repository apparatus; nothing of it ships in the npm package. |
| RWD-2026-0090 | **The register said which function a survivor lives in, and for seven rows it named one the line is not in.** The attribution recorded where a `function` STARTS and never where its span ends, so every line below the last function declaration in a module belonged to it forever — including the module-level data constants declared after it. Measured 2026-08-28 over the committed verdicts: `conformance` L230 was filed under `adrDecision` while it lives in the constant `ADR_SET_ASIDE`, L318 under `decisionCoverage` while it lives in `PATH_TOKEN`, `attestation` L27 under `rawFileSha256` while it lives in `RUNWARD_PREDICATE_TYPE`, `evidence` L28 under `symbolPresent` while it lives in `POINTER_PREFIX`; nine further rows said `(top level)` where a named constant was the truer answer. A trust artifact that states where a defect lives must not state it wrongly — this is RWD-2026-0085 one field over, a rule true of the common case (a mutant sits in a function) applied where it is not always true. Fixed by moving the attribution into `scripts/mutation-key.mjs`, beside the key it feeds, so one implementation serves both sides (ADR-0059 criterion 5), and by widening it: a `const`/`let`/`class` at column zero opens a span of its own, which names the constant AND closes the function above it. The 14 affected keys were migrated by joining filed verdicts to a fresh measurement on the WHOLE stable key — a first attempt joined on the verdict's `line` field and moved 145 keys instead of the 16 the measurement predicted, because `line` dates from the report a verdict was filed against and is documentation, not identity; that is the offset drift the stable key exists to remove, reintroduced by using it as identity. `found-by` = `while-reproducing`. | `test/unit/mutation-key.test.js` — the attribution over a module whose constants follow its functions, asserting a line inside a trailing constant is named by that constant, that a line inside a function is still named by it, and that an INDENTED declaration does not close its enclosing function; measured red against the old rule. Plus every re-measured module's ratchet exit 0 against keys built by that one implementation. | None needed — repository apparatus; nothing of it ships in the npm package. |
| RWD-2026-0091 | **The corpus verdict published its `missing` list in the order the filesystem returned, not in name order.** `corpusDivergence` sorts the mission-side listing and did not sort the package-side one, so `corpus.missing` in `check --json` came out in reader order. A directory reader walks UTF-8 BYTES while `Array#sort` compares UTF-16 CODE UNITS, and the two disagree above the BMP: measured 2026-08-29 on a package holding `\u{1F600}-rule.md` and `\uFFFD-rule.md`, `readdir` returned the second first (EF BF BD before F0 9F) while the sorted order is the reverse, and the published list followed the reader. Below the BMP the two orders coincide on APFS and diverge on ext4 and XFS, so the same tree published a different document depending on which runner read it — a machine surface that is not comparable across runs, in a project that holds every other emission to determinism (`test/sarif-shape.js` asserts two successive runs are byte-identical). No verdict, exit code or set membership moves: this is an ordering defect in a published document, found while building the net for a module the mutation campaign had just instructed. Fixed by sorting the package-side listing like its mission-side twin. `class` = `unguarded`, `effect` = `message`, `affected-from` = 0.35.0 through 0.37.0. `found-by` = `mutation-instruction`. | `test/unit/scaffold-lock-directory.test.js` — a package whose two rule names separate byte order from code-unit order, asserting `missing` comes back sorted; measured red against the unsorted listing. The same file guards the neighbouring holes the campaign found: a `.DS_Store` demanded as a missing rule, a subdirectory reported as an extra one, and an unshipped house rule making `check` die on ENOENT. | Compare the SET, not the order, when reading `corpus.missing` from a runward older than the fix. |
| RWD-2026-0092 | **The whole-net pass reported a mutant as caught because its own splice did not parse.** `scripts/mutation-wholenet.mjs` applies a mutant by SPLICING Stryker's `replacement` text at the reported offsets. That field is an AST node's text, valid where the AST puts it and not necessarily where a splice puts it. Measured 2026-09-01 on `conformance` L155: `(cols[0] ?? "").trim()` has the replacement `cols[0] ?? ""`, and splicing it into a `&&` chain yields `X && cols[0] ?? "" && Y` — `??` mixed with `&&` without parentheses, a SyntaxError. Every leg then failed at module load, the first one won, and the mutant was filed as caught by the self-gate. It had been logged that way three times (2026-08-28, twice on 2026-09-01) while a calm re-measure found the leg green each time; the verdict carried the discrepancy rather than hiding it, which is how the third occurrence was recognised as a pattern instead of noise. Stryker itself re-prints from the AST and keeps the parentheses, which is why the same mutant SURVIVES pass 1 — the two passes were mutating different programs. A trial whose own splice does not parse measures nothing, and reporting it as a detection turns an apparatus fault into a verdict about the code: the exact class of false green this register exists to refuse, pointed at itself. Note the trap inside the trap: `node --check` does NOT see it, because it parses a `.js` file as a script where that text is legal — the file has to be imported as a module. Fixed by verifying the spliced file loads as an ES module before any leg runs, and filing the trial `unapplicable` when it does not. `class` = `unguarded`, `effect` = `message`, `affected-from` = 2026-08-19 through 2026-09-01. `found-by` = `while-reproducing`. | `test/unit/mutation-wholenet-provenance.test.js` — a splice that does not parse is detected and refused as a detection, measured red against the pre-fix pass. | None needed — repository apparatus; nothing of it ships in the npm package. |
| RWD-2026-0093 | **A single reading was accepted as a detection, and two of the pass's own legs are unstable for reasons the mutant does not cause.** The whole-net pass called a mutant caught the first time a leg's exit differed from the baseline. Two mechanisms make one reading unreliable, both measured 2026-09-01. A leg exceeding its 120 s bound is reported as the sentinel `timeout`, which is never equal to a baseline exit code — so a leg merely SLOWED by a busy machine is indistinguishable from a leg the mutant broke; three `equivalent` verdicts on `territory-map` came back "caught" that way while a second driver ran, and the module re-measured quietly reported 0 of 16 caught where the contaminated run reported 3. And `self-gate` JUDGES THIS REPOSITORY, so a concurrent write to the tree changes its answer for reasons unrelated to the mutant. Two verdicts had already been re-filed `defence-in-depth` on those readings and are returned to `hole` with the contamination recorded on them. Pass 1 has refused this since the start — a Timeout is not a kill until it reproduces alone — and pass 2 did not. Fixed by re-running any differing leg once and counting only a difference that repeats, and by recording what the leg actually returned (`observed`/`expected`) so the next occurrence is self-diagnosing instead of arguable. `class` = `unguarded`, `effect` = `message`, `affected-from` = 2026-08-19 through 2026-09-01. `found-by` = `while-reproducing`. | The pass's own ledger, which now records the observed and expected values per detection; and the re-measure of `territory-map` and `conformance`, quiet, reporting 0 and 3 caught against 3 and 4 before. | None needed — repository apparatus. |
| RWD-2026-0094 | **The whole-net resume cache was keyed without its module, so a verdict could be replayed for a different file.** The ledger of `mutation-wholenet.mjs` keyed each trial on `line|column|endLine|endColumn|mutator|replacement` — a POSITION, with no module — and recorded no module either, so a mutant at the same position in two modules shared one verdict and nothing afterwards could detect the substitution. Fourth site of the family RWD-2026-0089 names, and the first where the collision is not hypothetical: measured 2026-09-01 across the sixteen current reports, FOUR positions are shared between modules — `conformance`+`territory` at line 304 (two mutants) and `spec-conformance`+`tool-adapters` at lines 45 and 65. The cache had 670 entries and no collision yet, which is exactly what the third site looked like the day before it bit. Fixed by putting the module in the key and in the entry; the existing ledger was migrated by attributing each position to the module whose report contains it, dropping 205 entries that matched no current report and purging the two modules being re-measured. `class` = `unguarded`, `effect` = `message`, `affected-from` = 2026-08-19 through 2026-09-01. `found-by` = `while-reproducing`. | `test/unit/mutation-wholenet-provenance.test.js` — the ledger key carries its module, and a fixture reproducing a cross-module position collision is refused. | None needed — repository apparatus. |
| RWD-2026-0095 | **`verify` never compared the two tables an assessor reads first, and never said so.** The predicate of `check --attest` carries `deliverables` (which phases hold which artifacts, in which state), `conformance` (every strict violation, by scope and rule) and `horizon` (the declared construction prefix). The 2026-08-26 fix that taught `verify` to re-derive the predicate body (RWD-2026-0042) covered `evidence`, `corpus`, `seal`, `criticalScope`, `gateNonScope` and the `gaps` counters — and left these three neither compared nor named in `notReDerived`, the worst combination: not checked, and not disclosed as unchecked. Measured 2026-09-02 by an adversarial investigation on the shipped binary: an attestation whose `deliverables` table described a mission that does not exist, whose `conformance` table was invented wholesale, and whose `horizon` was falsified answered `verified: true`, exit 0 — printed by the very command whose one sentence is "do not trust this attestation for this tree". The cause is the same shape RWD-2026-0090 names: the conformance rows were assembled inline in `check.ts`, pushed beside the render, so no other code could recompute them. Fixed by extracting the assembly into `conformanceRows()` in `check-contract.ts` — one implementation, consumed by the command that publishes and the command that re-derives (ADR-0059 criterion 5) — and adding the three `cmp()` calls, required-field semantics included: an older attestation missing them is named not-verified with `versionSkew` beside it, exactly as the 2026-08-26 rule already decided for `gateNonScope`. `class` = `wrong-verdict`, `effect` = `exit-code`, `affected-from` = 0.35.0 (the release that introduced `verify`) through 0.37.1. `found-by` = `adversarial-audit`. | `test/unit/verify-predicate.test.js` — three new rows in the tamper table (a fabricated `deliverables` table, an invented `conformance` table, a falsified `horizon`), each measured red against the pre-fix binary: 4 failures against the old code, 13 of 13 against the fix. | Before the fix: diff the attestation's `deliverables`/`conformance` against `check --strict --json` run fresh on the tree — `verify` did not do it for you. |
| RWD-2026-0096 | **The re-derivation line closed a parenthesis it never opened, and the code meant to catch it could not fire.** `verify`'s success line rendered as `verdict re-derives (clean) under --strict)` — a stray `)` in the template after the gate name — and the appended `.replace("))", ")")` searched for a doubled parenthesis that occurs in neither branch of the ternary, so it repaired nothing in every execution since the line was written. The second line a reader of a verification sees looked mistyped, on the command that asks to be trusted about bytes. Found 2026-09-02 while rehearsing the output on camera. Fixed by removing the stray parenthesis and the dead `.replace`. `class` = `unguarded`, `effect` = `message`, `affected-from` = 0.35.0 through 0.37.1. `found-by` = `adversarial-audit`. | `test/unit/verify-predicate.test.js` — the output must match `verdict re-derives (clean) under --strict` and must NOT match `under --strict)`; measured red against the pre-fix binary. | Cosmetic — ignore the stray parenthesis. |
| RWD-2026-0097 | **The manifest parser split on escaped pipes, so the template's own illustration row became five rows of garbage in the machine payload.** GFM makes `\|` inside a cell a LITERAL pipe; `readManifest` split on every `|` regardless. The scaffold ships `| [rule-slug] | applied \| deviated \| n/a | … |` in every gated deliverable as an illustration of the format, and that one line parsed as five columns — rule `[rule-slug]`, status the garbage `applied \`, evidence swallowing the two columns after it. Measured 2026-09-02 on a mission nobody had touched: `check --strict --json` reported `evidence.rows: 5`, published `status: "applied \\"` in the ADR-0030 contract a CI branches on, and raised a shared-cell warning about the template's own example — three lines under `0 applied`. The product already treated square brackets as placeholder vocabulary in two places (the n/a form-lint skips bracketed tokens; a per-consumer guard in `evidence.ts` skipped bracketed rules on one path of three) — the rule existed and the parser did not own it. Fixed by honouring the GFM escape in the split (one declared gap: a cell ending in `\\` before a real separator keeps its pipe — a shape no manifest has carried, accepted over full backslash-run parsing) and by making the parser skip bracketed rule names once, retiring the per-consumer guard. A 2026-07-16 test pinned the old behaviour as-is, with no stated reason — a pin of observation, not a decision — and is updated with the history in its comment. `class` = `machine-surface`, `effect` = `message`, `affected-from` = at least 0.30.0 through 0.37.1. `found-by` = `adversarial-audit`. | `test/unit/conformance-read-manifest.test.js` — the escaped pipe comes back as cell content and the bracketed row as nothing, both measured red against the pre-fix parser; `test/unit/conformance.test.js` carries the updated pin. | Ignore rows whose rule is bracketed when consuming `check --strict --json` from an older runward. |
| RWD-2026-0098 | **The shared-cell sentence said `applied` about a census that spans every status.** The duplicated-evidence census was widened to all statuses on 2026-08-26 (36 `deviated` rows citing one unrelated ADR had gone unnamed), and the sentence that prints it kept the word `applied` — so the first strict run of a fresh mission printed "4 `applied` row(s) share 1 identical Evidence cell(s)" three lines under "0 applied", the two statements contradicting each other on the screen every new operator sees first. With RWD-2026-0097 fixed the fresh-mission occurrence disappears, but the sentence stayed wrong for any mission whose duplicated cells sit on `deviated` or `n/a` rows. Fixed by making the sentence claim exactly what the census counts. The stale field docstring in `evidence.ts` ("`applied` rows whose Evidence cell…") is corrected in the same commit. `class` = `unguarded`, `effect` = `message`, `affected-from` = 0.37.0 through 0.37.1. `found-by` = `adversarial-audit`. | `test/unit/evidence-breakdown.test.js` — a source assertion tying the sentence to the census (the two live in different files and nothing else ties them), measured red against the pre-fix source. Its own first cut looked for unescaped backticks and passed vacuously; caught by its positive control and noted in the test. | Cosmetic — read the count, not the word. |
| RWD-2026-0099 | **The human readiness drafts were byte-identical over a green tree and a refused one.** `compliance <regime>` has asked the gate since 2026-08-26 and carried the answer in the OSCAL (`runward-gate-verdict`) — the machine surface. The three human drafts, the artifacts an assessor actually reads, never printed it: measured 2026-09-02, a mission whose evidence seal was violated (gate exit 1, the refusal being precisely that a cited proof changed after sealing) produced an ISO/IEC 42001 readiness draft byte-identical to the green mission's. Any refusal whose cause is not quoted verbatim in the pack — a seal violation, a corpus edit — was invisible to the draft's reader, on the product whose own code states "a caveat that stays home is a caveat that was not made". Fixed by one banner (`verdictBannerLines`, mirroring the OSCAL property's wording so the two surfaces cannot phrase one verdict two ways) printed in the header of all three drafts: the verdict and exit code when the gate ran, an unmissable REFUSED sentence when it refused, and an explicit "not run for this pack" when no verdict was supplied. `class` = `unproven-claim`, `effect` = `message`, `affected-from` = 0.36.0 through 0.37.1. `found-by` = `adversarial-audit`. | `test/unit/compliance.test.js` — the three renderers, in both directions plus the unasked case, measured red against the pre-fix renderers. | Read the OSCAL's `runward-gate-verdict` property beside the draft — the draft alone does not tell you. |
| RWD-2026-0100 | **The strict render said one sentence 36 times, and everything else drowned in it.** On a mission where 36 rules were missing their manifest row — the first screen every new operator sees after a bare `init` — the conformance loop printed the full 41-word guidance ("`runward manifest --sync` scaffolds the missing row(s)…") on every violating line: 1 476 of the render's 2 061 words (71.6 %) were one repeated sentence, the longest line ran 376 characters with no wrap, and the disclosures that matter (what was verified, what the gate does not cover) were statistically invisible. The product's own code states the principle: a disclosure nobody can read is a disclosure that was not made. Fixed in the render only: the per-line DIAGNOSIS stays on every line, a guidance clause shared by three or more rows is spelled out once under the lines it explains, with its count; a cause carried by fewer than three rows is never elided; problems with no clause separator are never elided. The ADR-0030 machine payload keeps the full sentence on every row — a consumer reads rows one at a time, a human reads a screen at a time. Measured after the fix: 780 words, guidance ×1, diagnosis ×36. `class` = `unguarded`, `effect` = `message`, `affected-from` = at least 0.30.0 through 0.37.1. `found-by` = `adversarial-audit`. | `test/unit/check-render-dedup.test.js` — the guidance exactly once and the diagnosis on every line on a bare mission (measured red against the pre-fix binary: 36 occurrences), and a singleton cause never elided on the example mission. | Pipe through `sort -u` to read the pre-fix output. |
| RWD-2026-0101 | **`init` wrote and announced its files in the filesystem's own order, so one commit produced two different transcripts on two OSes.** Five `readdirSync` loops (the reference-mission copy, workflows, rules, adapters, the scaffold-lock hash walk) iterated in reader order. APFS returns UTF-8 byte order and NTFS case-folded alphabetical order, so `adapters/README.md` printed before `bmad-review-layer.toml` on macOS and after `pre-commit` on Windows — RWD-2026-0091's phenomenon, one command over. Caught by the init golden's very first CI run, on the Windows leg, the day the fixture was recorded (2026-09-02): the guard found a live defect before it found a drift. No verdict or file content moved; the write ORDER and the printed transcript did, and the scaffold-lock's input iteration relied on object-key insertion order that differed with it. Fixed by sorting all five enumerations. `class` = `unguarded`, `effect` = `message`, `affected-from` = at least 0.30.0 through 0.37.1. `found-by` = `existing-guard`. | `test/unit/init-golden.test.js` — the transcript fixture is byte-exact and the Windows CI leg re-derives it; unsorted enumeration fails that leg (measured on the first push of the fixture). | None — cosmetic ordering; file contents were identical. |
| RWD-2026-0102 | **The scaffold lock recorded skill keys in the OS's own path separator, so a committed lock stopped being readable across operating systems.** `skillsForDir` built each recorded key with `join()`, which writes `\` on Windows: a mission scaffolded there committed `.agents\skills\runward-floor\SKILL.md` into `scaffold-lock.json`, and every reader on another OS — the CI, a teammate, the next laptop — rebuilt the lookup key with `/` and found nothing: `update` classified all five skills as never-recorded on a tree that had not moved, silently re-recording them. The RWD-2026-0075 family (same working tree, different answer by OS), on the one artifact whose whole purpose is to travel with the repository. Caught by the init golden's Windows leg on 2026-09-02, its second live defect in one day: of 121 hashed files, exactly one differed between OSes, and it was the lock itself. Fixed at both ends: the writer spells keys with forward slashes on every OS (a recorded key is a NAME, not a path; the filesystem path beside it stays `join`ed), and the reader normalises `\` to `/` so every lock committed by a pre-fix Windows binary keeps verifying instead of being orphaned by the fix. `class` = `wrong-verdict`, `effect` = `message`, `affected-from` = 0.36.0 (skills entered the lock) through 0.37.1, Windows-scaffolded missions only. `found-by` = `existing-guard`. | `test/unit/scaffold-lock-directory.test.js` — a backslashed skill-dir yields forward-slash keys (red against the pre-fix writer by construction), and a lock carrying backslash keys reads back normalised. Plus the init golden's Windows leg, which re-derives the lock byte-identically. | On a Windows-scaffolded mission: run `runward update` once anywhere; the re-recorded lock then reads everywhere. |
| RWD-2026-0103 | **The gate's own advice named a file it never checked for: `runward check --strict` sent every operator to `runward/workflows/verify.md`, and the measured mission — this repository's own — holds no `workflows/` directory at all.** The advice line in `check.ts` was a hard-coded string, one of two places (with the `VERIFY_FINDINGS` constant) that spelled the verify procedure's coordinates independently of the contract that declares them; the moment a mission predated the workflows scaffold, or renamed the findings artifact in its own contract copy, the gate's guidance pointed at a void while sounding certain. Measured 2026-09-02 while wiring W3: the product's own mission fails its own advice. Fixed by making ADR-0067's decision 4 literal: the advice now checks whether the mission holds `workflows/verify.md` and says where the procedure actually lives (mission copy, or shipped with the package with `runward update` as the gesture), and the findings path is DERIVED from the verify contract's `produces` (`verifyFindingsPath`, mission copy over package copy) — the constant survives only as the fallback for a mission with no contract at all. `class` = `wrong-guidance`, `effect` = `message`, `affected-from` = the advice's introduction through 0.37.1. `found-by` = `measurement`. | `test/unit/workflow-contract-gate.test.js` — a mission whose verify contract renames its findings artifact sees the gate read and report the renamed path; a mission without `workflows/` gets the package-copy advice, not the void. | None needed — guidance text; no verdict was wrong. |
| RWD-2026-0104 | **The shipped consultative tier never once put the verdict in the agent's loop, while the documentation argued the gate was "in the loop".** Measured 2026-09-02 on this repository's own history: 1,668 recorded agent turns, zero in which a red verdict reached the model — every shipped sample ends in `\|\| true`, so the harness swallowed the exit code by construction. ADR-0012's "the agent loop sees the verdict" argument was false AS SHIPPED: the loop could see it only if an operator hand-edited a sample nothing pointed them to. Meanwhile `packaging/README.md`'s single "Blocks?" column answered "no" for families whose harnesses CAN block — six of nine measured families offer an end-of-turn block with the refusal returned to the model's context (Claude Code, Copilot, Gemini CLI, Kiro CLI, Junie CLI, Codex) — conflating the harness's capability with the sample's choice. Fixed by ADR-0065 + H1/H3: the doc now carries both truths per channel (shipped tier and measured capability, sourced), and the armed tier exists as one explicit gesture — `wire --install` wiring `gate-hook`, which speaks each harness's native refusal, blocks once, and traces releases to a committed log. The consultative default did not change; what changed is that calling it "in the loop" without the armed tier is no longer possible. `class` = `unproven-claim`, `effect` = `message`, `affected-from` = the packagings' introduction through 0.37.1. `found-by` = `adversarial-audit`. | `test/unit/gate-hook.test.js` (each harness's native refusal shape, block-once, the committed trace) and `test/unit/wire-install.test.js` (the locks: no flag arms the gate for an agent). The doc claim itself is prose; its guard is the measured table's date stamp. | None — the consultative tier behaves as designed; the defect was the claim about it. |

**The pattern, and why it is one entry family rather than six.** Every one of these is the gap between
what a surface verified and what its own sentence says it verified. The verdict logic was correct in
each case; the emission read a subset of it. The project already applies the right discipline in one
place — `sarif.ts` repeats the non-scope in every rule's `fullDescription` *"so a consumer that keeps
the findings and drops the non-scope has to drop it deliberately"* — and had not applied it anywhere
else. `compliance.ts` states the principle it was breaking: *"A caveat that stays home is a caveat
that was not made."*

## Declared, and not fixable inside the repository

| id | Constraint | Why it is not closed |
|---|---|---|
| RWD-2026-0028 | Deleting `runward/evidence-lock.json` removes the seal check entirely, so a mission whose sealed evidence was tampered with goes from exit 1 to exit 0. Measured 2026-08-21 against 0.36.0. `found-by` = `declared`. | Sealing is opt-in, so an absent lock is the honest default and must not redden a mission that never sealed. Closing it at the verdict level would need an in-repository marker declaring "this mission seals" — and `src/lib/scaffold-lock.ts` already records, in the register's own words, why that buys nothing: *the lock is not the authority, it lives in the audited repository*, so anyone deliberate re-signs or removes the marker in the same commit, while honest teams pay a red gate. Against a deliberate actor with commit rights the trust anchor is the reviewed commit (ADR-0021), where deleting this file is a visible diff. Shipping a marker here would repeat a mistake this project has already paid for once. What was fixed instead is RWD-2026-0027, the silence. |

## What this register does not do

It does not discharge any requirement for you. `runward/contracts/port-contract.md` and `GATE_NON_SCOPE` are material you cite while writing your own justification. And it records its own late arrival: the nine classes above were, until 2026-08-04, in no ADR, no template, and not in `GATE_NON_SCOPE`, which is to say this register did not exist during the period it describes.
