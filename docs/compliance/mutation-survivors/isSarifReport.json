{
 "function": "isSarifReport",
 "probeMissions": [],
 "verdicts": [
  {
   "key": "19|12|20|38|LogicalOperator|/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i.test(content) || /\"version\"\\s*:\\s*\"2\\.[01]/.test(content) || /\"runs\"\\s*:/.test(content)",
   "line": 19,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "isSarifReport becomes \"any one of the three markers\": measured false -> true on a package-lock fragment carrying \"version\": \"2.1.4\", on a job log carrying \"runs\":, and on a sarif-stamped file with no runs. Each then routes to the SARIF adapter and returns absent, so 16 routed outputs turn a substring-resolved pointer into a gate violation.",
   "argument": "isSarifReport becomes \"any one of the three markers\": measured false -> true on a package-lock fragment carrying \"version\": \"2.1.4\", on a job log carrying \"runs\":, and on a sarif-stamped file with no runs. Each then routes to the SARIF adapter and returns absent, so 16 routed outputs turn a substring-resolved pointer into a gate violation.",
   "stableKey": "tool-adapters\u0001isSarifReport\u0001LogicalOperator\u0001/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i.test(content) || /\"version\"\\s*:\\s*\"2\\.[01]/.test(content) || /\"runs\"\\s*:/.test(content)\u0001(/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i.test(content) || /\"version\"\\s*:\\s*\"2\\.[01]/.test(content)) && /\"runs\"\\s*:/.test(content)\u0001return (/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i.test(content) || /\"version\"\\s*:\\s*\"2\\.[01]/.test(content))"
  },
  {
   "key": "19|13|19|102|ConditionalExpression|true",
   "line": 19,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "Recognition collapses to \"the file contains \\\"runs\\\":\": measured false -> true on a job log and on a JSON array, which then route to the SARIF adapter and read \"absent\" instead of the substring check — 11 differing outputs.",
   "argument": "Recognition collapses to \"the file contains \\\"runs\\\":\": measured false -> true on a job log and on a JSON array, which then route to the SARIF adapter and read \"absent\" instead of the substring check — 11 differing outputs.",
   "stableKey": "tool-adapters\u0001isSarifReport\u0001ConditionalExpression\u0001true\u0001/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i.test(content) || /\"version\"\\s*:\\s*\"2\\.[01]/.test(content)\u0001return (/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i.test(content) || /\"version\"\\s*:\\s*\"2\\.[01]/.test(content))"
  },
  {
   "key": "19|13|19|44|Regex|/\"\\$schema\"\\s:\\s*\"[^\"]*sarif/i",
   "line": 19,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "The $schema stamp now requires exactly one space before the colon, so it matches no spelling a JSON writer emits (compact, \": \", newline): 24 differing outputs. On a log carrying only the $schema stamp the pointer falls through to the substring check and reads GREEN over a scan recording an error-level finding for that very rule.",
   "argument": "The $schema stamp now requires exactly one space before the colon, so it matches no spelling a JSON writer emits (compact, \": \", newline): 24 differing outputs. On a log carrying only the $schema stamp the pointer falls through to the substring check and reads GREEN over a scan recording an error-level finding for that very rule.",
   "stableKey": "tool-adapters\u0001isSarifReport\u0001Regex\u0001/\"\\$schema\"\\s:\\s*\"[^\"]*sarif/i\u0001/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i\u0001return (/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i.test(content) || /\"version\"\\s*:\\s*\"2\\.[01]/.test(content))"
  },
  {
   "key": "19|13|19|44|Regex|/\"\\$schema\"\\S*:\\s*\"[^\"]*sarif/i",
   "line": 19,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "The `\"$schema\" : ` spelling (space before the colon) stops matching: 8 differing outputs, including a schema-only log with findings whose `#no-hardcoded-secrets` route flips from sarif:findings to substring:GREEN.",
   "argument": "The `\"$schema\" : ` spelling (space before the colon) stops matching: 8 differing outputs, including a schema-only log with findings whose `#no-hardcoded-secrets` route flips from sarif:findings to substring:GREEN.",
   "stableKey": "tool-adapters\u0001isSarifReport\u0001Regex\u0001/\"\\$schema\"\\S*:\\s*\"[^\"]*sarif/i\u0001/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i\u0001return (/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i.test(content) || /\"version\"\\s*:\\s*\"2\\.[01]/.test(content))"
  },
  {
   "key": "19|13|19|44|Regex|/\"\\$schema\"\\s*:\\s\"[^\"]*sarif/i",
   "line": 19,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "The compact `\"$schema\":\"…\"` spelling — what JSON.stringify emits — stops matching: 12 differing outputs, with a schema-only compact log's `#no-hardcoded-secrets` flipping from sarif:findings to substring:GREEN.",
   "argument": "The compact `\"$schema\":\"…\"` spelling — what JSON.stringify emits — stops matching: 12 differing outputs, with a schema-only compact log's `#no-hardcoded-secrets` flipping from sarif:findings to substring:GREEN.",
   "stableKey": "tool-adapters\u0001isSarifReport\u0001Regex\u0001/\"\\$schema\"\\s*:\\s\"[^\"]*sarif/i\u0001/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i\u0001return (/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i.test(content) || /\"version\"\\s*:\\s*\"2\\.[01]/.test(content))"
  },
  {
   "key": "19|13|19|44|Regex|/\"\\$schema\"\\s*:\\S*\"[^\"]*sarif/i",
   "line": 19,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "`\"$schema\": \"…\"` (one space after the colon, the standard pretty-printed spelling) stops matching: 19 differing outputs, with three schema-only logs falling through to substring:GREEN on a rule that records an error-level finding.",
   "argument": "`\"$schema\": \"…\"` (one space after the colon, the standard pretty-printed spelling) stops matching: 19 differing outputs, with three schema-only logs falling through to substring:GREEN on a rule that records an error-level finding.",
   "stableKey": "tool-adapters\u0001isSarifReport\u0001Regex\u0001/\"\\$schema\"\\s*:\\S*\"[^\"]*sarif/i\u0001/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i\u0001return (/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i.test(content) || /\"version\"\\s*:\\s*\"2\\.[01]/.test(content))"
  },
  {
   "key": "19|13|19|44|Regex|/\"\\$schema\"\\s*:\\s*\"[^\"]sarif/i",
   "line": 19,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "`[^\"]*` -> `[^\"]` requires exactly one character between the quote and \"sarif\", so no real schema URL matches: 25 differing outputs, four schema-only logs routing to substring:GREEN despite recorded findings.",
   "argument": "`[^\"]*` -> `[^\"]` requires exactly one character between the quote and \"sarif\", so no real schema URL matches: 25 differing outputs, four schema-only logs routing to substring:GREEN despite recorded findings.",
   "stableKey": "tool-adapters\u0001isSarifReport\u0001Regex\u0001/\"\\$schema\"\\s*:\\s*\"[^\"]sarif/i\u0001/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i\u0001return (/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i.test(content) || /\"version\"\\s*:\\s*\"2\\.[01]/.test(content))"
  },
  {
   "key": "19|13|19|44|Regex|/\"\\$schema\"\\s*:\\s*\"[\"]*sarif/i",
   "line": 19,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "`[\"]*sarif` never matches a schema URL (it demands quotes, not the URL path): 30 differing outputs, with the same substring:GREEN fall-through on logs that record an error-level finding.",
   "argument": "`[\"]*sarif` never matches a schema URL (it demands quotes, not the URL path): 30 differing outputs, with the same substring:GREEN fall-through on logs that record an error-level finding.",
   "stableKey": "tool-adapters\u0001isSarifReport\u0001Regex\u0001/\"\\$schema\"\\s*:\\s*\"[\"]*sarif/i\u0001/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i\u0001return (/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i.test(content) || /\"version\"\\s*:\\s*\"2\\.[01]/.test(content))"
  },
  {
   "key": "19|62|19|88|Regex|/\"version\"\\S*:\\s*\"2\\.[01]/",
   "line": 19,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "The `\"version\" : \"2.1.0\"` spelling stops matching: 6 differing outputs, and a version-only log with findings routes from sarif:findings to substring:GREEN.",
   "argument": "The `\"version\" : \"2.1.0\"` spelling stops matching: 6 differing outputs, and a version-only log with findings routes from sarif:findings to substring:GREEN.",
   "stableKey": "tool-adapters\u0001isSarifReport\u0001Regex\u0001/\"version\"\\S*:\\s*\"2\\.[01]/\u0001/\"version\"\\s*:\\s*\"2\\.[01]/\u0001return (/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i.test(content) || /\"version\"\\s*:\\s*\"2\\.[01]/.test(content))"
  },
  {
   "key": "19|62|19|88|Regex|/\"version\"\\s*:\\S*\"2\\.[01]/",
   "line": 19,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "`\"version\": \"2.1.0\"` (one space after the colon) stops matching: 18 differing outputs, with three version-only logs — the common shape for tools that emit no $schema — falling through to substring:GREEN on a rule that records an error-level finding.",
   "argument": "`\"version\": \"2.1.0\"` (one space after the colon) stops matching: 18 differing outputs, with three version-only logs — the common shape for tools that emit no $schema — falling through to substring:GREEN on a rule that records an error-level finding.",
   "stableKey": "tool-adapters\u0001isSarifReport\u0001Regex\u0001/\"version\"\\s*:\\S*\"2\\.[01]/\u0001/\"version\"\\s*:\\s*\"2\\.[01]/\u0001return (/\"\\$schema\"\\s*:\\s*\"[^\"]*sarif/i.test(content) || /\"version\"\\s*:\\s*\"2\\.[01]/.test(content))"
  },
  {
   "key": "20|12|20|24|Regex|/\"runs\"\\S*:/",
   "line": 20,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "`\"runs\" : [` stops matching, so a log pretty-printed with spaces before its colons is not recognised as SARIF at all: 12 differing outputs, `#no-hardcoded-secrets` flipping from sarif:findings to substring:GREEN.",
   "argument": "`\"runs\" : [` stops matching, so a log pretty-printed with spaces before its colons is not recognised as SARIF at all: 12 differing outputs, `#no-hardcoded-secrets` flipping from sarif:findings to substring:GREEN.",
   "stableKey": "tool-adapters\u0001isSarifReport\u0001Regex\u0001/\"runs\"\\S*:/\u0001/\"runs\"\\s*:/\u0001&& /\"runs\"\\s*:/.test(content);"
  }
 ]
}
