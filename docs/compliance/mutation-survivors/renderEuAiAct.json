{
 "function": "renderEuAiAct",
 "probeMissions": [],
 "verdicts": [
  {
   "key": "305|15|305|17|ArrayDeclaration|[\"Stryker was here\"]",
   "line": 305,
   "mutator": "ArrayDeclaration",
   "filing": "display-only",
   "evidence": "PROBE: rendered the draft with the mutant applied and diffed against the shipped build on two fixtures (a fresh `runward init` mission and examples/request-triage/runward). The single difference is the line `Stryker was here` prepended above the H1; the other 56 lines are byte-identical, so no claim, caveat, count, status or attribution changes. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output). Surface: the first line of runward/compliance/eu-ai-act-readiness.md. Filed display-only under the register's definition ('changes printed prose and nothing else'), but the finding it exposes is real and is shared with the fourteen separator mutants: the emitted markdown of this pack has NO byte-level net, so arbitrary injected text ships in a document a provider files. The golden fixture is a real net over the OSCAL JSON and guards nothing in this function's output.",
   "argument": "PROBE: rendered the draft with the mutant applied and diffed against the shipped build on two fixtures (a fresh `runward init` mission and examples/request-triage/runward). The single difference is the line `Stryker was here` prepended above the H1; the other 56 lines are byte-identical, so no claim, caveat, count, status or attribution changes. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output). Surface: the first line of runward/compliance/eu-ai-act-readiness.md. Filed display-only under the register's definition ('changes printed prose and nothing else'), but the finding it exposes is real and is shared with the fourteen separator mutants: the emitted markdown of this pack has NO byte-level net, so arbitrary injected text ships in a document a provider files. The golden fixture is a real net over the OSCAL JSON and guards nothing in this function's output.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001ArrayDeclaration\u0001[\"Stryker was here\"]\u0001[]\u0001const L = [];"
  },
  {
   "key": "306|12|306|89|StringLiteral|\"\"",
   "line": 306,
   "mutator": "StringLiteral",
   "filing": "defence-in-depth",
   "evidence": "NOT a hole: another leg of the net kills it. test/smoke.js asserts on runward/compliance/eu-ai-act-readiness.md that `md.includes(\"assessment-readiness draft\")`, and measured, that phrase occurs EXACTLY ONCE in the document - in this H1. VERIFIED BY RUNNING THE LEG, not by reading: `node test/smoke.js` on the shipped build prints 'smoke test OK' (~20 s); with this mutant applied to dist/lib/compliance.js the same run fails with one assertion, `FAIL  compliance eu-ai-act writes a readiness draft (Annex IV), never a compliance claim`. It survived the measurement only because scripts/mutation-testcmd.sh runs `node --test test/unit/*.test.js` alone; the unit suite's `assert.match(eu, /Draft/)` matches the capital-D '**Draft, incomplete - not a conformity assessment.**' line of the header block quote, not the lower-case 'draft' of the title. Leg named: the end-to-end smoke, i.e. pass 2 of this register's own method. This is not contention - the kill is a deterministic string-presence check by a leg that was never part of pass 1, and it reproduces.",
   "argument": "NOT a hole: another leg of the net kills it. test/smoke.js asserts on runward/compliance/eu-ai-act-readiness.md that `md.includes(\"assessment-readiness draft\")`, and measured, that phrase occurs EXACTLY ONCE in the document - in this H1. VERIFIED BY RUNNING THE LEG, not by reading: `node test/smoke.js` on the shipped build prints 'smoke test OK' (~20 s); with this mutant applied to dist/lib/compliance.js the same run fails with one assertion, `FAIL  compliance eu-ai-act writes a readiness draft (Annex IV), never a compliance claim`. It survived the measurement only because scripts/mutation-testcmd.sh runs `node --test test/unit/*.test.js` alone; the unit suite's `assert.match(eu, /Draft/)` matches the capital-D '**Draft, incomplete - not a conformity assessment.**' line of the header block quote, not the lower-case 'draft' of the title. Leg named: the end-to-end smoke, i.e. pass 2 of this register's own method. This is not contention - the kill is a deterministic string-presence check by a leg that was never part of pass 1, and it reproduces.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"\"\u0001\"# EU AI Act — Annex IV technical documentation — assessment-readiness draft\"\u0001L.push(\"# EU AI Act — Annex IV technical documentation — assessment-readiness draft\");"
  },
  {
   "key": "307|12|307|14|StringLiteral|\"Stryker was here!\"",
   "line": 307,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "SEPARATOR FAMILY - one argument for the fourteen `L.push(\"\")` mutants of this function, written here and referred to by the thirteen siblings. Each replaces one blank line of runward/compliance/eu-ai-act-readiness.md with the literal text `Stryker was here!`. PROBE: rendered the draft with the mutant applied to dist/lib/compliance.js and diffed against the shipped build, on two fixtures - a fresh `runward init` mission and examples/request-triage/runward. In every case the diff is exactly that one line, and every heading, table row, caveat, count, status and attribution is byte-identical, so no claim changes and no reader or GRC tool acts differently on the content. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output). Surface: the inter-block blank lines of the emitted markdown. THIS occurrence is the blank line between the H1 and the header block quote. Filed display-only per ADR-0046 decision 1 (no test should pin prose); the family's real lesson is the one stated on the `const L = []` mutant: this document has no byte-level net at all, so injected text ships in an artefact a provider files.",
   "argument": "SEPARATOR FAMILY - one argument for the fourteen `L.push(\"\")` mutants of this function, written here and referred to by the thirteen siblings. Each replaces one blank line of runward/compliance/eu-ai-act-readiness.md with the literal text `Stryker was here!`. PROBE: rendered the draft with the mutant applied to dist/lib/compliance.js and diffed against the shipped build, on two fixtures - a fresh `runward init` mission and examples/request-triage/runward. In every case the diff is exactly that one line, and every heading, table row, caveat, count, status and attribution is byte-identical, so no claim changes and no reader or GRC tool acts differently on the content. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output). Surface: the inter-block blank lines of the emitted markdown. THIS occurrence is the blank line between the H1 and the header block quote. Filed display-only per ADR-0046 decision 1 (no test should pin prose); the family's real lesson is the one stated on the `const L = []` mutant: this document has no byte-level net at all, so injected text ships in an artefact a provider files.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");"
  },
  {
   "key": "310|19|310|42|OptionalChaining|lens.highRisk.bindFrom",
   "line": 310,
   "mutator": "OptionalChaining",
   "filing": "defence-in-depth",
   "evidence": "`renderEuAiAct` is reachable only through `complianceCommand` with key 'eu-ai-act' (the REGIMES table in src/commands/compliance.ts), and its `lens` argument comes only from `loadRegime('eu-ai-act', opts.regimeVersion)`, which reads `regimes/<regime>@<version>.json` out of the packaged REGIMES_DIR and THROWS (the caller maps it to exit 2) on any version not shipped. Two eu-ai-act lenses ship - 2024-1689 and 2026-1744 - and both define `highRisk {bindFrom, scope}`, `articles.runtimeLogging` and the nine-row `annexIv`. PROBE: with the mutant applied the rendered document is byte-identical on both fixtures (fresh `runward init` mission and examples/request-triage/runward), so the guard never fires on any reachable input. SENSITIVITY CONTROL: on a lens with no `highRisk` key the two WOULD differ - the shipped code interpolates the literal `**undefined**` as the date on which high-risk obligations bind, the mutant throws a TypeError instead. That input cannot occur: (a) loadRegime accepts only the shipped filenames and refuses anything else with the shipped list; (b) test/unit/regimes.test.js refuses any stray file in regimes/ that is not `<known-regime>@<version>.json`; (c) test/unit/compliance.test.js asserts `!md.includes(\"undefined\")` on the default eu lens together with the exact `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and all nine `| N. ` Annex IV rows, so a future lens dropping a key reddens the unit suite before it can reach a user. Filed defence-in-depth rather than equivalent because the mutant is not semantically equivalent on the guarded input - it is only unreachable. Note the direction, since it matters to a regulated reader: on the guarded input the MUTANT is the safer of the two - it fails loudly where the shipped code prints 'undefined' into a legal date field.",
   "argument": "`renderEuAiAct` is reachable only through `complianceCommand` with key 'eu-ai-act' (the REGIMES table in src/commands/compliance.ts), and its `lens` argument comes only from `loadRegime('eu-ai-act', opts.regimeVersion)`, which reads `regimes/<regime>@<version>.json` out of the packaged REGIMES_DIR and THROWS (the caller maps it to exit 2) on any version not shipped. Two eu-ai-act lenses ship - 2024-1689 and 2026-1744 - and both define `highRisk {bindFrom, scope}`, `articles.runtimeLogging` and the nine-row `annexIv`. PROBE: with the mutant applied the rendered document is byte-identical on both fixtures (fresh `runward init` mission and examples/request-triage/runward), so the guard never fires on any reachable input. SENSITIVITY CONTROL: on a lens with no `highRisk` key the two WOULD differ - the shipped code interpolates the literal `**undefined**` as the date on which high-risk obligations bind, the mutant throws a TypeError instead. That input cannot occur: (a) loadRegime accepts only the shipped filenames and refuses anything else with the shipped list; (b) test/unit/regimes.test.js refuses any stray file in regimes/ that is not `<known-regime>@<version>.json`; (c) test/unit/compliance.test.js asserts `!md.includes(\"undefined\")` on the default eu lens together with the exact `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and all nine `| N. ` Annex IV rows, so a future lens dropping a key reddens the unit suite before it can reach a user. Filed defence-in-depth rather than equivalent because the mutant is not semantically equivalent on the guarded input - it is only unreachable. Note the direction, since it matters to a regulated reader: on the guarded input the MUTANT is the safer of the two - it fails loudly where the shipped code prints 'undefined' into a legal date field.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001OptionalChaining\u0001lens.highRisk.bindFrom\u0001lens.highRisk?.bindFrom\u0001L.push(`> **${lens.highRisk?.bindFrom}** (${lens.highRisk?.scope}). This populates Annex IV Point 2 (design & validation) and the design-rationale`);"
  },
  {
   "key": "310|49|310|69|OptionalChaining|lens.highRisk.scope",
   "line": 310,
   "mutator": "OptionalChaining",
   "filing": "defence-in-depth",
   "evidence": "Same reachability and the same shipped-lens control as the `lens.highRisk?.bindFrom` mutant on this line; this is the `scope` half of the same interpolation (rendered: 'Chapter III, Sections 1, 2 and 3 -- Art. 113, 3rd para, point (c)...'). PROBE: byte-identical output on both fixtures. SENSITIVITY CONTROL: a lens without `highRisk` makes the shipped code render '(undefined)' as the scope of the high-risk obligations while the mutant throws; that lens cannot be loaded, for the three reasons given on the `bindFrom` sibling.",
   "argument": "Same reachability and the same shipped-lens control as the `lens.highRisk?.bindFrom` mutant on this line; this is the `scope` half of the same interpolation (rendered: 'Chapter III, Sections 1, 2 and 3 -- Art. 113, 3rd para, point (c)...'). PROBE: byte-identical output on both fixtures. SENSITIVITY CONTROL: a lens without `highRisk` makes the shipped code render '(undefined)' as the scope of the high-risk obligations while the mutant throws; that lens cannot be loaded, for the three reasons given on the `bindFrom` sibling.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001OptionalChaining\u0001lens.highRisk.scope\u0001lens.highRisk?.scope\u0001L.push(`> **${lens.highRisk?.bindFrom}** (${lens.highRisk?.scope}). This populates Annex IV Point 2 (design & validation) and the design-rationale`);"
  },
  {
   "key": "311|12|311|148|StringLiteral|``",
   "line": 311,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "HOLE. The mutant blanks the whole line, so the emitted draft loses two explicit NEGATIVE claims about what it is not: 'it does **not** satisfy art. 12 runtime logging' and 'it is not a signed declaration of conformity'. It also truncates the header sentence mid-phrase - the paragraph now ends at '...and the design-rationale'. RECIPE: `runward init` then `runward compliance eu-ai-act`, open runward/compliance/eu-ai-act-readiness.md. BEFORE, line 6 reads `> history; it does **not** satisfy art. 12 runtime logging, and it is not a signed declaration of conformity.` AFTER, that line is empty (measured on both fixtures; the rest of the document byte-identical). MEASURED RESIDUE, so the entry survives a hostile reading: the lower-case string 'art. 12' occurs exactly once in the document - this line - so the only surviving mention is the provider-required bullet '**Art. 12 runtime event logs** - produced by the running system, not by runward', which says who OWES the logs, not that this pack fails to satisfy the article; and 'declaration of conformity' survives only as the provider-owed '**Point 8** the signed **EU declaration of conformity** (art. 47)', again an obligation rather than the disclaimer that THIS document is not one. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output).",
   "argument": "HOLE. The mutant blanks the whole line, so the emitted draft loses two explicit NEGATIVE claims about what it is not: 'it does **not** satisfy art. 12 runtime logging' and 'it is not a signed declaration of conformity'. It also truncates the header sentence mid-phrase - the paragraph now ends at '...and the design-rationale'. RECIPE: `runward init` then `runward compliance eu-ai-act`, open runward/compliance/eu-ai-act-readiness.md. BEFORE, line 6 reads `> history; it does **not** satisfy art. 12 runtime logging, and it is not a signed declaration of conformity.` AFTER, that line is empty (measured on both fixtures; the rest of the document byte-identical). MEASURED RESIDUE, so the entry survives a hostile reading: the lower-case string 'art. 12' occurs exactly once in the document - this line - so the only surviving mention is the provider-required bullet '**Art. 12 runtime event logs** - produced by the running system, not by runward', which says who OWES the logs, not that this pack fails to satisfy the article; and 'declaration of conformity' survives only as the provider-owed '**Point 8** the signed **EU declaration of conformity** (art. 47)', again an obligation rather than the disclaimer that THIS document is not one. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output).",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001``\u0001`> history; it does **not** satisfy ${lens.articles?.runtimeLogging} runtime logging, and it is not a signed declaration of conformity.`\u0001L.push(`> history; it does **not** satisfy ${lens.articles?.runtimeLogging} runtime logging, and it is not a signed declaration of conformity.`);"
  },
  {
   "key": "311|50|311|79|OptionalChaining|lens.articles.runtimeLogging",
   "line": 311,
   "mutator": "OptionalChaining",
   "filing": "defence-in-depth",
   "evidence": "Same reachability and the same shipped-lens control as the `lens.highRisk?.` mutants above; this one renders `articles.runtimeLogging` = 'art. 12', the article the draft states it does NOT satisfy. PROBE: byte-identical output on both fixtures. SENSITIVITY CONTROL: on a lens with no `articles` key the shipped code emits 'it does **not** satisfy undefined runtime logging' - a disclaimer naming no article - while the mutant throws; that lens cannot be loaded, for the three reasons given on the `bindFrom` sibling, and `!md.includes(\"undefined\")` in test/unit/compliance.test.js catches the shipped side of that case anyway.",
   "argument": "Same reachability and the same shipped-lens control as the `lens.highRisk?.` mutants above; this one renders `articles.runtimeLogging` = 'art. 12', the article the draft states it does NOT satisfy. PROBE: byte-identical output on both fixtures. SENSITIVITY CONTROL: on a lens with no `articles` key the shipped code emits 'it does **not** satisfy undefined runtime logging' - a disclaimer naming no article - while the mutant throws; that lens cannot be loaded, for the three reasons given on the `bindFrom` sibling, and `!md.includes(\"undefined\")` in test/unit/compliance.test.js catches the shipped side of that case anyway.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001OptionalChaining\u0001lens.articles.runtimeLogging\u0001lens.articles?.runtimeLogging\u0001L.push(`> history; it does **not** satisfy ${lens.articles?.runtimeLogging} runtime logging, and it is not a signed declaration of conformity.`);"
  },
  {
   "key": "312|12|312|71|StringLiteral|\"\"",
   "line": 312,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "HOLE, filed with its mitigation stated. The mutant removes '> Verify against the Official Journal text before filing.' from the header block quote - the framing block a filer meets first, beside '**Draft, incomplete - not a conformity assessment.**'. RECIPE: `runward init` then `runward compliance eu-ai-act`; BEFORE line 7 carries that sentence, AFTER it is empty (measured on both fixtures, rest byte-identical). MITIGATION: the closing disclaimer still says '_Engineering framing, not legal advice; Annex IV wording moves - confirm against the Official Journal (Reg. (EU) 2024/1689) before filing._', so the instruction still ships once ('Official Journal' occurs twice in the document; the mutant removes one). WHAT I COULD NOT CLEAR, and why it is filed hole rather than display-only: a blank line inside a Markdown block quote terminates it, so the mutation also splits the header reservation and detaches the lens stamp from it; and this project's own ADR-0040 lesson, recorded in a comment three lines below in this same function, is that a caveat the reader does not meet where the artefact is read is a caveat that was not made. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output).",
   "argument": "HOLE, filed with its mitigation stated. The mutant removes '> Verify against the Official Journal text before filing.' from the header block quote - the framing block a filer meets first, beside '**Draft, incomplete - not a conformity assessment.**'. RECIPE: `runward init` then `runward compliance eu-ai-act`; BEFORE line 7 carries that sentence, AFTER it is empty (measured on both fixtures, rest byte-identical). MITIGATION: the closing disclaimer still says '_Engineering framing, not legal advice; Annex IV wording moves - confirm against the Official Journal (Reg. (EU) 2024/1689) before filing._', so the instruction still ships once ('Official Journal' occurs twice in the document; the mutant removes one). WHAT I COULD NOT CLEAR, and why it is filed hole rather than display-only: a blank line inside a Markdown block quote terminates it, so the mutation also splits the header reservation and detaches the lens stamp from it; and this project's own ADR-0040 lesson, recorded in a comment three lines below in this same function, is that a caveat the reader does not meet where the artefact is read is a caveat that was not made. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output).",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"\"\u0001\"> Verify against the Official Journal text before filing.\"\u0001L.push(\"> Verify against the Official Journal text before filing.\");"
  },
  {
   "key": "314|12|314|14|StringLiteral|\"Stryker was here!\"",
   "line": 314,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the header block quote and the ADR-0040 non-scope block quote. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes. By the CommonMark lazy-continuation rule the injected line is absorbed into the preceding quote, so the stray text renders inside the 'Draft, incomplete - not a conformity assessment' reservation rather than beside it; no word of that reservation changes.",
   "argument": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the header block quote and the ADR-0040 non-scope block quote. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes. By the CommonMark lazy-continuation rule the injected line is absorbed into the preceding quote, so the stray text renders inside the 'Draft, incomplete - not a conformity assessment' reservation rather than beside it; no word of that reservation changes.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#2"
  },
  {
   "key": "318|12|318|70|StringLiteral|\"\"",
   "line": 318,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "HOLE, and the most instructive one in this group. The mutant blanks only the prefix `\"> **Declared non-scope of every green row (ADR-0040).** \"`; `+ GATE_NON_SCOPE` still concatenates, so the reservation's body ships verbatim. What the document loses: the leading `> ` (the paragraph stops being a quoted reservation and becomes body text), the label saying WHAT the reservation scopes - every green row - and the attribution to ADR-0040. RECIPE: any mission, `runward compliance eu-ai-act`; BEFORE line 10 begins `> **Declared non-scope of every green row (ADR-0040).** A green row proves...`; AFTER it begins `A green row proves...` with no quote marker and no label (measured, both fixtures, rest byte-identical). The finding worth registering is WHY nothing catches it: test/unit/compliance.test.js carries a test written for exactly this line - 'every pack carries the declared non-scope', added after the 2026-08-06 measurement where the reservation appeared in the ISO draft and zero times in this one - and it PASSES on the mutant, because it asserts a fragment strictly inside the imported constant ('It never proves the evidence truly implements') and nothing about the framing this function writes around it. The guard covers the borrowed sentence, not the sentence this renderer authors. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output).",
   "argument": "HOLE, and the most instructive one in this group. The mutant blanks only the prefix `\"> **Declared non-scope of every green row (ADR-0040).** \"`; `+ GATE_NON_SCOPE` still concatenates, so the reservation's body ships verbatim. What the document loses: the leading `> ` (the paragraph stops being a quoted reservation and becomes body text), the label saying WHAT the reservation scopes - every green row - and the attribution to ADR-0040. RECIPE: any mission, `runward compliance eu-ai-act`; BEFORE line 10 begins `> **Declared non-scope of every green row (ADR-0040).** A green row proves...`; AFTER it begins `A green row proves...` with no quote marker and no label (measured, both fixtures, rest byte-identical). The finding worth registering is WHY nothing catches it: test/unit/compliance.test.js carries a test written for exactly this line - 'every pack carries the declared non-scope', added after the 2026-08-06 measurement where the reservation appeared in the ISO draft and zero times in this one - and it PASSES on the mutant, because it asserts a fragment strictly inside the imported constant ('It never proves the evidence truly implements') and nothing about the framing this function writes around it. The guard covers the borrowed sentence, not the sentence this renderer authors. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output).",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"\"\u0001\"> **Declared non-scope of every green row (ADR-0040).** \"\u0001L.push(\"> **Declared non-scope of every green row (ADR-0040).** \" + GATE_NON_SCOPE);"
  },
  {
   "key": "319|12|319|14|StringLiteral|\"Stryker was here!\"",
   "line": 319,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the ADR-0040 non-scope block quote and '## Annex IV coverage map'. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes. Same lazy-continuation effect as the previous separator: the stray line is absorbed into the non-scope reservation; its text is untouched.",
   "argument": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the ADR-0040 non-scope block quote and '## Annex IV coverage map'. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes. Same lazy-continuation effect as the previous separator: the stray line is absorbed into the non-scope reservation; its text is untouched.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#3"
  },
  {
   "key": "320|12|320|38|StringLiteral|\"\"",
   "line": 320,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Display-only, and the only one of this function's five `##` section headings that is. The mutant blanks the heading; measured on both fixtures, the rest of the document is byte-identical, so the coverage table is untouched - and that table's own header row, `| Annex IV point | runward supplies | Required from the provider |`, restates the heading's content verbatim, so no reader loses the section's identity or its who-supplies-what framing. Surface: the `##` navigation heading of runward/compliance/eu-ai-act-readiness.md. No test and no machine contract reads it - grep over the repository finds the string only in src/lib/compliance.ts and CHANGELOG.md; the OSCAL links to this document by filename (`./eu-ai-act-readiness.md`), never by heading; and test/smoke.js's eu assertion still passes on the mutant's output. Contrast the four sibling heading mutants, filed hole because their bodies do NOT restate their heading.",
   "argument": "Display-only, and the only one of this function's five `##` section headings that is. The mutant blanks the heading; measured on both fixtures, the rest of the document is byte-identical, so the coverage table is untouched - and that table's own header row, `| Annex IV point | runward supplies | Required from the provider |`, restates the heading's content verbatim, so no reader loses the section's identity or its who-supplies-what framing. Surface: the `##` navigation heading of runward/compliance/eu-ai-act-readiness.md. No test and no machine contract reads it - grep over the repository finds the string only in src/lib/compliance.ts and CHANGELOG.md; the OSCAL links to this document by filename (`./eu-ai-act-readiness.md`), never by heading; and test/smoke.js's eu assertion still passes on the mutant's output. Contrast the four sibling heading mutants, filed hole because their bodies do NOT restate their heading.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"\"\u0001\"## Annex IV coverage map\"\u0001L.push(\"## Annex IV coverage map\");"
  },
  {
   "key": "321|12|321|14|StringLiteral|\"Stryker was here!\"",
   "line": 321,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between '## Annex IV coverage map' and the table's header row. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes. The coverage map still renders as a table: its header row and delimiter row remain adjacent.",
   "argument": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between '## Annex IV coverage map' and the table's header row. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes. The coverage map still renders as a table: its header row and delimiter row remain adjacent.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#4"
  },
  {
   "key": "322|12|322|80|StringLiteral|\"\"",
   "line": 322,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "HOLE. The mutant blanks the coverage map's column header row while leaving the delimiter `|---|---|---|` and the nine data rows in place. Measured: the string 'runward supplies' occurs EXACTLY ONCE in the whole document - in this row - so after the mutation the three columns of the Annex IV coverage map carry no labels at all and the middle column (what runward supplies) cannot be told from the right one (what the provider must produce). Row 8 then reads `| 8. Declaration of conformity | - | **signed legal act (art. 47)** |` with nothing saying the signed legal act is the PROVIDER's to produce: the overclaim shape of RWD-2026-0058, one document over. Secondary effect: with the header row blank the block is no longer a GFM table (a delimiter row must follow a header row), so it renders as pipe-delimited paragraph text. RECIPE: `runward init` then `runward compliance eu-ai-act`; BEFORE line 14 is `| Annex IV point | runward supplies | Required from the provider |`, AFTER it is empty (measured, both fixtures, rest byte-identical). Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output). In particular the nine `| N. ` assertions in test/unit/compliance.test.js read the DATA rows and never the header row.",
   "argument": "HOLE. The mutant blanks the coverage map's column header row while leaving the delimiter `|---|---|---|` and the nine data rows in place. Measured: the string 'runward supplies' occurs EXACTLY ONCE in the whole document - in this row - so after the mutation the three columns of the Annex IV coverage map carry no labels at all and the middle column (what runward supplies) cannot be told from the right one (what the provider must produce). Row 8 then reads `| 8. Declaration of conformity | - | **signed legal act (art. 47)** |` with nothing saying the signed legal act is the PROVIDER's to produce: the overclaim shape of RWD-2026-0058, one document over. Secondary effect: with the header row blank the block is no longer a GFM table (a delimiter row must follow a header row), so it renders as pipe-delimited paragraph text. RECIPE: `runward init` then `runward compliance eu-ai-act`; BEFORE line 14 is `| Annex IV point | runward supplies | Required from the provider |`, AFTER it is empty (measured, both fixtures, rest byte-identical). Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output). In particular the nine `| N. ` assertions in test/unit/compliance.test.js read the DATA rows and never the header row.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"\"\u0001\"| Annex IV point | runward supplies | Required from the provider |\"\u0001L.push(\"| Annex IV point | runward supplies | Required from the provider |\");"
  },
  {
   "key": "323|12|323|27|StringLiteral|\"\"",
   "line": 323,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Display-only. The mutant blanks the `|---|---|---|` delimiter, which stops the coverage map rendering as a GFM table (header row and nine data rows become paragraph text). Measured on both fixtures, nothing else changes: the labels 'Annex IV point / runward supplies / Required from the provider' still sit immediately above the rows, in order, so every claim and every who-owes-what attribution still ships and reads correctly. That is what separates it from the header-row mutant, which is filed hole: here the rendering degrades, no claim is lost. Surface: the GFM table rendering of the Annex IV coverage map. No test and no machine contract reads it - the golden pins the OSCAL JSON, the `| N. ` unit assertions match the untouched data rows, and test/smoke.js's eu assertion still passes on the mutant's output. ADR-0046 decision 1: no test should pin prose, and a table separator is prose.",
   "argument": "Display-only. The mutant blanks the `|---|---|---|` delimiter, which stops the coverage map rendering as a GFM table (header row and nine data rows become paragraph text). Measured on both fixtures, nothing else changes: the labels 'Annex IV point / runward supplies / Required from the provider' still sit immediately above the rows, in order, so every claim and every who-owes-what attribution still ships and reads correctly. That is what separates it from the header-row mutant, which is filed hole: here the rendering degrades, no claim is lost. Surface: the GFM table rendering of the Annex IV coverage map. No test and no machine contract reads it - the golden pins the OSCAL JSON, the `| N. ` unit assertions match the untouched data rows, and test/smoke.js's eu assertion still passes on the mutant's output. ADR-0046 decision 1: no test should pin prose, and a table separator is prose.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"\"\u0001\"|---|---|---|\"\u0001L.push(\"|---|---|---|\");"
  },
  {
   "key": "324|37|324|39|ArrayDeclaration|[\"Stryker was here\"]",
   "line": 324,
   "mutator": "ArrayDeclaration",
   "filing": "defence-in-depth",
   "evidence": "Same reachability argument as the `lens.highRisk?.` mutants: both shipped eu-ai-act lenses define `annexIv` as a nine-row array, so the `??` right operand is never evaluated and, PROBED, the rendered document is byte-identical on both fixtures. SENSITIVITY CONTROL: on a lens with no `annexIv`, the shipped code emits a coverage map with a header row and no data rows, while the mutant iterates the single string 'Stryker was here' and emits `| undefined | undefined | undefined |` - a coverage-map row asserting an Annex IV point that does not exist. That input cannot occur: (a) loadRegime accepts only the shipped filenames and refuses anything else with the shipped list; (b) test/unit/regimes.test.js refuses any stray file in regimes/ that is not `<known-regime>@<version>.json`; (c) test/unit/compliance.test.js asserts `!md.includes(\"undefined\")` on the default eu lens together with the exact `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and all nine `| N. ` Annex IV rows, so a future lens dropping a key reddens the unit suite before it can reach a user. Filed defence-in-depth rather than equivalent because the mutant is not semantically equivalent on the guarded input - it is only unreachable.",
   "argument": "Same reachability argument as the `lens.highRisk?.` mutants: both shipped eu-ai-act lenses define `annexIv` as a nine-row array, so the `??` right operand is never evaluated and, PROBED, the rendered document is byte-identical on both fixtures. SENSITIVITY CONTROL: on a lens with no `annexIv`, the shipped code emits a coverage map with a header row and no data rows, while the mutant iterates the single string 'Stryker was here' and emits `| undefined | undefined | undefined |` - a coverage-map row asserting an Annex IV point that does not exist. That input cannot occur: (a) loadRegime accepts only the shipped filenames and refuses anything else with the shipped list; (b) test/unit/regimes.test.js refuses any stray file in regimes/ that is not `<known-regime>@<version>.json`; (c) test/unit/compliance.test.js asserts `!md.includes(\"undefined\")` on the default eu lens together with the exact `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and all nine `| N. ` Annex IV rows, so a future lens dropping a key reddens the unit suite before it can reach a user. Filed defence-in-depth rather than equivalent because the mutant is not semantically equivalent on the guarded input - it is only unreachable.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001ArrayDeclaration\u0001[\"Stryker was here\"]\u0001[]\u0001for (const r of lens.annexIv ?? [])"
  },
  {
   "key": "326|12|326|14|StringLiteral|\"Stryker was here!\"",
   "line": 326,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the last Annex IV coverage row and the '## Point 2 - design decisions' heading. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes. The table terminates at the injected line exactly as it terminated at the blank one.",
   "argument": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the last Annex IV coverage row and the '## Point 2 - design decisions' heading. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes. The table terminates at the injected line exactly as it terminated at the blank one.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#5"
  },
  {
   "key": "327|12|327|100|StringLiteral|\"\"",
   "line": 327,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "HOLE - SECTION-HEADING FAMILY, argument written here and referred to by the three siblings. This document has no sectioning device other than its `##` headings, so blanking one does not merely delete a label: it MERGES that section's body into the section above. The document is an input to Annex IV technical documentation, filed point by point, and the headings are what say which Annex IV point each block of evidence answers. THIS one: '## Point 2 - design decisions (ADR journal, near-verbatim to the Annex IV requirement)'. RECIPE: `runward compliance eu-ai-act` on a mission with ratified ADRs (examples/request-triage has three); BEFORE, the ADR table sits under that heading; AFTER (measured, both fixtures, rest byte-identical) the heading line is empty and the `| ADR | Status |` table reads as a continuation of '## Annex IV coverage map'. What is lost is the only statement that this journal IS the Point 2 design-decision record and that it is 'near-verbatim to the Annex IV requirement'; what partially survives is coverage-map row 2, which names the ADR journal among Point 2's supplies - stated here so the entry survives a hostile reading. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output). No unit assertion reads any `##` heading of this document.",
   "argument": "HOLE - SECTION-HEADING FAMILY, argument written here and referred to by the three siblings. This document has no sectioning device other than its `##` headings, so blanking one does not merely delete a label: it MERGES that section's body into the section above. The document is an input to Annex IV technical documentation, filed point by point, and the headings are what say which Annex IV point each block of evidence answers. THIS one: '## Point 2 - design decisions (ADR journal, near-verbatim to the Annex IV requirement)'. RECIPE: `runward compliance eu-ai-act` on a mission with ratified ADRs (examples/request-triage has three); BEFORE, the ADR table sits under that heading; AFTER (measured, both fixtures, rest byte-identical) the heading line is empty and the `| ADR | Status |` table reads as a continuation of '## Annex IV coverage map'. What is lost is the only statement that this journal IS the Point 2 design-decision record and that it is 'near-verbatim to the Annex IV requirement'; what partially survives is coverage-map row 2, which names the ADR journal among Point 2's supplies - stated here so the entry survives a hostile reading. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output). No unit assertion reads any `##` heading of this document.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"\"\u0001\"## Point 2 — design decisions (ADR journal, near-verbatim to the Annex IV requirement)\"\u0001L.push(\"## Point 2 — design decisions (ADR journal, near-verbatim to the Annex IV requirement)\");"
  },
  {
   "key": "328|12|328|14|StringLiteral|\"Stryker was here!\"",
   "line": 328,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the '## Point 2 - design decisions' heading and the ADR table (or, on a fresh mission, the `_No ratified ADR found in runward/adr/._` line). Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes.",
   "argument": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the '## Point 2 - design decisions' heading and the ADR table (or, on a fresh mission, the `_No ratified ADR found in runward/adr/._` line). Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#6"
  },
  {
   "key": "330|12|330|14|StringLiteral|\"Stryker was here!\"",
   "line": 330,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the ADR table and the '## Agentic-risk coverage' heading. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes.",
   "argument": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the ADR table and the '## Agentic-risk coverage' heading. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#7"
  },
  {
   "key": "331|12|331|89|StringLiteral|\"\"",
   "line": 331,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "See the section-heading family argument on '## Point 2 - design decisions...'. THIS one: '## Agentic-risk coverage (OWASP ASI -> Point 2 cybersecurity / Point 5 risk)'. After the mutation (measured, both fixtures, rest byte-identical) the ten-row OWASP ASI table is merged into the '## Point 2 - design decisions (ADR journal...)' section, and the document no longer states anywhere that the ASI coverage feeds Point 2 cybersecurity and Point 5 risk management (art. 9): coverage-map row 5 lists Point 5's supplies as 'technical inputs (threat model, testing)' and never names the ASI table, so unlike the ADR case the attribution is restated nowhere. One trap named so nobody re-derives it: test/smoke.js DOES assert `includes(\"Agentic-risk coverage (OWASP ASI\")`, but on iso-42001-readiness.md - a different renderer - so it does not reach this line; verified by re-evaluating smoke's eu assertion against this mutant's output, which passes. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output).",
   "argument": "See the section-heading family argument on '## Point 2 - design decisions...'. THIS one: '## Agentic-risk coverage (OWASP ASI -> Point 2 cybersecurity / Point 5 risk)'. After the mutation (measured, both fixtures, rest byte-identical) the ten-row OWASP ASI table is merged into the '## Point 2 - design decisions (ADR journal...)' section, and the document no longer states anywhere that the ASI coverage feeds Point 2 cybersecurity and Point 5 risk management (art. 9): coverage-map row 5 lists Point 5's supplies as 'technical inputs (threat model, testing)' and never names the ASI table, so unlike the ADR case the attribution is restated nowhere. One trap named so nobody re-derives it: test/smoke.js DOES assert `includes(\"Agentic-risk coverage (OWASP ASI\")`, but on iso-42001-readiness.md - a different renderer - so it does not reach this line; verified by re-evaluating smoke's eu assertion against this mutant's output, which passes. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output).",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"\"\u0001\"## Agentic-risk coverage (OWASP ASI → Point 2 cybersecurity / Point 5 risk)\"\u0001L.push(\"## Agentic-risk coverage (OWASP ASI → Point 2 cybersecurity / Point 5 risk)\");"
  },
  {
   "key": "332|12|332|14|StringLiteral|\"Stryker was here!\"",
   "line": 332,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the '## Agentic-risk coverage' heading and the ASI table's header row. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes. The ASI table still renders: its header and delimiter rows remain adjacent.",
   "argument": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the '## Agentic-risk coverage' heading and the ASI table's header row. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes. The ASI table still renders: its header and delimiter rows remain adjacent.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#8"
  },
  {
   "key": "334|12|334|14|StringLiteral|\"Stryker was here!\"",
   "line": 334,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the ASI table and the '## Control-implementation status' heading. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes.",
   "argument": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the ASI table and the '## Control-implementation status' heading. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#9"
  },
  {
   "key": "335|12|335|73|StringLiteral|\"\"",
   "line": 335,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "See the section-heading family argument. THIS one: '## Control-implementation status (feeds Point 2 validation)'. After the mutation (measured on examples/request-triage, where the section carries the mission's real rule-conformance rows, and on a fresh mission where it carries the `_No filled Rule conformance manifest found yet_` line; rest of the document byte-identical) the `| Rule | Status | Evidence | Phase |` table is merged into '## Agentic-risk coverage (OWASP ASI -> Point 2 cybersecurity / Point 5 risk)', so the conformance manifest reads as agentic-risk coverage, and the phrase 'feeds Point 2 validation' - the only place this document binds the manifest to Annex IV Point 2 validation - disappears. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output).",
   "argument": "See the section-heading family argument. THIS one: '## Control-implementation status (feeds Point 2 validation)'. After the mutation (measured on examples/request-triage, where the section carries the mission's real rule-conformance rows, and on a fresh mission where it carries the `_No filled Rule conformance manifest found yet_` line; rest of the document byte-identical) the `| Rule | Status | Evidence | Phase |` table is merged into '## Agentic-risk coverage (OWASP ASI -> Point 2 cybersecurity / Point 5 risk)', so the conformance manifest reads as agentic-risk coverage, and the phrase 'feeds Point 2 validation' - the only place this document binds the manifest to Annex IV Point 2 validation - disappears. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output).",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"\"\u0001\"## Control-implementation status (feeds Point 2 validation)\"\u0001L.push(\"## Control-implementation status (feeds Point 2 validation)\");"
  },
  {
   "key": "336|12|336|14|StringLiteral|\"Stryker was here!\"",
   "line": 336,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the '## Control-implementation status' heading and the conformance table (or, on a fresh mission, the `_No filled Rule conformance manifest found yet_` line). Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes.",
   "argument": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the '## Control-implementation status' heading and the conformance table (or, on a fresh mission, the `_No filled Rule conformance manifest found yet_` line). Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#10"
  },
  {
   "key": "338|12|338|14|StringLiteral|\"Stryker was here!\"",
   "line": 338,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the conformance table and the '## Required from the provider' heading. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes.",
   "argument": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the conformance table and the '## Required from the provider' heading. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#11"
  },
  {
   "key": "339|12|339|73|StringLiteral|\"\"",
   "line": 339,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "See the section-heading family argument; this is its strongest instance. THIS one: '## Required from the provider (runward cannot produce this)'. After the mutation (measured, both fixtures, rest byte-identical) the three provider-owed bullets - '**Point 1** general description...', '**Point 7** harmonised-standards selection - **Point 8** the signed **EU declaration of conformity** (art. 47) - **Point 9** the **post-market monitoring plan** (art. 72(3)...)', and '**Art. 12 runtime event logs** - produced by the running system, not by runward' - are merged into '## Control-implementation status (feeds Point 2 validation)', immediately after the mission's rule-conformance table. A reader then meets the signed EU declaration of conformity and the post-market monitoring plan inside the section reporting what runward's gate has accounted for, with 'runward cannot produce this' gone. That inverts who owes what, in the one section whose job is to say what runward does NOT supply. Stated so the entry survives a hostile reading: 'Required from the provider' does occur once more, as the coverage map's third column header, but that heads a table some thirty-five lines earlier, not these bullets. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output).",
   "argument": "See the section-heading family argument; this is its strongest instance. THIS one: '## Required from the provider (runward cannot produce this)'. After the mutation (measured, both fixtures, rest byte-identical) the three provider-owed bullets - '**Point 1** general description...', '**Point 7** harmonised-standards selection - **Point 8** the signed **EU declaration of conformity** (art. 47) - **Point 9** the **post-market monitoring plan** (art. 72(3)...)', and '**Art. 12 runtime event logs** - produced by the running system, not by runward' - are merged into '## Control-implementation status (feeds Point 2 validation)', immediately after the mission's rule-conformance table. A reader then meets the signed EU declaration of conformity and the post-market monitoring plan inside the section reporting what runward's gate has accounted for, with 'runward cannot produce this' gone. That inverts who owes what, in the one section whose job is to say what runward does NOT supply. Stated so the entry survives a hostile reading: 'Required from the provider' does occur once more, as the coverage map's third column header, but that heads a table some thirty-five lines earlier, not these bullets. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output).",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"\"\u0001\"## Required from the provider (runward cannot produce this)\"\u0001L.push(\"## Required from the provider (runward cannot produce this)\");"
  },
  {
   "key": "340|12|340|14|StringLiteral|\"Stryker was here!\"",
   "line": 340,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the '## Required from the provider' heading and the first provider-required bullet. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes.",
   "argument": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the '## Required from the provider' heading and the first provider-required bullet. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#12"
  },
  {
   "key": "342|16|342|27|StringLiteral|``",
   "line": 342,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "HOLE, the most consequential in this group. Every item of the provider-required list is emitted as an EMPTY line, so '## Required from the provider (runward cannot produce this)' is followed by three blank lines and nothing else. RECIPE: `runward init` then `runward compliance eu-ai-act`, open runward/compliance/eu-ai-act-readiness.md. BEFORE, the section lists '**Point 1** general description (intended purpose, provider, versioning) - **Point 5** RMS governance & risk acceptance (art. 9).', '**Point 7** harmonised-standards selection - **Point 8** the signed **EU declaration of conformity** (art. 47) - **Point 9** the **post-market monitoring plan** (art. 72(3); the Commission must adopt guidance and a template by 2 September 2027).', and '**Art. 12 runtime event logs** - produced by the running system, not by runward.' AFTER (measured on both fixtures; the rest of the document byte-identical): three empty lines. A provider using the draft as their Annex IV gap list is told, by omission, that runward's pack leaves nothing for them to produce - the exact overclaim ADR-0040 and RWD-2026-0058 exist to prevent. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output). test/smoke.js does assert 'Required from the operator', but on iso-42001-readiness.md and on the heading, never on these items.",
   "argument": "HOLE, the most consequential in this group. Every item of the provider-required list is emitted as an EMPTY line, so '## Required from the provider (runward cannot produce this)' is followed by three blank lines and nothing else. RECIPE: `runward init` then `runward compliance eu-ai-act`, open runward/compliance/eu-ai-act-readiness.md. BEFORE, the section lists '**Point 1** general description (intended purpose, provider, versioning) - **Point 5** RMS governance & risk acceptance (art. 9).', '**Point 7** harmonised-standards selection - **Point 8** the signed **EU declaration of conformity** (art. 47) - **Point 9** the **post-market monitoring plan** (art. 72(3); the Commission must adopt guidance and a template by 2 September 2027).', and '**Art. 12 runtime event logs** - produced by the running system, not by runward.' AFTER (measured on both fixtures; the rest of the document byte-identical): three empty lines. A provider using the draft as their Annex IV gap list is told, by omission, that runward's pack leaves nothing for them to produce - the exact overclaim ADR-0040 and RWD-2026-0058 exist to prevent. Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output). test/smoke.js does assert 'Required from the operator', but on iso-42001-readiness.md and on the heading, never on these items.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001``\u0001`- ${item}`\u0001L.push(`- ${item}`);"
  },
  {
   "key": "343|12|343|14|StringLiteral|\"Stryker was here!\"",
   "line": 343,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the last provider-required bullet and the closing disclaimer. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes. Worth naming: with that blank line gone, Markdown lazy continuation folds both the injected line AND the `_Engineering framing, not legal advice; ..._` line into the last list item, so the closing disclaimer renders inside a bullet instead of as the document's closing statement. Its text still ships verbatim - measured - so no caveat is lost, which is why this stays display-only rather than joining the hole filed on the mutant that deletes that disclaimer outright.",
   "argument": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the blank line between the last provider-required bullet and the closing disclaimer. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes. Worth naming: with that blank line gone, Markdown lazy continuation folds both the injected line AND the `_Engineering framing, not legal advice; ..._` line into the last list item, so the closing disclaimer renders inside a bullet instead of as the document's closing statement. Its text still ships verbatim - measured - so no caveat is lost, which is why this stays display-only rather than joining the hole filed on the mutant that deletes that disclaimer outright.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#13"
  },
  {
   "key": "344|12|344|77|StringLiteral|``",
   "line": 344,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "HOLE. The mutant blanks the closing line, so the draft loses both halves of its closing qualifier: 'Engineering framing, not legal advice' and the lens's own disclaimerTail, 'Annex IV wording moves - confirm against the Official Journal (Reg. (EU) 2024/1689) before filing.' Measured: 'not legal advice' occurs EXACTLY ONCE in the document - this line - so after the mutation nothing anywhere in a document a provider files under Annex IV says it is not legal advice, and the only citation of the instrument the pack maps (Reg. (EU) 2024/1689) goes with it. RECIPE: any mission, `runward compliance eu-ai-act`; BEFORE the last non-empty line is `_Engineering framing, not legal advice; Annex IV wording moves - confirm against the Official Journal (Reg. (EU) 2024/1689) before filing._`; AFTER the document ends after the provider-required bullets (measured, both fixtures, rest byte-identical). Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output).",
   "argument": "HOLE. The mutant blanks the closing line, so the draft loses both halves of its closing qualifier: 'Engineering framing, not legal advice' and the lens's own disclaimerTail, 'Annex IV wording moves - confirm against the Official Journal (Reg. (EU) 2024/1689) before filing.' Measured: 'not legal advice' occurs EXACTLY ONCE in the document - this line - so after the mutation nothing anywhere in a document a provider files under Annex IV says it is not legal advice, and the only citation of the instrument the pack maps (Reg. (EU) 2024/1689) goes with it. RECIPE: any mission, `runward compliance eu-ai-act`; BEFORE the last non-empty line is `_Engineering framing, not legal advice; Annex IV wording moves - confirm against the Official Journal (Reg. (EU) 2024/1689) before filing._`; AFTER the document ends after the provider-required bullets (measured, both fixtures, rest byte-identical). Nothing in any leg catches it: test/unit/oscal-golden.test.js runs `runward compliance eu-ai-act` so this function EXECUTES on the covered path, but its byte assertion is on runward/compliance/oscal-component-definition.json (renderOscal's JSON) and never on the markdown this function returns; the only unit assertions that read this markdown are the four in test/unit/compliance.test.js, which pin a fragment of GATE_NON_SCOPE, /Draft/, the date, the absence of the literal 'undefined', 'not a conformity assessment', the lens stamp, the `bind from\\n> **2 December 2027 (Annex III) / 2 August 2028 (Annex I)**` block and the nine `| N. ` Annex IV data rows; and test/smoke.js's eu assertion still passes (re-evaluated against this mutant's rendered output).",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001``\u0001`_Engineering framing, not legal advice; ${lens.disclaimerTail}_`\u0001L.push(`_Engineering framing, not legal advice; ${lens.disclaimerTail}_`);"
  },
  {
   "key": "345|12|345|14|StringLiteral|\"Stryker was here!\"",
   "line": 345,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the final blank line, after the closing disclaimer; the emitted document then ends with the stray line. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes.",
   "argument": "See the separator-family argument on the first `L.push(\"\")` (occurrence 1). THIS occurrence is the final blank line, after the closing disclaimer; the emitted document then ends with the stray line. Measured on both fixtures: the rendered document differs by that one line only; no claim, caveat or attribution changes.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#14"
  },
  {
   "key": "346|27|346|31|StringLiteral|\"\"",
   "line": 346,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "AMBIGUITY RESOLVED FIRST, because the mutant line as given does not say which of the two `\"\\n\"` on `return L.join(\"\\n\") + \"\\n\";` it is. MEASURED: mutating the join separator (`L.join(\"\")`) collapses the whole document onto one line and is KILLED by test/unit/compliance.test.js, whose assertion `assert.match(eu, /bind from\\n> \\*\\*2 December 2027 \\(Annex III\\) \\/ 2 August 2028 \\(Annex I\\)\\*\\*/)` contains a literal newline - I applied it and the assertion fails. So the survivor is the second occurrence, the trailing `+ \"\\n\"`, which drops the emitted file's final newline. Effect measured on both fixtures: runward/compliance/eu-ai-act-readiness.md loses one trailing empty line and is otherwise byte-identical; every claim, caveat, table and attribution ships unchanged. Surface: the last byte of the emitted file. No test and no machine contract reads it - the golden fixture pins oscal-component-definition.json, not this document, and the evidence seal (evidence-lock.json) hashes the evidence the manifests point at, never a generated compliance artefact; test/smoke.js's eu assertion still passes on the mutant's output.",
   "argument": "AMBIGUITY RESOLVED FIRST, because the mutant line as given does not say which of the two `\"\\n\"` on `return L.join(\"\\n\") + \"\\n\";` it is. MEASURED: mutating the join separator (`L.join(\"\")`) collapses the whole document onto one line and is KILLED by test/unit/compliance.test.js, whose assertion `assert.match(eu, /bind from\\n> \\*\\*2 December 2027 \\(Annex III\\) \\/ 2 August 2028 \\(Annex I\\)\\*\\*/)` contains a literal newline - I applied it and the assertion fails. So the survivor is the second occurrence, the trailing `+ \"\\n\"`, which drops the emitted file's final newline. Effect measured on both fixtures: runward/compliance/eu-ai-act-readiness.md loses one trailing empty line and is otherwise byte-identical; every claim, caveat, table and attribution ships unchanged. Surface: the last byte of the emitted file. No test and no machine contract reads it - the golden fixture pins oscal-component-definition.json, not this document, and the evidence seal (evidence-lock.json) hashes the evidence the manifests point at, never a generated compliance artefact; test/smoke.js's eu assertion still passes on the mutant's output.",
   "stableKey": "compliance\u0001renderEuAiAct\u0001StringLiteral\u0001\"\"\u0001\"\\n\"\u0001return L.join(\"\\n\") + \"\\n\";"
  }
 ]
}
