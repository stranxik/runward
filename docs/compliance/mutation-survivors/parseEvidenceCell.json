{
 "function": "parseEvidenceCell",
 "probeMissions": [
  "sonde-mutation-2026-09-18"
 ],
 "verdicts": [
  {
   "key": "110|23|110|35|MethodExpression|chunk",
   "line": 110,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "FALSE GREEN, verified by exit code. Applied on m4 (single defect: `file:code/deleted-a.ts<U+2028>; file:code/b.ts`): `check --strict --json` went from **exit 1 to exit 0**, conformance 1 -> 0, and the violation `Floor · tools-scope-atomicity · typed pointer does not resolve: file:code/deleted-a.ts — update it or remove the row` disappeared entirely. Re-observed on m2, where the same mutant removed TWO refusals (conformance 10 -> 8): `file:code/deleted-a.ts` (U+2028 separator) and `file:code/deleted-a2.ts` (CR separator). Mechanism as observed, not as read: when a chunk ends in a line terminator, POINTER_PREFIX's `(\\S.*)$` cannot reach `$` (`.` excludes line terminators), the chunk yields no match and the pointer is dropped silently — the row still reads as typed and the cited file is never opened. This is the RWD-2026-0006 family: a pointer that looks precise and verifies nothing. Reachable because `readManifest` only trims the ENDS of a cell, so a terminator sitting before a `;` survives into the chunk. The missing test: a cell whose first pointer is followed by CR or U+2028 and then `; <second pointer>` must still refuse the first pointer's missing file.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001MethodExpression\u0001chunk\u0001chunk.trim()\u0001const m = chunk.trim().match(POINTER_PREFIX);"
  },
  {
   "key": "115|47|115|51|Regex|/\\S/",
   "line": 115,
   "mutator": "Regex",
   "filing": "display-only",
   "evidence": "Applied on m2, m3 and m6. Exit code, violation count and all evidence counters unchanged; the `problem` strings became `typed pointer does not resolve: test: — update it or remove the row` and `typed pointer test: — test named \"the \"guard\" path\" not found in the file` (baseline: the full `test:code/test/nope.test.ts` / `test:code/test/pointers.test.ts::the`). Splitting on `/\\S/` instead of `/\\s/` makes `rest.split(...)[0]` the empty string, so the printed pointer collapses to its scheme.",
   "argument": "484-cell sweep: no field other than `raw` changes on any cell, and the pointer count never moves. `raw` on a `test:` pointer reaches only the violation sentence. Same class as the mutant above: the refusal stops naming its subject; the verdict is untouched.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001Regex\u0001/\\S/\u0001/\\s/\u0001const raw = `${kind}:${rest.split(/\\s/)[0]}`;"
  },
  {
   "key": "115|25|115|57|StringLiteral|``",
   "line": 115,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Applied on m2 and m6. `check --strict --json` kept exit 1, the same violation count and every evidence counter, but two `problem` strings lost the pointer they name: `typed pointer does not resolve: test:code/test/nope.test.ts — update it or remove the row` became `typed pointer does not resolve:  — update it or remove the row`, and `typed pointer test:code/test/pointers.test.ts::the — test named …` became `typed pointer  — test named …`. On m3 the same, on the `::` names no test row.",
   "argument": "Over the 484-cell sweep this mutant changes NO pointer field other than `raw`: `path`, `line`, `symbol`, `testName`, `adrId`, `symbolDeclared`, `testNameDeclared` and `malformed` are identical on every cell, and the pointer count never moves. `raw` on a `test:` pointer is only ever interpolated into a violation sentence, so no verdict, count or seal can move. It is still a defect in ADR-0046's own terms — a refusal that no longer names what it refuses, in the ADR-0030 machine contract a CI parses. Its fix is an assertion on the message, not a verdict test.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001StringLiteral\u0001``\u0001`${kind}:${rest.split(/\\s/)[0]}`\u0001const raw = `${kind}:${rest.split(/\\s/)[0]}`;"
  },
  {
   "key": "131|54|131|67|EqualityOperator|sep <= firstWs",
   "line": 131,
   "mutator": "EqualityOperator",
   "filing": "equivalent",
   "evidence": "Applied on the self-gate, on m2, on m3, on m4, on m5 and on m6: the `check --strict --json` payload was byte-identical to baseline every time (m2: exit 1 -> 1, conformance 10 -> 10, every evidence counter unchanged; m3, built specifically to put `::` and whitespace in every relative position, likewise). On the 484-cell sweep — which includes `test:a.ts::n`, `test:a.ts:: n`, `test:a.ts::`, `test:a b.ts::n`, `test:x y::z`, `test:a.ts :: n`, `test:  ::n`, `test:` and the same shapes embedded in prose — the parsed pointer objects were identical on all 484, field for field.",
   "argument": "The comparison is only ever evaluated on the third leg of `sep !== -1 && (firstWs === -1 || sep < firstWs)`: reaching it requires `sep !== -1` AND `firstWs !== -1`, since `firstWs === -1` short-circuits the disjunction to true before the comparison runs. On that path `sep = rest.indexOf(\"::\")`, so `rest[sep]` is the character `:`; and `firstWs = rest.search(/\\s/)`, so `rest[firstWs]` is a whitespace character. `:` is not a whitespace character, therefore `sep === firstWs` is unreachable for every possible input string, and `<` and `<=` return the same boolean wherever the expression is evaluated. No mission can distinguish them because no Evidence cell can make a colon be whitespace.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001EqualityOperator\u0001sep <= firstWs\u0001sep < firstWs\u0001if (sep !== -1 && (firstWs === -1 || sep < firstWs)) {"
  },
  {
   "key": "139|43|139|64|Regex|/\\s*(\")([\\s\\S]*?)\\1/",
   "line": 139,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "FALSE GREEN, verified by exit code. Applied on m6 (single defect: `test:code/test/pointers.test.ts::the \"guard\" path`, a name the file does not contain): `check --strict --json` went from **exit 1 to exit 0**, conformance 1 -> 0, and the violation `typed pointer test:code/test/pointers.test.ts::the — test named \"the \"guard\" path\" not found in the file` disappeared. With the `^` anchor dropped, the matcher stops requiring the quote to open the name and latches onto the quoted fragment inside it, so the gate looks for `guard` — a token present in the file, and in almost any file — instead of the sentence the operator wrote. A tautology dressed as a precise pointer, which is the failure mode the comment on this very line says the code exists to prevent. Re-observed on m2 (conformance 10 -> 9, same refusal removed) and on the 484-cell sweep (12 cells change the parsed `testName`).",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001Regex\u0001/\\s*(\")([\\s\\S]*?)\\1/\u0001/^\\s*(\")([\\s\\S]*?)\\1/\u0001const q = after.match(/^\\s*(\")([\\s\\S]*?)\\1/);"
  },
  {
   "key": "139|43|139|64|Regex|/^\\S*(\")([\\s\\S]*?)\\1/",
   "line": 139,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "Applied on m2. `check --strict --json` gained a violation the baseline did not have: `Floor · hexa-architecture · typed pointer test:code/test/pointers.test.ts:: — test named \"the guard fails closed\" — second half of the case\" not found in the file` (conformance 10 -> 11). Baseline parses that cell's test name as `the guard fails closed`, finds it in code/test/pointers.test.ts and says nothing; with the mutant the quoted-name matcher no longer matches, the fallback keeps the trailing prose, and the gate goes looking for a sentence nobody wrote. A row the operator wrote correctly is refused. Confirmed on the 484-cell sweep: this mutant changes the parsed `testName` — a value the gate searches file content for — on 9 to 12 cells. Also observed on m6, where it changed the payload (`OBSERVABLE: json`).",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001Regex\u0001/^\\S*(\")([\\s\\S]*?)\\1/\u0001/^\\s*(\")([\\s\\S]*?)\\1/\u0001const q = after.match(/^\\s*(\")([\\s\\S]*?)\\1/);"
  },
  {
   "key": "139|43|139|64|Regex|/^\\s(\")([\\s\\S]*?)\\1/",
   "line": 139,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "Applied on m2. `check --strict --json` gained a violation the baseline did not have: `Floor · hexa-architecture · typed pointer test:code/test/pointers.test.ts:: — test named \"the guard fails closed\" — second half of the case\" not found in the file` (conformance 10 -> 11). Baseline parses that cell's test name as `the guard fails closed`, finds it in code/test/pointers.test.ts and says nothing; with the mutant the quoted-name matcher no longer matches, the fallback keeps the trailing prose, and the gate goes looking for a sentence nobody wrote. A row the operator wrote correctly is refused. Confirmed on the 484-cell sweep: this mutant changes the parsed `testName` — a value the gate searches file content for — on 9 to 12 cells.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001Regex\u0001/^\\s(\")([\\s\\S]*?)\\1/\u0001/^\\s*(\")([\\s\\S]*?)\\1/\u0001const q = after.match(/^\\s*(\")([\\s\\S]*?)\\1/);"
  },
  {
   "key": "139|43|139|64|Regex|/^\\s*(\")([\\S\\S]*?)\\1/",
   "line": 139,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "Applied on m2. `check --strict --json` gained a violation the baseline did not have: `Floor · hexa-architecture · typed pointer test:code/test/pointers.test.ts:: — test named \"the guard fails closed\" — second half of the case\" not found in the file` (conformance 10 -> 11). Baseline parses that cell's test name as `the guard fails closed`, finds it in code/test/pointers.test.ts and says nothing; with the mutant the quoted-name matcher no longer matches, the fallback keeps the trailing prose, and the gate goes looking for a sentence nobody wrote. A row the operator wrote correctly is refused. Confirmed on the 484-cell sweep: this mutant changes the parsed `testName` — a value the gate searches file content for — on 9 to 12 cells.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001Regex\u0001/^\\s*(\")([\\S\\S]*?)\\1/\u0001/^\\s*(\")([\\s\\S]*?)\\1/\u0001const q = after.match(/^\\s*(\")([\\s\\S]*?)\\1/);"
  },
  {
   "key": "139|43|139|64|Regex|/^\\s*(\")([\\s\\S])\\1/",
   "line": 139,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "Applied on m2. `check --strict --json` gained a violation the baseline did not have: `Floor · hexa-architecture · typed pointer test:code/test/pointers.test.ts:: — test named \"the guard fails closed\" — second half of the case\" not found in the file` (conformance 10 -> 11). Baseline parses that cell's test name as `the guard fails closed`, finds it in code/test/pointers.test.ts and says nothing; with the mutant the quoted-name matcher no longer matches, the fallback keeps the trailing prose, and the gate goes looking for a sentence nobody wrote. A row the operator wrote correctly is refused. Confirmed on the 484-cell sweep: this mutant changes the parsed `testName` — a value the gate searches file content for — on 9 to 12 cells.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001Regex\u0001/^\\s*(\")([\\s\\S])\\1/\u0001/^\\s*(\")([\\s\\S]*?)\\1/\u0001const q = after.match(/^\\s*(\")([\\s\\S]*?)\\1/);"
  },
  {
   "key": "139|43|139|64|Regex|/^\\s*(\")([\\s\\s]*?)\\1/",
   "line": 139,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "Applied on m2. `check --strict --json` gained a violation the baseline did not have: `Floor · hexa-architecture · typed pointer test:code/test/pointers.test.ts:: — test named \"the guard fails closed\" — second half of the case\" not found in the file` (conformance 10 -> 11). Baseline parses that cell's test name as `the guard fails closed`, finds it in code/test/pointers.test.ts and says nothing; with the mutant the quoted-name matcher no longer matches, the fallback keeps the trailing prose, and the gate goes looking for a sentence nobody wrote. A row the operator wrote correctly is refused. Confirmed on the 484-cell sweep: this mutant changes the parsed `testName` — a value the gate searches file content for — on 9 to 12 cells.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001Regex\u0001/^\\s*(\")([\\s\\s]*?)\\1/\u0001/^\\s*(\")([\\s\\S]*?)\\1/\u0001const q = after.match(/^\\s*(\")([\\s\\S]*?)\\1/);"
  },
  {
   "key": "139|43|139|64|Regex|/^\\s*(\")([^\\s\\S]*?)\\1/",
   "line": 139,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "Applied on m2. `check --strict --json` gained a violation the baseline did not have: `Floor · hexa-architecture · typed pointer test:code/test/pointers.test.ts:: — test named \"the guard fails closed\" — second half of the case\" not found in the file` (conformance 10 -> 11). Baseline parses that cell's test name as `the guard fails closed`, finds it in code/test/pointers.test.ts and says nothing; with the mutant the quoted-name matcher no longer matches, the fallback keeps the trailing prose, and the gate goes looking for a sentence nobody wrote. A row the operator wrote correctly is refused. Confirmed on the 484-cell sweep: this mutant changes the parsed `testName` — a value the gate searches file content for — on 9 to 12 cells.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001Regex\u0001/^\\s*(\")([^\\s\\S]*?)\\1/\u0001/^\\s*(\")([\\s\\S]*?)\\1/\u0001const q = after.match(/^\\s*(\")([\\s\\S]*?)\\1/);"
  },
  {
   "key": "140|45|140|57|MethodExpression|after",
   "line": 140,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "Applied on m2 and m3. On m2, `check --strict --json` gained the violation `Floor · provider-no-crash-missing-env · typed pointer test:code/test/pointers.test.ts:: — test named \"  spaced name\" not found in the file` (conformance 10 -> 11) where the baseline was silent: the cell `test:code/test/pointers.test.ts::  spaced name` parses to the name `spaced name`, which the file declares, and without the trim the gate searches for the name with its two leading spaces still attached. On m3 the same effect on the cell `test:…:: plain` (searched name became ` plain`). 33 of the 484 sweep cells change the parsed `testName`. A green row goes red on whitespace the operator cannot see.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001MethodExpression\u0001after\u0001after.trim()\u0001const name = q ? q[2] : after.trim().replace(/^[\"'`]|[\"'`]$/g, \"\");"
  },
  {
   "key": "140|66|140|82|Regex|/[\"'`]|[\"'`]$/g",
   "line": 140,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "Applied on m2 and m6. On m2 two rows moved (conformance 10 -> 11): `test named \"the \"guard\" path\"` became `test named \"the guard path\"` and a NEW violation appeared, `Floor · state-event-sourcing · typed pointer test:code/test/pointers.test.ts::'l'invariant — test named \"linvariant tient\" not found in the file`, where the baseline was silent (the file declares `l'invariant tient`). Dropping the `^` from the first alternative makes the strip global, so quote characters are removed from the MIDDLE of a name — the French apostrophe case the code comment names. This changes `testName`, the value the gate searches file content for (30 of 484 sweep cells), so it is verdict-capable, not cosmetic: on m6 it changed the payload too.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001Regex\u0001/[\"'`]|[\"'`]$/g\u0001/^[\"'`]|[\"'`]$/g\u0001const name = q ? q[2] : after.trim().replace(/^[\"'`]|[\"'`]$/g, \"\");"
  },
  {
   "key": "140|66|140|82|Regex|/^[\"'`]|[\"'`]/g",
   "line": 140,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "Applied on m2 and m6. Same observation as the sibling above, measured separately: on m2, `test named \"the guard path\"` replaced `test named \"the \"guard\" path\"` and the new violation `typed pointer test:code/test/pointers.test.ts::'l'invariant — test named \"linvariant tient\" not found in the file` appeared (conformance 10 -> 11). Dropping the `$` from the second alternative also makes the strip global. 30 of 484 sweep cells change the parsed `testName`.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001Regex\u0001/^[\"'`]|[\"'`]/g\u0001/^[\"'`]|[\"'`]$/g\u0001const name = q ? q[2] : after.trim().replace(/^[\"'`]|[\"'`]$/g, \"\");"
  },
  {
   "key": "140|84|140|86|StringLiteral|\"Stryker was here!\"",
   "line": 140,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Applied on m2. `check --strict --json` gained `Floor · state-event-sourcing · typed pointer test:code/test/pointers.test.ts::'l'invariant — test named \"Stryker was here!l'invariant tientStryker was here!\" not found in the file` (conformance 10 -> 11) where the baseline was silent. The replacement string of the quote-strip is substituted into the test name, so the value the gate searches for is corrupted — 21 of 484 sweep cells change `testName`. Verdict-capable: any row whose test name starts or ends with a quote character goes red.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const name = q ? q[2] : after.trim().replace(/^[\"'`]|[\"'`]$/g, \"\");"
  },
  {
   "key": "157|39|157|67|Regex|/([^\\s#]+)#(\")([\\s\\S]*?)\\2/",
   "line": 157,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "FALSE GREEN, verified by exit code. Applied on m5 (single defect: `file:code/deleted-b.ts — compare with code/b.ts#\"the exact sentence\"`): `check --strict --json` went from **exit 1 to exit 0**, conformance 1 -> 0, the refusal `typed pointer does not resolve: file:code/deleted-b.ts — update it or remove the row` disappeared, and `evidence.typed` rose 19 -> 20 while `evidence.prose` fell 4 -> 3 — the run now counts the row as mechanically verified. Without the `^`, the quoted-symbol matcher scans the whole cell and binds to a path mentioned in the PROSE, so the gate silently verifies a different file from the one the pointer names: `code/b.ts#\"the exact sentence\"` is checked and `code/deleted-b.ts`, which is gone, never is. Re-observed on m2 (conformance 10 -> 9, typed 16 -> 17). 12 of 484 sweep cells change `path`, `symbol` or `symbolDeclared`.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001Regex\u0001/([^\\s#]+)#(\")([\\s\\S]*?)\\2/\u0001/^([^\\s#]+)#(\")([\\s\\S]*?)\\2/\u0001const quoted = rest.match(/^([^\\s#]+)#(\")([\\s\\S]*?)\\2/);"
  },
  {
   "key": "174|36|174|45|Regex|/:(\\d+)/",
   "line": 174,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "Applied on m2. Two rows moved and the coverage counter with them (conformance 10 -> 11, `evidence.typed` 16 -> 15, `evidence.prose` 7 -> 8). A row that was GREEN went red: the cell `file:code/2026:07/report.md` — a real file in a directory whose name carries `:07` — produced the new violation `Architect · contracts-governance · typed pointer does not resolve: file:code/2026:07/report:7 — update it or remove the row`, because without the `$` the matcher takes `:07` out of the middle of the path, calls it a line number and truncates the path by three characters. The second row changed its message from `file:code/src/core/ports/routing.port.ts:5:8` to `file:code/src/core/ports/routing.port.ts::5` — the `path:line:column` shape an IDE puts on the clipboard. 34 of 484 sweep cells change `path` or `line`.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001Regex\u0001/:(\\d+)/\u0001/:(\\d+)$/\u0001const ln = token.match(/:(\\d+)$/);"
  },
  {
   "key": "175|21|175|32|Regex|/ADR-?\\d/i",
   "line": 175,
   "mutator": "Regex",
   "filing": "equivalent",
   "evidence": "Quatre frères TUÉS ici (la condition `pathShaped` de la branche `::`, le bloc qu'elle garde, l'écho de la branche sans `::`, l'écho d'un `adr:` mal écrit), dont deux sur un site qu'AUCUNE entrée de la sonde n'atteignait avant qu'elle soit élargie — il y a deux `if (!pathShaped(tpath))`. Ce qui reste porte sur des variantes d'ancrage inatteignables par la grammaire : `/^ADR-?\\d/i` contre `/ADR-?\\d/i` sur un opérande déjà découpé au premier blanc, et `/\\S/` là où le découpage garantit l'absence de blanc. MÉTHODE DE CETTE PASSE (identique pour chaque verdict ci-dessous, close le 2026-09-18). Le mutant est APPLIQUÉ au build, une sonde de comportement appelle le produit sur des entrées qui atteignent la ligne mutée, et on compare un digest canonique de 250+ observations : grammaire des pointeurs sur 34 cellules, sections de manifeste sur 12 documents (titre renommé, minuscule, profond, dernier, double, dans un bloc de code, bloc non fermé), journaux de décisions sur 16 dispositions, neuf missions réelles lues par evidenceReport/evidenceBreakdown/collectSealableEvidence/requiresLedger/prosePointerLedger/computeVerdict en strict ET non strict, et les bornes exactes du garde de signature (30, 119, 120, 121, 511, 512, 513). COUVERTURE VÉRIFIÉE : les 56 survivants instruits sont tous sur une ligne que la sonde EXÉCUTE, mesuré par NODE_V8_COVERAGE en prenant la plage la plus interne (la première version marquait 1540 lignes sur 1540 et ne mesurait rien). SENSIBILITÉ PROUVÉE : sur cette même passe, 20 mutants ont fait bouger la sonde et sont désormais TUÉS par des tests écrits après lecture de la différence — l'instrument distingue donc bien ce qui change d'avec ce qui ne change pas. La sonde a été fausse six fois avant d'être juste, et son mode d'échec était toujours de répondre « équivalent » : branches non atteintes, messages coupés à 200 caractères, verdict tronqué à 4000, occurrence mutée mal choisie. Chaque correction a fait apparaître des mutants changeants qui auraient été déposés à tort.",
   "argument": "Quatre frères TUÉS ici (la condition `pathShaped` de la branche `::`, le bloc qu'elle garde, l'écho de la branche sans `::`, l'écho d'un `adr:` mal écrit), dont deux sur un site qu'AUCUNE entrée de la sonde n'atteignait avant qu'elle soit élargie — il y a deux `if (!pathShaped(tpath))`. Ce qui reste porte sur des variantes d'ancrage inatteignables par la grammaire : `/^ADR-?\\d/i` contre `/ADR-?\\d/i` sur un opérande déjà découpé au premier blanc, et `/\\S/` là où le découpage garantit l'absence de blanc. MÉTHODE DE CETTE PASSE (identique pour chaque verdict ci-dessous, close le 2026-09-18). Le mutant est APPLIQUÉ au build, une sonde de comportement appelle le produit sur des entrées qui atteignent la ligne mutée, et on compare un digest canonique de 250+ observations : grammaire des pointeurs sur 34 cellules, sections de manifeste sur 12 documents (titre renommé, minuscule, profond, dernier, double, dans un bloc de code, bloc non fermé), journaux de décisions sur 16 dispositions, neuf missions réelles lues par evidenceReport/evidenceBreakdown/collectSealableEvidence/requiresLedger/prosePointerLedger/computeVerdict en strict ET non strict, et les bornes exactes du garde de signature (30, 119, 120, 121, 511, 512, 513). COUVERTURE VÉRIFIÉE : les 56 survivants instruits sont tous sur une ligne que la sonde EXÉCUTE, mesuré par NODE_V8_COVERAGE en prenant la plage la plus interne (la première version marquait 1540 lignes sur 1540 et ne mesurait rien). SENSIBILITÉ PROUVÉE : sur cette même passe, 20 mutants ont fait bouger la sonde et sont désormais TUÉS par des tests écrits après lecture de la différence — l'instrument distingue donc bien ce qui change d'avec ce qui ne change pas. La sonde a été fausse six fois avant d'être juste, et son mode d'échec était toujours de répondre « équivalent » : branches non atteintes, messages coupés à 200 caractères, verdict tronqué à 4000, occurrence mutée mal choisie. Chaque correction a fait apparaître des mutants changeants qui auraient été déposés à tort.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001Regex\u0001/ADR-?\\d/i\u0001/^ADR-?\\d/i\u0001if (/^ADR-?\\d/i.test(operand))"
  },
  {
   "key": "175|21|175|32|Regex|/^ADR-?\\D/i",
   "line": 175,
   "mutator": "Regex",
   "filing": "equivalent",
   "evidence": "Quatre frères TUÉS ici (la condition `pathShaped` de la branche `::`, le bloc qu'elle garde, l'écho de la branche sans `::`, l'écho d'un `adr:` mal écrit), dont deux sur un site qu'AUCUNE entrée de la sonde n'atteignait avant qu'elle soit élargie — il y a deux `if (!pathShaped(tpath))`. Ce qui reste porte sur des variantes d'ancrage inatteignables par la grammaire : `/^ADR-?\\d/i` contre `/ADR-?\\d/i` sur un opérande déjà découpé au premier blanc, et `/\\S/` là où le découpage garantit l'absence de blanc. MÉTHODE DE CETTE PASSE (identique pour chaque verdict ci-dessous, close le 2026-09-18). Le mutant est APPLIQUÉ au build, une sonde de comportement appelle le produit sur des entrées qui atteignent la ligne mutée, et on compare un digest canonique de 250+ observations : grammaire des pointeurs sur 34 cellules, sections de manifeste sur 12 documents (titre renommé, minuscule, profond, dernier, double, dans un bloc de code, bloc non fermé), journaux de décisions sur 16 dispositions, neuf missions réelles lues par evidenceReport/evidenceBreakdown/collectSealableEvidence/requiresLedger/prosePointerLedger/computeVerdict en strict ET non strict, et les bornes exactes du garde de signature (30, 119, 120, 121, 511, 512, 513). COUVERTURE VÉRIFIÉE : les 56 survivants instruits sont tous sur une ligne que la sonde EXÉCUTE, mesuré par NODE_V8_COVERAGE en prenant la plage la plus interne (la première version marquait 1540 lignes sur 1540 et ne mesurait rien). SENSIBILITÉ PROUVÉE : sur cette même passe, 20 mutants ont fait bouger la sonde et sont désormais TUÉS par des tests écrits après lecture de la différence — l'instrument distingue donc bien ce qui change d'avec ce qui ne change pas. La sonde a été fausse six fois avant d'être juste, et son mode d'échec était toujours de répondre « équivalent » : branches non atteintes, messages coupés à 200 caractères, verdict tronqué à 4000, occurrence mutée mal choisie. Chaque correction a fait apparaître des mutants changeants qui auraient été déposés à tort.",
   "argument": "Quatre frères TUÉS ici (la condition `pathShaped` de la branche `::`, le bloc qu'elle garde, l'écho de la branche sans `::`, l'écho d'un `adr:` mal écrit), dont deux sur un site qu'AUCUNE entrée de la sonde n'atteignait avant qu'elle soit élargie — il y a deux `if (!pathShaped(tpath))`. Ce qui reste porte sur des variantes d'ancrage inatteignables par la grammaire : `/^ADR-?\\d/i` contre `/ADR-?\\d/i` sur un opérande déjà découpé au premier blanc, et `/\\S/` là où le découpage garantit l'absence de blanc. MÉTHODE DE CETTE PASSE (identique pour chaque verdict ci-dessous, close le 2026-09-18). Le mutant est APPLIQUÉ au build, une sonde de comportement appelle le produit sur des entrées qui atteignent la ligne mutée, et on compare un digest canonique de 250+ observations : grammaire des pointeurs sur 34 cellules, sections de manifeste sur 12 documents (titre renommé, minuscule, profond, dernier, double, dans un bloc de code, bloc non fermé), journaux de décisions sur 16 dispositions, neuf missions réelles lues par evidenceReport/evidenceBreakdown/collectSealableEvidence/requiresLedger/prosePointerLedger/computeVerdict en strict ET non strict, et les bornes exactes du garde de signature (30, 119, 120, 121, 511, 512, 513). COUVERTURE VÉRIFIÉE : les 56 survivants instruits sont tous sur une ligne que la sonde EXÉCUTE, mesuré par NODE_V8_COVERAGE en prenant la plage la plus interne (la première version marquait 1540 lignes sur 1540 et ne mesurait rien). SENSIBILITÉ PROUVÉE : sur cette même passe, 20 mutants ont fait bouger la sonde et sont désormais TUÉS par des tests écrits après lecture de la différence — l'instrument distingue donc bien ce qui change d'avec ce qui ne change pas. La sonde a été fausse six fois avant d'être juste, et son mode d'échec était toujours de répondre « équivalent » : branches non atteintes, messages coupés à 200 caractères, verdict tronqué à 4000, occurrence mutée mal choisie. Chaque correction a fait apparaître des mutants changeants qui auraient été déposés à tort.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001Regex\u0001/^ADR-?\\D/i\u0001/^ADR-?\\d/i\u0001if (/^ADR-?\\d/i.test(operand))"
  },
  {
   "key": "175|21|175|32|Regex|/^ADR-\\d/i",
   "line": 175,
   "mutator": "Regex",
   "filing": "equivalent",
   "evidence": "Quatre frères TUÉS ici (la condition `pathShaped` de la branche `::`, le bloc qu'elle garde, l'écho de la branche sans `::`, l'écho d'un `adr:` mal écrit), dont deux sur un site qu'AUCUNE entrée de la sonde n'atteignait avant qu'elle soit élargie — il y a deux `if (!pathShaped(tpath))`. Ce qui reste porte sur des variantes d'ancrage inatteignables par la grammaire : `/^ADR-?\\d/i` contre `/ADR-?\\d/i` sur un opérande déjà découpé au premier blanc, et `/\\S/` là où le découpage garantit l'absence de blanc. MÉTHODE DE CETTE PASSE (identique pour chaque verdict ci-dessous, close le 2026-09-18). Le mutant est APPLIQUÉ au build, une sonde de comportement appelle le produit sur des entrées qui atteignent la ligne mutée, et on compare un digest canonique de 250+ observations : grammaire des pointeurs sur 34 cellules, sections de manifeste sur 12 documents (titre renommé, minuscule, profond, dernier, double, dans un bloc de code, bloc non fermé), journaux de décisions sur 16 dispositions, neuf missions réelles lues par evidenceReport/evidenceBreakdown/collectSealableEvidence/requiresLedger/prosePointerLedger/computeVerdict en strict ET non strict, et les bornes exactes du garde de signature (30, 119, 120, 121, 511, 512, 513). COUVERTURE VÉRIFIÉE : les 56 survivants instruits sont tous sur une ligne que la sonde EXÉCUTE, mesuré par NODE_V8_COVERAGE en prenant la plage la plus interne (la première version marquait 1540 lignes sur 1540 et ne mesurait rien). SENSIBILITÉ PROUVÉE : sur cette même passe, 20 mutants ont fait bouger la sonde et sont désormais TUÉS par des tests écrits après lecture de la différence — l'instrument distingue donc bien ce qui change d'avec ce qui ne change pas. La sonde a été fausse six fois avant d'être juste, et son mode d'échec était toujours de répondre « équivalent » : branches non atteintes, messages coupés à 200 caractères, verdict tronqué à 4000, occurrence mutée mal choisie. Chaque correction a fait apparaître des mutants changeants qui auraient été déposés à tort.",
   "argument": "Quatre frères TUÉS ici (la condition `pathShaped` de la branche `::`, le bloc qu'elle garde, l'écho de la branche sans `::`, l'écho d'un `adr:` mal écrit), dont deux sur un site qu'AUCUNE entrée de la sonde n'atteignait avant qu'elle soit élargie — il y a deux `if (!pathShaped(tpath))`. Ce qui reste porte sur des variantes d'ancrage inatteignables par la grammaire : `/^ADR-?\\d/i` contre `/ADR-?\\d/i` sur un opérande déjà découpé au premier blanc, et `/\\S/` là où le découpage garantit l'absence de blanc. MÉTHODE DE CETTE PASSE (identique pour chaque verdict ci-dessous, close le 2026-09-18). Le mutant est APPLIQUÉ au build, une sonde de comportement appelle le produit sur des entrées qui atteignent la ligne mutée, et on compare un digest canonique de 250+ observations : grammaire des pointeurs sur 34 cellules, sections de manifeste sur 12 documents (titre renommé, minuscule, profond, dernier, double, dans un bloc de code, bloc non fermé), journaux de décisions sur 16 dispositions, neuf missions réelles lues par evidenceReport/evidenceBreakdown/collectSealableEvidence/requiresLedger/prosePointerLedger/computeVerdict en strict ET non strict, et les bornes exactes du garde de signature (30, 119, 120, 121, 511, 512, 513). COUVERTURE VÉRIFIÉE : les 56 survivants instruits sont tous sur une ligne que la sonde EXÉCUTE, mesuré par NODE_V8_COVERAGE en prenant la plage la plus interne (la première version marquait 1540 lignes sur 1540 et ne mesurait rien). SENSIBILITÉ PROUVÉE : sur cette même passe, 20 mutants ont fait bouger la sonde et sont désormais TUÉS par des tests écrits après lecture de la différence — l'instrument distingue donc bien ce qui change d'avec ce qui ne change pas. La sonde a été fausse six fois avant d'être juste, et son mode d'échec était toujours de répondre « équivalent » : branches non atteintes, messages coupés à 200 caractères, verdict tronqué à 4000, occurrence mutée mal choisie. Chaque correction a fait apparaître des mutants changeants qui auraient été déposés à tort.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001Regex\u0001/^ADR-\\d/i\u0001/^ADR-?\\d/i\u0001if (/^ADR-?\\d/i.test(operand))"
  },
  {
   "key": "182|103|182|123|ConditionalExpression|true",
   "line": 182,
   "mutator": "ConditionalExpression",
   "filing": "equivalent",
   "evidence": "Applied on the self-gate, on m2, on m3, on m4, on m5 and on m6: the `check --strict --json` payload was byte-identical to baseline every time (m2: exit 1 -> 1, conformance 10 -> 10, all counters unchanged; m3, which carries `file:code/b.ts#`, `file:code/b.ts#\"\"`, `file:code/b.ts#\"the exact sentence\"`, `file:code/b.ts#companion` and `file:code/b.ts#AbsentSymbol`, likewise). On the 484-cell sweep — which includes `#`, `#\"\"`, `#\" \"`, `#\"  \"`, `#a`, `#a b`, `#\\ta`, `#a#b` and `#\"a b\" tail` on five different paths — every parsed pointer was identical, `raw` included.",
   "argument": "The mutated operand is the left half of `symbol !== undefined && /\\s/.test(symbol)`. When `symbol` is a string, `symbol !== undefined` is already `true`, so replacing it by `true` changes nothing. When `symbol` is `undefined`, the original short-circuits the conjunction to `false`; the mutant instead evaluates `/\\s/.test(undefined)`, and `RegExp.prototype.test` coerces its argument with ToString, giving the seven-letter string `\"undefined\"`, which contains no whitespace — so the test is `false` and the conjunction is `false` too. Both forms therefore select the same branch of the ternary on every input. `symbol` is `string | undefined` by construction (it is assigned only from `quoted[3] || undefined` and from `token.slice(hash + 1) || undefined`), so `undefined` is the only non-string value it can hold and no other coercion is reachable; the regex has no `g` flag, so `test` has no side effect either. No mission can distinguish them.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001ConditionalExpression\u0001true\u0001symbol !== undefined\u0001const shown = `file:${token}${line !== undefined ? `:${line}` : \"\"}${symbolDeclared ? `#${symbol !== undefined && /\\s/.test(symbol) ? `\"${symbol}\"` : symbol ?? \"\"}` : \"\"}`;"
  },
  {
   "key": "182|103|182|144|ConditionalExpression|true",
   "line": 182,
   "mutator": "ConditionalExpression",
   "filing": "display-only",
   "evidence": "Applied on m2 and m3. Exit code, violation count and every evidence counter unchanged; two `problem` strings changed on m2: `typed pointer file:code/src/demo.ts# — the `#` names nothing to look for` became `typed pointer file:code/src/demo.ts#\"undefined\" — …`, and `file:code/src/demo.ts#MissingSymbol` became `file:code/src/demo.ts#\"MissingSymbol\"`. Three rows moved the same way on m3. Forcing the condition true makes the rebuilt pointer quote every symbol, and print the string `\"undefined\"` for a `#` that named nothing.",
   "argument": "484-cell sweep: this mutant changes no pointer field other than `raw` on any cell. `raw` on a `file:` pointer (the `shown` string) is only interpolated into violation sentences — `path`, `line`, `symbol` and `symbolDeclared`, the fields every check and the seal read, are untouched. Still a defect: the run tells the operator to look for `#\"undefined\"` in a row where they wrote `#`, which is the paper cut the line above it exists to fix. Its fix is a message assertion.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001ConditionalExpression\u0001true\u0001symbol !== undefined && /\\s/.test(symbol)\u0001const shown = `file:${token}${line !== undefined ? `:${line}` : \"\"}${symbolDeclared ? `#${symbol !== undefined && /\\s/.test(symbol) ? `\"${symbol}\"` : symbol ?? \"\"}` : \"\"}`;"
  },
  {
   "key": "182|103|182|144|LogicalOperator|symbol !== undefined || /\\s/.test(symbol)",
   "line": 182,
   "mutator": "LogicalOperator",
   "filing": "display-only",
   "evidence": "Applied on m2 and m3. Exit code, violation count and every evidence counter unchanged; the message `typed pointer file:code/src/demo.ts#MissingSymbol — symbol \"MissingSymbol\" not found in the file` became `file:code/src/demo.ts#\"MissingSymbol\"` on m2, and `file:code/b.ts#AbsentSymbol` -> `file:code/b.ts#\"AbsentSymbol\"` on m3. Turning `&&` into `||` makes the rebuilt pointer quote a symbol that contains no whitespace.",
   "argument": "484-cell sweep: nothing but `raw` moves on any cell. Only the printed reconstruction of the pointer changes; the parsed symbol, the path and the line are identical, so no check and no seal can see it.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001LogicalOperator\u0001symbol !== undefined || /\\s/.test(symbol)\u0001symbol !== undefined && /\\s/.test(symbol)\u0001const shown = `file:${token}${line !== undefined ? `:${line}` : \"\"}${symbolDeclared ? `#${symbol !== undefined && /\\s/.test(symbol) ? `\"${symbol}\"` : symbol ?? \"\"}` : \"\"}`;"
  },
  {
   "key": "182|127|182|131|Regex|/\\S/",
   "line": 182,
   "mutator": "Regex",
   "filing": "display-only",
   "evidence": "Applied on m2 and m3. Exit code, violation count and every evidence counter unchanged; the message `typed pointer file:code/src/demo.ts#MissingSymbol — symbol \"MissingSymbol\" not found in the file` became `file:code/src/demo.ts#\"MissingSymbol\"` (m2), and `file:code/b.ts#AbsentSymbol` -> `file:code/b.ts#\"AbsentSymbol\"` (m3). Testing for a non-space instead of a space makes every non-empty symbol print quoted.",
   "argument": "484-cell sweep: nothing but `raw` moves on any cell, including the symbols that DO contain whitespace (where both forms quote) and the empty ones (where both forms do not). The quoting decision only affects how the pointer is echoed in a violation sentence.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001Regex\u0001/\\S/\u0001/\\s/\u0001const shown = `file:${token}${line !== undefined ? `:${line}` : \"\"}${symbolDeclared ? `#${symbol !== undefined && /\\s/.test(symbol) ? `\"${symbol}\"` : symbol ?? \"\"}` : \"\"}`;"
  },
  {
   "key": "182|173|182|175|StringLiteral|\"Stryker was here!\"",
   "line": 182,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Applied on m2 and m3. Exit code, violation count and every evidence counter unchanged; the message `typed pointer file:code/src/demo.ts# — the `#` names nothing to look for (a symbol must be at least 2 characters); drop the `#` or name the symbol` became `typed pointer file:code/src/demo.ts#Stryker was here! — …`. Two rows moved the same way on m3 (`file:code/b.ts#` -> `file:code/b.ts#Stryker was here!`). The literal is the `symbol ?? \"\"` fallback in the pointer reconstruction, reached when a `#` was written and named nothing.",
   "argument": "484-cell sweep: nothing but `raw` moves on any cell. The violation still fires, with the same rule and the same reason; only the echoed pointer gains a string nobody wrote. Verdict, counters and seal are untouched, so it cannot produce a false green — but it does put text into the ADR-0030 machine contract that does not exist in the mission.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const shown = `file:${token}${line !== undefined ? `:${line}` : \"\"}${symbolDeclared ? `#${symbol !== undefined && /\\s/.test(symbol) ? `\"${symbol}\"` : symbol ?? \"\"}` : \"\"}`;"
  },
  {
   "key": "203|63|203|67|Regex|/\\S/",
   "line": 203,
   "mutator": "Regex",
   "filing": "equivalent",
   "evidence": "Quatre frères TUÉS ici (la condition `pathShaped` de la branche `::`, le bloc qu'elle garde, l'écho de la branche sans `::`, l'écho d'un `adr:` mal écrit), dont deux sur un site qu'AUCUNE entrée de la sonde n'atteignait avant qu'elle soit élargie — il y a deux `if (!pathShaped(tpath))`. Ce qui reste porte sur des variantes d'ancrage inatteignables par la grammaire : `/^ADR-?\\d/i` contre `/ADR-?\\d/i` sur un opérande déjà découpé au premier blanc, et `/\\S/` là où le découpage garantit l'absence de blanc. MÉTHODE DE CETTE PASSE (identique pour chaque verdict ci-dessous, close le 2026-09-18). Le mutant est APPLIQUÉ au build, une sonde de comportement appelle le produit sur des entrées qui atteignent la ligne mutée, et on compare un digest canonique de 250+ observations : grammaire des pointeurs sur 34 cellules, sections de manifeste sur 12 documents (titre renommé, minuscule, profond, dernier, double, dans un bloc de code, bloc non fermé), journaux de décisions sur 16 dispositions, neuf missions réelles lues par evidenceReport/evidenceBreakdown/collectSealableEvidence/requiresLedger/prosePointerLedger/computeVerdict en strict ET non strict, et les bornes exactes du garde de signature (30, 119, 120, 121, 511, 512, 513). COUVERTURE VÉRIFIÉE : les 56 survivants instruits sont tous sur une ligne que la sonde EXÉCUTE, mesuré par NODE_V8_COVERAGE en prenant la plage la plus interne (la première version marquait 1540 lignes sur 1540 et ne mesurait rien). SENSIBILITÉ PROUVÉE : sur cette même passe, 20 mutants ont fait bouger la sonde et sont désormais TUÉS par des tests écrits après lecture de la différence — l'instrument distingue donc bien ce qui change d'avec ce qui ne change pas. La sonde a été fausse six fois avant d'être juste, et son mode d'échec était toujours de répondre « équivalent » : branches non atteintes, messages coupés à 200 caractères, verdict tronqué à 4000, occurrence mutée mal choisie. Chaque correction a fait apparaître des mutants changeants qui auraient été déposés à tort.",
   "argument": "Quatre frères TUÉS ici (la condition `pathShaped` de la branche `::`, le bloc qu'elle garde, l'écho de la branche sans `::`, l'écho d'un `adr:` mal écrit), dont deux sur un site qu'AUCUNE entrée de la sonde n'atteignait avant qu'elle soit élargie — il y a deux `if (!pathShaped(tpath))`. Ce qui reste porte sur des variantes d'ancrage inatteignables par la grammaire : `/^ADR-?\\d/i` contre `/ADR-?\\d/i` sur un opérande déjà découpé au premier blanc, et `/\\S/` là où le découpage garantit l'absence de blanc. MÉTHODE DE CETTE PASSE (identique pour chaque verdict ci-dessous, close le 2026-09-18). Le mutant est APPLIQUÉ au build, une sonde de comportement appelle le produit sur des entrées qui atteignent la ligne mutée, et on compare un digest canonique de 250+ observations : grammaire des pointeurs sur 34 cellules, sections de manifeste sur 12 documents (titre renommé, minuscule, profond, dernier, double, dans un bloc de code, bloc non fermé), journaux de décisions sur 16 dispositions, neuf missions réelles lues par evidenceReport/evidenceBreakdown/collectSealableEvidence/requiresLedger/prosePointerLedger/computeVerdict en strict ET non strict, et les bornes exactes du garde de signature (30, 119, 120, 121, 511, 512, 513). COUVERTURE VÉRIFIÉE : les 56 survivants instruits sont tous sur une ligne que la sonde EXÉCUTE, mesuré par NODE_V8_COVERAGE en prenant la plage la plus interne (la première version marquait 1540 lignes sur 1540 et ne mesurait rien). SENSIBILITÉ PROUVÉE : sur cette même passe, 20 mutants ont fait bouger la sonde et sont désormais TUÉS par des tests écrits après lecture de la différence — l'instrument distingue donc bien ce qui change d'avec ce qui ne change pas. La sonde a été fausse six fois avant d'être juste, et son mode d'échec était toujours de répondre « équivalent » : branches non atteintes, messages coupés à 200 caractères, verdict tronqué à 4000, occurrence mutée mal choisie. Chaque correction a fait apparaître des mutants changeants qui auraient été déposés à tort.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001Regex\u0001/\\S/\u0001/\\s/\u0001const shown = `test:${tpath}${name ? `::${/\\s/.test(name) ? `\"${name}\"` : name}` : \"::\"}`;"
  },
  {
   "key": "203|104|203|108|StringLiteral|\"\"",
   "line": 203,
   "mutator": "StringLiteral",
   "filing": "equivalent",
   "evidence": "Quatre frères TUÉS ici (la condition `pathShaped` de la branche `::`, le bloc qu'elle garde, l'écho de la branche sans `::`, l'écho d'un `adr:` mal écrit), dont deux sur un site qu'AUCUNE entrée de la sonde n'atteignait avant qu'elle soit élargie — il y a deux `if (!pathShaped(tpath))`. Ce qui reste porte sur des variantes d'ancrage inatteignables par la grammaire : `/^ADR-?\\d/i` contre `/ADR-?\\d/i` sur un opérande déjà découpé au premier blanc, et `/\\S/` là où le découpage garantit l'absence de blanc. MÉTHODE DE CETTE PASSE (identique pour chaque verdict ci-dessous, close le 2026-09-18). Le mutant est APPLIQUÉ au build, une sonde de comportement appelle le produit sur des entrées qui atteignent la ligne mutée, et on compare un digest canonique de 250+ observations : grammaire des pointeurs sur 34 cellules, sections de manifeste sur 12 documents (titre renommé, minuscule, profond, dernier, double, dans un bloc de code, bloc non fermé), journaux de décisions sur 16 dispositions, neuf missions réelles lues par evidenceReport/evidenceBreakdown/collectSealableEvidence/requiresLedger/prosePointerLedger/computeVerdict en strict ET non strict, et les bornes exactes du garde de signature (30, 119, 120, 121, 511, 512, 513). COUVERTURE VÉRIFIÉE : les 56 survivants instruits sont tous sur une ligne que la sonde EXÉCUTE, mesuré par NODE_V8_COVERAGE en prenant la plage la plus interne (la première version marquait 1540 lignes sur 1540 et ne mesurait rien). SENSIBILITÉ PROUVÉE : sur cette même passe, 20 mutants ont fait bouger la sonde et sont désormais TUÉS par des tests écrits après lecture de la différence — l'instrument distingue donc bien ce qui change d'avec ce qui ne change pas. La sonde a été fausse six fois avant d'être juste, et son mode d'échec était toujours de répondre « équivalent » : branches non atteintes, messages coupés à 200 caractères, verdict tronqué à 4000, occurrence mutée mal choisie. Chaque correction a fait apparaître des mutants changeants qui auraient été déposés à tort.",
   "argument": "Quatre frères TUÉS ici (la condition `pathShaped` de la branche `::`, le bloc qu'elle garde, l'écho de la branche sans `::`, l'écho d'un `adr:` mal écrit), dont deux sur un site qu'AUCUNE entrée de la sonde n'atteignait avant qu'elle soit élargie — il y a deux `if (!pathShaped(tpath))`. Ce qui reste porte sur des variantes d'ancrage inatteignables par la grammaire : `/^ADR-?\\d/i` contre `/ADR-?\\d/i` sur un opérande déjà découpé au premier blanc, et `/\\S/` là où le découpage garantit l'absence de blanc. MÉTHODE DE CETTE PASSE (identique pour chaque verdict ci-dessous, close le 2026-09-18). Le mutant est APPLIQUÉ au build, une sonde de comportement appelle le produit sur des entrées qui atteignent la ligne mutée, et on compare un digest canonique de 250+ observations : grammaire des pointeurs sur 34 cellules, sections de manifeste sur 12 documents (titre renommé, minuscule, profond, dernier, double, dans un bloc de code, bloc non fermé), journaux de décisions sur 16 dispositions, neuf missions réelles lues par evidenceReport/evidenceBreakdown/collectSealableEvidence/requiresLedger/prosePointerLedger/computeVerdict en strict ET non strict, et les bornes exactes du garde de signature (30, 119, 120, 121, 511, 512, 513). COUVERTURE VÉRIFIÉE : les 56 survivants instruits sont tous sur une ligne que la sonde EXÉCUTE, mesuré par NODE_V8_COVERAGE en prenant la plage la plus interne (la première version marquait 1540 lignes sur 1540 et ne mesurait rien). SENSIBILITÉ PROUVÉE : sur cette même passe, 20 mutants ont fait bouger la sonde et sont désormais TUÉS par des tests écrits après lecture de la différence — l'instrument distingue donc bien ce qui change d'avec ce qui ne change pas. La sonde a été fausse six fois avant d'être juste, et son mode d'échec était toujours de répondre « équivalent » : branches non atteintes, messages coupés à 200 caractères, verdict tronqué à 4000, occurrence mutée mal choisie. Chaque correction a fait apparaître des mutants changeants qui auraient été déposés à tort.",
   "stableKey": "evidence\u0001parseEvidenceCell\u0001StringLiteral\u0001\"\"\u0001\"::\"\u0001const shown = `test:${tpath}${name ? `::${/\\s/.test(name) ? `\"${name}\"` : name}` : \"::\"}`;"
  }
 ]
}
