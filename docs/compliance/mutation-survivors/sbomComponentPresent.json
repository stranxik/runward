{
 "function": "sbomComponentPresent",
 "probeMissions": [],
 "verdicts": [
  {
   "key": "288|11|290|6|BlockStatement|{}",
   "line": 288,
   "mutator": "BlockStatement",
   "filing": "equivalent",
   "evidence": "Emptying the catch leaves bom undefined, and the next guard `!Array.isArray(bom?.components)` returns the same \"unparseable\". Measured on 19 malformed documents crossed with 13 identities plus the full 2500-comparison fuzz: 0 differences, on a fuzz that reports differences for the known-behavioural mutants.",
   "argument": "Emptying the catch leaves bom undefined, and the next guard `!Array.isArray(bom?.components)` returns the same \"unparseable\". Measured on 19 malformed documents crossed with 13 identities plus the full 2500-comparison fuzz: 0 differences, on a fuzz that reports differences for the known-behavioural mutants.",
   "stableKey": "tool-adapters\u0001sbomComponentPresent\u0001BlockStatement\u0001{}\u0001{ return \"unparseable\"; }\u0001catch {"
  },
  {
   "key": "291|24|291|39|OptionalChaining|bom.components",
   "line": 291,
   "mutator": "OptionalChaining",
   "filing": "hole",
   "evidence": "Removing the optional chain dereferences a null BOM: measured sbomComponentPresent('null', 'a@1') returned \"unparseable\" before, throws TypeError \"Cannot read properties of null (reading 'components')\" after.",
   "argument": "Removing the optional chain dereferences a null BOM: measured sbomComponentPresent('null', 'a@1') returned \"unparseable\" before, throws TypeError \"Cannot read properties of null (reading 'components')\" after.",
   "stableKey": "tool-adapters\u0001sbomComponentPresent\u0001OptionalChaining\u0001bom.components\u0001bom?.components\u0001const components = bom?.components;"
  },
  {
   "key": "299|51|299|61|OptionalChaining|c.version",
   "line": 299,
   "mutator": "OptionalChaining",
   "filing": "equivalent",
   "evidence": "`c?.version` is only evaluated once `typeof c?.name === \"string\"` has succeeded, which already proves c is non-nullish, so the optional chain can never be the operand that saves the access. Measured: the full 75-case bench (including null, primitive and array components) and the 2500-comparison fuzz both show 0 differences, on a fuzz that flags the neighbouring name/version mutants.",
   "argument": "`c?.version` is only evaluated once `typeof c?.name === \"string\"` has succeeded, which already proves c is non-nullish, so the optional chain can never be the operand that saves the access. Measured: the full 75-case bench (including null, primitive and array components) and the 2500-comparison fuzz both show 0 differences, on a fuzz that flags the neighbouring name/version mutants.",
   "stableKey": "tool-adapters\u0001sbomComponentPresent\u0001OptionalChaining\u0001c.version\u0001c?.version\u0001if (typeof c?.name === \"string\" && typeof c?.version === \"string\" && `${c.name}@${c.version}` === want)"
  }
 ]
}
