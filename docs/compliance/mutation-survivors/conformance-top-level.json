{
 "function": "(top level)",
 "probeMissions": [],
 "verdicts": [
  {
   "key": "11|66|11|77|StringLiteral|\"\"",
   "line": 11,
   "mutator": "StringLiteral",
   "filing": "defence-in-depth",
   "evidence": "KILLED BY THE smoke LEG, verified by replay (2026-08-27, mutant applied to a package-shaped copy of dist inside an isolated worktree, never to the sources). The mutant empties the Architect entry's label in GATED_DELIVERABLES; measured on the shipped example mission: the green gated line renders '✓ : 6 rule(s) accounted for' instead of '✓ Architect: 6 rule(s) accounted for', and the same empty label flows into check --json (conformance[].scope) and the compliance pack. test/smoke.js replayed against the mutated build: exit 1, FAIL at 'example mission passes check --strict across architect/floor/govern/handover (exit 0)' — smoke.js:259 asserts exStrict.includes(\"Architect:\") on the example's strict run, which is exactly the string the mutant deletes. The mission's exit code itself never changes (labels do not gate), so the smoke assertion on the rendered label is the one thing standing between this mutant and a silently anonymous verdict.",
   "argument": "KILLED BY THE smoke LEG, verified by replay (2026-08-27, mutant applied to a package-shaped copy of dist inside an isolated worktree, never to the sources). The mutant empties the Architect entry's label in GATED_DELIVERABLES; measured on the shipped example mission: the green gated line renders '✓ : 6 rule(s) accounted for' instead of '✓ Architect: 6 rule(s) accounted for', and the same empty label flows into check --json (conformance[].scope) and the compliance pack. test/smoke.js replayed against the mutated build: exit 1, FAIL at 'example mission passes check --strict across architect/floor/govern/handover (exit 0)' — smoke.js:259 asserts exStrict.includes(\"Architect:\") on the example's strict run, which is exactly the string the mutant deletes. The mission's exit code itself never changes (labels do not gate), so the smoke assertion on the rendered label is the one thing standing between this mutant and a silently anonymous verdict.",
   "stableKey": "conformance\u0001(top level)\u0001StringLiteral\u0001\"\"\u0001\"Architect\"\u0001{ phase: \"architect\", deliverable: \"architecture.md\", label: \"Architect\" },"
  },
  {
   "key": "12|71|12|81|StringLiteral|\"\"",
   "line": 12,
   "mutator": "StringLiteral",
   "filing": "defence-in-depth",
   "evidence": "KILLED BY THE smoke LEG, verified by replay (2026-08-27). Measured on the shipped example: '✓ Topology: 4 rule(s) accounted for' becomes '✓ : 4 rule(s) accounted for' (exit code unchanged), and the label also empties in check --json and the compliance pack. test/smoke.js replayed against the mutated build: exit 1, FAIL at 'check --strict gates the execution-topology deliverable (ADR-0017 app/infra double vision)' — smoke.js:184 asserts strictFresh.includes(\"Topology\") on a fresh init's red strict run, and the only capital-T 'Topology' in that output is this gated label on the violation lines (the presence table says 'Execution topology', lower-case), so the assertion is genuinely load-bearing on the mutated string.",
   "argument": "KILLED BY THE smoke LEG, verified by replay (2026-08-27). Measured on the shipped example: '✓ Topology: 4 rule(s) accounted for' becomes '✓ : 4 rule(s) accounted for' (exit code unchanged), and the label also empties in check --json and the compliance pack. test/smoke.js replayed against the mutated build: exit 1, FAIL at 'check --strict gates the execution-topology deliverable (ADR-0017 app/infra double vision)' — smoke.js:184 asserts strictFresh.includes(\"Topology\") on a fresh init's red strict run, and the only capital-T 'Topology' in that output is this gated label on the violation lines (the presence table says 'Execution topology', lower-case), so the assertion is genuinely load-bearing on the mutated string.",
   "stableKey": "conformance\u0001(top level)\u0001StringLiteral\u0001\"\"\u0001\"Topology\"\u0001{ phase: \"topology\", deliverable: \"execution-topology.md\", label: \"Topology\" },"
  },
  {
   "key": "14|74|14|82|StringLiteral|\"\"",
   "line": 14,
   "mutator": "StringLiteral",
   "filing": "defence-in-depth",
   "evidence": "KILLED BY THE smoke LEG, verified by replay (2026-08-27). Measured on the shipped example: '✓ Govern: 12 rule(s) accounted for' becomes '✓ : 12 rule(s) accounted for' (exit code unchanged; the label also empties in check --json and the compliance pack). test/smoke.js replayed against the mutated build: exit 1, FAIL at 'example mission passes check --strict across architect/floor/govern/handover (exit 0)' — smoke.js:259 asserts exStrict.includes(\"Govern:\"), and the mission.js presence label is '5 · Govern (day zero)' (no colon), so only the gated label satisfies it.",
   "argument": "KILLED BY THE smoke LEG, verified by replay (2026-08-27). Measured on the shipped example: '✓ Govern: 12 rule(s) accounted for' becomes '✓ : 12 rule(s) accounted for' (exit code unchanged; the label also empties in check --json and the compliance pack). test/smoke.js replayed against the mutated build: exit 1, FAIL at 'example mission passes check --strict across architect/floor/govern/handover (exit 0)' — smoke.js:259 asserts exStrict.includes(\"Govern:\"), and the mission.js presence label is '5 · Govern (day zero)' (no colon), so only the gated label satisfies it.",
   "stableKey": "conformance\u0001(top level)\u0001StringLiteral\u0001\"\"\u0001\"Govern\"\u0001{ phase: \"govern\", deliverable: \"governance/threat-model.md\", label: \"Govern\" },"
  },
  {
   "key": "15|61|15|71|StringLiteral|\"\"",
   "line": 15,
   "mutator": "StringLiteral",
   "filing": "defence-in-depth",
   "evidence": "KILLED BY THE smoke LEG, verified by replay (2026-08-27). Measured on the shipped example: '✓ Handover: 4 rule(s) accounted for' becomes '✓ : 4 rule(s) accounted for' (exit code unchanged; the label also empties in check --json and the compliance pack). test/smoke.js replayed against the mutated build: exit 1, FAIL at 'example mission passes check --strict across architect/floor/govern/handover (exit 0)' — smoke.js:259 asserts exStrict.includes(\"Handover:\"), a string only the gated label produces.",
   "argument": "KILLED BY THE smoke LEG, verified by replay (2026-08-27). Measured on the shipped example: '✓ Handover: 4 rule(s) accounted for' becomes '✓ : 4 rule(s) accounted for' (exit code unchanged; the label also empties in check --json and the compliance pack). test/smoke.js replayed against the mutated build: exit 1, FAIL at 'example mission passes check --strict across architect/floor/govern/handover (exit 0)' — smoke.js:259 asserts exStrict.includes(\"Handover:\"), a string only the gated label produces.",
   "stableKey": "conformance\u0001(top level)\u0001StringLiteral\u0001\"\"\u0001\"Handover\"\u0001{ phase: \"handover\", deliverable: \"handover.md\", label: \"Handover\" },"
  },
  {
   "key": "17|21|17|50|Regex|/---\\r?\\n([\\s\\S]*?)\\r?\\n---/",
   "line": 17,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "The gate-side twin of the compliance anchor survivor (compliance-top-level.json, key 30|21|30|50), re-probed here on conformance.ts's own FRONTMATTER because this copy feeds parseRuleMeta — expectedRules, ruleSignatures, the verdict itself. RECIPE (2026-08-27, mutant applied to a package-shaped dist copy): shipped example + vendored runward/rules (all 64 package rules, hashes recorded in scaffold-lock.json so corpusDivergence is 'verifiable' and clean — house rules are explicitly legitimate) + one house note 'house-quoted-note.md' whose first 800 bytes are prose and which QUOTES a '---/impact: CRITICAL/phases: [architect]/---' block beyond byte 800 (past the 800-byte head corpusDivergence's own extra-rule sniff reads). MEASURED: shipped build exit 0; mutated build exit 1 — '✗ Architect · house-quoted-note — not accounted for in the Rule conformance manifest'. Dropping the anchor lets any frontmatter-shaped block anywhere in a rule file be read as frontmatter, so a prose note becomes an expected CRITICAL rule and the gate reddens a green mission — a false red the operator can only clear by writing a manifest row for a rule nobody wrote; the same mechanism can hand ruleSignatures a phantom 'signature:' quoted in prose and turn the regex screen on against text. Opposite failure direction from the compliance twin (there: coverage inflated in the pack that leaves the building), same blind spot: test/unit/frontmatter-crlf.test.js's fixtures all carry their frontmatter at byte 0, so the '^' is never load-bearing in the unit net, and audit-corpus's CRLF honest case keeps frontmatter at byte 0 too. All six full-net legs plus the self-gate replayed against the mutated build: PASS.",
   "argument": "The gate-side twin of the compliance anchor survivor (compliance-top-level.json, key 30|21|30|50), re-probed here on conformance.ts's own FRONTMATTER because this copy feeds parseRuleMeta — expectedRules, ruleSignatures, the verdict itself. RECIPE (2026-08-27, mutant applied to a package-shaped dist copy): shipped example + vendored runward/rules (all 64 package rules, hashes recorded in scaffold-lock.json so corpusDivergence is 'verifiable' and clean — house rules are explicitly legitimate) + one house note 'house-quoted-note.md' whose first 800 bytes are prose and which QUOTES a '---/impact: CRITICAL/phases: [architect]/---' block beyond byte 800 (past the 800-byte head corpusDivergence's own extra-rule sniff reads). MEASURED: shipped build exit 0; mutated build exit 1 — '✗ Architect · house-quoted-note — not accounted for in the Rule conformance manifest'. Dropping the anchor lets any frontmatter-shaped block anywhere in a rule file be read as frontmatter, so a prose note becomes an expected CRITICAL rule and the gate reddens a green mission — a false red the operator can only clear by writing a manifest row for a rule nobody wrote; the same mechanism can hand ruleSignatures a phantom 'signature:' quoted in prose and turn the regex screen on against text. Opposite failure direction from the compliance twin (there: coverage inflated in the pack that leaves the building), same blind spot: test/unit/frontmatter-crlf.test.js's fixtures all carry their frontmatter at byte 0, so the '^' is never load-bearing in the unit net, and audit-corpus's CRLF honest case keeps frontmatter at byte 0 too. All six full-net legs plus the self-gate replayed against the mutated build: PASS.",
   "stableKey": "conformance\u0001(top level)\u0001Regex\u0001/---\\r?\\n([\\s\\S]*?)\\r?\\n---/\u0001/^---\\r?\\n([\\s\\S]*?)\\r?\\n---/\u0001const FRONTMATTER = /^---\\r?\\n([\\s\\S]*?)\\r?\\n---/;"
  }
 ]
}
