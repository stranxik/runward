{
 "function": "corpusDivergence",
 "probeMissions": [],
 "verdicts": [
  {
   "key": "117|11|117|76|MethodExpression|readdirSync(missionRules).filter(f => f.endsWith(\".md\"))",
   "line": 117,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "The extra[] list loses its deterministic order. APFS here returns readdir already byte-sorted, so I re-ran the module against a shim whose readdirSync reverses the order (ext4/XFS behaviour): extra goes from [aa,bb,mm,qq,zz] to [zz,qq,mm,bb,aa], i.e. the machine output ordering becomes filesystem-dependent. Set membership and exit code are unchanged.",
   "argument": "The extra[] list loses its deterministic order. APFS here returns readdir already byte-sorted, so I re-ran the module against a shim whose readdirSync reverses the order (ext4/XFS behaviour): extra goes from [aa,bb,mm,qq,zz] to [zz,qq,mm,bb,aa], i.e. the machine output ordering becomes filesystem-dependent. Set membership and exit code are unchanged.",
   "stableKey": "scaffold-lock\u0001corpusDivergence\u0001MethodExpression\u0001readdirSync(missionRules).filter(f => f.endsWith(\".md\"))\u0001readdirSync(missionRules).filter((f) => f.endsWith(\".md\")).sort()\u0001? readdirSync(missionRules).filter((f) => f.endsWith(\".md\")).sort()"
  },
  {
   "key": "118|11|118|13|ArrayDeclaration|[\"Stryker was here\"]",
   "line": 118,
   "mutator": "ArrayDeclaration",
   "filing": "equivalent",
   "evidence": "The `: []` branch is unreachable: the function already returned status \"package\" unless existsSync(missionRules) was true. Measured: poisoning it produces byte-identical output across the 50-fixture unit probe, the 25-scenario CLI cycle (init/update/check --strict --json) and the reversed-readdir shim; the positive control that poisons the reachable branch instead changes 54 probe lines.",
   "argument": "The `: []` branch is unreachable: the function already returned status \"package\" unless existsSync(missionRules) was true. Measured: poisoning it produces byte-identical output across the 50-fixture unit probe, the 25-scenario CLI cycle (init/update/check --strict --json) and the reversed-readdir shim; the positive control that poisons the reachable branch instead changes 54 probe lines.",
   "stableKey": "scaffold-lock\u0001corpusDivergence\u0001ArrayDeclaration\u0001[\"Stryker was here\"]\u0001[]\u0001: [];"
  },
  {
   "key": "135|36|135|48|StringLiteral|``",
   "line": 135,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Files absent from disk are pushed by the shipped-names loop instead of the recorded-keys loop, so corpus.missing is emitted in a different order. Measured on `runward check --strict --json` for a mission with 3 recorded rules deleted and 3 more absent from the lock: same 6-element set, order changes from [config-typing-zod, frontier-deterministic-boundary, scaling-distributed-rate-limiting, async-scheduled-maintenance, ...] to alphabetical-by-package-readdir.",
   "argument": "Files absent from disk are pushed by the shipped-names loop instead of the recorded-keys loop, so corpus.missing is emitted in a different order. Measured on `runward check --strict --json` for a mission with 3 recorded rules deleted and 3 more absent from the lock: same 6-element set, order changes from [config-typing-zod, frontier-deterministic-boundary, scaling-distributed-rate-limiting, async-scheduled-maintenance, ...] to alphabetical-by-package-readdir.",
   "stableKey": "scaffold-lock\u0001corpusDivergence\u0001StringLiteral\u0001``\u0001`rules/${f}`\u0001if (!onDiskSet.has(f) && !(`rules/${f}` in recorded))"
  },
  {
   "key": "138|23|138|38|MethodExpression|ruleKeys",
   "line": 138,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "When the lock's `files` keys are not stored sorted (a hand-edited or third-party-written lock), edited[] and missing[] lose their deterministic order. Measured on `runward check --strict --json` with a lock whose keys were reversed: corpus.missing keeps the same 6-element set but its tail order flips.",
   "argument": "When the lock's `files` keys are not stored sorted (a hand-edited or third-party-written lock), edited[] and missing[] lose their deterministic order. Measured on `runward check --strict --json` with a lock whose keys were reversed: corpus.missing keeps the same 6-element set but its tail order flips.",
   "stableKey": "scaffold-lock\u0001corpusDivergence\u0001MethodExpression\u0001ruleKeys\u0001ruleKeys.sort()\u0001for (const key of ruleKeys.sort()) {"
  },
  {
   "key": "142|17|142|40|ConditionalExpression|true",
   "line": 142,
   "mutator": "ConditionalExpression",
   "filing": "equivalent",
   "evidence": "The guard never fires: the shipped-names loop only pushes files ABSENT from `recorded`, while this loop only iterates keys PRESENT in `recorded`, so the two sets are disjoint. Measured by instrumenting the condition — 0 occurrences of missing.includes(file)===true across the unit probe, the CLI cycle and the shim run; the positive control that forces a double push changes 4 probe lines.",
   "argument": "The guard never fires: the shipped-names loop only pushes files ABSENT from `recorded`, while this loop only iterates keys PRESENT in `recorded`, so the two sets are disjoint. Measured by instrumenting the condition — 0 occurrences of missing.includes(file)===true across the unit probe, the CLI cycle and the shim run; the positive control that forces a double push changes 4 probe lines.",
   "stableKey": "scaffold-lock\u0001corpusDivergence\u0001ConditionalExpression\u0001true\u0001!missing.includes(file)\u0001if (!missing.includes(file))"
  },
  {
   "key": "164|20|164|22|StringLiteral|\"Stryker was here!\"",
   "line": 164,
   "mutator": "StringLiteral",
   "filing": "equivalent",
   "evidence": "The initial value of `head` is never read: it is overwritten by the readFileSync on the success path, and the catch path returns true before touching it. Measured by initialising it to null instead — no crash across the probe, the CLI cycle and the shim (rc 0/0/0) — while the positive control that stops reassigning it crashes with TypeError.",
   "argument": "The initial value of `head` is never read: it is overwritten by the readFileSync on the success path, and the catch path returns true before touching it. Measured by initialising it to null instead — no crash across the probe, the CLI cycle and the shim (rc 0/0/0) — while the positive control that stops reassigning it crashes with TypeError.",
   "stableKey": "scaffold-lock\u0001corpusDivergence\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001let head = \"\";"
  },
  {
   "key": "166|20|166|77|MethodExpression|readFileSync(join(missionRules, f), \"utf8\")",
   "line": 166,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "Removing the 800-character window makes the whole file searched for gated frontmatter. Measured on the CLI with a house rule whose `impact: HIGH` / `phases: [floor]` lines sit after 900 characters of body: corpus.extra goes from [] to [\"zz-house.md\"] and strict conformance gaps from 36 to 37.",
   "argument": "Removing the 800-character window makes the whole file searched for gated frontmatter. Measured on the CLI with a house rule whose `impact: HIGH` / `phases: [floor]` lines sit after 900 characters of body: corpus.extra goes from [] to [\"zz-house.md\"] and strict conformance gaps from 36 to 37.",
   "stableKey": "scaffold-lock\u0001corpusDivergence\u0001MethodExpression\u0001readFileSync(join(missionRules, f), \"utf8\")\u0001readFileSync(join(missionRules, f), \"utf8\").slice(0, 800)\u0001head = readFileSync(join(missionRules, f), \"utf8\").slice(0, 800);"
  },
  {
   "key": "171|24|171|79|OptionalChaining|head.match(/^impact:\\s*([A-Za-z]+)/m)?.[1].toUpperCase",
   "line": 171,
   "mutator": "OptionalChaining",
   "filing": "equivalent",
   "evidence": "The regex capture group ([A-Za-z]+) is mandatory, so whenever match() returns non-null, index 1 is a string and the second optional chain is dead. Measured by instrumenting the site: 0 occurrences of (match !== null && match[1] === undefined) across the whole probe, CLI cycle and shim; output byte-identical.",
   "argument": "The regex capture group ([A-Za-z]+) is mandatory, so whenever match() returns non-null, index 1 is a string and the second optional chain is dead. Measured by instrumenting the site: 0 occurrences of (match !== null && match[1] === undefined) across the whole probe, CLI cycle and shim; output byte-identical.",
   "stableKey": "scaffold-lock\u0001corpusDivergence\u0001OptionalChaining\u0001head.match(/^impact:\\s*([A-Za-z]+)/m)?.[1].toUpperCase\u0001head.match(/^impact:\\s*([A-Za-z]+)/m)?.[1]?.toUpperCase\u0001const impact = head.match(/^impact:\\s*([A-Za-z]+)/m)?.[1]?.toUpperCase() ?? \"\";"
  },
  {
   "key": "171|35|171|60|Regex|/impact:\\s*([A-Za-z]+)/m",
   "line": 171,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "Dropping the ^ anchor makes any mid-line `impact:` match. Measured on the CLI with a house rule whose frontmatter reads `meta_impact: HIGH` plus `phases: [floor]`: corpus.extra goes from [] to [\"zz-house.md\"] and strict conformance gaps from 36 to 37.",
   "argument": "Dropping the ^ anchor makes any mid-line `impact:` match. Measured on the CLI with a house rule whose frontmatter reads `meta_impact: HIGH` plus `phases: [floor]`: corpus.extra goes from [] to [\"zz-house.md\"] and strict conformance gaps from 36 to 37.",
   "stableKey": "scaffold-lock\u0001corpusDivergence\u0001Regex\u0001/impact:\\s*([A-Za-z]+)/m\u0001/^impact:\\s*([A-Za-z]+)/m\u0001const impact = head.match(/^impact:\\s*([A-Za-z]+)/m)?.[1]?.toUpperCase() ?? \"\";"
  },
  {
   "key": "171|35|171|60|Regex|/^impact:\\s([A-Za-z]+)/m",
   "line": 171,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "`\\s*` to `\\s` stops matching zero-space and two-space spellings, so a gated extension escapes the check the code exists to enforce. Measured on the CLI with a house rule spelled `impact:HIGH` / `phases:[floor]`: corpus.extra goes from [\"zz-house.md\"] to [] and strict conformance gaps from 38 down to 37.",
   "argument": "`\\s*` to `\\s` stops matching zero-space and two-space spellings, so a gated extension escapes the check the code exists to enforce. Measured on the CLI with a house rule spelled `impact:HIGH` / `phases:[floor]`: corpus.extra goes from [\"zz-house.md\"] to [] and strict conformance gaps from 38 down to 37.",
   "stableKey": "scaffold-lock\u0001corpusDivergence\u0001Regex\u0001/^impact:\\s([A-Za-z]+)/m\u0001/^impact:\\s*([A-Za-z]+)/m\u0001const impact = head.match(/^impact:\\s*([A-Za-z]+)/m)?.[1]?.toUpperCase() ?? \"\";"
  },
  {
   "key": "171|85|171|87|StringLiteral|\"Stryker was here!\"",
   "line": 171,
   "mutator": "StringLiteral",
   "filing": "equivalent",
   "evidence": "The fallback value only feeds `impact === \"CRITICAL\" || impact === \"HIGH\"`, which \"Stryker was here!\" fails exactly as \"\" does. Measured by instrumenting the site: the ?? fallback fires 5 times across the fixtures, every time with counts=false, and the probe/cycle/shim output is byte-identical.",
   "argument": "The fallback value only feeds `impact === \"CRITICAL\" || impact === \"HIGH\"`, which \"Stryker was here!\" fails exactly as \"\" does. Measured by instrumenting the site: the ?? fallback fires 5 times across the fixtures, every time with counts=false, and the probe/cycle/shim output is byte-identical.",
   "stableKey": "scaffold-lock\u0001corpusDivergence\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const impact = head.match(/^impact:\\s*([A-Za-z]+)/m)?.[1]?.toUpperCase() ?? \"\";"
  },
  {
   "key": "172|35|172|61|Regex|/phases:\\s*\\[([^\\]]*)\\]/m",
   "line": 172,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "Dropping the ^ anchor makes any mid-line `phases:` match. Measured on the CLI with a house rule spelled `impact: HIGH` plus `meta_phases: [floor]`: corpus.extra goes from [] to [\"zz-house.md\"] and strict conformance gaps from 36 to 37.",
   "argument": "Dropping the ^ anchor makes any mid-line `phases:` match. Measured on the CLI with a house rule spelled `impact: HIGH` plus `meta_phases: [floor]`: corpus.extra goes from [] to [\"zz-house.md\"] and strict conformance gaps from 36 to 37.",
   "stableKey": "scaffold-lock\u0001corpusDivergence\u0001Regex\u0001/phases:\\s*\\[([^\\]]*)\\]/m\u0001/^phases:\\s*\\[([^\\]]*)\\]/m\u0001const phases = head.match(/^phases:\\s*\\[([^\\]]*)\\]/m)?.[1] ?? \"\";"
  },
  {
   "key": "172|35|172|61|Regex|/^phases:\\s\\[([^\\]]*)\\]/m",
   "line": 172,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "`\\s*` to `\\s` stops matching `phases:[floor]` and `phases:  [floor]`. Measured on the CLI with a house rule spelled `impact:HIGH` / `phases:[floor]`: corpus.extra goes from [\"zz-house.md\"] to [] and strict conformance gaps from 38 down to 37, letting an extension that inflates a gated floor through.",
   "argument": "`\\s*` to `\\s` stops matching `phases:[floor]` and `phases:  [floor]`. Measured on the CLI with a house rule spelled `impact:HIGH` / `phases:[floor]`: corpus.extra goes from [\"zz-house.md\"] to [] and strict conformance gaps from 38 down to 37, letting an extension that inflates a gated floor through.",
   "stableKey": "scaffold-lock\u0001corpusDivergence\u0001Regex\u0001/^phases:\\s\\[([^\\]]*)\\]/m\u0001/^phases:\\s*\\[([^\\]]*)\\]/m\u0001const phases = head.match(/^phases:\\s*\\[([^\\]]*)\\]/m)?.[1] ?? \"\";"
  },
  {
   "key": "172|71|172|73|StringLiteral|\"Stryker was here!\"",
   "line": 172,
   "mutator": "StringLiteral",
   "filing": "equivalent",
   "evidence": "The fallback feeds split/trim/dequote then GATED.has, and \"Stryker was here!\" is not a gated phase any more than \"\" is. Measured by instrumenting the site: the ?? fallback fires 6 times, both values yielding gated=false, and the probe/cycle/shim output is byte-identical.",
   "argument": "The fallback feeds split/trim/dequote then GATED.has, and \"Stryker was here!\" is not a gated phase any more than \"\" is. Measured by instrumenting the site: the ?? fallback fires 6 times, both values yielding gated=false, and the probe/cycle/shim output is byte-identical.",
   "stableKey": "scaffold-lock\u0001corpusDivergence\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const phases = head.match(/^phases:\\s*\\[([^\\]]*)\\]/m)?.[1] ?? \"\";"
  },
  {
   "key": "174|16|174|101|MethodExpression|phases.split(\",\").map(x => x.trim().replace(/[\"']/g, \"\")).every(x => GATED.has(x))",
   "line": 174,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "`some` to `every` demands that ALL phases be gated. Measured on the CLI with two house rules: `phases: [run, floor]` and `phases: [\"govern\", \"run\"]` both stop being reported — corpus.extra [\"zz-house.md\"] -> [] with strict conformance gaps 38->37 and 37->36.",
   "argument": "`some` to `every` demands that ALL phases be gated. Measured on the CLI with two house rules: `phases: [run, floor]` and `phases: [\"govern\", \"run\"]` both stop being reported — corpus.extra [\"zz-house.md\"] -> [] with strict conformance gaps 38->37 and 37->36.",
   "stableKey": "scaffold-lock\u0001corpusDivergence\u0001MethodExpression\u0001phases.split(\",\").map(x => x.trim().replace(/[\"']/g, \"\")).every(x => GATED.has(x))\u0001phases.split(\",\").map((x) => x.trim().replace(/[\"']/g, \"\")).some((x) => GATED.has(x))\u0001&& phases.split(\",\").map((x) => x.trim().replace(/[\"']/g, \"\")).some((x) => GATED.has(x));"
  },
  {
   "key": "174|45|174|53|MethodExpression|x",
   "line": 174,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "Dropping trim() leaves the leading space on every phase after the first. Measured on the CLI with a house rule declaring `phases: [run, floor]`: \" floor\" no longer matches GATED, corpus.extra goes from [\"zz-house.md\"] to [] and strict conformance gaps from 38 to 37.",
   "argument": "Dropping trim() leaves the leading space on every phase after the first. Measured on the CLI with a house rule declaring `phases: [run, floor]`: \" floor\" no longer matches GATED, corpus.extra goes from [\"zz-house.md\"] to [] and strict conformance gaps from 38 to 37.",
   "stableKey": "scaffold-lock\u0001corpusDivergence\u0001MethodExpression\u0001x\u0001x.trim()\u0001&& phases.split(\",\").map((x) => x.trim().replace(/[\"']/g, \"\")).some((x) => GATED.has(x));"
  },
  {
   "key": "174|71|174|73|StringLiteral|\"Stryker was here!\"",
   "line": 174,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Replacing quotes with a token instead of removing them breaks the quoted YAML spelling. Measured on the CLI with a house rule declaring `phases: [\"govern\", \"run\"]`: corpus.extra goes from [\"zz-house.md\"] to [] and strict conformance gaps from 37 to 36.",
   "argument": "Replacing quotes with a token instead of removing them breaks the quoted YAML spelling. Measured on the CLI with a house rule declaring `phases: [\"govern\", \"run\"]`: corpus.extra goes from [\"zz-house.md\"] to [] and strict conformance gaps from 37 to 36.",
   "stableKey": "scaffold-lock\u0001corpusDivergence\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001&& phases.split(\",\").map((x) => x.trim().replace(/[\"']/g, \"\")).some((x) => GATED.has(x));"
  }
 ]
}
