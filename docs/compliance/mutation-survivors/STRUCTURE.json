{
 "function": "STRUCTURE",
 "probeMissions": [
  "consolidated-2026-09-04"
 ],
 "verdicts": [
  {
   "key": "181|19|181|122|ArrayDeclaration|[]",
   "line": 181,
   "mutator": "ArrayDeclaration",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrayDeclaration\u0001[]\u0001[\"1. Problem\", \"3. Observable success criterion\", \"6. Named deferrals\", \"9. Definition of Ready check\"]\u0001sections: [\"1. Problem\", \"3. Observable success criterion\", \"6. Named deferrals\", \"9. Definition of Ready check\"],"
  },
  {
   "key": "182|17|186|10|ArrayDeclaration|[]",
   "line": 182,
   "mutator": "ArrayDeclaration",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 182, the machine payload of `check --strict --json` on the `declencheur` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 182, the machine payload of `check --strict --json` on the `declencheur` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrayDeclaration\u0001[]\u0001[ { name: \"Date\", shape: ISO_DATE, hint: \"an ISO date (YYYY-MM-DD)\" }, { name: \"Metric\", shape: /^\\S.*$/, hint: \"one non-empty line: the quantity observed\" }, { name: \"Threshold \\\\(success\\\\)\", shape: /(<|<=|>|>=|=)\\s*\\S/, hint: \"a comparator and a target (e.g. >= 80, > manual baseline)\" }, ]\u0001fields: ["
  },
  {
   "key": "183|52|183|78|StringLiteral|\"\"",
   "line": 183,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Display-only, measured 2026-09-04: the whole `check --strict --json` payload is byte-identical on all three probe missions; the rendered text differs on `declencheur` only. The literal reaches the screen and never a machine contract. Survived the CI unit pass and the whole-net pass. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Display-only, measured 2026-09-04: the whole `check --strict --json` payload is byte-identical on all three probe missions; the rendered text differs on `declencheur` only. The literal reaches the screen and never a machine contract. Survived the CI unit pass and the whole-net pass. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"an ISO date (YYYY-MM-DD)\"\u0001{ name: \"Date\", shape: ISO_DATE, hint: \"an ISO date (YYYY-MM-DD)\" },"
  },
  {
   "key": "184|38|184|46|Regex|/\\S.*$/",
   "line": 184,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\S.*$/\u0001/^\\S.*$/\u0001{ name: \"Metric\", shape: /^\\S.*$/, hint: \"one non-empty line: the quantity observed\" },"
  },
  {
   "key": "184|38|184|46|Regex|/^\\S.*/",
   "line": 184,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\S.*/\u0001/^\\S.*$/\u0001{ name: \"Metric\", shape: /^\\S.*$/, hint: \"one non-empty line: the quantity observed\" },"
  },
  {
   "key": "184|54|184|97|StringLiteral|\"\"",
   "line": 184,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"one non-empty line: the quantity observed\"\u0001{ name: \"Metric\", shape: /^\\S.*$/, hint: \"one non-empty line: the quantity observed\" },"
  },
  {
   "key": "185|55|185|75|Regex|/(<|<=|>|>=|=)\\s\\S/",
   "line": 185,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/(<|<=|>|>=|=)\\s\\S/\u0001/(<|<=|>|>=|=)\\s*\\S/\u0001{ name: \"Threshold \\\\(success\\\\)\", shape: /(<|<=|>|>=|=)\\s*\\S/, hint: \"a comparator and a target (e.g. >= 80, > manual baseline)\" },"
  },
  {
   "key": "185|55|185|75|Regex|/(<|<=|>|>=|=)\\s*\\s/",
   "line": 185,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/(<|<=|>|>=|=)\\s*\\s/\u0001/(<|<=|>|>=|=)\\s*\\S/\u0001{ name: \"Threshold \\\\(success\\\\)\", shape: /(<|<=|>|>=|=)\\s*\\S/, hint: \"a comparator and a target (e.g. >= 80, > manual baseline)\" },"
  },
  {
   "key": "185|83|185|142|StringLiteral|\"\"",
   "line": 185,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"a comparator and a target (e.g. >= 80, > manual baseline)\"\u0001{ name: \"Threshold \\\\(success\\\\)\", shape: /(<|<=|>|>=|=)\\s*\\S/, hint: \"a comparator and a target (e.g. >= 80, > manual baseline)\" },"
  },
  {
   "key": "187|18|187|99|ArrayDeclaration|[]",
   "line": 187,
   "mutator": "ArrayDeclaration",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrayDeclaration\u0001[]\u0001[{ section: \"9. Definition of Ready check\", column: 0, values: [\"met\", \"risk\"] }]\u0001domains: [{ section: \"9. Definition of Ready check\", column: 0, values: [\"met\", \"risk\"] }],"
  },
  {
   "key": "187|30|187|60|StringLiteral|\"\"",
   "line": 187,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"9. Definition of Ready check\"\u0001domains: [{ section: \"9. Definition of Ready check\", column: 0, values: [\"met\", \"risk\"] }],"
  },
  {
   "key": "188|19|193|15|ArrayDeclaration|[]",
   "line": 188,
   "mutator": "ArrayDeclaration",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrayDeclaration\u0001[]\u0001[{ section: \"9. Definition of Ready check\", description: \"a risk names its risk — a dash is an answer only beside met\", check: (cells) => cells[1] === \"risk\" && (!cells[2] || cells[2] === \"—\" || cells[2] === \"-\") ? \"Status is risk but the third cell names no risk\" : null, }]\u0001rowRules: [{"
  },
  {
   "key": "189|26|189|56|StringLiteral|\"\"",
   "line": 189,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"9. Definition of Ready check\"\u0001section: \"9. Definition of Ready check\","
  },
  {
   "key": "190|30|190|91|StringLiteral|\"\"",
   "line": 190,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"a risk names its risk — a dash is an answer only beside met\"\u0001description: \"a risk names its risk — a dash is an answer only beside met\","
  },
  {
   "key": "191|24|192|79|ArrowFunction|() => undefined",
   "line": 191,
   "mutator": "ArrowFunction",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrowFunction\u0001() => undefined\u0001(cells) => cells[1] === \"risk\" && (!cells[2] || cells[2] === \"—\" || cells[2] === \"-\") ? \"Status is risk but the third cell names no risk\" : null\u0001check: (cells) => cells[1] === \"risk\" && (!cells[2] || cells[2] === \"—\" || cells[2] === \"-\")"
  },
  {
   "key": "191|35|191|109|ConditionalExpression|false",
   "line": 191,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001cells[1] === \"risk\" && (!cells[2] || cells[2] === \"—\" || cells[2] === \"-\")\u0001check: (cells) => cells[1] === \"risk\" && (!cells[2] || cells[2] === \"—\" || cells[2] === \"-\")"
  },
  {
   "key": "191|48|191|54|StringLiteral|\"\"",
   "line": 191,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"risk\"\u0001check: (cells) => cells[1] === \"risk\" && (!cells[2] || cells[2] === \"—\" || cells[2] === \"-\")"
  },
  {
   "key": "191|59|191|108|ConditionalExpression|true",
   "line": 191,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001!cells[2] || cells[2] === \"—\" || cells[2] === \"-\"\u0001check: (cells) => cells[1] === \"risk\" && (!cells[2] || cells[2] === \"—\" || cells[2] === \"-\")"
  },
  {
   "key": "191|59|191|108|LogicalOperator|(!cells[2] || cells[2] === \"—\") && cells[2] === \"-\"",
   "line": 191,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001(!cells[2] || cells[2] === \"—\") && cells[2] === \"-\"\u0001!cells[2] || cells[2] === \"—\" || cells[2] === \"-\"\u0001check: (cells) => cells[1] === \"risk\" && (!cells[2] || cells[2] === \"—\" || cells[2] === \"-\")"
  },
  {
   "key": "191|59|191|88|ConditionalExpression|false",
   "line": 191,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!cells[2] || cells[2] === \"—\"\u0001check: (cells) => cells[1] === \"risk\" && (!cells[2] || cells[2] === \"—\" || cells[2] === \"-\")"
  },
  {
   "key": "191|59|191|88|LogicalOperator|!cells[2] && cells[2] === \"—\"",
   "line": 191,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001!cells[2] && cells[2] === \"—\"\u0001!cells[2] || cells[2] === \"—\"\u0001check: (cells) => cells[1] === \"risk\" && (!cells[2] || cells[2] === \"—\" || cells[2] === \"-\")"
  },
  {
   "key": "191|59|191|68|BooleanLiteral|cells[2]",
   "line": 191,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BooleanLiteral\u0001cells[2]\u0001!cells[2]\u0001check: (cells) => cells[1] === \"risk\" && (!cells[2] || cells[2] === \"—\" || cells[2] === \"-\")"
  },
  {
   "key": "191|72|191|88|ConditionalExpression|false",
   "line": 191,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001cells[2] === \"—\"\u0001check: (cells) => cells[1] === \"risk\" && (!cells[2] || cells[2] === \"—\" || cells[2] === \"-\")"
  },
  {
   "key": "191|72|191|88|EqualityOperator|cells[2] !== \"—\"",
   "line": 191,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001EqualityOperator\u0001cells[2] !== \"—\"\u0001cells[2] === \"—\"\u0001check: (cells) => cells[1] === \"risk\" && (!cells[2] || cells[2] === \"—\" || cells[2] === \"-\")"
  },
  {
   "key": "191|85|191|88|StringLiteral|\"\"",
   "line": 191,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"—\"\u0001check: (cells) => cells[1] === \"risk\" && (!cells[2] || cells[2] === \"—\" || cells[2] === \"-\")"
  },
  {
   "key": "191|92|191|108|ConditionalExpression|false",
   "line": 191,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001cells[2] === \"-\"\u0001check: (cells) => cells[1] === \"risk\" && (!cells[2] || cells[2] === \"—\" || cells[2] === \"-\")"
  },
  {
   "key": "191|92|191|108|EqualityOperator|cells[2] !== \"-\"",
   "line": 191,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001EqualityOperator\u0001cells[2] !== \"-\"\u0001cells[2] === \"-\"\u0001check: (cells) => cells[1] === \"risk\" && (!cells[2] || cells[2] === \"—\" || cells[2] === \"-\")"
  },
  {
   "key": "191|105|191|108|StringLiteral|\"\"",
   "line": 191,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"-\"\u0001check: (cells) => cells[1] === \"risk\" && (!cells[2] || cells[2] === \"—\" || cells[2] === \"-\")"
  },
  {
   "key": "192|23|192|72|StringLiteral|\"\"",
   "line": 192,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"Status is risk but the third cell names no risk\"\u0001? \"Status is risk but the third cell names no risk\" : null,"
  },
  {
   "key": "196|19|196|107|ArrayDeclaration|[]",
   "line": 196,
   "mutator": "ArrayDeclaration",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrayDeclaration\u0001[]\u0001[\"1. Scope shipped\", \"2. Proof against the success criterion\", \"3. Gaps and deviations\"]\u0001sections: [\"1. Scope shipped\", \"2. Proof against the success criterion\", \"3. Gaps and deviations\"],"
  },
  {
   "key": "198|38|198|80|Regex|/\\d{4}-\\d{2}-\\d{2}\\.\\.\\d{4}-\\d{2}-\\d{2}$/",
   "line": 198,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\d{4}-\\d{2}-\\d{2}\\.\\.\\d{4}-\\d{2}-\\d{2}$/\u0001/^\\d{4}-\\d{2}-\\d{2}\\.\\.\\d{4}-\\d{2}-\\d{2}$/\u0001{ name: \"Period\", shape: /^\\d{4}-\\d{2}-\\d{2}\\.\\.\\d{4}-\\d{2}-\\d{2}$/, hint: \"a date range (YYYY-MM-DD..YYYY-MM-DD)\" },"
  },
  {
   "key": "198|38|198|80|Regex|/^\\d{4}-\\d{2}-\\d{2}\\.\\.\\d{4}-\\d{2}-\\d{2}/",
   "line": 198,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\d{4}-\\d{2}-\\d{2}\\.\\.\\d{4}-\\d{2}-\\d{2}/\u0001/^\\d{4}-\\d{2}-\\d{2}\\.\\.\\d{4}-\\d{2}-\\d{2}$/\u0001{ name: \"Period\", shape: /^\\d{4}-\\d{2}-\\d{2}\\.\\.\\d{4}-\\d{2}-\\d{2}$/, hint: \"a date range (YYYY-MM-DD..YYYY-MM-DD)\" },"
  },
  {
   "key": "198|88|198|127|StringLiteral|\"\"",
   "line": 198,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"a date range (YYYY-MM-DD..YYYY-MM-DD)\"\u0001{ name: \"Period\", shape: /^\\d{4}-\\d{2}-\\d{2}\\.\\.\\d{4}-\\d{2}-\\d{2}$/, hint: \"a date range (YYYY-MM-DD..YYYY-MM-DD)\" },"
  },
  {
   "key": "199|39|199|71|Regex|/(met|partially-met|not-met)\\b/",
   "line": 199,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/(met|partially-met|not-met)\\b/\u0001/^(met|partially-met|not-met)\\b/\u0001{ name: \"Verdict\", shape: /^(met|partially-met|not-met)\\b/, hint: \"met | partially-met | not-met\" },"
  },
  {
   "key": "199|79|199|110|StringLiteral|\"\"",
   "line": 199,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"met | partially-met | not-met\"\u0001{ name: \"Verdict\", shape: /^(met|partially-met|not-met)\\b/, hint: \"met | partially-met | not-met\" },"
  },
  {
   "key": "201|17|204|15|ArrayDeclaration|[]",
   "line": 201,
   "mutator": "ArrayDeclaration",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrayDeclaration\u0001[]\u0001[{ fromSection: \"2. Proof against the success criterion\", inFile: \"framing.md\", linePrefix: /^\\*\\*(Metric|Threshold \\(success\\))\\*\\*/, }]\u0001echoes: [{"
  },
  {
   "key": "202|30|202|70|StringLiteral|\"\"",
   "line": 202,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"2. Proof against the success criterion\"\u0001fromSection: \"2. Proof against the success criterion\", inFile: \"framing.md\","
  },
  {
   "key": "203|29|203|70|Regex|/\\*\\*(Metric|Threshold \\(success\\))\\*\\*/",
   "line": 203,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*(Metric|Threshold \\(success\\))\\*\\*/\u0001/^\\*\\*(Metric|Threshold \\(success\\))\\*\\*/\u0001linePrefix: /^\\*\\*(Metric|Threshold \\(success\\))\\*\\*/,"
  },
  {
   "key": "205|21|233|15|ArrayDeclaration|[]",
   "line": 205,
   "mutator": "ArrayDeclaration",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrayDeclaration\u0001[]\u0001[{ description: \"a Verdict below met requires at least one named gap row in §3\", check: (content) => { const v = content.match(/\\*\\*Verdict\\*\\*\\s*:\\s*([^·\\n]+)/); if (!v || /^met\\b/.test(v[1].trim()) || /^\\[/.test(v[1].trim())) return null; // bracketed = the template itself const idx = content.search(/^#{2,3} 3\\. Gaps and deviations\\s*$/m); if (idx === -1) return null; let headerSeen = false; for (const line of content.slice(idx).split(\"\\n\").slice(1)) { if (/^#{1,6}\\s/.test(line)) break; const t = line.trim(); if (!t.startsWith(\"|\")) continue; const cells = tableCells(t); if (cells.every((x) => /^:?-+:?$/.test(x))) continue; if (!headerSeen) { headerSeen = true; continue; } if (cells.some((x) => x && !/^\\[[^\\]]*\\]$/.test(x))) return null; // one real gap row } return `the Verdict reads \"${v[1].trim()}\" and §3 names no gap — a shortfall with zero gaps is a shape error`; }, }]\u0001conditions: [{"
  },
  {
   "key": "206|30|206|93|StringLiteral|\"\"",
   "line": 206,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"a Verdict below met requires at least one named gap row in §3\"\u0001description: \"a Verdict below met requires at least one named gap row in §3\","
  },
  {
   "key": "207|37|232|18|BlockStatement|{}",
   "line": 207,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ const v = content.match(/\\*\\*Verdict\\*\\*\\s*:\\s*([^·\\n]+)/); if (!v || /^met\\b/.test(v[1].trim()) || /^\\[/.test(v[1].trim())) return null; // bracketed = the template itself const idx = content.search(/^#{2,3} 3\\. Gaps and deviations\\s*$/m); if (idx === -1) return null; let headerSeen = false; for (const line of content.slice(idx).split(\"\\n\").slice(1)) { if (/^#{1,6}\\s/.test(line)) break; const t = line.trim(); if (!t.startsWith(\"|\")) continue; const cells = tableCells(t); if (cells.every((x) => /^:?-+:?$/.test(x))) continue; if (!headerSeen) { headerSeen = true; continue; } if (cells.some((x) => x && !/^\\[[^\\]]*\\]$/.test(x))) return null; // one real gap row } return `the Verdict reads \"${v[1].trim()}\" and §3 names no gap — a shortfall with zero gaps is a shape error`; }\u0001check: (content) => {"
  },
  {
   "key": "208|45|208|78|Regex|/\\*\\*Verdict\\*\\*\\s:\\s*([^·\\n]+)/",
   "line": 208,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Verdict\\*\\*\\s:\\s*([^·\\n]+)/\u0001/\\*\\*Verdict\\*\\*\\s*:\\s*([^·\\n]+)/\u0001const v = content.match(/\\*\\*Verdict\\*\\*\\s*:\\s*([^·\\n]+)/);"
  },
  {
   "key": "208|45|208|78|Regex|/\\*\\*Verdict\\*\\*\\S*:\\s*([^·\\n]+)/",
   "line": 208,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Verdict\\*\\*\\S*:\\s*([^·\\n]+)/\u0001/\\*\\*Verdict\\*\\*\\s*:\\s*([^·\\n]+)/\u0001const v = content.match(/\\*\\*Verdict\\*\\*\\s*:\\s*([^·\\n]+)/);"
  },
  {
   "key": "208|45|208|78|Regex|/\\*\\*Verdict\\*\\*\\s*:\\s([^·\\n]+)/",
   "line": 208,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Verdict\\*\\*\\s*:\\s([^·\\n]+)/\u0001/\\*\\*Verdict\\*\\*\\s*:\\s*([^·\\n]+)/\u0001const v = content.match(/\\*\\*Verdict\\*\\*\\s*:\\s*([^·\\n]+)/);"
  },
  {
   "key": "208|45|208|78|Regex|/\\*\\*Verdict\\*\\*\\s*:\\S*([^·\\n]+)/",
   "line": 208,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Verdict\\*\\*\\s*:\\S*([^·\\n]+)/\u0001/\\*\\*Verdict\\*\\*\\s*:\\s*([^·\\n]+)/\u0001const v = content.match(/\\*\\*Verdict\\*\\*\\s*:\\s*([^·\\n]+)/);"
  },
  {
   "key": "208|45|208|78|Regex|/\\*\\*Verdict\\*\\*\\s*:\\s*([^·\\n])/",
   "line": 208,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Verdict\\*\\*\\s*:\\s*([^·\\n])/\u0001/\\*\\*Verdict\\*\\*\\s*:\\s*([^·\\n]+)/\u0001const v = content.match(/\\*\\*Verdict\\*\\*\\s*:\\s*([^·\\n]+)/);"
  },
  {
   "key": "208|45|208|78|Regex|/\\*\\*Verdict\\*\\*\\s*:\\s*([·\\n]+)/",
   "line": 208,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Verdict\\*\\*\\s*:\\s*([·\\n]+)/\u0001/\\*\\*Verdict\\*\\*\\s*:\\s*([^·\\n]+)/\u0001const v = content.match(/\\*\\*Verdict\\*\\*\\s*:\\s*([^·\\n]+)/);"
  },
  {
   "key": "209|25|209|84|ConditionalExpression|true",
   "line": 209,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001!v || /^met\\b/.test(v[1].trim()) || /^\\[/.test(v[1].trim())\u0001if (!v || /^met\\b/.test(v[1].trim()) || /^\\[/.test(v[1].trim()))"
  },
  {
   "key": "209|25|209|84|ConditionalExpression|false",
   "line": 209,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!v || /^met\\b/.test(v[1].trim()) || /^\\[/.test(v[1].trim())\u0001if (!v || /^met\\b/.test(v[1].trim()) || /^\\[/.test(v[1].trim()))"
  },
  {
   "key": "209|25|209|84|LogicalOperator|(!v || /^met\\b/.test(v[1].trim())) && /^\\[/.test(v[1].trim())",
   "line": 209,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001(!v || /^met\\b/.test(v[1].trim())) && /^\\[/.test(v[1].trim())\u0001!v || /^met\\b/.test(v[1].trim()) || /^\\[/.test(v[1].trim())\u0001if (!v || /^met\\b/.test(v[1].trim()) || /^\\[/.test(v[1].trim()))"
  },
  {
   "key": "209|25|209|57|ConditionalExpression|false",
   "line": 209,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!v || /^met\\b/.test(v[1].trim())\u0001if (!v || /^met\\b/.test(v[1].trim()) || /^\\[/.test(v[1].trim()))"
  },
  {
   "key": "209|25|209|57|LogicalOperator|!v && /^met\\b/.test(v[1].trim())",
   "line": 209,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001!v && /^met\\b/.test(v[1].trim())\u0001!v || /^met\\b/.test(v[1].trim())\u0001if (!v || /^met\\b/.test(v[1].trim()) || /^\\[/.test(v[1].trim()))"
  },
  {
   "key": "209|25|209|27|BooleanLiteral|v",
   "line": 209,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BooleanLiteral\u0001v\u0001!v\u0001if (!v || /^met\\b/.test(v[1].trim()) || /^\\[/.test(v[1].trim()))"
  },
  {
   "key": "209|31|209|39|Regex|/met\\b/",
   "line": 209,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/met\\b/\u0001/^met\\b/\u0001if (!v || /^met\\b/.test(v[1].trim()) || /^\\[/.test(v[1].trim()))"
  },
  {
   "key": "209|45|209|56|MethodExpression|v[1]",
   "line": 209,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001v[1]\u0001v[1].trim()\u0001if (!v || /^met\\b/.test(v[1].trim()) || /^\\[/.test(v[1].trim()))"
  },
  {
   "key": "209|61|209|66|Regex|/\\[/",
   "line": 209,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[/\u0001/^\\[/\u0001if (!v || /^met\\b/.test(v[1].trim()) || /^\\[/.test(v[1].trim()))"
  },
  {
   "key": "209|72|209|83|MethodExpression|v[1]",
   "line": 209,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001v[1]\u0001v[1].trim()\u0001if (!v || /^met\\b/.test(v[1].trim()) || /^\\[/.test(v[1].trim()))\u0001#2"
  },
  {
   "key": "211|48|211|86|Regex|/#{2,3} 3\\. Gaps and deviations\\s*$/m",
   "line": 211,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/#{2,3} 3\\. Gaps and deviations\\s*$/m\u0001/^#{2,3} 3\\. Gaps and deviations\\s*$/m\u0001const idx = content.search(/^#{2,3} 3\\. Gaps and deviations\\s*$/m);"
  },
  {
   "key": "211|48|211|86|Regex|/^#{2,3} 3\\. Gaps and deviations\\s*/m",
   "line": 211,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^#{2,3} 3\\. Gaps and deviations\\s*/m\u0001/^#{2,3} 3\\. Gaps and deviations\\s*$/m\u0001const idx = content.search(/^#{2,3} 3\\. Gaps and deviations\\s*$/m);"
  },
  {
   "key": "211|48|211|86|Regex|/^# 3\\. Gaps and deviations\\s*$/m",
   "line": 211,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^# 3\\. Gaps and deviations\\s*$/m\u0001/^#{2,3} 3\\. Gaps and deviations\\s*$/m\u0001const idx = content.search(/^#{2,3} 3\\. Gaps and deviations\\s*$/m);"
  },
  {
   "key": "211|48|211|86|Regex|/^#{2,3} 3\\. Gaps and deviations\\s$/m",
   "line": 211,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^#{2,3} 3\\. Gaps and deviations\\s$/m\u0001/^#{2,3} 3\\. Gaps and deviations\\s*$/m\u0001const idx = content.search(/^#{2,3} 3\\. Gaps and deviations\\s*$/m);"
  },
  {
   "key": "211|48|211|86|Regex|/^#{2,3} 3\\. Gaps and deviations\\S*$/m",
   "line": 211,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^#{2,3} 3\\. Gaps and deviations\\S*$/m\u0001/^#{2,3} 3\\. Gaps and deviations\\s*$/m\u0001const idx = content.search(/^#{2,3} 3\\. Gaps and deviations\\s*$/m);"
  },
  {
   "key": "212|25|212|35|ConditionalExpression|true",
   "line": 212,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001idx === -1\u0001if (idx === -1)"
  },
  {
   "key": "212|25|212|35|ConditionalExpression|false",
   "line": 212,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001idx === -1\u0001if (idx === -1)"
  },
  {
   "key": "212|25|212|35|EqualityOperator|idx !== -1",
   "line": 212,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001EqualityOperator\u0001idx !== -1\u0001idx === -1\u0001if (idx === -1)"
  },
  {
   "key": "212|33|212|35|UnaryOperator|+1",
   "line": 212,
   "mutator": "UnaryOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001UnaryOperator\u0001+1\u0001-1\u0001if (idx === -1)"
  },
  {
   "key": "214|38|214|43|BooleanLiteral|true",
   "line": 214,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BooleanLiteral\u0001true\u0001false\u0001let headerSeen = false;"
  },
  {
   "key": "216|29|216|51|ConditionalExpression|false",
   "line": 216,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001/^#{1,6}\\s/.test(line)\u0001if (/^#{1,6}\\s/.test(line))"
  },
  {
   "key": "216|29|216|40|Regex|/#{1,6}\\s/",
   "line": 216,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/#{1,6}\\s/\u0001/^#{1,6}\\s/\u0001if (/^#{1,6}\\s/.test(line))"
  },
  {
   "key": "216|29|216|40|Regex|/^#\\s/",
   "line": 216,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^#\\s/\u0001/^#{1,6}\\s/\u0001if (/^#{1,6}\\s/.test(line))"
  },
  {
   "key": "216|29|216|40|Regex|/^#{1,6}\\S/",
   "line": 216,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^#{1,6}\\S/\u0001/^#{1,6}\\s/\u0001if (/^#{1,6}\\s/.test(line))"
  },
  {
   "key": "218|35|218|46|MethodExpression|line",
   "line": 218,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001line\u0001line.trim()\u0001const t = line.trim();"
  },
  {
   "key": "219|29|219|47|ConditionalExpression|false",
   "line": 219,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!t.startsWith(\"|\")\u0001if (!t.startsWith(\"|\"))"
  },
  {
   "key": "219|30|219|47|MethodExpression|t.endsWith(\"|\")",
   "line": 219,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001t.endsWith(\"|\")\u0001t.startsWith(\"|\")\u0001if (!t.startsWith(\"|\"))"
  },
  {
   "key": "219|43|219|46|StringLiteral|\"\"",
   "line": 219,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"|\"\u0001if (!t.startsWith(\"|\"))"
  },
  {
   "key": "222|29|222|67|ConditionalExpression|false",
   "line": 222,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001cells.every((x) => /^:?-+:?$/.test(x))\u0001if (cells.every((x) => /^:?-+:?$/.test(x)))"
  },
  {
   "key": "222|29|222|67|MethodExpression|cells.some(x => /^:?-+:?$/.test(x))",
   "line": 222,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001cells.some(x => /^:?-+:?$/.test(x))\u0001cells.every((x) => /^:?-+:?$/.test(x))\u0001if (cells.every((x) => /^:?-+:?$/.test(x)))"
  },
  {
   "key": "222|41|222|66|ArrowFunction|() => undefined",
   "line": 222,
   "mutator": "ArrowFunction",
   "filing": "display-only",
   "evidence": "Display-only, measured 2026-09-04: the whole `check --strict --json` payload is byte-identical on all three probe missions; the rendered text differs on `nu` only. The literal reaches the screen and never a machine contract. Survived the CI unit pass and the whole-net pass. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Display-only, measured 2026-09-04: the whole `check --strict --json` payload is byte-identical on all three probe missions; the rendered text differs on `nu` only. The literal reaches the screen and never a machine contract. Survived the CI unit pass and the whole-net pass. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrowFunction\u0001() => undefined\u0001(x) => /^:?-+:?$/.test(x)\u0001if (cells.every((x) => /^:?-+:?$/.test(x)))"
  },
  {
   "key": "222|48|222|58|Regex|/:?-+:?$/",
   "line": 222,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/:?-+:?$/\u0001/^:?-+:?$/\u0001if (cells.every((x) => /^:?-+:?$/.test(x)))"
  },
  {
   "key": "222|48|222|58|Regex|/^:?-+:?/",
   "line": 222,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^:?-+:?/\u0001/^:?-+:?$/\u0001if (cells.every((x) => /^:?-+:?$/.test(x)))"
  },
  {
   "key": "222|48|222|58|Regex|/^:-+:?$/",
   "line": 222,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^:-+:?$/\u0001/^:?-+:?$/\u0001if (cells.every((x) => /^:?-+:?$/.test(x)))"
  },
  {
   "key": "222|48|222|58|Regex|/^:?-:?$/",
   "line": 222,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 222, the machine payload of `check --strict --json` on the `optin` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 222, the machine payload of `check --strict --json` on the `optin` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^:?-:?$/\u0001/^:?-+:?$/\u0001if (cells.every((x) => /^:?-+:?$/.test(x)))"
  },
  {
   "key": "222|48|222|58|Regex|/^:?-+:$/",
   "line": 222,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^:?-+:$/\u0001/^:?-+:?$/\u0001if (cells.every((x) => /^:?-+:?$/.test(x)))"
  },
  {
   "key": "224|29|224|40|BooleanLiteral|headerSeen",
   "line": 224,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BooleanLiteral\u0001headerSeen\u0001!headerSeen\u0001if (!headerSeen) {"
  },
  {
   "key": "224|29|224|40|ConditionalExpression|false",
   "line": 224,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!headerSeen\u0001if (!headerSeen) {"
  },
  {
   "key": "224|42|227|26|BlockStatement|{}",
   "line": 224,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ headerSeen = true; continue; }\u0001if (!headerSeen) {"
  },
  {
   "key": "228|29|228|76|ConditionalExpression|true",
   "line": 228,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001cells.some((x) => x && !/^\\[[^\\]]*\\]$/.test(x))\u0001if (cells.some((x) => x && !/^\\[[^\\]]*\\]$/.test(x)))"
  },
  {
   "key": "228|29|228|76|MethodExpression|cells.every(x => x && !/^\\[[^\\]]*\\]$/.test(x))",
   "line": 228,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001cells.every(x => x && !/^\\[[^\\]]*\\]$/.test(x))\u0001cells.some((x) => x && !/^\\[[^\\]]*\\]$/.test(x))\u0001if (cells.some((x) => x && !/^\\[[^\\]]*\\]$/.test(x)))"
  },
  {
   "key": "228|47|228|75|ConditionalExpression|true",
   "line": 228,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001x && !/^\\[[^\\]]*\\]$/.test(x)\u0001if (cells.some((x) => x && !/^\\[[^\\]]*\\]$/.test(x)))"
  },
  {
   "key": "228|47|228|75|LogicalOperator|x || !/^\\[[^\\]]*\\]$/.test(x)",
   "line": 228,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001x || !/^\\[[^\\]]*\\]$/.test(x)\u0001x && !/^\\[[^\\]]*\\]$/.test(x)\u0001if (cells.some((x) => x && !/^\\[[^\\]]*\\]$/.test(x)))"
  },
  {
   "key": "228|53|228|67|Regex|/\\[[^\\]]*\\]$/",
   "line": 228,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[[^\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (cells.some((x) => x && !/^\\[[^\\]]*\\]$/.test(x)))"
  },
  {
   "key": "228|53|228|67|Regex|/^\\[[^\\]]*\\]/",
   "line": 228,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]*\\]/\u0001/^\\[[^\\]]*\\]$/\u0001if (cells.some((x) => x && !/^\\[[^\\]]*\\]$/.test(x)))"
  },
  {
   "key": "228|53|228|67|Regex|/^\\[[^\\]]\\]$/",
   "line": 228,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (cells.some((x) => x && !/^\\[[^\\]]*\\]$/.test(x)))"
  },
  {
   "key": "228|53|228|67|Regex|/^\\[[\\]]*\\]$/",
   "line": 228,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (cells.some((x) => x && !/^\\[[^\\]]*\\]$/.test(x)))"
  },
  {
   "key": "231|28|231|130|StringLiteral|``",
   "line": 231,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001``\u0001`the Verdict reads \"${v[1].trim()}\" and §3 names no gap — a shortfall with zero gaps is a shape error`\u0001return `the Verdict reads \"${v[1].trim()}\" and §3 names no gap — a shortfall with zero gaps is a shape error`;"
  },
  {
   "key": "231|50|231|61|MethodExpression|v[1]",
   "line": 231,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001v[1]\u0001v[1].trim()\u0001return `the Verdict reads \"${v[1].trim()}\" and §3 names no gap — a shortfall with zero gaps is a shape error`;"
  },
  {
   "key": "303|30|303|105|StringLiteral|\"\"",
   "line": 303,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"every §3 port reappears in the topology map — the two visions stay joined\"\u0001description: \"every §3 port reappears in the topology map — the two visions stay joined\","
  },
  {
   "key": "304|49|317|18|BlockStatement|{}",
   "line": 304,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ const topoPath = join(missionDir, \"execution-topology.md\"); if (!existsSync(topoPath)) return null; // its own artifact state owns that absence const topo = readFileSync(topoPath, \"utf8\"); for (const cells of sectionTableRows(content, /^## 3\\. Ports\\s*$/m)) { const port = (cells[0] ?? \"\").replace(/^\\*+|\\*+$/g, \"\"); if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port)) continue; if (!topo.includes(port)) return `port \"${port}\" has no line in execution-topology.md's placement map`; } return null; }\u0001check: (content, missionDir) => {"
  },
  {
   "key": "306|25|306|46|BooleanLiteral|existsSync(topoPath)",
   "line": 306,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BooleanLiteral\u0001existsSync(topoPath)\u0001!existsSync(topoPath)\u0001if (!existsSync(topoPath))"
  },
  {
   "key": "306|25|306|46|ConditionalExpression|true",
   "line": 306,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001!existsSync(topoPath)\u0001if (!existsSync(topoPath))"
  },
  {
   "key": "306|25|306|46|ConditionalExpression|false",
   "line": 306,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!existsSync(topoPath)\u0001if (!existsSync(topoPath))"
  },
  {
   "key": "308|57|308|63|StringLiteral|\"\"",
   "line": 308,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"utf8\"\u0001const topo = readFileSync(topoPath, \"utf8\");"
  },
  {
   "key": "309|67|309|87|Regex|/## 3\\. Ports\\s*$/m",
   "line": 309,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/## 3\\. Ports\\s*$/m\u0001/^## 3\\. Ports\\s*$/m\u0001for (const cells of sectionTableRows(content, /^## 3\\. Ports\\s*$/m)) {\u0001#2"
  },
  {
   "key": "309|67|309|87|Regex|/^## 3\\. Ports\\s*/m",
   "line": 309,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^## 3\\. Ports\\s*/m\u0001/^## 3\\. Ports\\s*$/m\u0001for (const cells of sectionTableRows(content, /^## 3\\. Ports\\s*$/m)) {\u0001#2"
  },
  {
   "key": "309|67|309|87|Regex|/^## 3\\. Ports\\s$/m",
   "line": 309,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^## 3\\. Ports\\s$/m\u0001/^## 3\\. Ports\\s*$/m\u0001for (const cells of sectionTableRows(content, /^## 3\\. Ports\\s*$/m)) {\u0001#2"
  },
  {
   "key": "309|67|309|87|Regex|/^## 3\\. Ports\\S*$/m",
   "line": 309,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^## 3\\. Ports\\S*$/m\u0001/^## 3\\. Ports\\s*$/m\u0001for (const cells of sectionTableRows(content, /^## 3\\. Ports\\s*$/m)) {\u0001#2"
  },
  {
   "key": "309|90|315|22|BlockStatement|{}",
   "line": 309,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ const port = (cells[0] ?? \"\").replace(/^\\*+|\\*+$/g, \"\"); if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port)) continue; if (!topo.includes(port)) return `port \"${port}\" has no line in execution-topology.md's placement map`; }\u0001for (const cells of sectionTableRows(content, /^## 3\\. Ports\\s*$/m)) {"
  },
  {
   "key": "310|39|310|53|LogicalOperator|cells[0] && \"\"",
   "line": 310,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001cells[0] && \"\"\u0001cells[0] ?? \"\"\u0001const port = (cells[0] ?? \"\").replace(/^\\*+|\\*+$/g, \"\");"
  },
  {
   "key": "310|51|310|53|StringLiteral|\"Stryker was here!\"",
   "line": 310,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const port = (cells[0] ?? \"\").replace(/^\\*+|\\*+$/g, \"\");"
  },
  {
   "key": "310|63|310|75|Regex|/\\*+|\\*+$/g",
   "line": 310,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*+|\\*+$/g\u0001/^\\*+|\\*+$/g\u0001const port = (cells[0] ?? \"\").replace(/^\\*+|\\*+$/g, \"\");"
  },
  {
   "key": "310|63|310|75|Regex|/^\\*|\\*+$/g",
   "line": 310,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\*|\\*+$/g\u0001/^\\*+|\\*+$/g\u0001const port = (cells[0] ?? \"\").replace(/^\\*+|\\*+$/g, \"\");"
  },
  {
   "key": "310|63|310|75|Regex|/^\\*+|\\*+/g",
   "line": 310,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\*+|\\*+/g\u0001/^\\*+|\\*+$/g\u0001const port = (cells[0] ?? \"\").replace(/^\\*+|\\*+$/g, \"\");"
  },
  {
   "key": "310|63|310|75|Regex|/^\\*+|\\*$/g",
   "line": 310,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\*+|\\*$/g\u0001/^\\*+|\\*+$/g\u0001const port = (cells[0] ?? \"\").replace(/^\\*+|\\*+$/g, \"\");"
  },
  {
   "key": "310|77|310|79|StringLiteral|\"Stryker was here!\"",
   "line": 310,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const port = (cells[0] ?? \"\").replace(/^\\*+|\\*+$/g, \"\");\u0001#2"
  },
  {
   "key": "311|29|311|69|ConditionalExpression|true",
   "line": 311,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001port === \"\" || /^\\[[^\\]]*\\]$/.test(port)\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port))"
  },
  {
   "key": "311|29|311|69|ConditionalExpression|false",
   "line": 311,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001port === \"\" || /^\\[[^\\]]*\\]$/.test(port)\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port))"
  },
  {
   "key": "311|29|311|69|LogicalOperator|port === \"\" && /^\\[[^\\]]*\\]$/.test(port)",
   "line": 311,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001port === \"\" && /^\\[[^\\]]*\\]$/.test(port)\u0001port === \"\" || /^\\[[^\\]]*\\]$/.test(port)\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port))"
  },
  {
   "key": "311|29|311|40|ConditionalExpression|false",
   "line": 311,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001port === \"\"\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port))"
  },
  {
   "key": "311|29|311|40|EqualityOperator|port !== \"\"",
   "line": 311,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001EqualityOperator\u0001port !== \"\"\u0001port === \"\"\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port))"
  },
  {
   "key": "311|38|311|40|StringLiteral|\"Stryker was here!\"",
   "line": 311,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Display-only, measured 2026-09-04: the whole `check --strict --json` payload is byte-identical on all three probe missions; the rendered text differs on `optin` only. The literal reaches the screen and never a machine contract. Survived the CI unit pass and the whole-net pass. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Display-only, measured 2026-09-04: the whole `check --strict --json` payload is byte-identical on all three probe missions; the rendered text differs on `optin` only. The literal reaches the screen and never a machine contract. Survived the CI unit pass and the whole-net pass. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port))"
  },
  {
   "key": "311|44|311|58|Regex|/\\[[^\\]]*\\]$/",
   "line": 311,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[[^\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port))"
  },
  {
   "key": "311|44|311|58|Regex|/^\\[[^\\]]*\\]/",
   "line": 311,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]*\\]/\u0001/^\\[[^\\]]*\\]$/\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port))"
  },
  {
   "key": "311|44|311|58|Regex|/^\\[[^\\]]\\]$/",
   "line": 311,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port))"
  },
  {
   "key": "311|44|311|58|Regex|/^\\[[\\]]*\\]$/",
   "line": 311,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port))"
  },
  {
   "key": "313|29|313|49|ConditionalExpression|false",
   "line": 313,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!topo.includes(port)\u0001if (!topo.includes(port))"
  },
  {
   "key": "314|36|314|105|StringLiteral|``",
   "line": 314,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001``\u0001`port \"${port}\" has no line in execution-topology.md's placement map`\u0001return `port \"${port}\" has no line in execution-topology.md's placement map`;"
  },
  {
   "key": "321|30|356|6|ObjectLiteral|{}",
   "line": 321,
   "mutator": "ObjectLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ObjectLiteral\u0001{}\u0001{ // M3a. The rule this template already WROTE in prose (\"any placement that is not in-app // carries an ADR\") finally gains its reader; the Location family is the closed list // shared-bricks.md authored. Annotations after the family stay yours (prefix match). conditions: [{ description: \"the family is one of the five shared-bricks families, and a non-In-app placement cites a resolving ADR\", check: (content, missionDir) => { // The template says \"The port → placement map\"; the shipped example numbers it // (\"2. Port → placement map\"). Both are the same section — number and article optional. const MAP = /^#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\s*$/m; if (!MAP.test(content)) return \"the port → placement map section is missing\"; const FAMILIES = [\"In-app\", \"Existing infrastructure\", \"Dedicated internal platform\", \"Managed infrastructure service\", \"Managed model-vendor runtime\"]; for (const cells of sectionTableRows(content, MAP)) { const port = cells[0] ?? \"\"; if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port) || /^_e\\.g\\._/.test(port) || /^(…|\\.\\.\\.)$/.test(port)) continue; const family = (cells[2] ?? \"\").replace(/^\\*+/, \"\"); if (family === \"\" || /^\\[[^\\]]*\\]$/.test(family)) continue; if (!FAMILIES.some((f) => family.startsWith(f))) { return `\"${port}\" places in \"${family.slice(0, 40)}\", which starts with none of the five shared-bricks families`; } if (!family.startsWith(\"In-app\")) { const adrCell = cells[5] ?? \"\"; const id = adrCell.match(/ADR-(\\d+)/); if (!id) return `\"${port}\" leaves In-app (${family.slice(0, 30)}) and its ADR cell cites no ADR — the template's own rule, now read`; if (!adrIdExists(missionDir, `ADR-${id[1]}`)) return `\"${port}\" cites ADR-${id[1]}, which does not exist in adr/`; } } return null; }, }], }\u0001\"execution-topology.md\": {"
  },
  {
   "key": "325|21|355|15|ArrayDeclaration|[]",
   "line": 325,
   "mutator": "ArrayDeclaration",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrayDeclaration\u0001[]\u0001[{ description: \"the family is one of the five shared-bricks families, and a non-In-app placement cites a resolving ADR\", check: (content, missionDir) => { // The template says \"The port → placement map\"; the shipped example numbers it // (\"2. Port → placement map\"). Both are the same section — number and article optional. const MAP = /^#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\s*$/m; if (!MAP.test(content)) return \"the port → placement map section is missing\"; const FAMILIES = [\"In-app\", \"Existing infrastructure\", \"Dedicated internal platform\", \"Managed infrastructure service\", \"Managed model-vendor runtime\"]; for (const cells of sectionTableRows(content, MAP)) { const port = cells[0] ?? \"\"; if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port) || /^_e\\.g\\._/.test(port) || /^(…|\\.\\.\\.)$/.test(port)) continue; const family = (cells[2] ?? \"\").replace(/^\\*+/, \"\"); if (family === \"\" || /^\\[[^\\]]*\\]$/.test(family)) continue; if (!FAMILIES.some((f) => family.startsWith(f))) { return `\"${port}\" places in \"${family.slice(0, 40)}\", which starts with none of the five shared-bricks families`; } if (!family.startsWith(\"In-app\")) { const adrCell = cells[5] ?? \"\"; const id = adrCell.match(/ADR-(\\d+)/); if (!id) return `\"${port}\" leaves In-app (${family.slice(0, 30)}) and its ADR cell cites no ADR — the template's own rule, now read`; if (!adrIdExists(missionDir, `ADR-${id[1]}`)) return `\"${port}\" cites ADR-${id[1]}, which does not exist in adr/`; } } return null; }, }]\u0001conditions: [{"
  },
  {
   "key": "326|30|326|134|StringLiteral|\"\"",
   "line": 326,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"the family is one of the five shared-bricks families, and a non-In-app placement cites a resolving ADR\"\u0001description: \"the family is one of the five shared-bricks families, and a non-In-app placement cites a resolving ADR\","
  },
  {
   "key": "327|49|354|18|BlockStatement|{}",
   "line": 327,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ // The template says \"The port → placement map\"; the shipped example numbers it // (\"2. Port → placement map\"). Both are the same section — number and article optional. const MAP = /^#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\s*$/m; if (!MAP.test(content)) return \"the port → placement map section is missing\"; const FAMILIES = [\"In-app\", \"Existing infrastructure\", \"Dedicated internal platform\", \"Managed infrastructure service\", \"Managed model-vendor runtime\"]; for (const cells of sectionTableRows(content, MAP)) { const port = cells[0] ?? \"\"; if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port) || /^_e\\.g\\._/.test(port) || /^(…|\\.\\.\\.)$/.test(port)) continue; const family = (cells[2] ?? \"\").replace(/^\\*+/, \"\"); if (family === \"\" || /^\\[[^\\]]*\\]$/.test(family)) continue; if (!FAMILIES.some((f) => family.startsWith(f))) { return `\"${port}\" places in \"${family.slice(0, 40)}\", which starts with none of the five shared-bricks families`; } if (!family.startsWith(\"In-app\")) { const adrCell = cells[5] ?? \"\"; const id = adrCell.match(/ADR-(\\d+)/); if (!id) return `\"${port}\" leaves In-app (${family.slice(0, 30)}) and its ADR cell cites no ADR — the template's own rule, now read`; if (!adrIdExists(missionDir, `ADR-${id[1]}`)) return `\"${port}\" cites ADR-${id[1]}, which does not exist in adr/`; } } return null; }\u0001check: (content, missionDir) => {"
  },
  {
   "key": "330|33|330|91|Regex|/#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\s*$/m",
   "line": 330,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\s*$/m\u0001/^#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\s*$/m\u0001const MAP = /^#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\s*$/m;"
  },
  {
   "key": "330|33|330|91|Regex|/^#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\s*/m",
   "line": 330,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\s*/m\u0001/^#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\s*$/m\u0001const MAP = /^#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\s*$/m;"
  },
  {
   "key": "330|33|330|91|Regex|/^#{2,3} (?:\\d\\. )?(?:The )?[Pp]ort → placement map\\s*$/m",
   "line": 330,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^#{2,3} (?:\\d\\. )?(?:The )?[Pp]ort → placement map\\s*$/m\u0001/^#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\s*$/m\u0001const MAP = /^#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\s*$/m;"
  },
  {
   "key": "330|33|330|91|Regex|/^#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\s$/m",
   "line": 330,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\s$/m\u0001/^#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\s*$/m\u0001const MAP = /^#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\s*$/m;"
  },
  {
   "key": "330|33|330|91|Regex|/^#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\S*$/m",
   "line": 330,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\S*$/m\u0001/^#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\s*$/m\u0001const MAP = /^#{2,3} (?:\\d+\\. )?(?:The )?[Pp]ort → placement map\\s*$/m;"
  },
  {
   "key": "331|25|331|43|ConditionalExpression|false",
   "line": 331,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!MAP.test(content)\u0001if (!MAP.test(content))"
  },
  {
   "key": "332|32|332|77|StringLiteral|\"\"",
   "line": 332,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"the port → placement map section is missing\"\u0001return \"the port → placement map section is missing\";"
  },
  {
   "key": "333|39|333|47|StringLiteral|\"\"",
   "line": 333,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"In-app\"\u0001const FAMILIES = [\"In-app\", \"Existing infrastructure\", \"Dedicated internal platform\", \"Managed infrastructure service\", \"Managed model-vendor runtime\"];"
  },
  {
   "key": "333|49|333|74|StringLiteral|\"\"",
   "line": 333,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"Existing infrastructure\"\u0001const FAMILIES = [\"In-app\", \"Existing infrastructure\", \"Dedicated internal platform\", \"Managed infrastructure service\", \"Managed model-vendor runtime\"];"
  },
  {
   "key": "333|76|333|105|StringLiteral|\"\"",
   "line": 333,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Display-only, measured 2026-09-04: the whole `check --strict --json` payload is byte-identical on all three probe missions; the rendered text differs on `optin` only. The literal reaches the screen and never a machine contract. Survived the CI unit pass and the whole-net pass. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Display-only, measured 2026-09-04: the whole `check --strict --json` payload is byte-identical on all three probe missions; the rendered text differs on `optin` only. The literal reaches the screen and never a machine contract. Survived the CI unit pass and the whole-net pass. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"Dedicated internal platform\"\u0001const FAMILIES = [\"In-app\", \"Existing infrastructure\", \"Dedicated internal platform\", \"Managed infrastructure service\", \"Managed model-vendor runtime\"];"
  },
  {
   "key": "333|107|333|139|StringLiteral|\"\"",
   "line": 333,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"Managed infrastructure service\"\u0001const FAMILIES = [\"In-app\", \"Existing infrastructure\", \"Dedicated internal platform\", \"Managed infrastructure service\", \"Managed model-vendor runtime\"];"
  },
  {
   "key": "333|141|333|171|StringLiteral|\"\"",
   "line": 333,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"Managed model-vendor runtime\"\u0001const FAMILIES = [\"In-app\", \"Existing infrastructure\", \"Dedicated internal platform\", \"Managed infrastructure service\", \"Managed model-vendor runtime\"];"
  },
  {
   "key": "334|73|352|22|BlockStatement|{}",
   "line": 334,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ const port = cells[0] ?? \"\"; if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port) || /^_e\\.g\\._/.test(port) || /^(…|\\.\\.\\.)$/.test(port)) continue; const family = (cells[2] ?? \"\").replace(/^\\*+/, \"\"); if (family === \"\" || /^\\[[^\\]]*\\]$/.test(family)) continue; if (!FAMILIES.some((f) => family.startsWith(f))) { return `\"${port}\" places in \"${family.slice(0, 40)}\", which starts with none of the five shared-bricks families`; } if (!family.startsWith(\"In-app\")) { const adrCell = cells[5] ?? \"\"; const id = adrCell.match(/ADR-(\\d+)/); if (!id) return `\"${port}\" leaves In-app (${family.slice(0, 30)}) and its ADR cell cites no ADR — the template's own rule, now read`; if (!adrIdExists(missionDir, `ADR-${id[1]}`)) return `\"${port}\" cites ADR-${id[1]}, which does not exist in adr/`; } }\u0001for (const cells of sectionTableRows(content, MAP)) {"
  },
  {
   "key": "335|38|335|52|LogicalOperator|cells[0] && \"\"",
   "line": 335,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 335, the machine payload of `check --strict --json` on the `declencheur` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 335, the machine payload of `check --strict --json` on the `declencheur` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001cells[0] && \"\"\u0001cells[0] ?? \"\"\u0001const port = cells[0] ?? \"\";"
  },
  {
   "key": "335|50|335|52|StringLiteral|\"Stryker was here!\"",
   "line": 335,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const port = cells[0] ?? \"\";"
  },
  {
   "key": "336|29|336|124|ConditionalExpression|true",
   "line": 336,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001port === \"\" || /^\\[[^\\]]*\\]$/.test(port) || /^_e\\.g\\._/.test(port) || /^(…|\\.\\.\\.)$/.test(port)\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port) || /^_e\\.g\\._/.test(port) || /^(…|\\.\\.\\.)$/.test(port))"
  },
  {
   "key": "336|29|336|69|ConditionalExpression|false",
   "line": 336,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001port === \"\" || /^\\[[^\\]]*\\]$/.test(port)\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port) || /^_e\\.g\\._/.test(port) || /^(…|\\.\\.\\.)$/.test(port))"
  },
  {
   "key": "336|29|336|69|LogicalOperator|port === \"\" && /^\\[[^\\]]*\\]$/.test(port)",
   "line": 336,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001port === \"\" && /^\\[[^\\]]*\\]$/.test(port)\u0001port === \"\" || /^\\[[^\\]]*\\]$/.test(port)\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port) || /^_e\\.g\\._/.test(port) || /^(…|\\.\\.\\.)$/.test(port))"
  },
  {
   "key": "336|29|336|40|ConditionalExpression|false",
   "line": 336,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 336, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 336, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001port === \"\"\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port) || /^_e\\.g\\._/.test(port) || /^(…|\\.\\.\\.)$/.test(port))"
  },
  {
   "key": "336|29|336|40|EqualityOperator|port !== \"\"",
   "line": 336,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001EqualityOperator\u0001port !== \"\"\u0001port === \"\"\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port) || /^_e\\.g\\._/.test(port) || /^(…|\\.\\.\\.)$/.test(port))"
  },
  {
   "key": "336|38|336|40|StringLiteral|\"Stryker was here!\"",
   "line": 336,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port) || /^_e\\.g\\._/.test(port) || /^(…|\\.\\.\\.)$/.test(port))"
  },
  {
   "key": "336|44|336|58|Regex|/\\[[^\\]]*\\]$/",
   "line": 336,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[[^\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port) || /^_e\\.g\\._/.test(port) || /^(…|\\.\\.\\.)$/.test(port))"
  },
  {
   "key": "336|44|336|58|Regex|/^\\[[^\\]]*\\]/",
   "line": 336,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]*\\]/\u0001/^\\[[^\\]]*\\]$/\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port) || /^_e\\.g\\._/.test(port) || /^(…|\\.\\.\\.)$/.test(port))"
  },
  {
   "key": "336|44|336|58|Regex|/^\\[[^\\]]\\]$/",
   "line": 336,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port) || /^_e\\.g\\._/.test(port) || /^(…|\\.\\.\\.)$/.test(port))"
  },
  {
   "key": "336|44|336|58|Regex|/^\\[[\\]]*\\]$/",
   "line": 336,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port) || /^_e\\.g\\._/.test(port) || /^(…|\\.\\.\\.)$/.test(port))"
  },
  {
   "key": "336|73|336|84|Regex|/_e\\.g\\._/",
   "line": 336,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/_e\\.g\\._/\u0001/^_e\\.g\\._/\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port) || /^_e\\.g\\._/.test(port) || /^(…|\\.\\.\\.)$/.test(port))"
  },
  {
   "key": "336|99|336|113|Regex|/(…|\\.\\.\\.)$/",
   "line": 336,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/(…|\\.\\.\\.)$/\u0001/^(…|\\.\\.\\.)$/\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port) || /^_e\\.g\\._/.test(port) || /^(…|\\.\\.\\.)$/.test(port))"
  },
  {
   "key": "336|99|336|113|Regex|/^(…|\\.\\.\\.)/",
   "line": 336,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^(…|\\.\\.\\.)/\u0001/^(…|\\.\\.\\.)$/\u0001if (port === \"\" || /^\\[[^\\]]*\\]$/.test(port) || /^_e\\.g\\._/.test(port) || /^(…|\\.\\.\\.)$/.test(port))"
  },
  {
   "key": "338|41|338|55|LogicalOperator|cells[2] && \"\"",
   "line": 338,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001cells[2] && \"\"\u0001cells[2] ?? \"\"\u0001const family = (cells[2] ?? \"\").replace(/^\\*+/, \"\");"
  },
  {
   "key": "338|53|338|55|StringLiteral|\"Stryker was here!\"",
   "line": 338,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const family = (cells[2] ?? \"\").replace(/^\\*+/, \"\");"
  },
  {
   "key": "338|65|338|71|Regex|/\\*+/",
   "line": 338,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*+/\u0001/^\\*+/\u0001const family = (cells[2] ?? \"\").replace(/^\\*+/, \"\");"
  },
  {
   "key": "338|65|338|71|Regex|/^\\*/",
   "line": 338,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\*/\u0001/^\\*+/\u0001const family = (cells[2] ?? \"\").replace(/^\\*+/, \"\");"
  },
  {
   "key": "338|73|338|75|StringLiteral|\"Stryker was here!\"",
   "line": 338,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const family = (cells[2] ?? \"\").replace(/^\\*+/, \"\");\u0001#2"
  },
  {
   "key": "339|29|339|73|ConditionalExpression|true",
   "line": 339,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001family === \"\" || /^\\[[^\\]]*\\]$/.test(family)\u0001if (family === \"\" || /^\\[[^\\]]*\\]$/.test(family))"
  },
  {
   "key": "339|29|339|73|ConditionalExpression|false",
   "line": 339,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001family === \"\" || /^\\[[^\\]]*\\]$/.test(family)\u0001if (family === \"\" || /^\\[[^\\]]*\\]$/.test(family))"
  },
  {
   "key": "339|29|339|73|LogicalOperator|family === \"\" && /^\\[[^\\]]*\\]$/.test(family)",
   "line": 339,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001family === \"\" && /^\\[[^\\]]*\\]$/.test(family)\u0001family === \"\" || /^\\[[^\\]]*\\]$/.test(family)\u0001if (family === \"\" || /^\\[[^\\]]*\\]$/.test(family))"
  },
  {
   "key": "339|29|339|42|ConditionalExpression|false",
   "line": 339,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001family === \"\"\u0001if (family === \"\" || /^\\[[^\\]]*\\]$/.test(family))"
  },
  {
   "key": "339|29|339|42|EqualityOperator|family !== \"\"",
   "line": 339,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001EqualityOperator\u0001family !== \"\"\u0001family === \"\"\u0001if (family === \"\" || /^\\[[^\\]]*\\]$/.test(family))"
  },
  {
   "key": "339|40|339|42|StringLiteral|\"Stryker was here!\"",
   "line": 339,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001if (family === \"\" || /^\\[[^\\]]*\\]$/.test(family))"
  },
  {
   "key": "339|46|339|60|Regex|/\\[[^\\]]*\\]$/",
   "line": 339,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[[^\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (family === \"\" || /^\\[[^\\]]*\\]$/.test(family))"
  },
  {
   "key": "339|46|339|60|Regex|/^\\[[^\\]]*\\]/",
   "line": 339,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]*\\]/\u0001/^\\[[^\\]]*\\]$/\u0001if (family === \"\" || /^\\[[^\\]]*\\]$/.test(family))"
  },
  {
   "key": "339|46|339|60|Regex|/^\\[[^\\]]\\]$/",
   "line": 339,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (family === \"\" || /^\\[[^\\]]*\\]$/.test(family))"
  },
  {
   "key": "339|46|339|60|Regex|/^\\[[\\]]*\\]$/",
   "line": 339,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (family === \"\" || /^\\[[^\\]]*\\]$/.test(family))"
  },
  {
   "key": "341|29|341|72|ConditionalExpression|false",
   "line": 341,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!FAMILIES.some((f) => family.startsWith(f))\u0001if (!FAMILIES.some((f) => family.startsWith(f))) {"
  },
  {
   "key": "341|74|343|26|BlockStatement|{}",
   "line": 341,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 341, the machine payload of `check --strict --json` on the `optin` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 341, the machine payload of `check --strict --json` on the `optin` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ return `\"${port}\" places in \"${family.slice(0, 40)}\", which starts with none of the five shared-bricks families`; }\u0001if (!FAMILIES.some((f) => family.startsWith(f))) {"
  },
  {
   "key": "342|36|342|141|StringLiteral|``",
   "line": 342,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001``\u0001`\"${port}\" places in \"${family.slice(0, 40)}\", which starts with none of the five shared-bricks families`\u0001return `\"${port}\" places in \"${family.slice(0, 40)}\", which starts with none of the five shared-bricks families`;"
  },
  {
   "key": "342|60|342|79|MethodExpression|family",
   "line": 342,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001family\u0001family.slice(0, 40)\u0001return `\"${port}\" places in \"${family.slice(0, 40)}\", which starts with none of the five shared-bricks families`;"
  },
  {
   "key": "344|29|344|57|ConditionalExpression|false",
   "line": 344,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!family.startsWith(\"In-app\")\u0001if (!family.startsWith(\"In-app\")) {"
  },
  {
   "key": "344|48|344|56|StringLiteral|\"\"",
   "line": 344,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"In-app\"\u0001if (!family.startsWith(\"In-app\")) {"
  },
  {
   "key": "344|59|351|26|BlockStatement|{}",
   "line": 344,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 344, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 344, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ const adrCell = cells[5] ?? \"\"; const id = adrCell.match(/ADR-(\\d+)/); if (!id) return `\"${port}\" leaves In-app (${family.slice(0, 30)}) and its ADR cell cites no ADR — the template's own rule, now read`; if (!adrIdExists(missionDir, `ADR-${id[1]}`)) return `\"${port}\" cites ADR-${id[1]}, which does not exist in adr/`; }\u0001if (!family.startsWith(\"In-app\")) {"
  },
  {
   "key": "345|57|345|59|StringLiteral|\"Stryker was here!\"",
   "line": 345,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const adrCell = cells[5] ?? \"\";"
  },
  {
   "key": "347|33|347|36|ConditionalExpression|false",
   "line": 347,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!id\u0001if (!id)"
  },
  {
   "key": "348|40|348|156|StringLiteral|``",
   "line": 348,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001``\u0001`\"${port}\" leaves In-app (${family.slice(0, 30)}) and its ADR cell cites no ADR — the template's own rule, now read`\u0001return `\"${port}\" leaves In-app (${family.slice(0, 30)}) and its ADR cell cites no ADR — the template's own rule, now read`;"
  },
  {
   "key": "348|68|348|87|MethodExpression|family",
   "line": 348,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001family\u0001family.slice(0, 30)\u0001return `\"${port}\" leaves In-app (${family.slice(0, 30)}) and its ADR cell cites no ADR — the template's own rule, now read`;"
  },
  {
   "key": "349|33|349|73|ConditionalExpression|false",
   "line": 349,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 349, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 349, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!adrIdExists(missionDir, `ADR-${id[1]}`)\u0001if (!adrIdExists(missionDir, `ADR-${id[1]}`))\u0001#2"
  },
  {
   "key": "349|58|349|72|StringLiteral|``",
   "line": 349,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001``\u0001`ADR-${id[1]}`\u0001if (!adrIdExists(missionDir, `ADR-${id[1]}`))\u0001#2"
  },
  {
   "key": "350|40|350|100|StringLiteral|``",
   "line": 350,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001``\u0001`\"${port}\" cites ADR-${id[1]}, which does not exist in adr/`\u0001return `\"${port}\" cites ADR-${id[1]}, which does not exist in adr/`;"
  },
  {
   "key": "361|19|361|37|ArrayDeclaration|[]",
   "line": 361,
   "mutator": "ArrayDeclaration",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrayDeclaration\u0001[]\u0001[\"Positions held\"]\u0001sections: [\"Positions held\"],"
  },
  {
   "key": "364|30|364|139|StringLiteral|\"\"",
   "line": 364,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"a position is default or switched — the matrix above holds the doctrine, this table holds YOUR arbitrations\"\u0001description: \"a position is default or switched — the matrix above holds the doctrine, this table holds YOUR arbitrations\","
  },
  {
   "key": "366|46|366|48|StringLiteral|\"Stryker was here!\"",
   "line": 366,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 366, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 366, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const pos = (cells[1] ?? \"\").replace(/^\\*+/, \"\");"
  },
  {
   "key": "366|58|366|64|Regex|/\\*+/",
   "line": 366,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*+/\u0001/^\\*+/\u0001const pos = (cells[1] ?? \"\").replace(/^\\*+/, \"\");"
  },
  {
   "key": "366|58|366|64|Regex|/^\\*/",
   "line": 366,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\*/\u0001/^\\*+/\u0001const pos = (cells[1] ?? \"\").replace(/^\\*+/, \"\");"
  },
  {
   "key": "366|66|366|68|StringLiteral|\"Stryker was here!\"",
   "line": 366,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const pos = (cells[1] ?? \"\").replace(/^\\*+/, \"\");\u0001#2"
  },
  {
   "key": "367|25|367|35|ConditionalExpression|false",
   "line": 367,
   "mutator": "ConditionalExpression",
   "filing": "display-only",
   "evidence": "Display-only, measured 2026-09-04: the whole `check --strict --json` payload is byte-identical on all three probe missions; the rendered text differs on `declencheur` only. The literal reaches the screen and never a machine contract. Survived the CI unit pass and the whole-net pass. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Display-only, measured 2026-09-04: the whole `check --strict --json` payload is byte-identical on all three probe missions; the rendered text differs on `declencheur` only. The literal reaches the screen and never a machine contract. Survived the CI unit pass and the whole-net pass. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001pos === \"\"\u0001if (pos === \"\")"
  },
  {
   "key": "367|33|367|35|StringLiteral|\"Stryker was here!\"",
   "line": 367,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001if (pos === \"\")"
  },
  {
   "key": "369|26|369|49|Regex|/(default|switched)\\b/",
   "line": 369,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/(default|switched)\\b/\u0001/^(default|switched)\\b/\u0001if (!/^(default|switched)\\b/.test(pos))"
  },
  {
   "key": "370|55|370|71|MethodExpression|pos",
   "line": 370,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001pos\u0001pos.slice(0, 30)\u0001return `the Position reads \"${pos.slice(0, 30)}\" — default or switched, with your annotation after it`;"
  },
  {
   "key": "374|21|389|15|ArrayDeclaration|[]",
   "line": 374,
   "mutator": "ArrayDeclaration",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrayDeclaration\u0001[]\u0001[{ description: \"a switched position cites an ADR that exists\", check: (content, missionDir) => { for (const cells of sectionTableRows(content, /^## Positions held\\s*$/m)) { const pos = (cells[1] ?? \"\").replace(/^\\*+/, \"\"); if (!/^switched\\b/.test(pos)) continue; const id = (cells[2] ?? \"\").match(/ADR-(\\d+)/); if (!id) return `\"${cells[0]}\" is switched and cites no ADR — a switch without its trigger traced is the drift this table exists to refuse`; if (!adrIdExists(missionDir, `ADR-${id[1]}`)) return `\"${cells[0]}\" cites ADR-${id[1]}, which does not exist in adr/`; } return null; }, }]\u0001conditions: [{"
  },
  {
   "key": "375|30|375|76|StringLiteral|\"\"",
   "line": 375,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 375, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 375, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"a switched position cites an ADR that exists\"\u0001description: \"a switched position cites an ADR that exists\","
  },
  {
   "key": "376|49|388|18|BlockStatement|{}",
   "line": 376,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ for (const cells of sectionTableRows(content, /^## Positions held\\s*$/m)) { const pos = (cells[1] ?? \"\").replace(/^\\*+/, \"\"); if (!/^switched\\b/.test(pos)) continue; const id = (cells[2] ?? \"\").match(/ADR-(\\d+)/); if (!id) return `\"${cells[0]}\" is switched and cites no ADR — a switch without its trigger traced is the drift this table exists to refuse`; if (!adrIdExists(missionDir, `ADR-${id[1]}`)) return `\"${cells[0]}\" cites ADR-${id[1]}, which does not exist in adr/`; } return null; }\u0001check: (content, missionDir) => {"
  },
  {
   "key": "377|67|377|92|Regex|/## Positions held\\s*$/m",
   "line": 377,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/## Positions held\\s*$/m\u0001/^## Positions held\\s*$/m\u0001for (const cells of sectionTableRows(content, /^## Positions held\\s*$/m)) {"
  },
  {
   "key": "377|67|377|92|Regex|/^## Positions held\\s*/m",
   "line": 377,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^## Positions held\\s*/m\u0001/^## Positions held\\s*$/m\u0001for (const cells of sectionTableRows(content, /^## Positions held\\s*$/m)) {"
  },
  {
   "key": "377|67|377|92|Regex|/^## Positions held\\s$/m",
   "line": 377,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^## Positions held\\s$/m\u0001/^## Positions held\\s*$/m\u0001for (const cells of sectionTableRows(content, /^## Positions held\\s*$/m)) {"
  },
  {
   "key": "377|67|377|92|Regex|/^## Positions held\\S*$/m",
   "line": 377,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 377, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 377, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^## Positions held\\S*$/m\u0001/^## Positions held\\s*$/m\u0001for (const cells of sectionTableRows(content, /^## Positions held\\s*$/m)) {"
  },
  {
   "key": "377|95|386|22|BlockStatement|{}",
   "line": 377,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ const pos = (cells[1] ?? \"\").replace(/^\\*+/, \"\"); if (!/^switched\\b/.test(pos)) continue; const id = (cells[2] ?? \"\").match(/ADR-(\\d+)/); if (!id) return `\"${cells[0]}\" is switched and cites no ADR — a switch without its trigger traced is the drift this table exists to refuse`; if (!adrIdExists(missionDir, `ADR-${id[1]}`)) return `\"${cells[0]}\" cites ADR-${id[1]}, which does not exist in adr/`; }\u0001for (const cells of sectionTableRows(content, /^## Positions held\\s*$/m)) {"
  },
  {
   "key": "378|38|378|52|LogicalOperator|cells[1] && \"\"",
   "line": 378,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001cells[1] && \"\"\u0001cells[1] ?? \"\"\u0001const pos = (cells[1] ?? \"\").replace(/^\\*+/, \"\");"
  },
  {
   "key": "378|50|378|52|StringLiteral|\"Stryker was here!\"",
   "line": 378,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const pos = (cells[1] ?? \"\").replace(/^\\*+/, \"\");\u0001#3"
  },
  {
   "key": "378|62|378|68|Regex|/\\*+/",
   "line": 378,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*+/\u0001/^\\*+/\u0001const pos = (cells[1] ?? \"\").replace(/^\\*+/, \"\");\u0001#2"
  },
  {
   "key": "378|62|378|68|Regex|/^\\*/",
   "line": 378,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\*/\u0001/^\\*+/\u0001const pos = (cells[1] ?? \"\").replace(/^\\*+/, \"\");\u0001#2"
  },
  {
   "key": "378|70|378|72|StringLiteral|\"Stryker was here!\"",
   "line": 378,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const pos = (cells[1] ?? \"\").replace(/^\\*+/, \"\");\u0001#4"
  },
  {
   "key": "379|29|379|53|ConditionalExpression|true",
   "line": 379,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001!/^switched\\b/.test(pos)\u0001if (!/^switched\\b/.test(pos))"
  },
  {
   "key": "379|30|379|43|Regex|/switched\\b/",
   "line": 379,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/switched\\b/\u0001/^switched\\b/\u0001if (!/^switched\\b/.test(pos))"
  },
  {
   "key": "381|37|381|51|LogicalOperator|cells[2] && \"\"",
   "line": 381,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001cells[2] && \"\"\u0001cells[2] ?? \"\"\u0001const id = (cells[2] ?? \"\").match(/ADR-(\\d+)/);"
  },
  {
   "key": "381|49|381|51|StringLiteral|\"Stryker was here!\"",
   "line": 381,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const id = (cells[2] ?? \"\").match(/ADR-(\\d+)/);"
  },
  {
   "key": "381|59|381|70|Regex|/ADR-(\\d)/",
   "line": 381,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/ADR-(\\d)/\u0001/ADR-(\\d+)/\u0001const id = (cells[2] ?? \"\").match(/ADR-(\\d+)/);"
  },
  {
   "key": "381|59|381|70|Regex|/ADR-(\\D+)/",
   "line": 381,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/ADR-(\\D+)/\u0001/ADR-(\\d+)/\u0001const id = (cells[2] ?? \"\").match(/ADR-(\\d+)/);"
  },
  {
   "key": "382|29|382|32|BooleanLiteral|id",
   "line": 382,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BooleanLiteral\u0001id\u0001!id\u0001if (!id)"
  },
  {
   "key": "382|29|382|32|ConditionalExpression|true",
   "line": 382,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001!id\u0001if (!id)"
  },
  {
   "key": "382|29|382|32|ConditionalExpression|false",
   "line": 382,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!id\u0001if (!id)\u0001#2"
  },
  {
   "key": "383|36|383|159|StringLiteral|``",
   "line": 383,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001``\u0001`\"${cells[0]}\" is switched and cites no ADR — a switch without its trigger traced is the drift this table exists to refuse`\u0001return `\"${cells[0]}\" is switched and cites no ADR — a switch without its trigger traced is the drift this table exists to refuse`;"
  },
  {
   "key": "384|29|384|69|BooleanLiteral|adrIdExists(missionDir, `ADR-${id[1]}`)",
   "line": 384,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BooleanLiteral\u0001adrIdExists(missionDir, `ADR-${id[1]}`)\u0001!adrIdExists(missionDir, `ADR-${id[1]}`)\u0001if (!adrIdExists(missionDir, `ADR-${id[1]}`))"
  },
  {
   "key": "384|29|384|69|ConditionalExpression|true",
   "line": 384,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001!adrIdExists(missionDir, `ADR-${id[1]}`)\u0001if (!adrIdExists(missionDir, `ADR-${id[1]}`))"
  },
  {
   "key": "384|29|384|69|ConditionalExpression|false",
   "line": 384,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!adrIdExists(missionDir, `ADR-${id[1]}`)\u0001if (!adrIdExists(missionDir, `ADR-${id[1]}`))\u0001#3"
  },
  {
   "key": "384|54|384|68|StringLiteral|``",
   "line": 384,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001``\u0001`ADR-${id[1]}`\u0001if (!adrIdExists(missionDir, `ADR-${id[1]}`))\u0001#3"
  },
  {
   "key": "385|36|385|100|StringLiteral|``",
   "line": 385,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001``\u0001`\"${cells[0]}\" cites ADR-${id[1]}, which does not exist in adr/`\u0001return `\"${cells[0]}\" cites ADR-${id[1]}, which does not exist in adr/`;\u0001#2"
  },
  {
   "key": "394|19|394|55|ArrayDeclaration|[]",
   "line": 394,
   "mutator": "ArrayDeclaration",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrayDeclaration\u0001[]\u0001[\"2. Scoring scale\", \"3. Scenarios\"]\u0001sections: [\"2. Scoring scale\", \"3. Scenarios\"],"
  },
  {
   "key": "396|30|396|96|StringLiteral|\"\"",
   "line": 396,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"the scale is numeric and at least one scenario carries its terms\"\u0001description: \"the scale is numeric and at least one scenario carries its terms\","
  },
  {
   "key": "398|36|399|96|MethodExpression|sectionTableRows(content, /^#{2,3} 2\\. Scoring scale\\s*$/m)",
   "line": 398,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001sectionTableRows(content, /^#{2,3} 2\\. Scoring scale\\s*$/m)\u0001sectionTableRows(content, /^#{2,3} 2\\. Scoring scale\\s*$/m) .filter((c) => (c[1] ?? \"\") !== \"\" && !/^\\[[^\\]]*\\]$/.test(c[1] ?? \"\"))\u0001const scores = sectionTableRows(content, /^#{2,3} 2\\. Scoring scale\\s*$/m)"
  },
  {
   "key": "398|62|398|94|Regex|/#{2,3} 2\\. Scoring scale\\s*$/m",
   "line": 398,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/#{2,3} 2\\. Scoring scale\\s*$/m\u0001/^#{2,3} 2\\. Scoring scale\\s*$/m\u0001const scores = sectionTableRows(content, /^#{2,3} 2\\. Scoring scale\\s*$/m)"
  },
  {
   "key": "398|62|398|94|Regex|/^#{2,3} 2\\. Scoring scale\\s*/m",
   "line": 398,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^#{2,3} 2\\. Scoring scale\\s*/m\u0001/^#{2,3} 2\\. Scoring scale\\s*$/m\u0001const scores = sectionTableRows(content, /^#{2,3} 2\\. Scoring scale\\s*$/m)"
  },
  {
   "key": "398|62|398|94|Regex|/^#{2,3} 2\\. Scoring scale\\s$/m",
   "line": 398,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^#{2,3} 2\\. Scoring scale\\s$/m\u0001/^#{2,3} 2\\. Scoring scale\\s*$/m\u0001const scores = sectionTableRows(content, /^#{2,3} 2\\. Scoring scale\\s*$/m)"
  },
  {
   "key": "398|62|398|94|Regex|/^#{2,3} 2\\. Scoring scale\\S*$/m",
   "line": 398,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^#{2,3} 2\\. Scoring scale\\S*$/m\u0001/^#{2,3} 2\\. Scoring scale\\s*$/m\u0001const scores = sectionTableRows(content, /^#{2,3} 2\\. Scoring scale\\s*$/m)"
  },
  {
   "key": "399|40|399|95|ConditionalExpression|true",
   "line": 399,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001(c[1] ?? \"\") !== \"\" && !/^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")\u0001.filter((c) => (c[1] ?? \"\") !== \"\" && !/^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")); // bracketed = the template teaching"
  },
  {
   "key": "399|40|399|95|LogicalOperator|(c[1] ?? \"\") !== \"\" || !/^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")",
   "line": 399,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001(c[1] ?? \"\") !== \"\" || !/^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")\u0001(c[1] ?? \"\") !== \"\" && !/^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")\u0001.filter((c) => (c[1] ?? \"\") !== \"\" && !/^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")); // bracketed = the template teaching"
  },
  {
   "key": "399|40|399|59|ConditionalExpression|true",
   "line": 399,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001(c[1] ?? \"\") !== \"\"\u0001.filter((c) => (c[1] ?? \"\") !== \"\" && !/^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")); // bracketed = the template teaching"
  },
  {
   "key": "399|49|399|51|StringLiteral|\"Stryker was here!\"",
   "line": 399,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001.filter((c) => (c[1] ?? \"\") !== \"\" && !/^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")); // bracketed = the template teaching"
  },
  {
   "key": "399|57|399|59|StringLiteral|\"Stryker was here!\"",
   "line": 399,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001.filter((c) => (c[1] ?? \"\") !== \"\" && !/^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")); // bracketed = the template teaching\u0001#2"
  },
  {
   "key": "399|64|399|78|Regex|/\\[[^\\]]*\\]$/",
   "line": 399,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[[^\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001.filter((c) => (c[1] ?? \"\") !== \"\" && !/^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")); // bracketed = the template teaching"
  },
  {
   "key": "399|64|399|78|Regex|/^\\[[^\\]]*\\]/",
   "line": 399,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]*\\]/\u0001/^\\[[^\\]]*\\]$/\u0001.filter((c) => (c[1] ?? \"\") !== \"\" && !/^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")); // bracketed = the template teaching"
  },
  {
   "key": "399|64|399|78|Regex|/^\\[[^\\]]\\]$/",
   "line": 399,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001.filter((c) => (c[1] ?? \"\") !== \"\" && !/^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")); // bracketed = the template teaching"
  },
  {
   "key": "399|64|399|78|Regex|/^\\[[\\]]*\\]$/",
   "line": 399,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001.filter((c) => (c[1] ?? \"\") !== \"\" && !/^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")); // bracketed = the template teaching"
  },
  {
   "key": "399|84|399|94|LogicalOperator|c[1] && \"\"",
   "line": 399,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 399, the machine payload of `check --strict --json` on the `declencheur` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 399, the machine payload of `check --strict --json` on the `declencheur` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001c[1] && \"\"\u0001c[1] ?? \"\"\u0001.filter((c) => (c[1] ?? \"\") !== \"\" && !/^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")); // bracketed = the template teaching"
  },
  {
   "key": "399|92|399|94|StringLiteral|\"Stryker was here!\"",
   "line": 399,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001.filter((c) => (c[1] ?? \"\") !== \"\" && !/^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")); // bracketed = the template teaching\u0001#3"
  },
  {
   "key": "400|37|400|82|MethodExpression|scores.every(c => /-?−?\\d/.test(c[1] ?? \"\"))",
   "line": 400,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001scores.every(c => /-?−?\\d/.test(c[1] ?? \"\"))\u0001scores.some((c) => /-?−?\\d/.test(c[1] ?? \"\"))\u0001const numeric = scores.some((c) => /-?−?\\d/.test(c[1] ?? \"\"));"
  },
  {
   "key": "400|56|400|64|Regex|/-?−\\d/",
   "line": 400,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/-?−\\d/\u0001/-?−?\\d/\u0001const numeric = scores.some((c) => /-?−?\\d/.test(c[1] ?? \"\"));"
  },
  {
   "key": "400|78|400|80|StringLiteral|\"Stryker was here!\"",
   "line": 400,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const numeric = scores.some((c) => /-?−?\\d/.test(c[1] ?? \"\"));"
  },
  {
   "key": "401|25|401|42|ConditionalExpression|true",
   "line": 401,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001scores.length > 0\u0001if (scores.length > 0 && !numeric)"
  },
  {
   "key": "401|25|401|42|EqualityOperator|scores.length >= 0",
   "line": 401,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001EqualityOperator\u0001scores.length >= 0\u0001scores.length > 0\u0001if (scores.length > 0 && !numeric)"
  },
  {
   "key": "403|25|403|57|ConditionalExpression|false",
   "line": 403,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!/^### Scenario /m.test(content)\u0001if (!/^### Scenario /m.test(content))"
  },
  {
   "key": "403|26|403|43|Regex|/### Scenario /m",
   "line": 403,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/### Scenario /m\u0001/^### Scenario /m\u0001if (!/^### Scenario /m.test(content))"
  },
  {
   "key": "404|32|404|97|StringLiteral|\"\"",
   "line": 404,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"no scenario block — a bench without a scenario measures nothing\"\u0001return \"no scenario block — a bench without a scenario measures nothing\";"
  },
  {
   "key": "405|25|405|116|ConditionalExpression|false",
   "line": 405,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!/\\*\\*Expected terms\\*\\*\\s*:/.test(content) || !/\\*\\*Forbidden terms\\*\\*\\s*:/.test(content)\u0001if (!/\\*\\*Expected terms\\*\\*\\s*:/.test(content) || !/\\*\\*Forbidden terms\\*\\*\\s*:/.test(content)) {"
  },
  {
   "key": "405|25|405|116|LogicalOperator|!/\\*\\*Expected terms\\*\\*\\s*:/.test(content) && !/\\*\\*Forbidden terms\\*\\*\\s*:/.test(content)",
   "line": 405,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001!/\\*\\*Expected terms\\*\\*\\s*:/.test(content) && !/\\*\\*Forbidden terms\\*\\*\\s*:/.test(content)\u0001!/\\*\\*Expected terms\\*\\*\\s*:/.test(content) || !/\\*\\*Forbidden terms\\*\\*\\s*:/.test(content)\u0001if (!/\\*\\*Expected terms\\*\\*\\s*:/.test(content) || !/\\*\\*Forbidden terms\\*\\*\\s*:/.test(content)) {"
  },
  {
   "key": "405|26|405|54|Regex|/\\*\\*Expected terms\\*\\*\\S*:/",
   "line": 405,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Expected terms\\*\\*\\S*:/\u0001/\\*\\*Expected terms\\*\\*\\s*:/\u0001if (!/\\*\\*Expected terms\\*\\*\\s*:/.test(content) || !/\\*\\*Forbidden terms\\*\\*\\s*:/.test(content)) {"
  },
  {
   "key": "405|73|405|102|Regex|/\\*\\*Forbidden terms\\*\\*\\S*:/",
   "line": 405,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Forbidden terms\\*\\*\\S*:/\u0001/\\*\\*Forbidden terms\\*\\*\\s*:/\u0001if (!/\\*\\*Expected terms\\*\\*\\s*:/.test(content) || !/\\*\\*Forbidden terms\\*\\*\\s*:/.test(content)) {"
  },
  {
   "key": "405|118|407|22|BlockStatement|{}",
   "line": 405,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ return \"a scenario is missing its Expected terms or Forbidden terms line\"; }\u0001if (!/\\*\\*Expected terms\\*\\*\\s*:/.test(content) || !/\\*\\*Forbidden terms\\*\\*\\s*:/.test(content)) {"
  },
  {
   "key": "406|32|406|98|StringLiteral|\"\"",
   "line": 406,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"a scenario is missing its Expected terms or Forbidden terms line\"\u0001return \"a scenario is missing its Expected terms or Forbidden terms line\";"
  },
  {
   "key": "415|19|415|67|ArrayDeclaration|[]",
   "line": 415,
   "mutator": "ArrayDeclaration",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrayDeclaration\u0001[]\u0001[\"2. Propagated request ID\", \"5. Cost ceilings\"]\u0001sections: [\"2. Propagated request ID\", \"5. Cost ceilings\"],"
  },
  {
   "key": "417|30|417|111|StringLiteral|\"\"",
   "line": 417,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"the carrier field is a literal backticked token and the ceilings carry a number\"\u0001description: \"the carrier field is a literal backticked token and the ceilings carry a number\","
  },
  {
   "key": "419|51|419|89|Regex|/\\*\\*Carrier field\\*\\*\\S*:\\s*([^\\n]+)/",
   "line": 419,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Carrier field\\*\\*\\S*:\\s*([^\\n]+)/\u0001/\\*\\*Carrier field\\*\\*\\s*:\\s*([^\\n]+)/\u0001const carrier = content.match(/\\*\\*Carrier field\\*\\*\\s*:\\s*([^\\n]+)/);"
  },
  {
   "key": "419|51|419|89|Regex|/\\*\\*Carrier field\\*\\*\\s*:\\s([^\\n]+)/",
   "line": 419,
   "mutator": "Regex",
   "filing": "display-only",
   "evidence": "Display-only, measured 2026-09-04: the whole `check --strict --json` payload is byte-identical on all three probe missions; the rendered text differs on `optin` only. The literal reaches the screen and never a machine contract. Survived the CI unit pass and the whole-net pass. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Display-only, measured 2026-09-04: the whole `check --strict --json` payload is byte-identical on all three probe missions; the rendered text differs on `optin` only. The literal reaches the screen and never a machine contract. Survived the CI unit pass and the whole-net pass. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Carrier field\\*\\*\\s*:\\s([^\\n]+)/\u0001/\\*\\*Carrier field\\*\\*\\s*:\\s*([^\\n]+)/\u0001const carrier = content.match(/\\*\\*Carrier field\\*\\*\\s*:\\s*([^\\n]+)/);"
  },
  {
   "key": "419|51|419|89|Regex|/\\*\\*Carrier field\\*\\*\\s*:\\S*([^\\n]+)/",
   "line": 419,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Carrier field\\*\\*\\s*:\\S*([^\\n]+)/\u0001/\\*\\*Carrier field\\*\\*\\s*:\\s*([^\\n]+)/\u0001const carrier = content.match(/\\*\\*Carrier field\\*\\*\\s*:\\s*([^\\n]+)/);"
  },
  {
   "key": "420|25|420|66|ConditionalExpression|true",
   "line": 420,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001carrier && !/^\\[/.test(carrier[1].trim())\u0001if (carrier && !/^\\[/.test(carrier[1].trim()) && !/`[^`]+`/.test(carrier[1])) {"
  },
  {
   "key": "420|25|420|66|LogicalOperator|carrier || !/^\\[/.test(carrier[1].trim())",
   "line": 420,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 420, the machine payload of `check --strict --json` on the `optin` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 420, the machine payload of `check --strict --json` on the `optin` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001carrier || !/^\\[/.test(carrier[1].trim())\u0001carrier && !/^\\[/.test(carrier[1].trim())\u0001if (carrier && !/^\\[/.test(carrier[1].trim()) && !/`[^`]+`/.test(carrier[1])) {"
  },
  {
   "key": "420|37|420|42|Regex|/\\[/",
   "line": 420,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[/\u0001/^\\[/\u0001if (carrier && !/^\\[/.test(carrier[1].trim()) && !/`[^`]+`/.test(carrier[1])) {"
  },
  {
   "key": "420|48|420|65|MethodExpression|carrier[1]",
   "line": 420,
   "mutator": "MethodExpression",
   "filing": "display-only",
   "evidence": "Display-only, measured 2026-09-04: the whole `check --strict --json` payload is byte-identical on all three probe missions; the rendered text differs on `nu` only. The literal reaches the screen and never a machine contract. Survived the CI unit pass and the whole-net pass. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Display-only, measured 2026-09-04: the whole `check --strict --json` payload is byte-identical on all three probe missions; the rendered text differs on `nu` only. The literal reaches the screen and never a machine contract. Survived the CI unit pass and the whole-net pass. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001carrier[1]\u0001carrier[1].trim()\u0001if (carrier && !/^\\[/.test(carrier[1].trim()) && !/`[^`]+`/.test(carrier[1])) {"
  },
  {
   "key": "481|60|481|71|Regex|/\\n# /",
   "line": 481,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\n# /\u0001/\\n#{2,3} /\u0001const block = content.slice(idx).split(/\\n#{2,3} /)[0];\u0001#2"
  },
  {
   "key": "482|50|482|84|Regex|/\\*\\*Date[^*]\\*\\*\\s*:\\s*([^\\n]+)/",
   "line": 482,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Date[^*]\\*\\*\\s*:\\s*([^\\n]+)/\u0001/\\*\\*Date[^*]*\\*\\*\\s*:\\s*([^\\n]+)/\u0001const dateLine = block.match(/\\*\\*Date[^*]*\\*\\*\\s*:\\s*([^\\n]+)/);"
  },
  {
   "key": "482|50|482|84|Regex|/\\*\\*Date[*]*\\*\\*\\s*:\\s*([^\\n]+)/",
   "line": 482,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Date[*]*\\*\\*\\s*:\\s*([^\\n]+)/\u0001/\\*\\*Date[^*]*\\*\\*\\s*:\\s*([^\\n]+)/\u0001const dateLine = block.match(/\\*\\*Date[^*]*\\*\\*\\s*:\\s*([^\\n]+)/);"
  },
  {
   "key": "482|50|482|84|Regex|/\\*\\*Date[^*]*\\*\\*\\s:\\s*([^\\n]+)/",
   "line": 482,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Date[^*]*\\*\\*\\s:\\s*([^\\n]+)/\u0001/\\*\\*Date[^*]*\\*\\*\\s*:\\s*([^\\n]+)/\u0001const dateLine = block.match(/\\*\\*Date[^*]*\\*\\*\\s*:\\s*([^\\n]+)/);"
  },
  {
   "key": "482|50|482|84|Regex|/\\*\\*Date[^*]*\\*\\*\\S*:\\s*([^\\n]+)/",
   "line": 482,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Date[^*]*\\*\\*\\S*:\\s*([^\\n]+)/\u0001/\\*\\*Date[^*]*\\*\\*\\s*:\\s*([^\\n]+)/\u0001const dateLine = block.match(/\\*\\*Date[^*]*\\*\\*\\s*:\\s*([^\\n]+)/);"
  },
  {
   "key": "482|50|482|84|Regex|/\\*\\*Date[^*]*\\*\\*\\s*:\\s([^\\n]+)/",
   "line": 482,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Date[^*]*\\*\\*\\s*:\\s([^\\n]+)/\u0001/\\*\\*Date[^*]*\\*\\*\\s*:\\s*([^\\n]+)/\u0001const dateLine = block.match(/\\*\\*Date[^*]*\\*\\*\\s*:\\s*([^\\n]+)/);"
  },
  {
   "key": "482|50|482|84|Regex|/\\*\\*Date[^*]*\\*\\*\\s*:\\S*([^\\n]+)/",
   "line": 482,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Date[^*]*\\*\\*\\s*:\\S*([^\\n]+)/\u0001/\\*\\*Date[^*]*\\*\\*\\s*:\\s*([^\\n]+)/\u0001const dateLine = block.match(/\\*\\*Date[^*]*\\*\\*\\s*:\\s*([^\\n]+)/);"
  },
  {
   "key": "482|50|482|84|Regex|/\\*\\*Date[^*]*\\*\\*\\s*:\\s*([\\n]+)/",
   "line": 482,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Date[^*]*\\*\\*\\s*:\\s*([\\n]+)/\u0001/\\*\\*Date[^*]*\\*\\*\\s*:\\s*([^\\n]+)/\u0001const dateLine = block.match(/\\*\\*Date[^*]*\\*\\*\\s*:\\s*([^\\n]+)/);"
  },
  {
   "key": "483|25|483|110|ConditionalExpression|false",
   "line": 483,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001dateLine && !/^\\[/.test(dateLine[1].trim()) && !/\\d{4}-\\d{2}-\\d{2}/.test(dateLine[1])\u0001if (dateLine && !/^\\[/.test(dateLine[1].trim()) && !/\\d{4}-\\d{2}-\\d{2}/.test(dateLine[1])) {"
  },
  {
   "key": "483|25|483|68|ConditionalExpression|true",
   "line": 483,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001dateLine && !/^\\[/.test(dateLine[1].trim())\u0001if (dateLine && !/^\\[/.test(dateLine[1].trim()) && !/\\d{4}-\\d{2}-\\d{2}/.test(dateLine[1])) {"
  },
  {
   "key": "483|25|483|68|LogicalOperator|dateLine || !/^\\[/.test(dateLine[1].trim())",
   "line": 483,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 483, the machine payload of `check --strict --json` on the `optin` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 483, the machine payload of `check --strict --json` on the `optin` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001dateLine || !/^\\[/.test(dateLine[1].trim())\u0001dateLine && !/^\\[/.test(dateLine[1].trim())\u0001if (dateLine && !/^\\[/.test(dateLine[1].trim()) && !/\\d{4}-\\d{2}-\\d{2}/.test(dateLine[1])) {"
  },
  {
   "key": "483|37|483|68|BooleanLiteral|/^\\[/.test(dateLine[1].trim())",
   "line": 483,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BooleanLiteral\u0001/^\\[/.test(dateLine[1].trim())\u0001!/^\\[/.test(dateLine[1].trim())\u0001if (dateLine && !/^\\[/.test(dateLine[1].trim()) && !/\\d{4}-\\d{2}-\\d{2}/.test(dateLine[1])) {"
  },
  {
   "key": "483|38|483|43|Regex|/\\[/",
   "line": 483,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[/\u0001/^\\[/\u0001if (dateLine && !/^\\[/.test(dateLine[1].trim()) && !/\\d{4}-\\d{2}-\\d{2}/.test(dateLine[1])) {"
  },
  {
   "key": "483|49|483|67|MethodExpression|dateLine[1]",
   "line": 483,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001dateLine[1]\u0001dateLine[1].trim()\u0001if (dateLine && !/^\\[/.test(dateLine[1].trim()) && !/\\d{4}-\\d{2}-\\d{2}/.test(dateLine[1])) {"
  },
  {
   "key": "483|73|483|92|Regex|/\\d-\\d{2}-\\d{2}/",
   "line": 483,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\d-\\d{2}-\\d{2}/\u0001/\\d{4}-\\d{2}-\\d{2}/\u0001if (dateLine && !/^\\[/.test(dateLine[1].trim()) && !/\\d{4}-\\d{2}-\\d{2}/.test(dateLine[1])) {"
  },
  {
   "key": "483|73|483|92|Regex|/\\d{4}-\\d{2}-\\d/",
   "line": 483,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\d{4}-\\d{2}-\\d/\u0001/\\d{4}-\\d{2}-\\d{2}/\u0001if (dateLine && !/^\\[/.test(dateLine[1].trim()) && !/\\d{4}-\\d{2}-\\d{2}/.test(dateLine[1])) {"
  },
  {
   "key": "483|112|485|22|BlockStatement|{}",
   "line": 483,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ return \"the redone task's Date line carries no ISO date — an undated proof cannot be sequenced\"; }\u0001if (dateLine && !/^\\[/.test(dateLine[1].trim()) && !/\\d{4}-\\d{2}-\\d{2}/.test(dateLine[1])) {"
  },
  {
   "key": "486|50|486|83|Regex|/\\*\\*Evidence\\*\\*\\s:\\s*([^\\n]+)/",
   "line": 486,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Evidence\\*\\*\\s:\\s*([^\\n]+)/\u0001/\\*\\*Evidence\\*\\*\\s*:\\s*([^\\n]+)/\u0001const evidence = block.match(/\\*\\*Evidence\\*\\*\\s*:\\s*([^\\n]+)/);"
  },
  {
   "key": "486|50|486|83|Regex|/\\*\\*Evidence\\*\\*\\S*:\\s*([^\\n]+)/",
   "line": 486,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Evidence\\*\\*\\S*:\\s*([^\\n]+)/\u0001/\\*\\*Evidence\\*\\*\\s*:\\s*([^\\n]+)/\u0001const evidence = block.match(/\\*\\*Evidence\\*\\*\\s*:\\s*([^\\n]+)/);"
  },
  {
   "key": "486|50|486|83|Regex|/\\*\\*Evidence\\*\\*\\s*:\\s([^\\n]+)/",
   "line": 486,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 486, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 486, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Evidence\\*\\*\\s*:\\s([^\\n]+)/\u0001/\\*\\*Evidence\\*\\*\\s*:\\s*([^\\n]+)/\u0001const evidence = block.match(/\\*\\*Evidence\\*\\*\\s*:\\s*([^\\n]+)/);"
  },
  {
   "key": "486|50|486|83|Regex|/\\*\\*Evidence\\*\\*\\s*:\\S*([^\\n]+)/",
   "line": 486,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Evidence\\*\\*\\s*:\\S*([^\\n]+)/\u0001/\\*\\*Evidence\\*\\*\\s*:\\s*([^\\n]+)/\u0001const evidence = block.match(/\\*\\*Evidence\\*\\*\\s*:\\s*([^\\n]+)/);"
  },
  {
   "key": "486|50|486|83|Regex|/\\*\\*Evidence\\*\\*\\s*:\\s*([\\n]+)/",
   "line": 486,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*\\*Evidence\\*\\*\\s*:\\s*([\\n]+)/\u0001/\\*\\*Evidence\\*\\*\\s*:\\s*([^\\n]+)/\u0001const evidence = block.match(/\\*\\*Evidence\\*\\*\\s*:\\s*([^\\n]+)/);"
  },
  {
   "key": "487|25|487|111|ConditionalExpression|false",
   "line": 487,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001evidence && !/^\\[/.test(evidence[1].trim()) && !/(file:|adr:|test:)/.test(evidence[1])\u0001if (evidence && !/^\\[/.test(evidence[1].trim()) && !/(file:|adr:|test:)/.test(evidence[1])) {"
  },
  {
   "key": "487|25|487|68|ConditionalExpression|true",
   "line": 487,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001evidence && !/^\\[/.test(evidence[1].trim())\u0001if (evidence && !/^\\[/.test(evidence[1].trim()) && !/(file:|adr:|test:)/.test(evidence[1])) {"
  },
  {
   "key": "487|25|487|68|LogicalOperator|evidence || !/^\\[/.test(evidence[1].trim())",
   "line": 487,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 487, the machine payload of `check --strict --json` on the `optin` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 487, the machine payload of `check --strict --json` on the `optin` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001evidence || !/^\\[/.test(evidence[1].trim())\u0001evidence && !/^\\[/.test(evidence[1].trim())\u0001if (evidence && !/^\\[/.test(evidence[1].trim()) && !/(file:|adr:|test:)/.test(evidence[1])) {"
  },
  {
   "key": "487|37|487|68|BooleanLiteral|/^\\[/.test(evidence[1].trim())",
   "line": 487,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BooleanLiteral\u0001/^\\[/.test(evidence[1].trim())\u0001!/^\\[/.test(evidence[1].trim())\u0001if (evidence && !/^\\[/.test(evidence[1].trim()) && !/(file:|adr:|test:)/.test(evidence[1])) {"
  },
  {
   "key": "487|38|487|43|Regex|/\\[/",
   "line": 487,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[/\u0001/^\\[/\u0001if (evidence && !/^\\[/.test(evidence[1].trim()) && !/(file:|adr:|test:)/.test(evidence[1])) {"
  },
  {
   "key": "487|49|487|67|MethodExpression|evidence[1]",
   "line": 487,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001evidence[1]\u0001evidence[1].trim()\u0001if (evidence && !/^\\[/.test(evidence[1].trim()) && !/(file:|adr:|test:)/.test(evidence[1])) {"
  },
  {
   "key": "487|113|489|22|BlockStatement|{}",
   "line": 487,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ return `the redone task's Evidence line reads \"${evidence[1].trim().slice(0, 40)}\" and carries no typed pointer — the template's own promise, now read`; }\u0001if (evidence && !/^\\[/.test(evidence[1].trim()) && !/(file:|adr:|test:)/.test(evidence[1])) {"
  },
  {
   "key": "488|32|488|176|StringLiteral|``",
   "line": 488,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001``\u0001`the redone task's Evidence line reads \"${evidence[1].trim().slice(0, 40)}\" and carries no typed pointer — the template's own promise, now read`\u0001return `the redone task's Evidence line reads \"${evidence[1].trim().slice(0, 40)}\" and carries no typed pointer — the template's own promise, now read`;"
  },
  {
   "key": "488|74|488|105|MethodExpression|evidence[1].trim()",
   "line": 488,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001evidence[1].trim()\u0001evidence[1].trim().slice(0, 40)\u0001return `the redone task's Evidence line reads \"${evidence[1].trim().slice(0, 40)}\" and carries no typed pointer — the template's own promise, now read`;"
  },
  {
   "key": "488|74|488|92|MethodExpression|evidence[1]",
   "line": 488,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001evidence[1]\u0001evidence[1].trim()\u0001return `the redone task's Evidence line reads \"${evidence[1].trim().slice(0, 40)}\" and carries no typed pointer — the template's own promise, now read`;"
  },
  {
   "key": "495|19|495|85|ArrayDeclaration|[]",
   "line": 495,
   "mutator": "ArrayDeclaration",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrayDeclaration\u0001[]\u0001[\"1. Attack surfaces\", \"2. Lethal trifecta\", \"4. Approval points\"]\u0001sections: [\"1. Attack surfaces\", \"2. Lethal trifecta\", \"4. Approval points\"],"
  },
  {
   "key": "496|64|496|90|StringLiteral|\"\"",
   "line": 496,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"an ISO date (YYYY-MM-DD)\"\u0001fields: [{ name: \"Last review\", shape: ISO_DATE, hint: \"an ISO date (YYYY-MM-DD)\" }],"
  },
  {
   "key": "497|19|514|15|ArrayDeclaration|[]",
   "line": 497,
   "mutator": "ArrayDeclaration",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrayDeclaration\u0001[]\u0001[{ section: \"2. Lethal trifecta\", description: \"three yes must not read safe; two or fewer must — your answers and your verdict must agree\", check: (cells) => { const answers = [cells[1], cells[2], cells[3]].map((x) => yesNo(x ?? \"\")); if (answers.some((a) => a === null)) return null; // annotated/absent cells stay yours to judge const yeses = answers.filter((a) => a === \"yes\").length; const verdict = (cells[4] ?? \"\").trim(); if (!verdict) return null; if (yeses === 3 && /^safe\\b/i.test(verdict)) return \"all three properties meet on this path and the Verdict reads safe\"; if (yeses < 3 && !/^safe\\b/i.test(verdict) && !/^\\[[^\\]]*\\]$/.test(verdict)) return `only ${yeses} of 3 properties meet and the Verdict does not read safe`; return null; }, }]\u0001rowRules: [{"
  },
  {
   "key": "498|26|498|46|StringLiteral|\"\"",
   "line": 498,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"2. Lethal trifecta\"\u0001section: \"2. Lethal trifecta\","
  },
  {
   "key": "499|30|499|122|StringLiteral|\"\"",
   "line": 499,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"three yes must not read safe; two or fewer must — your answers and your verdict must agree\"\u0001description: \"three yes must not read safe; two or fewer must — your answers and your verdict must agree\","
  },
  {
   "key": "500|35|513|18|BlockStatement|{}",
   "line": 500,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ const answers = [cells[1], cells[2], cells[3]].map((x) => yesNo(x ?? \"\")); if (answers.some((a) => a === null)) return null; // annotated/absent cells stay yours to judge const yeses = answers.filter((a) => a === \"yes\").length; const verdict = (cells[4] ?? \"\").trim(); if (!verdict) return null; if (yeses === 3 && /^safe\\b/i.test(verdict)) return \"all three properties meet on this path and the Verdict reads safe\"; if (yeses < 3 && !/^safe\\b/i.test(verdict) && !/^\\[[^\\]]*\\]$/.test(verdict)) return `only ${yeses} of 3 properties meet and the Verdict does not read safe`; return null; }\u0001check: (cells) => {"
  },
  {
   "key": "501|85|501|92|LogicalOperator|x && \"\"",
   "line": 501,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001x && \"\"\u0001x ?? \"\"\u0001const answers = [cells[1], cells[2], cells[3]].map((x) => yesNo(x ?? \"\"));"
  },
  {
   "key": "501|90|501|92|StringLiteral|\"Stryker was here!\"",
   "line": 501,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const answers = [cells[1], cells[2], cells[3]].map((x) => yesNo(x ?? \"\"));"
  },
  {
   "key": "502|25|502|56|ConditionalExpression|true",
   "line": 502,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001answers.some((a) => a === null)\u0001if (answers.some((a) => a === null))"
  },
  {
   "key": "502|25|502|56|ConditionalExpression|false",
   "line": 502,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001answers.some((a) => a === null)\u0001if (answers.some((a) => a === null))"
  },
  {
   "key": "502|25|502|56|MethodExpression|answers.every(a => a === null)",
   "line": 502,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001answers.every(a => a === null)\u0001answers.some((a) => a === null)\u0001if (answers.some((a) => a === null))"
  },
  {
   "key": "502|38|502|55|ArrowFunction|() => undefined",
   "line": 502,
   "mutator": "ArrowFunction",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrowFunction\u0001() => undefined\u0001(a) => a === null\u0001if (answers.some((a) => a === null))"
  },
  {
   "key": "502|45|502|55|ConditionalExpression|true",
   "line": 502,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001a === null\u0001if (answers.some((a) => a === null))"
  },
  {
   "key": "502|45|502|55|ConditionalExpression|false",
   "line": 502,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001a === null\u0001if (answers.some((a) => a === null))"
  },
  {
   "key": "502|45|502|55|EqualityOperator|a !== null",
   "line": 502,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001EqualityOperator\u0001a !== null\u0001a === null\u0001if (answers.some((a) => a === null))"
  },
  {
   "key": "505|37|505|60|MethodExpression|cells[4] ?? \"\"",
   "line": 505,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001cells[4] ?? \"\"\u0001(cells[4] ?? \"\").trim()\u0001const verdict = (cells[4] ?? \"\").trim();"
  },
  {
   "key": "505|38|505|52|LogicalOperator|cells[4] && \"\"",
   "line": 505,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001cells[4] && \"\"\u0001cells[4] ?? \"\"\u0001const verdict = (cells[4] ?? \"\").trim();"
  },
  {
   "key": "505|50|505|52|StringLiteral|\"Stryker was here!\"",
   "line": 505,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const verdict = (cells[4] ?? \"\").trim();"
  },
  {
   "key": "506|25|506|33|BooleanLiteral|verdict",
   "line": 506,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BooleanLiteral\u0001verdict\u0001!verdict\u0001if (!verdict)"
  },
  {
   "key": "506|25|506|33|ConditionalExpression|true",
   "line": 506,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001!verdict\u0001if (!verdict)"
  },
  {
   "key": "506|25|506|33|ConditionalExpression|false",
   "line": 506,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!verdict\u0001if (!verdict)"
  },
  {
   "key": "508|25|508|64|ConditionalExpression|false",
   "line": 508,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001yeses === 3 && /^safe\\b/i.test(verdict)\u0001if (yeses === 3 && /^safe\\b/i.test(verdict))"
  },
  {
   "key": "508|40|508|50|Regex|/safe\\b/i",
   "line": 508,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/safe\\b/i\u0001/^safe\\b/i\u0001if (yeses === 3 && /^safe\\b/i.test(verdict))"
  },
  {
   "key": "509|32|509|99|StringLiteral|\"\"",
   "line": 509,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"all three properties meet on this path and the Verdict reads safe\"\u0001return \"all three properties meet on this path and the Verdict reads safe\";"
  },
  {
   "key": "510|25|510|96|ConditionalExpression|false",
   "line": 510,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001yeses < 3 && !/^safe\\b/i.test(verdict) && !/^\\[[^\\]]*\\]$/.test(verdict)\u0001if (yeses < 3 && !/^safe\\b/i.test(verdict) && !/^\\[[^\\]]*\\]$/.test(verdict))"
  },
  {
   "key": "510|39|510|49|Regex|/safe\\b/i",
   "line": 510,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/safe\\b/i\u0001/^safe\\b/i\u0001if (yeses < 3 && !/^safe\\b/i.test(verdict) && !/^\\[[^\\]]*\\]$/.test(verdict))"
  },
  {
   "key": "510|67|510|96|BooleanLiteral|/^\\[[^\\]]*\\]$/.test(verdict)",
   "line": 510,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BooleanLiteral\u0001/^\\[[^\\]]*\\]$/.test(verdict)\u0001!/^\\[[^\\]]*\\]$/.test(verdict)\u0001if (yeses < 3 && !/^safe\\b/i.test(verdict) && !/^\\[[^\\]]*\\]$/.test(verdict))"
  },
  {
   "key": "510|68|510|82|Regex|/\\[[^\\]]*\\]$/",
   "line": 510,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[[^\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (yeses < 3 && !/^safe\\b/i.test(verdict) && !/^\\[[^\\]]*\\]$/.test(verdict))"
  },
  {
   "key": "510|68|510|82|Regex|/^\\[[^\\]]*\\]/",
   "line": 510,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]*\\]/\u0001/^\\[[^\\]]*\\]$/\u0001if (yeses < 3 && !/^safe\\b/i.test(verdict) && !/^\\[[^\\]]*\\]$/.test(verdict))"
  },
  {
   "key": "510|68|510|82|Regex|/^\\[[^\\]]\\]$/",
   "line": 510,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (yeses < 3 && !/^safe\\b/i.test(verdict) && !/^\\[[^\\]]*\\]$/.test(verdict))"
  },
  {
   "key": "510|68|510|82|Regex|/^\\[[\\]]*\\]$/",
   "line": 510,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (yeses < 3 && !/^safe\\b/i.test(verdict) && !/^\\[[^\\]]*\\]$/.test(verdict))"
  },
  {
   "key": "511|32|511|103|StringLiteral|``",
   "line": 511,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001``\u0001`only ${yeses} of 3 properties meet and the Verdict does not read safe`\u0001return `only ${yeses} of 3 properties meet and the Verdict does not read safe`;"
  },
  {
   "key": "241|30|241|157|StringLiteral|\"\"",
   "line": 241,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"the contract table is co-signed: every field carries a real value, and the retained engagements name at least one of the four\"\u0001description: \"the contract table is co-signed: every field carries a real value, and the retained engagements name at least one of the four\","
  },
  {
   "key": "243|60|243|90|Regex|/## The contract,.*sponsor$/m",
   "line": 243,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/## The contract,.*sponsor$/m\u0001/^## The contract,.*sponsor$/m\u0001const rows = sectionTableRows(content, /^## The contract,.*sponsor$/m);"
  },
  {
   "key": "243|60|243|90|Regex|/^## The contract,.*sponsor/m",
   "line": 243,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^## The contract,.*sponsor/m\u0001/^## The contract,.*sponsor$/m\u0001const rows = sectionTableRows(content, /^## The contract,.*sponsor$/m);"
  },
  {
   "key": "244|25|244|42|ConditionalExpression|false",
   "line": 244,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001rows.length === 0\u0001if (rows.length === 0)"
  },
  {
   "key": "245|32|245|108|StringLiteral|\"\"",
   "line": 245,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"the contract section (\\\"The contract, … sponsor\\\") or its table is missing\"\u0001return \"the contract section (\\\"The contract, … sponsor\\\") or its table is missing\";"
  },
  {
   "key": "247|52|247|54|StringLiteral|\"Stryker was here!\"",
   "line": 247,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const field = (cells[0] ?? \"\").replace(/^\\*+|\\*+$/g, \"\");"
  },
  {
   "key": "247|64|247|76|Regex|/\\*+|\\*+$/g",
   "line": 247,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*+|\\*+$/g\u0001/^\\*+|\\*+$/g\u0001const field = (cells[0] ?? \"\").replace(/^\\*+|\\*+$/g, \"\");"
  },
  {
   "key": "247|64|247|76|Regex|/^\\*+|\\*+/g",
   "line": 247,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\*+|\\*+/g\u0001/^\\*+|\\*+$/g\u0001const field = (cells[0] ?? \"\").replace(/^\\*+|\\*+$/g, \"\");"
  },
  {
   "key": "248|51|248|53|StringLiteral|\"Stryker was here!\"",
   "line": 248,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const value = cells[1] ?? \"\";"
  },
  {
   "key": "249|29|249|71|ConditionalExpression|false",
   "line": 249,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001value === \"\" || /^\\[[^\\]]*\\]$/.test(value)\u0001if (value === \"\" || /^\\[[^\\]]*\\]$/.test(value))"
  },
  {
   "key": "249|29|249|71|LogicalOperator|value === \"\" && /^\\[[^\\]]*\\]$/.test(value)",
   "line": 249,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001value === \"\" && /^\\[[^\\]]*\\]$/.test(value)\u0001value === \"\" || /^\\[[^\\]]*\\]$/.test(value)\u0001if (value === \"\" || /^\\[[^\\]]*\\]$/.test(value))"
  },
  {
   "key": "249|29|249|41|ConditionalExpression|false",
   "line": 249,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001value === \"\"\u0001if (value === \"\" || /^\\[[^\\]]*\\]$/.test(value))"
  },
  {
   "key": "249|39|249|41|StringLiteral|\"Stryker was here!\"",
   "line": 249,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001if (value === \"\" || /^\\[[^\\]]*\\]$/.test(value))"
  },
  {
   "key": "249|45|249|59|Regex|/\\[[^\\]]*\\]$/",
   "line": 249,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[[^\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (value === \"\" || /^\\[[^\\]]*\\]$/.test(value))"
  },
  {
   "key": "249|45|249|59|Regex|/^\\[[^\\]]*\\]/",
   "line": 249,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]*\\]/\u0001/^\\[[^\\]]*\\]$/\u0001if (value === \"\" || /^\\[[^\\]]*\\]$/.test(value))"
  },
  {
   "key": "249|45|249|59|Regex|/^\\[[^\\]]\\]$/",
   "line": 249,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (value === \"\" || /^\\[[^\\]]*\\]$/.test(value))"
  },
  {
   "key": "249|45|249|59|Regex|/^\\[[\\]]*\\]$/",
   "line": 249,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (value === \"\" || /^\\[[^\\]]*\\]$/.test(value))"
  },
  {
   "key": "251|29|251|54|Regex|/Engagements retained$/i",
   "line": 251,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/Engagements retained$/i\u0001/^Engagements retained$/i\u0001if (/^Engagements retained$/i.test(field)"
  },
  {
   "key": "251|29|251|54|Regex|/^Engagements retained/i",
   "line": 251,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^Engagements retained/i\u0001/^Engagements retained$/i\u0001if (/^Engagements retained$/i.test(field)"
  },
  {
   "key": "253|69|253|87|MethodExpression|value",
   "line": 253,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001value\u0001value.slice(0, 50)\u0001return `\"Engagements retained\" reads \"${value.slice(0, 50)}\" and names none of the four engagements`;"
  },
  {
   "key": "256|39|256|113|MethodExpression|rows",
   "line": 256,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001rows\u0001rows.filter((c) => (c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\"))\u0001const undecided = rows.filter((c) => (c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\"));"
  },
  {
   "key": "256|51|256|112|ArrowFunction|() => undefined",
   "line": 256,
   "mutator": "ArrowFunction",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrowFunction\u0001() => undefined\u0001(c) => (c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")\u0001const undecided = rows.filter((c) => (c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\"));"
  },
  {
   "key": "256|58|256|112|ConditionalExpression|true",
   "line": 256,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001(c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")\u0001const undecided = rows.filter((c) => (c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\"));"
  },
  {
   "key": "256|58|256|112|ConditionalExpression|false",
   "line": 256,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001(c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")\u0001const undecided = rows.filter((c) => (c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\"));"
  },
  {
   "key": "256|58|256|112|LogicalOperator|(c[1] ?? \"\") === \"\" && /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")",
   "line": 256,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001(c[1] ?? \"\") === \"\" && /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")\u0001(c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\")\u0001const undecided = rows.filter((c) => (c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\"));"
  },
  {
   "key": "256|58|256|77|ConditionalExpression|false",
   "line": 256,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001(c[1] ?? \"\") === \"\"\u0001const undecided = rows.filter((c) => (c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\"));"
  },
  {
   "key": "256|58|256|77|EqualityOperator|(c[1] ?? \"\") !== \"\"",
   "line": 256,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001EqualityOperator\u0001(c[1] ?? \"\") !== \"\"\u0001(c[1] ?? \"\") === \"\"\u0001const undecided = rows.filter((c) => (c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\"));"
  },
  {
   "key": "256|59|256|69|LogicalOperator|c[1] && \"\"",
   "line": 256,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001c[1] && \"\"\u0001c[1] ?? \"\"\u0001const undecided = rows.filter((c) => (c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\"));"
  },
  {
   "key": "256|67|256|69|StringLiteral|\"Stryker was here!\"",
   "line": 256,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const undecided = rows.filter((c) => (c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\"));"
  },
  {
   "key": "256|75|256|77|StringLiteral|\"Stryker was here!\"",
   "line": 256,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const undecided = rows.filter((c) => (c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\"));\u0001#2"
  },
  {
   "key": "256|81|256|95|Regex|/\\[[^\\]]*\\]$/",
   "line": 256,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[[^\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001const undecided = rows.filter((c) => (c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\"));"
  },
  {
   "key": "256|81|256|95|Regex|/^\\[[^\\]]*\\]/",
   "line": 256,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]*\\]/\u0001/^\\[[^\\]]*\\]$/\u0001const undecided = rows.filter((c) => (c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\"));"
  },
  {
   "key": "256|81|256|95|Regex|/^\\[[^\\]]\\]$/",
   "line": 256,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001const undecided = rows.filter((c) => (c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\"));"
  },
  {
   "key": "256|81|256|95|Regex|/^\\[[\\]]*\\]$/",
   "line": 256,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001const undecided = rows.filter((c) => (c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\"));"
  },
  {
   "key": "256|101|256|111|LogicalOperator|c[1] && \"\"",
   "line": 256,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001c[1] && \"\"\u0001c[1] ?? \"\"\u0001const undecided = rows.filter((c) => (c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\"));\u0001#2"
  },
  {
   "key": "256|109|256|111|StringLiteral|\"Stryker was here!\"",
   "line": 256,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const undecided = rows.filter((c) => (c[1] ?? \"\") === \"\" || /^\\[[^\\]]*\\]$/.test(c[1] ?? \"\"));\u0001#3"
  },
  {
   "key": "257|25|257|79|ConditionalExpression|false",
   "line": 257,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001undecided.length > 0 && undecided.length < rows.length\u0001if (undecided.length > 0 && undecided.length < rows.length) {"
  },
  {
   "key": "257|49|257|79|ConditionalExpression|true",
   "line": 257,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001undecided.length < rows.length\u0001if (undecided.length > 0 && undecided.length < rows.length) {"
  },
  {
   "key": "257|49|257|79|EqualityOperator|undecided.length <= rows.length",
   "line": 257,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001EqualityOperator\u0001undecided.length <= rows.length\u0001undecided.length < rows.length\u0001if (undecided.length > 0 && undecided.length < rows.length) {"
  },
  {
   "key": "257|49|257|79|EqualityOperator|undecided.length >= rows.length",
   "line": 257,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001EqualityOperator\u0001undecided.length >= rows.length\u0001undecided.length < rows.length\u0001if (undecided.length > 0 && undecided.length < rows.length) {"
  },
  {
   "key": "257|81|259|22|BlockStatement|{}",
   "line": 257,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ return `${undecided.length} contract field(s) still carry no agreed value (${undecided.map((c) => (c[0] ?? \"\").replace(/\\*+/g, \"\")).slice(0, 3).join(\", \")}…)`; }\u0001if (undecided.length > 0 && undecided.length < rows.length) {"
  },
  {
   "key": "258|32|258|183|StringLiteral|``",
   "line": 258,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001``\u0001`${undecided.length} contract field(s) still carry no agreed value (${undecided.map((c) => (c[0] ?? \"\").replace(/\\*+/g, \"\")).slice(0, 3).join(\", \")}…)`\u0001return `${undecided.length} contract field(s) still carry no agreed value (${undecided.map((c) => (c[0] ?? \"\").replace(/\\*+/g, \"\")).slice(0, 3).join(\", \")}…)`;"
  },
  {
   "key": "258|102|258|168|MethodExpression|undecided.map(c => (c[0] ?? \"\").replace(/\\*+/g, \"\"))",
   "line": 258,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001undecided.map(c => (c[0] ?? \"\").replace(/\\*+/g, \"\"))\u0001undecided.map((c) => (c[0] ?? \"\").replace(/\\*+/g, \"\")).slice(0, 3)\u0001return `${undecided.length} contract field(s) still carry no agreed value (${undecided.map((c) => (c[0] ?? \"\").replace(/\\*+/g, \"\")).slice(0, 3).join(\", \")}…)`;"
  },
  {
   "key": "258|116|258|155|ArrowFunction|() => undefined",
   "line": 258,
   "mutator": "ArrowFunction",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrowFunction\u0001() => undefined\u0001(c) => (c[0] ?? \"\").replace(/\\*+/g, \"\")\u0001return `${undecided.length} contract field(s) still carry no agreed value (${undecided.map((c) => (c[0] ?? \"\").replace(/\\*+/g, \"\")).slice(0, 3).join(\", \")}…)`;"
  },
  {
   "key": "258|124|258|134|LogicalOperator|c[0] && \"\"",
   "line": 258,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001c[0] && \"\"\u0001c[0] ?? \"\"\u0001return `${undecided.length} contract field(s) still carry no agreed value (${undecided.map((c) => (c[0] ?? \"\").replace(/\\*+/g, \"\")).slice(0, 3).join(\", \")}…)`;"
  },
  {
   "key": "258|132|258|134|StringLiteral|\"Stryker was here!\"",
   "line": 258,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001return `${undecided.length} contract field(s) still carry no agreed value (${undecided.map((c) => (c[0] ?? \"\").replace(/\\*+/g, \"\")).slice(0, 3).join(\", \")}…)`;"
  },
  {
   "key": "258|144|258|150|Regex|/\\*/g",
   "line": 258,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*/g\u0001/\\*+/g\u0001return `${undecided.length} contract field(s) still carry no agreed value (${undecided.map((c) => (c[0] ?? \"\").replace(/\\*+/g, \"\")).slice(0, 3).join(\", \")}…)`;"
  },
  {
   "key": "258|152|258|154|StringLiteral|\"Stryker was here!\"",
   "line": 258,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001return `${undecided.length} contract field(s) still carry no agreed value (${undecided.map((c) => (c[0] ?? \"\").replace(/\\*+/g, \"\")).slice(0, 3).join(\", \")}…)`;\u0001#2"
  },
  {
   "key": "258|174|258|178|StringLiteral|\"\"",
   "line": 258,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\", \"\u0001return `${undecided.length} contract field(s) still carry no agreed value (${undecided.map((c) => (c[0] ?? \"\").replace(/\\*+/g, \"\")).slice(0, 3).join(\", \")}…)`;"
  },
  {
   "key": "268|19|268|47|ArrayDeclaration|[]",
   "line": 268,
   "mutator": "ArrayDeclaration",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrayDeclaration\u0001[]\u0001[\"3. Ports\", \"8. Decisions\"]\u0001sections: [\"3. Ports\", \"8. Decisions\"],"
  },
  {
   "key": "271|30|271|91|StringLiteral|\"\"",
   "line": 271,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"every §8 decision cites an ADR that exists, or says pending\"\u0001description: \"every §8 decision cites an ADR that exists, or says pending\","
  },
  {
   "key": "272|49|286|18|BlockStatement|{}",
   "line": 272,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ for (const cells of sectionTableRows(content, /^## 8\\. Decisions\\s*$/m)) { const adrCell = cells[1] ?? \"\"; if (adrCell === \"\" || /^\\[[^\\]]*\\]$/.test(adrCell)) continue; if (/pending/i.test(adrCell)) continue; const id = adrCell.match(/ADR-(\\d+)/); if (!id) return `\"${cells[0]}\" cites no ADR and does not say pending — a decision here is a pointer, never a claim`; if (!adrIdExists(missionDir, `ADR-${id[1]}`)) return `\"${cells[0]}\" cites ADR-${id[1]}, which does not exist in adr/`; } return null; }\u0001check: (content, missionDir) => {"
  },
  {
   "key": "273|67|273|91|Regex|/## 8\\. Decisions\\s*$/m",
   "line": 273,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/## 8\\. Decisions\\s*$/m\u0001/^## 8\\. Decisions\\s*$/m\u0001for (const cells of sectionTableRows(content, /^## 8\\. Decisions\\s*$/m)) {"
  },
  {
   "key": "273|67|273|91|Regex|/^## 8\\. Decisions\\s*/m",
   "line": 273,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^## 8\\. Decisions\\s*/m\u0001/^## 8\\. Decisions\\s*$/m\u0001for (const cells of sectionTableRows(content, /^## 8\\. Decisions\\s*$/m)) {"
  },
  {
   "key": "273|67|273|91|Regex|/^## 8\\. Decisions\\s$/m",
   "line": 273,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^## 8\\. Decisions\\s$/m\u0001/^## 8\\. Decisions\\s*$/m\u0001for (const cells of sectionTableRows(content, /^## 8\\. Decisions\\s*$/m)) {"
  },
  {
   "key": "273|67|273|91|Regex|/^## 8\\. Decisions\\S*$/m",
   "line": 273,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^## 8\\. Decisions\\S*$/m\u0001/^## 8\\. Decisions\\s*$/m\u0001for (const cells of sectionTableRows(content, /^## 8\\. Decisions\\s*$/m)) {"
  },
  {
   "key": "273|94|284|22|BlockStatement|{}",
   "line": 273,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ const adrCell = cells[1] ?? \"\"; if (adrCell === \"\" || /^\\[[^\\]]*\\]$/.test(adrCell)) continue; if (/pending/i.test(adrCell)) continue; const id = adrCell.match(/ADR-(\\d+)/); if (!id) return `\"${cells[0]}\" cites no ADR and does not say pending — a decision here is a pointer, never a claim`; if (!adrIdExists(missionDir, `ADR-${id[1]}`)) return `\"${cells[0]}\" cites ADR-${id[1]}, which does not exist in adr/`; }\u0001for (const cells of sectionTableRows(content, /^## 8\\. Decisions\\s*$/m)) {"
  },
  {
   "key": "274|41|274|55|LogicalOperator|cells[1] && \"\"",
   "line": 274,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001cells[1] && \"\"\u0001cells[1] ?? \"\"\u0001const adrCell = cells[1] ?? \"\";"
  },
  {
   "key": "274|53|274|55|StringLiteral|\"Stryker was here!\"",
   "line": 274,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const adrCell = cells[1] ?? \"\";"
  },
  {
   "key": "275|29|275|75|ConditionalExpression|true",
   "line": 275,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001adrCell === \"\" || /^\\[[^\\]]*\\]$/.test(adrCell)\u0001if (adrCell === \"\" || /^\\[[^\\]]*\\]$/.test(adrCell))"
  },
  {
   "key": "275|29|275|75|ConditionalExpression|false",
   "line": 275,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001adrCell === \"\" || /^\\[[^\\]]*\\]$/.test(adrCell)\u0001if (adrCell === \"\" || /^\\[[^\\]]*\\]$/.test(adrCell))"
  },
  {
   "key": "275|29|275|75|LogicalOperator|adrCell === \"\" && /^\\[[^\\]]*\\]$/.test(adrCell)",
   "line": 275,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001adrCell === \"\" && /^\\[[^\\]]*\\]$/.test(adrCell)\u0001adrCell === \"\" || /^\\[[^\\]]*\\]$/.test(adrCell)\u0001if (adrCell === \"\" || /^\\[[^\\]]*\\]$/.test(adrCell))"
  },
  {
   "key": "275|29|275|43|ConditionalExpression|false",
   "line": 275,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001adrCell === \"\"\u0001if (adrCell === \"\" || /^\\[[^\\]]*\\]$/.test(adrCell))"
  },
  {
   "key": "275|29|275|43|EqualityOperator|adrCell !== \"\"",
   "line": 275,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001EqualityOperator\u0001adrCell !== \"\"\u0001adrCell === \"\"\u0001if (adrCell === \"\" || /^\\[[^\\]]*\\]$/.test(adrCell))"
  },
  {
   "key": "275|41|275|43|StringLiteral|\"Stryker was here!\"",
   "line": 275,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001if (adrCell === \"\" || /^\\[[^\\]]*\\]$/.test(adrCell))"
  },
  {
   "key": "275|47|275|61|Regex|/\\[[^\\]]*\\]$/",
   "line": 275,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[[^\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (adrCell === \"\" || /^\\[[^\\]]*\\]$/.test(adrCell))"
  },
  {
   "key": "275|47|275|61|Regex|/^\\[[^\\]]*\\]/",
   "line": 275,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]*\\]/\u0001/^\\[[^\\]]*\\]$/\u0001if (adrCell === \"\" || /^\\[[^\\]]*\\]$/.test(adrCell))"
  },
  {
   "key": "275|47|275|61|Regex|/^\\[[^\\]]\\]$/",
   "line": 275,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (adrCell === \"\" || /^\\[[^\\]]*\\]$/.test(adrCell))"
  },
  {
   "key": "275|47|275|61|Regex|/^\\[[\\]]*\\]$/",
   "line": 275,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (adrCell === \"\" || /^\\[[^\\]]*\\]$/.test(adrCell))"
  },
  {
   "key": "277|29|277|53|ConditionalExpression|true",
   "line": 277,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001/pending/i.test(adrCell)\u0001if (/pending/i.test(adrCell))"
  },
  {
   "key": "281|36|281|135|StringLiteral|``",
   "line": 281,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001``\u0001`\"${cells[0]}\" cites no ADR and does not say pending — a decision here is a pointer, never a claim`\u0001return `\"${cells[0]}\" cites no ADR and does not say pending — a decision here is a pointer, never a claim`;"
  },
  {
   "key": "282|29|282|69|ConditionalExpression|false",
   "line": 282,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!adrIdExists(missionDir, `ADR-${id[1]}`)\u0001if (!adrIdExists(missionDir, `ADR-${id[1]}`))"
  },
  {
   "key": "282|54|282|68|StringLiteral|``",
   "line": 282,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001``\u0001`ADR-${id[1]}`\u0001if (!adrIdExists(missionDir, `ADR-${id[1]}`))"
  },
  {
   "key": "283|36|283|100|StringLiteral|``",
   "line": 283,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001``\u0001`\"${cells[0]}\" cites ADR-${id[1]}, which does not exist in adr/`\u0001return `\"${cells[0]}\" cites ADR-${id[1]}, which does not exist in adr/`;"
  },
  {
   "key": "289|30|289|79|StringLiteral|\"\"",
   "line": 289,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"every §3 Spec resolves to a file in the mission\"\u0001description: \"every §3 Spec resolves to a file in the mission\","
  },
  {
   "key": "290|49|300|18|BlockStatement|{}",
   "line": 290,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ for (const cells of sectionTableRows(content, /^## 3\\. Ports\\s*$/m)) { const spec = cells[4] ?? \"\"; if (spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\") continue; const target = spec.replace(/^\\[|\\]$/g, \"\").split(\"](\").pop().replace(/\\)$/, \"\"); if (!existsSync(join(missionDir, target))) return `port \"${cells[0]}\" cites spec \"${spec.slice(0, 40)}\", which does not resolve in the mission`; } return null; }\u0001check: (content, missionDir) => {"
  },
  {
   "key": "291|67|291|87|Regex|/## 3\\. Ports\\s*$/m",
   "line": 291,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/## 3\\. Ports\\s*$/m\u0001/^## 3\\. Ports\\s*$/m\u0001for (const cells of sectionTableRows(content, /^## 3\\. Ports\\s*$/m)) {"
  },
  {
   "key": "291|67|291|87|Regex|/^## 3\\. Ports\\s*/m",
   "line": 291,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^## 3\\. Ports\\s*/m\u0001/^## 3\\. Ports\\s*$/m\u0001for (const cells of sectionTableRows(content, /^## 3\\. Ports\\s*$/m)) {"
  },
  {
   "key": "291|67|291|87|Regex|/^## 3\\. Ports\\s$/m",
   "line": 291,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^## 3\\. Ports\\s$/m\u0001/^## 3\\. Ports\\s*$/m\u0001for (const cells of sectionTableRows(content, /^## 3\\. Ports\\s*$/m)) {"
  },
  {
   "key": "291|67|291|87|Regex|/^## 3\\. Ports\\S*$/m",
   "line": 291,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^## 3\\. Ports\\S*$/m\u0001/^## 3\\. Ports\\s*$/m\u0001for (const cells of sectionTableRows(content, /^## 3\\. Ports\\s*$/m)) {"
  },
  {
   "key": "291|90|298|22|BlockStatement|{}",
   "line": 291,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ const spec = cells[4] ?? \"\"; if (spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\") continue; const target = spec.replace(/^\\[|\\]$/g, \"\").split(\"](\").pop().replace(/\\)$/, \"\"); if (!existsSync(join(missionDir, target))) return `port \"${cells[0]}\" cites spec \"${spec.slice(0, 40)}\", which does not resolve in the mission`; }\u0001for (const cells of sectionTableRows(content, /^## 3\\. Ports\\s*$/m)) {"
  },
  {
   "key": "292|38|292|52|LogicalOperator|cells[4] && \"\"",
   "line": 292,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001cells[4] && \"\"\u0001cells[4] ?? \"\"\u0001const spec = cells[4] ?? \"\";"
  },
  {
   "key": "292|50|292|52|StringLiteral|\"Stryker was here!\"",
   "line": 292,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const spec = cells[4] ?? \"\";"
  },
  {
   "key": "293|29|293|85|ConditionalExpression|true",
   "line": 293,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\"\u0001if (spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\")"
  },
  {
   "key": "293|29|293|85|ConditionalExpression|false",
   "line": 293,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\"\u0001if (spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\")"
  },
  {
   "key": "293|29|293|85|LogicalOperator|(spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec)) && spec === \"—\"",
   "line": 293,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001(spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec)) && spec === \"—\"\u0001spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\"\u0001if (spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\")"
  },
  {
   "key": "293|29|293|69|ConditionalExpression|false",
   "line": 293,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec)\u0001if (spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\")"
  },
  {
   "key": "293|29|293|69|LogicalOperator|spec === \"\" && /^\\[[^\\]]*\\]$/.test(spec)",
   "line": 293,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001spec === \"\" && /^\\[[^\\]]*\\]$/.test(spec)\u0001spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec)\u0001if (spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\")"
  },
  {
   "key": "293|29|293|40|ConditionalExpression|false",
   "line": 293,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001spec === \"\"\u0001if (spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\")"
  },
  {
   "key": "293|29|293|40|EqualityOperator|spec !== \"\"",
   "line": 293,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001EqualityOperator\u0001spec !== \"\"\u0001spec === \"\"\u0001if (spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\")"
  },
  {
   "key": "293|38|293|40|StringLiteral|\"Stryker was here!\"",
   "line": 293,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001if (spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\")"
  },
  {
   "key": "293|44|293|58|Regex|/\\[[^\\]]*\\]$/",
   "line": 293,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[[^\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\")"
  },
  {
   "key": "293|44|293|58|Regex|/^\\[[^\\]]*\\]/",
   "line": 293,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]*\\]/\u0001/^\\[[^\\]]*\\]$/\u0001if (spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\")"
  },
  {
   "key": "293|44|293|58|Regex|/^\\[[^\\]]\\]$/",
   "line": 293,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\")"
  },
  {
   "key": "293|44|293|58|Regex|/^\\[[\\]]*\\]$/",
   "line": 293,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\")"
  },
  {
   "key": "293|73|293|85|ConditionalExpression|false",
   "line": 293,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001spec === \"—\"\u0001if (spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\")"
  },
  {
   "key": "293|73|293|85|EqualityOperator|spec !== \"—\"",
   "line": 293,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001EqualityOperator\u0001spec !== \"—\"\u0001spec === \"—\"\u0001if (spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\")"
  },
  {
   "key": "293|82|293|85|StringLiteral|\"\"",
   "line": 293,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"—\"\u0001if (spec === \"\" || /^\\[[^\\]]*\\]$/.test(spec) || spec === \"—\")"
  },
  {
   "key": "295|53|295|63|Regex|/\\[|\\]$/g",
   "line": 295,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[|\\]$/g\u0001/^\\[|\\]$/g\u0001const target = spec.replace(/^\\[|\\]$/g, \"\").split(\"](\").pop().replace(/\\)$/, \"\");"
  },
  {
   "key": "295|53|295|63|Regex|/^\\[|\\]/g",
   "line": 295,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[|\\]/g\u0001/^\\[|\\]$/g\u0001const target = spec.replace(/^\\[|\\]$/g, \"\").split(\"](\").pop().replace(/\\)$/, \"\");"
  },
  {
   "key": "295|65|295|67|StringLiteral|\"Stryker was here!\"",
   "line": 295,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const target = spec.replace(/^\\[|\\]$/g, \"\").split(\"](\").pop().replace(/\\)$/, \"\");"
  },
  {
   "key": "295|95|295|100|Regex|/\\)/",
   "line": 295,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\)/\u0001/\\)$/\u0001const target = spec.replace(/^\\[|\\]$/g, \"\").split(\"](\").pop().replace(/\\)$/, \"\");"
  },
  {
   "key": "295|102|295|104|StringLiteral|\"Stryker was here!\"",
   "line": 295,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const target = spec.replace(/^\\[|\\]$/g, \"\").split(\"](\").pop().replace(/\\)$/, \"\");\u0001#2"
  },
  {
   "key": "296|29|296|66|ConditionalExpression|false",
   "line": 296,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!existsSync(join(missionDir, target))\u0001if (!existsSync(join(missionDir, target)))"
  },
  {
   "key": "297|36|297|129|StringLiteral|``",
   "line": 297,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001``\u0001`port \"${cells[0]}\" cites spec \"${spec.slice(0, 40)}\", which does not resolve in the mission`\u0001return `port \"${cells[0]}\" cites spec \"${spec.slice(0, 40)}\", which does not resolve in the mission`;"
  },
  {
   "key": "297|70|297|87|MethodExpression|spec",
   "line": 297,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001spec\u0001spec.slice(0, 40)\u0001return `port \"${cells[0]}\" cites spec \"${spec.slice(0, 40)}\", which does not resolve in the mission`;"
  },
  {
   "key": "421|60|421|90|MethodExpression|carrier[1].trim()",
   "line": 421,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001carrier[1].trim()\u0001carrier[1].trim().slice(0, 40)\u0001return `the Carrier field reads \"${carrier[1].trim().slice(0, 40)}\" and names no backticked literal token`;"
  },
  {
   "key": "421|60|421|77|MethodExpression|carrier[1]",
   "line": 421,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001carrier[1]\u0001carrier[1].trim()\u0001return `the Carrier field reads \"${carrier[1].trim().slice(0, 40)}\" and names no backticked literal token`;"
  },
  {
   "key": "423|48|423|80|Regex|/#{2,3} 5\\. Cost ceilings\\s*$/m",
   "line": 423,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/#{2,3} 5\\. Cost ceilings\\s*$/m\u0001/^#{2,3} 5\\. Cost ceilings\\s*$/m\u0001const idx = content.search(/^#{2,3} 5\\. Cost ceilings\\s*$/m);"
  },
  {
   "key": "423|48|423|80|Regex|/^#{2,3} 5\\. Cost ceilings\\s*/m",
   "line": 423,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^#{2,3} 5\\. Cost ceilings\\s*/m\u0001/^#{2,3} 5\\. Cost ceilings\\s*$/m\u0001const idx = content.search(/^#{2,3} 5\\. Cost ceilings\\s*$/m);"
  },
  {
   "key": "423|48|423|80|Regex|/^# 5\\. Cost ceilings\\s*$/m",
   "line": 423,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^# 5\\. Cost ceilings\\s*$/m\u0001/^#{2,3} 5\\. Cost ceilings\\s*$/m\u0001const idx = content.search(/^#{2,3} 5\\. Cost ceilings\\s*$/m);"
  },
  {
   "key": "423|48|423|80|Regex|/^#{2,3} 5\\. Cost ceilings\\s$/m",
   "line": 423,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^#{2,3} 5\\. Cost ceilings\\s$/m\u0001/^#{2,3} 5\\. Cost ceilings\\s*$/m\u0001const idx = content.search(/^#{2,3} 5\\. Cost ceilings\\s*$/m);"
  },
  {
   "key": "423|48|423|80|Regex|/^#{2,3} 5\\. Cost ceilings\\S*$/m",
   "line": 423,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^#{2,3} 5\\. Cost ceilings\\S*$/m\u0001/^#{2,3} 5\\. Cost ceilings\\s*$/m\u0001const idx = content.search(/^#{2,3} 5\\. Cost ceilings\\s*$/m);"
  },
  {
   "key": "424|25|424|35|ConditionalExpression|true",
   "line": 424,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001idx !== -1\u0001if (idx !== -1) {"
  },
  {
   "key": "424|25|424|35|ConditionalExpression|false",
   "line": 424,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001idx !== -1\u0001if (idx !== -1) {"
  },
  {
   "key": "424|25|424|35|EqualityOperator|idx === -1",
   "line": 424,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001EqualityOperator\u0001idx === -1\u0001idx !== -1\u0001if (idx !== -1) {"
  },
  {
   "key": "424|33|424|35|UnaryOperator|+1",
   "line": 424,
   "mutator": "UnaryOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001UnaryOperator\u0001+1\u0001-1\u0001if (idx !== -1) {"
  },
  {
   "key": "424|37|428|22|BlockStatement|{}",
   "line": 424,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ const block = content.slice(idx).split(/\\n#{2,3} /)[0]; if (!/\\[[^\\]]*\\]/.test(block) && !/\\d/.test(block)) return \"the cost ceilings carry no number — a ceiling without a value bounds nothing\"; }\u0001if (idx !== -1) {"
  },
  {
   "key": "425|39|425|57|MethodExpression|content",
   "line": 425,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001content\u0001content.slice(idx)\u0001const block = content.slice(idx).split(/\\n#{2,3} /)[0];"
  },
  {
   "key": "425|64|425|75|Regex|/\\n# /",
   "line": 425,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\n# /\u0001/\\n#{2,3} /\u0001const block = content.slice(idx).split(/\\n#{2,3} /)[0];"
  },
  {
   "key": "426|29|426|75|ConditionalExpression|false",
   "line": 426,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001!/\\[[^\\]]*\\]/.test(block) && !/\\d/.test(block)\u0001if (!/\\[[^\\]]*\\]/.test(block) && !/\\d/.test(block))"
  },
  {
   "key": "426|29|426|54|BooleanLiteral|/\\[[^\\]]*\\]/.test(block)",
   "line": 426,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BooleanLiteral\u0001/\\[[^\\]]*\\]/.test(block)\u0001!/\\[[^\\]]*\\]/.test(block)\u0001if (!/\\[[^\\]]*\\]/.test(block) && !/\\d/.test(block))"
  },
  {
   "key": "426|30|426|42|Regex|/\\[[^\\]]\\]/",
   "line": 426,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[[^\\]]\\]/\u0001/\\[[^\\]]*\\]/\u0001if (!/\\[[^\\]]*\\]/.test(block) && !/\\d/.test(block))"
  },
  {
   "key": "426|30|426|42|Regex|/\\[[\\]]*\\]/",
   "line": 426,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[[\\]]*\\]/\u0001/\\[[^\\]]*\\]/\u0001if (!/\\[[^\\]]*\\]/.test(block) && !/\\d/.test(block))"
  },
  {
   "key": "426|59|426|63|Regex|/\\D/",
   "line": 426,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\D/\u0001/\\d/\u0001if (!/\\[[^\\]]*\\]/.test(block) && !/\\d/.test(block))"
  },
  {
   "key": "427|36|427|114|StringLiteral|\"\"",
   "line": 427,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"the cost ceilings carry no number — a ceiling without a value bounds nothing\"\u0001return \"the cost ceilings carry no number — a ceiling without a value bounds nothing\";"
  },
  {
   "key": "436|19|436|72|ArrayDeclaration|[]",
   "line": 436,
   "mutator": "ArrayDeclaration",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrayDeclaration\u0001[]\u0001[\"2. Dependencies and degraded modes\", \"5. Contacts\"]\u0001sections: [\"2. Dependencies and degraded modes\", \"5. Contacts\"],"
  },
  {
   "key": "439|30|439|122|StringLiteral|\"\"",
   "line": 439,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"Criticality starts with critical or non-critical, and the failure behaviour names its mode\"\u0001description: \"Criticality starts with critical or non-critical, and the failure behaviour names its mode\","
  },
  {
   "key": "441|35|441|49|LogicalOperator|cells[2] && \"\"",
   "line": 441,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001cells[2] && \"\"\u0001cells[2] ?? \"\"\u0001const crit = (cells[2] ?? \"\").replace(/^\\*+/, \"\");"
  },
  {
   "key": "441|47|441|49|StringLiteral|\"Stryker was here!\"",
   "line": 441,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const crit = (cells[2] ?? \"\").replace(/^\\*+/, \"\");"
  },
  {
   "key": "441|59|441|65|Regex|/\\*+/",
   "line": 441,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\*+/\u0001/^\\*+/\u0001const crit = (cells[2] ?? \"\").replace(/^\\*+/, \"\");"
  },
  {
   "key": "441|59|441|65|Regex|/^\\*/",
   "line": 441,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\*/\u0001/^\\*+/\u0001const crit = (cells[2] ?? \"\").replace(/^\\*+/, \"\");"
  },
  {
   "key": "441|67|441|69|StringLiteral|\"Stryker was here!\"",
   "line": 441,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const crit = (cells[2] ?? \"\").replace(/^\\*+/, \"\");\u0001#2"
  },
  {
   "key": "443|25|443|101|ConditionalExpression|false",
   "line": 443,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001crit && !/^\\[/.test(crit) && !/^(critical|non-critical|degraded)/.test(crit)\u0001if (crit && !/^\\[/.test(crit) && !/^(critical|non-critical|degraded)/.test(crit))"
  },
  {
   "key": "443|25|443|50|ConditionalExpression|true",
   "line": 443,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001crit && !/^\\[/.test(crit)\u0001if (crit && !/^\\[/.test(crit) && !/^(critical|non-critical|degraded)/.test(crit))"
  },
  {
   "key": "443|25|443|50|LogicalOperator|crit || !/^\\[/.test(crit)",
   "line": 443,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 443, the machine payload of `check --strict --json` on the `optin` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 443, the machine payload of `check --strict --json` on the `optin` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001crit || !/^\\[/.test(crit)\u0001crit && !/^\\[/.test(crit)\u0001if (crit && !/^\\[/.test(crit) && !/^(critical|non-critical|degraded)/.test(crit))"
  },
  {
   "key": "443|33|443|50|BooleanLiteral|/^\\[/.test(crit)",
   "line": 443,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BooleanLiteral\u0001/^\\[/.test(crit)\u0001!/^\\[/.test(crit)\u0001if (crit && !/^\\[/.test(crit) && !/^(critical|non-critical|degraded)/.test(crit))"
  },
  {
   "key": "443|34|443|39|Regex|/\\[/",
   "line": 443,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[/\u0001/^\\[/\u0001if (crit && !/^\\[/.test(crit) && !/^(critical|non-critical|degraded)/.test(crit))"
  },
  {
   "key": "443|55|443|90|Regex|/(critical|non-critical|degraded)/",
   "line": 443,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/(critical|non-critical|degraded)/\u0001/^(critical|non-critical|degraded)/\u0001if (crit && !/^\\[/.test(crit) && !/^(critical|non-critical|degraded)/.test(crit))"
  },
  {
   "key": "444|54|444|71|MethodExpression|crit",
   "line": 444,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001crit\u0001crit.slice(0, 30)\u0001return `Criticality reads \"${crit.slice(0, 30)}\" — critical, non-critical or degraded-capable, with your annotation after it`;"
  },
  {
   "key": "445|39|445|53|LogicalOperator|cells[3] && \"\"",
   "line": 445,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001cells[3] && \"\"\u0001cells[3] ?? \"\"\u0001const behaviour = cells[3] ?? \"\";"
  },
  {
   "key": "445|51|445|53|StringLiteral|\"Stryker was here!\"",
   "line": 445,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const behaviour = cells[3] ?? \"\";"
  },
  {
   "key": "448|25|448|149|ConditionalExpression|false",
   "line": 448,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001behaviour && !/^\\[/.test(behaviour) && !/(fail-open|fail-closed|failover|fallback|switch|degraded|escalat)/i.test(behaviour)\u0001if (behaviour && !/^\\[/.test(behaviour) && !/(fail-open|fail-closed|failover|fallback|switch|degraded|escalat)/i.test(behaviour)) {"
  },
  {
   "key": "448|25|448|60|ConditionalExpression|true",
   "line": 448,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001behaviour && !/^\\[/.test(behaviour)\u0001if (behaviour && !/^\\[/.test(behaviour) && !/(fail-open|fail-closed|failover|fallback|switch|degraded|escalat)/i.test(behaviour)) {"
  },
  {
   "key": "448|25|448|60|LogicalOperator|behaviour || !/^\\[/.test(behaviour)",
   "line": 448,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 448, the machine payload of `check --strict --json` on the `optin` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 448, the machine payload of `check --strict --json` on the `optin` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001behaviour || !/^\\[/.test(behaviour)\u0001behaviour && !/^\\[/.test(behaviour)\u0001if (behaviour && !/^\\[/.test(behaviour) && !/(fail-open|fail-closed|failover|fallback|switch|degraded|escalat)/i.test(behaviour)) {"
  },
  {
   "key": "448|38|448|60|BooleanLiteral|/^\\[/.test(behaviour)",
   "line": 448,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BooleanLiteral\u0001/^\\[/.test(behaviour)\u0001!/^\\[/.test(behaviour)\u0001if (behaviour && !/^\\[/.test(behaviour) && !/(fail-open|fail-closed|failover|fallback|switch|degraded|escalat)/i.test(behaviour)) {"
  },
  {
   "key": "448|39|448|44|Regex|/\\[/",
   "line": 448,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[/\u0001/^\\[/\u0001if (behaviour && !/^\\[/.test(behaviour) && !/(fail-open|fail-closed|failover|fallback|switch|degraded|escalat)/i.test(behaviour)) {"
  },
  {
   "key": "448|151|450|22|BlockStatement|{}",
   "line": 448,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ return `the failure behaviour names none of fail-open | fail-closed | failover | fallback | switch | degraded | escalate`; }\u0001if (behaviour && !/^\\[/.test(behaviour) && !/(fail-open|fail-closed|failover|fallback|switch|degraded|escalat)/i.test(behaviour)) {"
  },
  {
   "key": "449|32|449|146|StringLiteral|``",
   "line": 449,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001``\u0001`the failure behaviour names none of fail-open | fail-closed | failover | fallback | switch | degraded | escalate`\u0001return `the failure behaviour names none of fail-open | fail-closed | failover | fallback | switch | degraded | escalate`;"
  },
  {
   "key": "454|21|466|15|ArrayDeclaration|[]",
   "line": 454,
   "mutator": "ArrayDeclaration",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrayDeclaration\u0001[]\u0001[{ description: \"at least one real contact row — a contacts table of placeholders is what turns an incident into an event\", check: (content) => { const rows = sectionTableRows(content, /^#{2,3} 5\\. Contacts\\s*$/m); if (rows.length === 0) return null; // the missing section is its own violation // A table of fully bracketed rows is the raw template: the presence layer owns that state. if (rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x)))) return null; const real = rows.some((c) => c.length >= 4 && c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x))); return real ? null : \"no contact row carries four real cells — the template says it plainly: filling in contacts is what turns an incident into a non-event\"; }, }]\u0001conditions: [{"
  },
  {
   "key": "455|30|455|136|StringLiteral|\"\"",
   "line": 455,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"at least one real contact row — a contacts table of placeholders is what turns an incident into an event\"\u0001description: \"at least one real contact row — a contacts table of placeholders is what turns an incident into an event\","
  },
  {
   "key": "456|37|465|18|BlockStatement|{}",
   "line": 456,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001BlockStatement\u0001{}\u0001{ const rows = sectionTableRows(content, /^#{2,3} 5\\. Contacts\\s*$/m); if (rows.length === 0) return null; // the missing section is its own violation // A table of fully bracketed rows is the raw template: the presence layer owns that state. if (rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x)))) return null; const real = rows.some((c) => c.length >= 4 && c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x))); return real ? null : \"no contact row carries four real cells — the template says it plainly: filling in contacts is what turns an incident into a non-event\"; }\u0001check: (content) => {"
  },
  {
   "key": "457|60|457|87|Regex|/#{2,3} 5\\. Contacts\\s*$/m",
   "line": 457,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/#{2,3} 5\\. Contacts\\s*$/m\u0001/^#{2,3} 5\\. Contacts\\s*$/m\u0001const rows = sectionTableRows(content, /^#{2,3} 5\\. Contacts\\s*$/m);"
  },
  {
   "key": "457|60|457|87|Regex|/^#{2,3} 5\\. Contacts\\s*/m",
   "line": 457,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^#{2,3} 5\\. Contacts\\s*/m\u0001/^#{2,3} 5\\. Contacts\\s*$/m\u0001const rows = sectionTableRows(content, /^#{2,3} 5\\. Contacts\\s*$/m);"
  },
  {
   "key": "457|60|457|87|Regex|/^# 5\\. Contacts\\s*$/m",
   "line": 457,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^# 5\\. Contacts\\s*$/m\u0001/^#{2,3} 5\\. Contacts\\s*$/m\u0001const rows = sectionTableRows(content, /^#{2,3} 5\\. Contacts\\s*$/m);"
  },
  {
   "key": "457|60|457|87|Regex|/^#{2,3} 5\\. Contacts\\s$/m",
   "line": 457,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^#{2,3} 5\\. Contacts\\s$/m\u0001/^#{2,3} 5\\. Contacts\\s*$/m\u0001const rows = sectionTableRows(content, /^#{2,3} 5\\. Contacts\\s*$/m);"
  },
  {
   "key": "457|60|457|87|Regex|/^#{2,3} 5\\. Contacts\\S*$/m",
   "line": 457,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^#{2,3} 5\\. Contacts\\S*$/m\u0001/^#{2,3} 5\\. Contacts\\s*$/m\u0001const rows = sectionTableRows(content, /^#{2,3} 5\\. Contacts\\s*$/m);"
  },
  {
   "key": "458|25|458|42|ConditionalExpression|true",
   "line": 458,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001rows.length === 0\u0001if (rows.length === 0)"
  },
  {
   "key": "458|25|458|42|ConditionalExpression|false",
   "line": 458,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001rows.length === 0\u0001if (rows.length === 0)\u0001#2"
  },
  {
   "key": "458|25|458|42|EqualityOperator|rows.length !== 0",
   "line": 458,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001EqualityOperator\u0001rows.length !== 0\u0001rows.length === 0\u0001if (rows.length === 0)"
  },
  {
   "key": "461|25|461|94|ConditionalExpression|true",
   "line": 461,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x)))\u0001if (rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))))"
  },
  {
   "key": "461|25|461|94|ConditionalExpression|false",
   "line": 461,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x)))\u0001if (rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))))"
  },
  {
   "key": "461|25|461|94|MethodExpression|rows.some(c => c.every(x => x === \"\" || /^\\[[^\\]]*\\]$/.test(x)))",
   "line": 461,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001rows.some(c => c.every(x => x === \"\" || /^\\[[^\\]]*\\]$/.test(x)))\u0001rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x)))\u0001if (rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))))"
  },
  {
   "key": "461|36|461|93|ArrowFunction|() => undefined",
   "line": 461,
   "mutator": "ArrowFunction",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrowFunction\u0001() => undefined\u0001(c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))\u0001if (rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))))"
  },
  {
   "key": "461|43|461|93|MethodExpression|c.some(x => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))",
   "line": 461,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001c.some(x => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))\u0001c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))\u0001if (rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))))"
  },
  {
   "key": "461|51|461|92|ArrowFunction|() => undefined",
   "line": 461,
   "mutator": "ArrowFunction",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrowFunction\u0001() => undefined\u0001(x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x)\u0001if (rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))))"
  },
  {
   "key": "461|58|461|92|ConditionalExpression|true",
   "line": 461,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001x === \"\" || /^\\[[^\\]]*\\]$/.test(x)\u0001if (rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))))"
  },
  {
   "key": "461|58|461|92|ConditionalExpression|false",
   "line": 461,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001x === \"\" || /^\\[[^\\]]*\\]$/.test(x)\u0001if (rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))))"
  },
  {
   "key": "461|58|461|92|LogicalOperator|x === \"\" && /^\\[[^\\]]*\\]$/.test(x)",
   "line": 461,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001x === \"\" && /^\\[[^\\]]*\\]$/.test(x)\u0001x === \"\" || /^\\[[^\\]]*\\]$/.test(x)\u0001if (rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))))"
  },
  {
   "key": "461|58|461|66|ConditionalExpression|false",
   "line": 461,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001x === \"\"\u0001if (rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))))"
  },
  {
   "key": "461|58|461|66|EqualityOperator|x !== \"\"",
   "line": 461,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001EqualityOperator\u0001x !== \"\"\u0001x === \"\"\u0001if (rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))))"
  },
  {
   "key": "461|64|461|66|StringLiteral|\"Stryker was here!\"",
   "line": 461,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001if (rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))))"
  },
  {
   "key": "461|70|461|84|Regex|/\\[[^\\]]*\\]$/",
   "line": 461,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[[^\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))))"
  },
  {
   "key": "461|70|461|84|Regex|/^\\[[^\\]]*\\]/",
   "line": 461,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]*\\]/\u0001/^\\[[^\\]]*\\]$/\u0001if (rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))))"
  },
  {
   "key": "461|70|461|84|Regex|/^\\[[^\\]]\\]$/",
   "line": 461,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))))"
  },
  {
   "key": "461|70|461|84|Regex|/^\\[[\\]]*\\]$/",
   "line": 461,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001if (rows.every((c) => c.every((x) => x === \"\" || /^\\[[^\\]]*\\]$/.test(x))))"
  },
  {
   "key": "463|34|463|132|MethodExpression|rows.every(c => c.length >= 4 && c.slice(0, 4).every(x => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x)))",
   "line": 463,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001rows.every(c => c.length >= 4 && c.slice(0, 4).every(x => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x)))\u0001rows.some((c) => c.length >= 4 && c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x)))\u0001const real = rows.some((c) => c.length >= 4 && c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x)));"
  },
  {
   "key": "463|51|463|131|ConditionalExpression|true",
   "line": 463,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001c.length >= 4 && c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x))\u0001const real = rows.some((c) => c.length >= 4 && c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x)));"
  },
  {
   "key": "463|51|463|131|LogicalOperator|c.length >= 4 || c.slice(0, 4).every(x => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x))",
   "line": 463,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001c.length >= 4 || c.slice(0, 4).every(x => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x))\u0001c.length >= 4 && c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x))\u0001const real = rows.some((c) => c.length >= 4 && c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x)));"
  },
  {
   "key": "463|51|463|64|ConditionalExpression|true",
   "line": 463,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001c.length >= 4\u0001const real = rows.some((c) => c.length >= 4 && c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x)));"
  },
  {
   "key": "463|68|463|131|MethodExpression|c.slice(0, 4).some(x => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x))",
   "line": 463,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001c.slice(0, 4).some(x => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x))\u0001c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x))\u0001const real = rows.some((c) => c.length >= 4 && c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x)));"
  },
  {
   "key": "463|68|463|81|MethodExpression|c",
   "line": 463,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001MethodExpression\u0001c\u0001c.slice(0, 4)\u0001const real = rows.some((c) => c.length >= 4 && c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x)));"
  },
  {
   "key": "463|95|463|130|ConditionalExpression|true",
   "line": 463,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x)\u0001const real = rows.some((c) => c.length >= 4 && c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x)));"
  },
  {
   "key": "463|95|463|130|LogicalOperator|x !== \"\" || !/^\\[[^\\]]*\\]$/.test(x)",
   "line": 463,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001LogicalOperator\u0001x !== \"\" || !/^\\[[^\\]]*\\]$/.test(x)\u0001x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x)\u0001const real = rows.some((c) => c.length >= 4 && c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x)));"
  },
  {
   "key": "463|95|463|103|ConditionalExpression|true",
   "line": 463,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001true\u0001x !== \"\"\u0001const real = rows.some((c) => c.length >= 4 && c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x)));"
  },
  {
   "key": "463|101|463|103|StringLiteral|\"Stryker was here!\"",
   "line": 463,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const real = rows.some((c) => c.length >= 4 && c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x)));"
  },
  {
   "key": "463|108|463|122|Regex|/\\[[^\\]]*\\]$/",
   "line": 463,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/\\[[^\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001const real = rows.some((c) => c.length >= 4 && c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x)));"
  },
  {
   "key": "463|108|463|122|Regex|/^\\[[^\\]]*\\]/",
   "line": 463,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]*\\]/\u0001/^\\[[^\\]]*\\]$/\u0001const real = rows.some((c) => c.length >= 4 && c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x)));"
  },
  {
   "key": "463|108|463|122|Regex|/^\\[[^\\]]\\]$/",
   "line": 463,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[^\\]]\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001const real = rows.some((c) => c.length >= 4 && c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x)));"
  },
  {
   "key": "463|108|463|122|Regex|/^\\[[\\]]*\\]$/",
   "line": 463,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/^\\[[\\]]*\\]$/\u0001/^\\[[^\\]]*\\]$/\u0001const real = rows.some((c) => c.length >= 4 && c.slice(0, 4).every((x) => x !== \"\" && !/^\\[[^\\]]*\\]$/.test(x)));"
  },
  {
   "key": "464|42|464|177|StringLiteral|\"\"",
   "line": 464,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"no contact row carries four real cells — the template says it plainly: filling in contacts is what turns an incident into a non-event\"\u0001return real ? null : \"no contact row carries four real cells — the template says it plainly: filling in contacts is what turns an incident into a non-event\";"
  },
  {
   "key": "474|19|474|93|ArrayDeclaration|[]",
   "line": 474,
   "mutator": "ArrayDeclaration",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ArrayDeclaration\u0001[]\u0001[\"1. The kit\", \"2. The redone task (the proof)\", \"4. Provider-swap drill\"]\u0001sections: [\"1. The kit\", \"2. The redone task (the proof)\", \"4. Provider-swap drill\"],"
  },
  {
   "key": "476|30|476|117|StringLiteral|\"\"",
   "line": 476,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001StringLiteral\u0001\"\"\u0001\"the redone task carries an ISO date and typed evidence (file:/adr:) the gate can open\"\u0001description: \"the redone task carries an ISO date and typed evidence (file:/adr:) the gate can open\","
  },
  {
   "key": "478|48|478|78|Regex|/#{2,3} 2\\. The redone task/m",
   "line": 478,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001Regex\u0001/#{2,3} 2\\. The redone task/m\u0001/^#{2,3} 2\\. The redone task/m\u0001const idx = content.search(/^#{2,3} 2\\. The redone task/m);"
  },
  {
   "key": "479|25|479|35|ConditionalExpression|false",
   "line": 479,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001ConditionalExpression\u0001false\u0001idx === -1\u0001if (idx === -1)\u0001#2"
  },
  {
   "key": "479|33|479|35|UnaryOperator|+1",
   "line": 479,
   "mutator": "UnaryOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The mutant lives in the spec registry (sections, field shapes, hints, domain lists, check bodies) that only the structureContract opt-in reads, and the calibrated exemplars do not exercise the mutated branch — the parametric family test pins one canonical degradation per element, and this variant is outside it. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001STRUCTURE\u0001UnaryOperator\u0001+1\u0001-1\u0001if (idx === -1)\u0001#2"
  }
 ]
}
