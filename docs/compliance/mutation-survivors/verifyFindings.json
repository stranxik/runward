{
 "function": "verifyFindings",
 "probeMissions": [],
 "verdicts": [
  {
   "key": "12|9|12|26|ConditionalExpression|true",
   "line": 12,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "Forcing the guard to true makes verifyFindings always return {present:false}: on my fixtures FRESH/STALE/EQUAL/NO_DELIVS all went from {present:true,date,fresh} to {present:false}, and check --strict says \"no verify findings recorded yet\" on a mission where the file exists. Nothing catches it: the 812-test unit suite and test/smoke.js both stay green.",
   "argument": "Forcing the guard to true makes verifyFindings always return {present:false}: on my fixtures FRESH/STALE/EQUAL/NO_DELIVS all went from {present:true,date,fresh} to {present:false}, and check --strict says \"no verify findings recorded yet\" on a mission where the file exists. Nothing catches it: the 812-test unit suite and test/smoke.js both stay green.",
   "stableKey": "verify-findings\u0001verifyFindings\u0001ConditionalExpression\u0001true\u0001!existsSync(path)\u0001if (!existsSync(path))"
  },
  {
   "key": "13|16|13|34|ObjectLiteral|{}",
   "line": 13,
   "mutator": "ObjectLiteral",
   "filing": "hole",
   "evidence": "Returning {} instead of {present:false} drops the `present` field the TypeScript signature declares as non-optional; my probe printed {} where the pristine printed {\"present\":false} for both absent-file fixtures. The rendered check --strict output is byte-identical in all three CLI scenarios and smoke.js stays green, so the gap is on the exported API contract, and no test asserts the returned shape.",
   "argument": "Returning {} instead of {present:false} drops the `present` field the TypeScript signature declares as non-optional; my probe printed {} where the pristine printed {\"present\":false} for both absent-file fixtures. The rendered check --strict output is byte-identical in all three CLI scenarios and smoke.js stays green, so the gap is on the exported API contract, and no test asserts the returned shape.",
   "stableKey": "verify-findings\u0001verifyFindings\u0001ObjectLiteral\u0001{}\u0001{ present: false }\u0001return { present: false };"
  },
  {
   "key": "13|27|13|32|BooleanLiteral|true",
   "line": 13,
   "mutator": "BooleanLiteral",
   "filing": "defence-in-depth",
   "evidence": "Returning {present:true} for a missing file makes check --strict print \"verify findings runward/governance/verify-findings.md (undefined) stale\" on a mission that has no findings at all. test/smoke.js reds (assertion \"a green --strict names the verify-findings artifact it would surface\", exit 1), a layer the mutation runner never invokes since its command is test/unit/*.test.js only.",
   "argument": "Returning {present:true} for a missing file makes check --strict print \"verify findings runward/governance/verify-findings.md (undefined) stale\" on a mission that has no findings at all. test/smoke.js reds (assertion \"a green --strict names the verify-findings artifact it would surface\", exit 1), a layer the mutation runner never invokes since its command is test/unit/*.test.js only.",
   "stableKey": "verify-findings\u0001verifyFindings\u0001BooleanLiteral\u0001true\u0001false\u0001return { present: false };"
  },
  {
   "key": "15|17|15|21|BooleanLiteral|false",
   "line": 15,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "Initialising fresh to false makes every present findings file report stale: measured fresh:true -> fresh:false on the FRESH, EQUAL and NO_DELIVS fixtures, and check --strict flips \"fresh\" to \"stale (a gated manifest changed since — re-run the verify workflow)\" on a findings file newer than every deliverable. Unit suite and smoke.js both green.",
   "argument": "Initialising fresh to false makes every present findings file report stale: measured fresh:true -> fresh:false on the FRESH, EQUAL and NO_DELIVS fixtures, and check --strict flips \"fresh\" to \"stale (a gated manifest changed since — re-run the verify workflow)\" on a findings file newer than every deliverable. Unit suite and smoke.js both green.",
   "stableKey": "verify-findings\u0001verifyFindings\u0001BooleanLiteral\u0001false\u0001true\u0001let fresh = true;"
  },
  {
   "key": "16|55|22|6|BlockStatement|{}",
   "line": 16,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "Emptying the loop body removes freshness detection entirely: STALE_LAST and STALE_FIRST went fresh:false -> fresh:true, and check --strict reports a findings file older than every gated manifest as \"fresh\". The operator is told the semantic pass still holds when a manifest was edited after it; nothing reds (unit suite green, smoke.js green).",
   "argument": "Emptying the loop body removes freshness detection entirely: STALE_LAST and STALE_FIRST went fresh:false -> fresh:true, and check --strict reports a findings file older than every gated manifest as \"fresh\". The operator is told the semantic pass still holds when a manifest was edited after it; nothing reds (unit suite green, smoke.js green).",
   "stableKey": "verify-findings\u0001verifyFindings\u0001BlockStatement\u0001{}\u0001{ const d = join(missionDir, deliverable); if (existsSync(d) && statSync(d).mtimeMs > mtime) { fresh = false; break; } }\u0001for (const { deliverable } of GATED_DELIVERABLES) {"
  },
  {
   "key": "18|13|18|57|ConditionalExpression|true",
   "line": 18,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "Forcing the staleness test to true breaks on the first deliverable, so every present findings file is stale: FRESH/EQUAL/NO_DELIVS went fresh:true -> fresh:false and check --strict prints \"stale — re-run the verify workflow\" on a genuinely fresh pass. No layer reds (812 unit tests green, smoke.js green).",
   "argument": "Forcing the staleness test to true breaks on the first deliverable, so every present findings file is stale: FRESH/EQUAL/NO_DELIVS went fresh:true -> fresh:false and check --strict prints \"stale — re-run the verify workflow\" on a genuinely fresh pass. No layer reds (812 unit tests green, smoke.js green).",
   "stableKey": "verify-findings\u0001verifyFindings\u0001ConditionalExpression\u0001true\u0001existsSync(d) && statSync(d).mtimeMs > mtime\u0001if (existsSync(d) && statSync(d).mtimeMs > mtime) {"
  },
  {
   "key": "18|13|18|57|ConditionalExpression|false",
   "line": 18,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "Forcing the staleness test to false makes fresh always true: STALE_LAST and STALE_FIRST went fresh:false -> fresh:true, and check --strict calls a findings file dated 2000-01-01 \"fresh\" against 2026 manifests. The freshness signal is dead and nothing catches it (unit suite green, smoke.js green).",
   "argument": "Forcing the staleness test to false makes fresh always true: STALE_LAST and STALE_FIRST went fresh:false -> fresh:true, and check --strict calls a findings file dated 2000-01-01 \"fresh\" against 2026 manifests. The freshness signal is dead and nothing catches it (unit suite green, smoke.js green).",
   "stableKey": "verify-findings\u0001verifyFindings\u0001ConditionalExpression\u0001false\u0001existsSync(d) && statSync(d).mtimeMs > mtime\u0001if (existsSync(d) && statSync(d).mtimeMs > mtime) {"
  },
  {
   "key": "18|13|18|57|LogicalOperator|existsSync(d) || statSync(d).mtimeMs > mtime",
   "line": 18,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "Turning && into || makes the mere existence of a gated deliverable mark the findings stale (FRESH and EQUAL went fresh:true -> fresh:false), and when a deliverable is absent — a mission not yet through that phase — the short-circuit no longer guards statSync, so the call throws \"ENOENT: no such file or directory, stat .../architecture.md\" where pristine returned {present:true,fresh:true}. An uncaught crash, and both the unit suite and smoke.js stay green.",
   "argument": "Turning && into || makes the mere existence of a gated deliverable mark the findings stale (FRESH and EQUAL went fresh:true -> fresh:false), and when a deliverable is absent — a mission not yet through that phase — the short-circuit no longer guards statSync, so the call throws \"ENOENT: no such file or directory, stat .../architecture.md\" where pristine returned {present:true,fresh:true}. An uncaught crash, and both the unit suite and smoke.js stay green.",
   "stableKey": "verify-findings\u0001verifyFindings\u0001LogicalOperator\u0001existsSync(d) || statSync(d).mtimeMs > mtime\u0001existsSync(d) && statSync(d).mtimeMs > mtime\u0001if (existsSync(d) && statSync(d).mtimeMs > mtime) {"
  },
  {
   "key": "18|30|18|57|ConditionalExpression|true",
   "line": 18,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "Replacing the mtime comparison with true reduces the test to existsSync(d), so any existing gated deliverable marks the findings stale: FRESH and EQUAL went fresh:true -> fresh:false and check --strict prints \"stale\" on a findings file newer than every manifest. Unit suite and smoke.js both green.",
   "argument": "Replacing the mtime comparison with true reduces the test to existsSync(d), so any existing gated deliverable marks the findings stale: FRESH and EQUAL went fresh:true -> fresh:false and check --strict prints \"stale\" on a findings file newer than every manifest. Unit suite and smoke.js both green.",
   "stableKey": "verify-findings\u0001verifyFindings\u0001ConditionalExpression\u0001true\u0001statSync(d).mtimeMs > mtime\u0001if (existsSync(d) && statSync(d).mtimeMs > mtime) {"
  },
  {
   "key": "18|30|18|57|EqualityOperator|statSync(d).mtimeMs >= mtime",
   "line": 18,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "Widening > to >= flips the boundary: on the EQUAL fixture, where a deliverable's mtime is set exactly equal to the findings' mtime via utimes, the result went fresh:true -> fresh:false. A reachable filesystem state (cp -p, archive extraction, same-write timestamps) now reports a valid pass as stale, and no test covers the equality case — unit suite and smoke.js both green.",
   "argument": "Widening > to >= flips the boundary: on the EQUAL fixture, where a deliverable's mtime is set exactly equal to the findings' mtime via utimes, the result went fresh:true -> fresh:false. A reachable filesystem state (cp -p, archive extraction, same-write timestamps) now reports a valid pass as stale, and no test covers the equality case — unit suite and smoke.js both green.",
   "stableKey": "verify-findings\u0001verifyFindings\u0001EqualityOperator\u0001statSync(d).mtimeMs >= mtime\u0001statSync(d).mtimeMs > mtime\u0001if (existsSync(d) && statSync(d).mtimeMs > mtime) {"
  },
  {
   "key": "18|30|18|57|EqualityOperator|statSync(d).mtimeMs <= mtime",
   "line": 18,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "Inverting > to <= inverts the freshness verdict: the FRESH fixture went fresh:true -> fresh:false, and in the CLI the year-2000 findings file against 2026 manifests flipped from \"stale\" to \"fresh\". I ran the entire unit suite under this mutant — 812 tests, 0 failures, exit 0 — and smoke.js is green too; the only unit file naming verify-findings asserts the verdict is unchanged by the findings, which is precisely why it cannot kill it.",
   "argument": "Inverting > to <= inverts the freshness verdict: the FRESH fixture went fresh:true -> fresh:false, and in the CLI the year-2000 findings file against 2026 manifests flipped from \"stale\" to \"fresh\". I ran the entire unit suite under this mutant — 812 tests, 0 failures, exit 0 — and smoke.js is green too; the only unit file naming verify-findings asserts the verdict is unchanged by the findings, which is precisely why it cannot kill it.",
   "stableKey": "verify-findings\u0001verifyFindings\u0001EqualityOperator\u0001statSync(d).mtimeMs <= mtime\u0001statSync(d).mtimeMs > mtime\u0001if (existsSync(d) && statSync(d).mtimeMs > mtime) {"
  },
  {
   "key": "18|59|21|10|BlockStatement|{}",
   "line": 18,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "Emptying the if body removes both `fresh = false` and the break, so staleness is never recorded: STALE_LAST and STALE_FIRST went fresh:false -> fresh:true and check --strict reports the year-2000 findings file as \"fresh\". Nothing reds (unit suite green, smoke.js green).",
   "argument": "Emptying the if body removes both `fresh = false` and the break, so staleness is never recorded: STALE_LAST and STALE_FIRST went fresh:false -> fresh:true and check --strict reports the year-2000 findings file as \"fresh\". Nothing reds (unit suite green, smoke.js green).",
   "stableKey": "verify-findings\u0001verifyFindings\u0001BlockStatement\u0001{}\u0001{ fresh = false; break; }\u0001if (existsSync(d) && statSync(d).mtimeMs > mtime) {"
  },
  {
   "key": "19|21|19|26|BooleanLiteral|true",
   "line": 19,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "Flipping the assignment to fresh = true makes the loop confirm freshness instead of denying it: STALE_LAST and STALE_FIRST went fresh:false -> fresh:true, and the CLI stale case printed \"fresh\" instead of \"stale (a gated manifest changed since — re-run the verify workflow)\". Unit suite and smoke.js both green.",
   "argument": "Flipping the assignment to fresh = true makes the loop confirm freshness instead of denying it: STALE_LAST and STALE_FIRST went fresh:false -> fresh:true, and the CLI stale case printed \"fresh\" instead of \"stale (a gated manifest changed since — re-run the verify workflow)\". Unit suite and smoke.js both green.",
   "stableKey": "verify-findings\u0001verifyFindings\u0001BooleanLiteral\u0001true\u0001false\u0001fresh = false;"
  },
  {
   "key": "23|12|23|101|ObjectLiteral|{}",
   "line": 23,
   "mutator": "ObjectLiteral",
   "filing": "hole",
   "evidence": "Returning {} for a present findings file wipes present, date and fresh: all five present fixtures printed {} instead of {present:true,date,fresh}, and check --strict falls back to \"no verify findings recorded yet\" on a mission where the file is on disk. The whole advisory signal disappears silently; unit suite and smoke.js both green.",
   "argument": "Returning {} for a present findings file wipes present, date and fresh: all five present fixtures printed {} instead of {present:true,date,fresh}, and check --strict falls back to \"no verify findings recorded yet\" on a mission where the file is on disk. The whole advisory signal disappears silently; unit suite and smoke.js both green.",
   "stableKey": "verify-findings\u0001verifyFindings\u0001ObjectLiteral\u0001{}\u0001{ present: true, date: new Date(statSync(path).mtime).toISOString().slice(0, 10), fresh }\u0001return { present: true, date: new Date(statSync(path).mtime).toISOString().slice(0, 10), fresh };"
  },
  {
   "key": "23|23|23|27|BooleanLiteral|false",
   "line": 23,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "Setting present:false on the present branch makes an existing findings file invisible: all five present fixtures went present:true -> present:false and check --strict prints \"no verify findings recorded yet\" for a file it just stat'ed. smoke.js only exercises the absent case so it stays green, and the unit suite is green too.",
   "argument": "Setting present:false on the present branch makes an existing findings file invisible: all five present fixtures went present:true -> present:false and check --strict prints \"no verify findings recorded yet\" for a file it just stat'ed. smoke.js only exercises the absent case so it stays green, and the unit suite is green too.",
   "stableKey": "verify-findings\u0001verifyFindings\u0001BooleanLiteral\u0001false\u0001true\u0001return { present: true, date: new Date(statSync(path).mtime).toISOString().slice(0, 10), fresh };"
  },
  {
   "key": "23|35|23|92|MethodExpression|new Date(statSync(path).mtime).toISOString()",
   "line": 23,
   "mutator": "MethodExpression",
   "filing": "display-only",
   "evidence": "Dropping .slice(0, 10) widens the date field from \"2025-06-15\" to \"2025-06-15T15:08:20.000Z\" — the same instant, rendered longer, in the parenthesised part of one check --strict line. Nothing branches on `date`: grep -rn verifyFindings over src/ returns only check.ts:317-322 (three log calls), and check --strict --json contains neither \"verify-findings\" nor \"fresh\", so no verdict, exit code or machine artifact moves; fresh/stale and present are untouched on all fixtures.",
   "argument": "Dropping .slice(0, 10) widens the date field from \"2025-06-15\" to \"2025-06-15T15:08:20.000Z\" — the same instant, rendered longer, in the parenthesised part of one check --strict line. Nothing branches on `date`: grep -rn verifyFindings over src/ returns only check.ts:317-322 (three log calls), and check --strict --json contains neither \"verify-findings\" nor \"fresh\", so no verdict, exit code or machine artifact moves; fresh/stale and present are untouched on all fixtures.",
   "stableKey": "verify-findings\u0001verifyFindings\u0001MethodExpression\u0001new Date(statSync(path).mtime).toISOString()\u0001new Date(statSync(path).mtime).toISOString().slice(0, 10)\u0001return { present: true, date: new Date(statSync(path).mtime).toISOString().slice(0, 10), fresh };"
  }
 ]
}
