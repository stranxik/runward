{
 "function": "inProgressCause",
 "probeMissions": [
  "consolidated-2026-09-04"
 ],
 "verdicts": [
  {
   "key": "117|11|119|6|BlockStatement|{}",
   "line": 117,
   "mutator": "BlockStatement",
   "filing": "equivalent",
   "evidence": "Le catch mute couvre le readFileSync interne d'inProgressCause, qui n'est atteint qu'APRÈS que artifactState(missionDir, a) a retourné 'in-progress' — donc après que artifactState a lui-même lu le même fichier via un readFileSync NON gardé. Toute défaillance déterministe de lecture (permissions, EISDIR) frappe d'abord artifactState : contrôle de sensibilité mesuré, fixture b-perm.md (template+ligne, chmod 000) jette THROW:EACCES identiquement sur build livré et build muté — l'exception part de artifactState, le catch mute n'est jamais entré. L'entrée voisine qui ferait diverger les deux formes est un fichier qui devient illisible ENTRE les deux lectures du même appel (TOCTOU) : impossible dans une exécution du gate, mono-processus et en lecture seule, sans écrivain concurrent. Batterie complète (17 états + 4 analyze) byte-identique à la baseline. Note : si la course existait, le mutant transformerait un null propre en TypeError non rattrapée (crash de check) — dead code défensif, pas de comportement observable.",
   "argument": "Le catch mute couvre le readFileSync interne d'inProgressCause, qui n'est atteint qu'APRÈS que artifactState(missionDir, a) a retourné 'in-progress' — donc après que artifactState a lui-même lu le même fichier via un readFileSync NON gardé. Toute défaillance déterministe de lecture (permissions, EISDIR) frappe d'abord artifactState : contrôle de sensibilité mesuré, fixture b-perm.md (template+ligne, chmod 000) jette THROW:EACCES identiquement sur build livré et build muté — l'exception part de artifactState, le catch mute n'est jamais entré. L'entrée voisine qui ferait diverger les deux formes est un fichier qui devient illisible ENTRE les deux lectures du même appel (TOCTOU) : impossible dans une exécution du gate, mono-processus et en lecture seule, sans écrivain concurrent. Batterie complète (17 états + 4 analyze) byte-identique à la baseline. Note : si la course existait, le mutant transformerait un null propre en TypeError non rattrapée (crash de check) — dead code défensif, pas de comportement observable.",
   "stableKey": "mission\u0001inProgressCause\u0001BlockStatement\u0001{}\u0001{ return null; }\u0001catch {"
  },
  {
   "key": "122|9|122|55|EqualityOperator|(content.match(PLACEHOLDER) || []).length > 3",
   "line": 122,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "RECETTE : mission scaffold (runward init) dont runward/floor.md et governance/threat-model.md contiennent EXACTEMENT 3 placeholders ([le p99]…) plus de la prose divergente (fixture fxph3). artifactState garde son propre >=3 (le mutant ne touche que inProgressCause) donc l'état reste in-progress, mais la CAUSE bascule. MESURÉ sur les DEUX surfaces : check — la ligne '◐ Floor note … — placeholders remain' devient '— started, but too close to the template to count as filled' (faux : le fichier diverge largement, la vraie raison est les placeholders) ; check --json — cause 'placeholders' -> 'below-floor' ; compliance iso-42001 — stdout 'Governance threat model raw template' -> 'below floor' et le pack ISO '**not counted** (raw template)' -> '(below floor)'. Contrôle de bord : à 4 placeholders (b-4ph) les deux formes disent 'placeholders' — la bascule est exactement sur ==3, le terrain typique du mutant d'inégalité. Rien ne le voit, VÉRIFIÉ en rejouant les 7 jambes (self-gate, smoke, audit-corpus, oscal, intoto, sarif, spelling) : toutes PASS — les codes de sortie sont inchangés partout (la cause est du vocabulaire de remédiation, pas le verdict), et la seule assertion de wording du filet (smoke.js:452, garde de divergence) accepte délibérément les trois formulations. C'est la remédiation ADR-0051 qui ment : l'opérateur d'un fichier à 3 placeholders est envoyé 'étoffer le contenu' au lieu de remplacer ses placeholders, sur check ET sur le pack.",
   "argument": "RECETTE : mission scaffold (runward init) dont runward/floor.md et governance/threat-model.md contiennent EXACTEMENT 3 placeholders ([le p99]…) plus de la prose divergente (fixture fxph3). artifactState garde son propre >=3 (le mutant ne touche que inProgressCause) donc l'état reste in-progress, mais la CAUSE bascule. MESURÉ sur les DEUX surfaces : check — la ligne '◐ Floor note … — placeholders remain' devient '— started, but too close to the template to count as filled' (faux : le fichier diverge largement, la vraie raison est les placeholders) ; check --json — cause 'placeholders' -> 'below-floor' ; compliance iso-42001 — stdout 'Governance threat model raw template' -> 'below floor' et le pack ISO '**not counted** (raw template)' -> '(below floor)'. Contrôle de bord : à 4 placeholders (b-4ph) les deux formes disent 'placeholders' — la bascule est exactement sur ==3, le terrain typique du mutant d'inégalité. Rien ne le voit, VÉRIFIÉ en rejouant les 7 jambes (self-gate, smoke, audit-corpus, oscal, intoto, sarif, spelling) : toutes PASS — les codes de sortie sont inchangés partout (la cause est du vocabulaire de remédiation, pas le verdict), et la seule assertion de wording du filet (smoke.js:452, garde de divergence) accepte délibérément les trois formulations. C'est la remédiation ADR-0051 qui ment : l'opérateur d'un fichier à 3 placeholders est envoyé 'étoffer le contenu' au lieu de remplacer ses placeholders, sur check ET sur le pack.",
   "stableKey": "mission\u0001inProgressCause\u0001EqualityOperator\u0001(content.match(PLACEHOLDER) || []).length > 3\u0001(content.match(PLACEHOLDER) || []).length >= 3\u0001if ((content.match(PLACEHOLDER) || []).length >= 3)"
  },
  {
   "key": "122|40|122|42|ArrayDeclaration|[\"Stryker was here\"]",
   "line": 122,
   "mutator": "ArrayDeclaration",
   "filing": "equivalent",
   "evidence": "Le repli [] ne sert que quand content.match(PLACEHOLDER) est null, c'est-à-dire zéro placeholder ; le tableau n'est consommé que par .length, comparé à 3. Original : 0 >= 3 = false ; muté : 1 >= 3 = false — même chute vers 'below-floor'. CONTRÔLE DE SENSIBILITÉ : les deux formes ne divergeraient que si le seuil était dans (0,1] (il est figé à 3, PLACEHOLDER_FLOOR partagé avec artifactState) ou si le contenu du tableau était lu ailleurs (il ne l'est pas : seule .length est consultée). MESURÉ sur le chemin match-null réellement atteint : fxws (decision-matrix, 0 placeholder, in-progress) et b-x (template+1 octet) donnent in-progress/below-floor identique sur p0 et sur le muté ; batterie complète byte-identique.",
   "argument": "Le repli [] ne sert que quand content.match(PLACEHOLDER) est null, c'est-à-dire zéro placeholder ; le tableau n'est consommé que par .length, comparé à 3. Original : 0 >= 3 = false ; muté : 1 >= 3 = false — même chute vers 'below-floor'. CONTRÔLE DE SENSIBILITÉ : les deux formes ne divergeraient que si le seuil était dans (0,1] (il est figé à 3, PLACEHOLDER_FLOOR partagé avec artifactState) ou si le contenu du tableau était lu ailleurs (il ne l'est pas : seule .length est consultée). MESURÉ sur le chemin match-null réellement atteint : fxws (decision-matrix, 0 placeholder, in-progress) et b-x (template+1 octet) donnent in-progress/below-floor identique sur p0 et sur le muté ; batterie complète byte-identique.",
   "stableKey": "mission\u0001inProgressCause\u0001ArrayDeclaration\u0001[\"Stryker was here\"]\u0001[]\u0001if ((content.match(PLACEHOLDER) || []).length >= 3)"
  },
  {
   "key": "145|9|145|51|ConditionalExpression|false",
   "line": 145,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 145, the machine payload of `check --strict --json` on the `declencheur` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 145, the machine payload of `check --strict --json` on the `declencheur` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001inProgressCause\u0001ConditionalExpression\u0001false\u0001spec && structureContractOptIn(missionDir)\u0001if (spec && structureContractOptIn(missionDir)) {"
  },
  {
   "key": "145|53|149|6|BlockStatement|{}",
   "line": 145,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 145, the machine payload of `check --strict --json` on the `declencheur` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 145, the machine payload of `check --strict --json` on the `declencheur` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001inProgressCause\u0001BlockStatement\u0001{}\u0001{ const v = structureViolations(missionDir, content, spec); if (v.length > 0) return v[0].cause; }\u0001if (spec && structureContractOptIn(missionDir)) {"
  },
  {
   "key": "147|13|147|25|ConditionalExpression|true",
   "line": 147,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001inProgressCause\u0001ConditionalExpression\u0001true\u0001v.length > 0\u0001if (v.length > 0)"
  },
  {
   "key": "147|13|147|25|ConditionalExpression|false",
   "line": 147,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 147, the machine payload of `check --strict --json` on the `declencheur` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 147, the machine payload of `check --strict --json` on the `declencheur` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001inProgressCause\u0001ConditionalExpression\u0001false\u0001v.length > 0\u0001if (v.length > 0)"
  },
  {
   "key": "147|13|147|25|EqualityOperator|v.length >= 0",
   "line": 147,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001inProgressCause\u0001EqualityOperator\u0001v.length >= 0\u0001v.length > 0\u0001if (v.length > 0)"
  },
  {
   "key": "147|13|147|25|EqualityOperator|v.length <= 0",
   "line": 147,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 147, the machine payload of `check --strict --json` on the `declencheur` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 147, the machine payload of `check --strict --json` on the `declencheur` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001inProgressCause\u0001EqualityOperator\u0001v.length <= 0\u0001v.length > 0\u0001if (v.length > 0)"
  }
 ]
}
