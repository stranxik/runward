{
 "function": "readRuleSet",
 "probeMissions": [],
 "verdicts": [
  {
   "key": "201|12|203|16|MethodExpression|readdirSync(dir).filter(f => f.endsWith(\".md\"))",
   "line": 201,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "Removing .sort() makes the 'deterministic inventory' depend on readdir order, which Node leaves unspecified. Measured on a fixture rules directory where APFS's UTF-8 byte order differs from JS code-unit order (astral-plane vs BMP filenames): readRuleSet returned slugs in a different order than pristine. On the shipped corpus this filesystem happens to return sorted order, so smoke's sortedness assertion and every other layer stayed green.",
   "argument": "Removing .sort() makes the 'deterministic inventory' depend on readdir order, which Node leaves unspecified. Measured on a fixture rules directory where APFS's UTF-8 byte order differs from JS code-unit order (astral-plane vs BMP filenames): readRuleSet returned slugs in a different order than pristine. On the shipped corpus this filesystem happens to return sorted order, so smoke's sortedness assertion and every other layer stayed green.",
   "stableKey": "rules\u0001readRuleSet\u0001MethodExpression\u0001readdirSync(dir).filter(f => f.endsWith(\".md\"))\u0001readdirSync(dir) .filter((f) => f.endsWith(\".md\")) .sort()\u0001return readdirSync(dir)"
  }
 ]
}
