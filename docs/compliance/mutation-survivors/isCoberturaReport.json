{
 "function": "isCoberturaReport",
 "probeMissions": [],
 "verdicts": [
  {
   "key": "177|12|177|108|LogicalOperator|/<coverage\\b[^>]*\\bline-rate\\s*=/i.test(content) || /<class\\b[^>]*\\bfilename\\s*=/i.test(content)",
   "line": 177,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "Requiring only one of the two structural markers misrecognises other XML as Cobertura and reroutes the pointer to the coverage adapter. Measured: isCoberturaReport goes false -> true both for a <coverage line-rate=...> root with no class record and for a non-coverage XML carrying <class filename=...>.",
   "argument": "Requiring only one of the two structural markers misrecognises other XML as Cobertura and reroutes the pointer to the coverage adapter. Measured: isCoberturaReport goes false -> true both for a <coverage line-rate=...> root with no class record and for a non-coverage XML carrying <class filename=...>.",
   "stableKey": "tool-adapters\u0001isCoberturaReport\u0001LogicalOperator\u0001/<coverage\\b[^>]*\\bline-rate\\s*=/i.test(content) || /<class\\b[^>]*\\bfilename\\s*=/i.test(content)\u0001/<coverage\\b[^>]*\\bline-rate\\s*=/i.test(content) && /<class\\b[^>]*\\bfilename\\s*=/i.test(content)\u0001return /<coverage\\b[^>]*\\bline-rate\\s*=/i.test(content) && /<class\\b[^>]*\\bfilename\\s*=/i.test(content);"
  },
  {
   "key": "177|12|177|46|Regex|/<coverage\\b[^>]\\bline-rate\\s*=/i",
   "line": 177,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "`[^>]` requires exactly one character between <coverage and line-rate, so line-rate must be the first attribute. Measured on a coverage.py-shaped root (version and timestamp before line-rate): isCoberturaReport goes true -> false, and the report stops being read as coverage at all.",
   "argument": "`[^>]` requires exactly one character between <coverage and line-rate, so line-rate must be the first attribute. Measured on a coverage.py-shaped root (version and timestamp before line-rate): isCoberturaReport goes true -> false, and the report stops being read as coverage at all.",
   "stableKey": "tool-adapters\u0001isCoberturaReport\u0001Regex\u0001/<coverage\\b[^>]\\bline-rate\\s*=/i\u0001/<coverage\\b[^>]*\\bline-rate\\s*=/i\u0001return /<coverage\\b[^>]*\\bline-rate\\s*=/i.test(content) && /<class\\b[^>]*\\bfilename\\s*=/i.test(content);"
  },
  {
   "key": "177|12|177|46|Regex|/<coverage\\b[^>]*\\bline-rate\\S*=/i",
   "line": 177,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "`line-rate\\S*=` cannot cross whitespace before the equals sign. Measured on a legal XML root written `line-rate = \"0.5\"`: isCoberturaReport goes true -> false.",
   "argument": "`line-rate\\S*=` cannot cross whitespace before the equals sign. Measured on a legal XML root written `line-rate = \"0.5\"`: isCoberturaReport goes true -> false.",
   "stableKey": "tool-adapters\u0001isCoberturaReport\u0001Regex\u0001/<coverage\\b[^>]*\\bline-rate\\S*=/i\u0001/<coverage\\b[^>]*\\bline-rate\\s*=/i\u0001return /<coverage\\b[^>]*\\bline-rate\\s*=/i.test(content) && /<class\\b[^>]*\\bfilename\\s*=/i.test(content);"
  },
  {
   "key": "177|64|177|94|Regex|/<class\\b[^>]*\\bfilename\\S*=/i",
   "line": 177,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "`filename\\S*=` cannot cross whitespace before the equals sign. Measured on a report whose class records are written `filename = \"src/ws.ts\"`: isCoberturaReport goes true -> false.",
   "argument": "`filename\\S*=` cannot cross whitespace before the equals sign. Measured on a report whose class records are written `filename = \"src/ws.ts\"`: isCoberturaReport goes true -> false.",
   "stableKey": "tool-adapters\u0001isCoberturaReport\u0001Regex\u0001/<class\\b[^>]*\\bfilename\\S*=/i\u0001/<class\\b[^>]*\\bfilename\\s*=/i\u0001return /<coverage\\b[^>]*\\bline-rate\\s*=/i.test(content) && /<class\\b[^>]*\\bfilename\\s*=/i.test(content);"
  }
 ]
}
