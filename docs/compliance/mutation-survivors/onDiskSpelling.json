{
 "function": "onDiskSpelling",
 "probeMissions": [
  "M1 `m1-ladder` — the whole ladder as an operator would plainly write it. Copy examples/request-triage to a temp root; add src/guard.ts, src/lib/deep/guard.ts and src/cafe\\u0301.ts (NFD on disk), each exporting assertGrounded; rewrite the Evidence cell of the eight `applied` rows of runward/floor.md with, in order: file:src/guard.ts#assertGrounded (exact), file:src/Guard.TS (wrong case in the FILE component), file:SRC/guard.ts (wrong case in a DIRECTORY component), file:src/LIB/deep/guard.ts (wrong case in a nested directory), file:src/nope.ts (does not exist), file:../outside.ts (escapes upward), file:src/caf\\u00e9.ts (typed NFC, spelled NFD on disk); the remaining rows become `n/a` with a reason. Baseline `check --strict --json`: exit 1, 6 conformance gaps, three of them the spelling message. RESULT: 0/15 mutants observable — every case divergence here is re-derived by spellingViaRealpath.",
  "M2 `m2-unmasked` — the same divergences written with a redundant `./` segment (file:./src/Guard.TS, file:./SRC/guard.ts, file:./src/LIB/deep/guard.ts, file:./SRC/lib/DEEP/guard.ts, file:./src/caf\\u00e9.ts) plus one exact row. Baseline: exit 1, 5 conformance gaps, all five the spelling message. RESULT: 3/15 observable (the three L274 rungs), 5 gaps -> 1.",
  "M3 `m3-exact` — every pointer spelled exactly as the disk spells it, in all four forms (plain, nested, `./`, NFD). Baseline: exit 0, verdict clean, 0 conformance gaps. The false-RED detector: a guard that refuses everything would satisfy the other missions. RESULT: 0/15 observable — no mutant invents a spelling violation.",
  "M4 `m4-unreadable` — a directory the walk can traverse but not list. Same base mission plus `locked/guard.ts`, then `chmod 111 locked`, so readdirSync throws EACCES while existsSync/statSync/realpath/readFile all still work. Rows: file:locked/guard.ts (exact), file:locked/Guard.TS (wrong case, still caught by the realpath fallback), file:./locked/Guard.TS (wrong case, unmasked, and reported by nothing today). Baseline: exit 1, 1 conformance gap. This is the only way into the `catch`. RESULT: 1/15 observable (the empty catch).",
  "M5 `m5-repo` — the repository fallback of resolvePointer (evidence.ts:316), which calls onDiskSpelling with no realpath fallback. Layout: <repo>/.git (marker), <repo>/shared/{guard.ts,lib/deep/guard.ts,cafe\\u0301.ts}, and the mission at <repo>/proj (a copy of examples/request-triage). Rows point out of the three bases but stay inside the repo: file:../shared/Guard.TS, file:../SHARED/guard.ts, file:../SHARED/lib/DEEP/guard.ts, file:../shared/caf\\u00e9.ts, plus one exact row. Baseline: exit 1, 4 conformance gaps. RESULT: 3/15 observable (the three L274 rungs).",
  "M6 `m6-case-only` — M2 with the Unicode row removed, so the ONLY defect in the mission is case divergence. Baseline: exit 1, verdict `gaps`, 4 conformance gaps. This is the false-GREEN detector and it is where the headline evidence comes from. RESULT: 3/15 observable, each as an EXIT CODE flip 1 -> 0.",
  "M7 `m7-repo` — M5 with the Unicode row removed: the same false-green detector on the resolvePointer branch that has no fallback behind it. Baseline: exit 1, 3 conformance gaps. RESULT: 3/15 observable, each as an exit code flip 1 -> 0.",
  "Plus a sweep against runward's own mission: 0/15 observable, as expected — no pointer in this repository has a diverging spelling, so the self-gate cannot exercise this function whatever the probe does. Eight ledgers, 120 measurements, at reports/mutation/probe-{selfgate,m1..m7}.jsonl in the probe worktree, all written by the corrected scripts/mutation-probe.mjs.",
  "Builder (rebuilds all seven from scratch): scratchpad/build-missions.mjs; clean-tree baseline reader: scratchpad/baselines.mjs; single-mutant observer: scratchpad/observe.mjs; whole-net runner: scratchpad/net.mjs. All four use `check --strict --json`.",
  "base: init --yes --example, green. Every other mission is a copy carrying exactly one deliberate change, and the pristine verdict of each was read BEFORE any mutant was applied.",
  "m-crow-green: a self-citation, plus one line above the heading with a LEADING pipe, NO trailing pipe, and a first cell carrying whitespace. Pristine exit 1, one gap. The shape that separates startsWith from endsWith.",
  "m-crow-red: the same self-citation, and above the heading an honest sentence that merely ENDS with a pipe. Pristine exit 0, clean.",
  "m-crow-prose and m-crow-bare: pipe-free prose, and nothing at all. The controls for the pipe-free region and for the must-stay-red case.",
  "m-spell and m-spell-dir: one pointer mis-cased on its file, and on a DIRECTORY segment, nothing else changed, so the divergence is the ONLY violation. Pristine exit 1 with exactly one gap. m-spell-control: untouched, carried through every capture so 'this mission did not move' is distinguishable from 'nothing moves'.",
  "conformanceRow BATTERY, 4591 markdown blocks read through evidenceReport itself using the unit test's own oracle: 0 problems means the block was KEPT, 1 means the function called it a row. Leads times first cells times separators times statuses times tails; the same shapes behind three fence styles; 237 pipe-free shapes including five pipe LOOK-ALIKE characters, NBSP, U+2028, BOM, 4000-character lines and mixed blocks; plus all 23 cases the unit test pins, asserted to read as that test pins them.",
  "conformanceRow FUZZ, 6000 blocks from a seeded xorshift32 so it is a fixture and not a lottery, 4000 of them guaranteed pipe-free — density in the one region the two guard mutants can act in.",
  "onDiskSpelling DIRECT BATTERY, 329 calls on a purpose-built fixture: mixed-case directories and files, the five Unicode fold rungs, an NFD-named file, a ten-deep path, an empty directory and a 0111 directory. Each target fed in seven shapes — plain, one/two/three trailing separators, a doubled inner separator, an extra leading separator, every segment doubled — plus 14 absolute edge cases. Paths concatenated BY HAND, not with path.join, because join() normalises away exactly the shapes the loop bound is about.",
  "CASE-SENSITIVE REPRODUCTION: a 300 MB case-sensitive APFS sparse image, verified case-sensitive by probe. Every mission copied onto it and re-measured, and the whole unit net re-run with TMPDIR pointing into it, so 'survives on ubuntu-latest' is MEASURED here rather than inferred from process.platform. The volume was probed for normalisation too: a file written NFC is found under its NFD name, so it is case-sensitive but normalisation-insensitive, and that limit is stated wherever it matters."
 ],
 "verdicts": [
  {
   "key": "338|13|338|18|ConditionalExpression|false",
   "line": 338,
   "mutator": "ConditionalExpression",
   "replacement": "false",
   "filing": "hole",
   "cleared": false,
   "evidence": "Identical `check --strict --json` payload on all seven probe missions and the self-gate (8/8 'no observable difference'). COULD NOT CLEAR. Removing the `!want` skip only matters when a path component is empty. Measured, not read: onDiskSpelling only ever receives a `path.resolve()` result, `path.posix.resolve` collapses '//' (resolve('//x/y','z').split('/') -> ['','x','y','z']) and produces an empty component at index 0 only, while the loop starts at index 1 — so on this platform the guard never fires and no mission can reach it. The one shape that does reach it is a Windows UNC or extended-length path: win32.resolve('\\\\\\\\server\\\\share','x\\\\y').split('\\\\') -> ['','','server','share','x','y'] and '\\\\\\\\?\\\\C:\\\\proj' -> ['','','?','C:',...], both with an empty component at index 1. I cannot run either here, and the suite has no win32 fixture, so nothing in runward's net distinguishes this mutant on any platform. Filed as a hole rather than argued equivalent, per the rule that an undecidable mutant is over-reported. It shares the remedy of the five drive-root mutants above: one exported, path-flavour-parameterised walk that a unit test can feed win32-shaped input to.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001ConditionalExpression\u0001false\u0001!want\u0001if (!want)"
  },
  {
   "key": "376|86|376|96|ArithmeticOperator|from - sep",
   "line": 376,
   "mutator": "ArithmeticOperator",
   "filing": "defence-in-depth",
   "evidence": "Neutralises the bound, so base is null and the walk restarts at the filesystem root — RWD-2026-0074 verbatim. Caught, by a named leg: test/spelling-conformance.js case ancestor-permissions-do-not-change-the-verdict [RWD-2026-0074] fails under the applied mutant (\"expected accepted, got [... cannot list a directory on this path ...]\"). That case requires only posixPermissions, true on ubuntu-latest, so it catches this on the measurement runner too. It read as a survivor because that file was in NEITHER pass — the unit stage runs test/unit/*.test.js only, and the whole net did not name it. Both were corrected on 2026-08-27. Here from - sep is NaN, so startsWith(\"NaN\") is false for every absolute path and only abs === from remains.",
   "argument": "Neutralises the bound, so base is null and the walk restarts at the filesystem root — RWD-2026-0074 verbatim. Caught, by a named leg: test/spelling-conformance.js case ancestor-permissions-do-not-change-the-verdict [RWD-2026-0074] fails under the applied mutant (\"expected accepted, got [... cannot list a directory on this path ...]\"). That case requires only posixPermissions, true on ubuntu-latest, so it catches this on the measurement runner too. It read as a survivor because that file was in NEITHER pass — the unit stage runs test/unit/*.test.js only, and the whole net did not name it. Both were corrected on 2026-08-27. Here from - sep is NaN, so startsWith(\"NaN\") is false for every absolute path and only abs === from remains.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001ArithmeticOperator\u0001from - sep\u0001from + sep\u0001const base = from && (abs === from || abs.startsWith(from.endsWith(sep) ? from : from + sep)) ? from : null;"
  },
  {
   "key": "376|18|376|98|ConditionalExpression|true",
   "line": 376,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "Forces the bound ON unconditionally. Reachable through the workspace allowance: repo && (real === repo || real.startsWith(repo + sep)) calls onDiskSpelling(abs, resolve(b)) on a path legitimately OUTSIDE the project root. With the bound forced on, abs.slice(base.length) cuts at an arbitrary offset, the first segment matches nothing, and the walk returns null — and null means \"the spelling already matches\". MISSION: <outer>/.git; <outer>/shared/guard.ts; <outer>/proj/runward/floor.md with row `| rule-a | applied | file:../shared/Guard.TS |`. Shipped build refuses (spelling differs); mutant applied to dist/lib/evidence.js ACCEPTS — exit 0 on a pointer no case-sensitive runner resolves. Nothing catches it: spelling-conformance 0 failures, check --strict exit 0, and evidence-spelling-ladder.test.js never passes a second argument to onDiskSpelling, so the bounded branch has no unit coverage at all.",
   "argument": "Forces the bound ON unconditionally. Reachable through the workspace allowance: repo && (real === repo || real.startsWith(repo + sep)) calls onDiskSpelling(abs, resolve(b)) on a path legitimately OUTSIDE the project root. With the bound forced on, abs.slice(base.length) cuts at an arbitrary offset, the first segment matches nothing, and the walk returns null — and null means \"the spelling already matches\". MISSION: <outer>/.git; <outer>/shared/guard.ts; <outer>/proj/runward/floor.md with row `| rule-a | applied | file:../shared/Guard.TS |`. Shipped build refuses (spelling differs); mutant applied to dist/lib/evidence.js ACCEPTS — exit 0 on a pointer no case-sensitive runner resolves. Nothing catches it: spelling-conformance 0 failures, check --strict exit 0, and evidence-spelling-ladder.test.js never passes a second argument to onDiskSpelling, so the bounded branch has no unit coverage at all.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001ConditionalExpression\u0001true\u0001from && (abs === from || abs.startsWith(from.endsWith(sep) ? from : from + sep))\u0001const base = from && (abs === from || abs.startsWith(from.endsWith(sep) ? from : from + sep)) ? from : null;"
  },
  {
   "key": "376|18|376|98|ConditionalExpression|false",
   "line": 376,
   "mutator": "ConditionalExpression",
   "filing": "defence-in-depth",
   "evidence": "Neutralises the bound, so base is null and the walk restarts at the filesystem root — RWD-2026-0074 verbatim. Caught, by a named leg: test/spelling-conformance.js case ancestor-permissions-do-not-change-the-verdict [RWD-2026-0074] fails under the applied mutant (\"expected accepted, got [... cannot list a directory on this path ...]\"). That case requires only posixPermissions, true on ubuntu-latest, so it catches this on the measurement runner too. It read as a survivor because that file was in NEITHER pass — the unit stage runs test/unit/*.test.js only, and the whole net did not name it. Both were corrected on 2026-08-27.",
   "argument": "Neutralises the bound, so base is null and the walk restarts at the filesystem root — RWD-2026-0074 verbatim. Caught, by a named leg: test/spelling-conformance.js case ancestor-permissions-do-not-change-the-verdict [RWD-2026-0074] fails under the applied mutant (\"expected accepted, got [... cannot list a directory on this path ...]\"). That case requires only posixPermissions, true on ubuntu-latest, so it catches this on the measurement runner too. It read as a survivor because that file was in NEITHER pass — the unit stage runs test/unit/*.test.js only, and the whole net did not name it. Both were corrected on 2026-08-27.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001ConditionalExpression\u0001false\u0001from && (abs === from || abs.startsWith(from.endsWith(sep) ? from : from + sep))\u0001const base = from && (abs === from || abs.startsWith(from.endsWith(sep) ? from : from + sep)) ? from : null;"
  },
  {
   "key": "376|27|376|97|ConditionalExpression|true",
   "line": 376,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "Forces the bound ON unconditionally. Reachable through the workspace allowance: repo && (real === repo || real.startsWith(repo + sep)) calls onDiskSpelling(abs, resolve(b)) on a path legitimately OUTSIDE the project root. With the bound forced on, abs.slice(base.length) cuts at an arbitrary offset, the first segment matches nothing, and the walk returns null — and null means \"the spelling already matches\". MISSION: <outer>/.git; <outer>/shared/guard.ts; <outer>/proj/runward/floor.md with row `| rule-a | applied | file:../shared/Guard.TS |`. Shipped build refuses (spelling differs); mutant applied to dist/lib/evidence.js ACCEPTS — exit 0 on a pointer no case-sensitive runner resolves. Nothing catches it: spelling-conformance 0 failures, check --strict exit 0, and evidence-spelling-ladder.test.js never passes a second argument to onDiskSpelling, so the bounded branch has no unit coverage at all. Applied and read independently of the outer-condition mutant on the line above.",
   "argument": "Forces the bound ON unconditionally. Reachable through the workspace allowance: repo && (real === repo || real.startsWith(repo + sep)) calls onDiskSpelling(abs, resolve(b)) on a path legitimately OUTSIDE the project root. With the bound forced on, abs.slice(base.length) cuts at an arbitrary offset, the first segment matches nothing, and the walk returns null — and null means \"the spelling already matches\". MISSION: <outer>/.git; <outer>/shared/guard.ts; <outer>/proj/runward/floor.md with row `| rule-a | applied | file:../shared/Guard.TS |`. Shipped build refuses (spelling differs); mutant applied to dist/lib/evidence.js ACCEPTS — exit 0 on a pointer no case-sensitive runner resolves. Nothing catches it: spelling-conformance 0 failures, check --strict exit 0, and evidence-spelling-ladder.test.js never passes a second argument to onDiskSpelling, so the bounded branch has no unit coverage at all. Applied and read independently of the outer-condition mutant on the line above.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001ConditionalExpression\u0001true\u0001abs === from || abs.startsWith(from.endsWith(sep) ? from : from + sep)\u0001const base = from && (abs === from || abs.startsWith(from.endsWith(sep) ? from : from + sep)) ? from : null;"
  },
  {
   "key": "376|27|376|39|ConditionalExpression|false",
   "line": 376,
   "mutator": "ConditionalExpression",
   "filing": "equivalent",
   "evidence": "The removed disjunct decides only when abs === from; everywhere else abs.startsWith(from + sep) already answers true. SENSITIVITY CONTROL: the nearby input is `file:.`, resolving exactly to the project root — there the shipped build takes base = from and the mutant base = null (walking from the filesystem root). That difference cannot reach a verdict, because abs === from means the pointer resolves to a DIRECTORY and evidenceReport emits \"typed pointer resolves to a directory, not a file\" from the !isRegularFile(abs) branch BEFORE it reads r.spelling. Applied and read: eight probe scenarios and all 11 applicable conformance cases identical.",
   "argument": "The removed disjunct decides only when abs === from; everywhere else abs.startsWith(from + sep) already answers true. SENSITIVITY CONTROL: the nearby input is `file:.`, resolving exactly to the project root — there the shipped build takes base = from and the mutant base = null (walking from the filesystem root). That difference cannot reach a verdict, because abs === from means the pointer resolves to a DIRECTORY and evidenceReport emits \"typed pointer resolves to a directory, not a file\" from the !isRegularFile(abs) branch BEFORE it reads r.spelling. Applied and read: eight probe scenarios and all 11 applicable conformance cases identical.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001ConditionalExpression\u0001false\u0001abs === from\u0001const base = from && (abs === from || abs.startsWith(from.endsWith(sep) ? from : from + sep)) ? from : null;"
  },
  {
   "key": "376|27|376|39|EqualityOperator|abs !== from",
   "line": 376,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "Inverts the identity test, so the disjunction is true whenever abs !== from — i.e. for every pointer, including those NOT under from. Forces the bound ON unconditionally. Reachable through the workspace allowance: repo && (real === repo || real.startsWith(repo + sep)) calls onDiskSpelling(abs, resolve(b)) on a path legitimately OUTSIDE the project root. With the bound forced on, abs.slice(base.length) cuts at an arbitrary offset, the first segment matches nothing, and the walk returns null — and null means \"the spelling already matches\". MISSION: <outer>/.git; <outer>/shared/guard.ts; <outer>/proj/runward/floor.md with row `| rule-a | applied | file:../shared/Guard.TS |`. Shipped build refuses (spelling differs); mutant applied to dist/lib/evidence.js ACCEPTS — exit 0 on a pointer no case-sensitive runner resolves. Nothing catches it: spelling-conformance 0 failures, check --strict exit 0, and evidence-spelling-ladder.test.js never passes a second argument to onDiskSpelling, so the bounded branch has no unit coverage at all. It also flips the abs === from case to base = null, which the !isRegularFile(abs) refusal shields; the workspace case is shielded by nothing.",
   "argument": "Inverts the identity test, so the disjunction is true whenever abs !== from — i.e. for every pointer, including those NOT under from. Forces the bound ON unconditionally. Reachable through the workspace allowance: repo && (real === repo || real.startsWith(repo + sep)) calls onDiskSpelling(abs, resolve(b)) on a path legitimately OUTSIDE the project root. With the bound forced on, abs.slice(base.length) cuts at an arbitrary offset, the first segment matches nothing, and the walk returns null — and null means \"the spelling already matches\". MISSION: <outer>/.git; <outer>/shared/guard.ts; <outer>/proj/runward/floor.md with row `| rule-a | applied | file:../shared/Guard.TS |`. Shipped build refuses (spelling differs); mutant applied to dist/lib/evidence.js ACCEPTS — exit 0 on a pointer no case-sensitive runner resolves. Nothing catches it: spelling-conformance 0 failures, check --strict exit 0, and evidence-spelling-ladder.test.js never passes a second argument to onDiskSpelling, so the bounded branch has no unit coverage at all. It also flips the abs === from case to base = null, which the !isRegularFile(abs) refusal shields; the workspace case is shielded by nothing.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001EqualityOperator\u0001abs !== from\u0001abs === from\u0001const base = from && (abs === from || abs.startsWith(from.endsWith(sep) ? from : from + sep)) ? from : null;"
  },
  {
   "key": "376|27|376|97|LogicalOperator|abs === from && abs.startsWith(from.endsWith(sep) ? from : from + sep)",
   "line": 376,
   "mutator": "LogicalOperator",
   "filing": "defence-in-depth",
   "evidence": "Neutralises the bound, so base is null and the walk restarts at the filesystem root — RWD-2026-0074 verbatim. Caught, by a named leg: test/spelling-conformance.js case ancestor-permissions-do-not-change-the-verdict [RWD-2026-0074] fails under the applied mutant (\"expected accepted, got [... cannot list a directory on this path ...]\"). That case requires only posixPermissions, true on ubuntu-latest, so it catches this on the measurement runner too. It read as a survivor because that file was in NEITHER pass — the unit stage runs test/unit/*.test.js only, and the whole net did not name it. Both were corrected on 2026-08-27. Here abs === from is false for every pointer under the project, so the conjunction is false.",
   "argument": "Neutralises the bound, so base is null and the walk restarts at the filesystem root — RWD-2026-0074 verbatim. Caught, by a named leg: test/spelling-conformance.js case ancestor-permissions-do-not-change-the-verdict [RWD-2026-0074] fails under the applied mutant (\"expected accepted, got [... cannot list a directory on this path ...]\"). That case requires only posixPermissions, true on ubuntu-latest, so it catches this on the measurement runner too. It read as a survivor because that file was in NEITHER pass — the unit stage runs test/unit/*.test.js only, and the whole net did not name it. Both were corrected on 2026-08-27. Here abs === from is false for every pointer under the project, so the conjunction is false.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001LogicalOperator\u0001abs === from && abs.startsWith(from.endsWith(sep) ? from : from + sep)\u0001abs === from || abs.startsWith(from.endsWith(sep) ? from : from + sep)\u0001const base = from && (abs === from || abs.startsWith(from.endsWith(sep) ? from : from + sep)) ? from : null;"
  },
  {
   "key": "376|43|376|97|MethodExpression|abs.endsWith(from.endsWith(sep) ? from : from + sep)",
   "line": 376,
   "mutator": "MethodExpression",
   "filing": "defence-in-depth",
   "evidence": "Neutralises the bound, so base is null and the walk restarts at the filesystem root — RWD-2026-0074 verbatim. Caught, by a named leg: test/spelling-conformance.js case ancestor-permissions-do-not-change-the-verdict [RWD-2026-0074] fails under the applied mutant (\"expected accepted, got [... cannot list a directory on this path ...]\"). That case requires only posixPermissions, true on ubuntu-latest, so it catches this on the measurement runner too. It read as a survivor because that file was in NEITHER pass — the unit stage runs test/unit/*.test.js only, and the whole net did not name it. Both were corrected on 2026-08-27. Here a path never ENDS with its own ancestor prefix, so the disjunct is dead and only abs === from remains.",
   "argument": "Neutralises the bound, so base is null and the walk restarts at the filesystem root — RWD-2026-0074 verbatim. Caught, by a named leg: test/spelling-conformance.js case ancestor-permissions-do-not-change-the-verdict [RWD-2026-0074] fails under the applied mutant (\"expected accepted, got [... cannot list a directory on this path ...]\"). That case requires only posixPermissions, true on ubuntu-latest, so it catches this on the measurement runner too. It read as a survivor because that file was in NEITHER pass — the unit stage runs test/unit/*.test.js only, and the whole net did not name it. Both were corrected on 2026-08-27. Here a path never ENDS with its own ancestor prefix, so the disjunct is dead and only abs === from remains.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001MethodExpression\u0001abs.endsWith(from.endsWith(sep) ? from : from + sep)\u0001abs.startsWith(from.endsWith(sep) ? from : from + sep)\u0001const base = from && (abs === from || abs.startsWith(from.endsWith(sep) ? from : from + sep)) ? from : null;"
  },
  {
   "key": "376|58|376|76|MethodExpression|from.startsWith(sep)",
   "line": 376,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "On POSIX from is absolute, so from.startsWith(sep) is invariantly true and the ternary yields from instead of from + sep: the bound degrades to a bare string prefix and swallows any SIBLING whose name begins with the project root name. MISSION: <outer>/.git; project root <outer>/proj; <outer>/proj-other/guard.ts; row `| rule-a | applied | file:../proj-other/Guard.TS |`, admitted by the workspace allowance so the walk runs. Shipped build refuses; mutant ACCEPTS, exit 0. Verified by applying it: spelling-conformance 0 failures, check --strict exit 0, unit suite green. This is the mirror of RWD-2026-0081 — a bound with the wrong BOUNDARY rather than the wrong namespace. Inert on Windows, where \"C:\\…\".startsWith(\"\\\") is false.",
   "argument": "On POSIX from is absolute, so from.startsWith(sep) is invariantly true and the ternary yields from instead of from + sep: the bound degrades to a bare string prefix and swallows any SIBLING whose name begins with the project root name. MISSION: <outer>/.git; project root <outer>/proj; <outer>/proj-other/guard.ts; row `| rule-a | applied | file:../proj-other/Guard.TS |`, admitted by the workspace allowance so the walk runs. Shipped build refuses; mutant ACCEPTS, exit 0. Verified by applying it: spelling-conformance 0 failures, check --strict exit 0, unit suite green. This is the mirror of RWD-2026-0081 — a bound with the wrong BOUNDARY rather than the wrong namespace. Inert on Windows, where \"C:\\…\".startsWith(\"\\\") is false.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001MethodExpression\u0001from.startsWith(sep)\u0001from.endsWith(sep)\u0001const base = from && (abs === from || abs.startsWith(from.endsWith(sep) ? from : from + sep)) ? from : null;"
  },
  {
   "key": "377|27|377|49|MethodExpression|abs",
   "line": 377,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "Passes the WHOLE absolute path where the remainder below the bound was intended. With the bound engaged the walk matches no first segment and returns null for every in-project pointer. On a case-insensitive volume spellingViaRealpath silently re-derives most of that loss (resolvePointer consults it exactly on null), which is why plain case divergence still refuses — but that rung compares with toLowerCase(), which leaves U+017F and U+1E9E untouched. MISSION: code/sguard.ts on disk, row `| rule-a | applied | file:code/ſguard.ts#guardFields |`. Shipped build refuses; mutant ACCEPTS, exit 0 — the RWD-2026-0031/0035 false green re-opened. Applied and read: spelling-conformance fails unicode-fold-long-s and unicode-fold-capital-sharp-s on APFS, but both cases require unicodeFold and runward CI had no macOS leg until 2026-08-27, so on every runner the project used they were skipped and nothing fired. Self-gate exit 0.",
   "argument": "Passes the WHOLE absolute path where the remainder below the bound was intended. With the bound engaged the walk matches no first segment and returns null for every in-project pointer. On a case-insensitive volume spellingViaRealpath silently re-derives most of that loss (resolvePointer consults it exactly on null), which is why plain case divergence still refuses — but that rung compares with toLowerCase(), which leaves U+017F and U+1E9E untouched. MISSION: code/sguard.ts on disk, row `| rule-a | applied | file:code/ſguard.ts#guardFields |`. Shipped build refuses; mutant ACCEPTS, exit 0 — the RWD-2026-0031/0035 false green re-opened. Applied and read: spelling-conformance fails unicode-fold-long-s and unicode-fold-capital-sharp-s on APFS, but both cases require unicodeFold and runward CI had no macOS leg until 2026-08-27, so on every runner the project used they were skipped and nothing fired. Self-gate exit 0.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001MethodExpression\u0001abs\u0001abs.slice(base.length)\u0001const parts = (base ? abs.slice(base.length).replace(new RegExp(\"^\" + sep.replace(\"\\\\\", \"\\\\\\\\\")), \"\") : abs).split(sep);"
  },
  {
   "key": "377|69|377|72|StringLiteral|\"\"",
   "line": 377,
   "mutator": "StringLiteral",
   "filing": "equivalent",
   "evidence": "Un-anchors the separator strip, which cannot matter because the string it runs on always BEGINS with the separator: base is non-null only when abs.startsWith(from + sep) held, so the slice starts at that separator and the first match of / is at index 0 either way. SENSITIVITY CONTROL: the separating input is a remainder that contains a separator without starting with one, which requires from to already end with sep — the project root being the filesystem root, resolve(dirname(missionDir)) === \"/\". Applied and read: eight probe scenarios and all 11 applicable conformance cases identical.",
   "argument": "Un-anchors the separator strip, which cannot matter because the string it runs on always BEGINS with the separator: base is non-null only when abs.startsWith(from + sep) held, so the slice starts at that separator and the first match of / is at index 0 either way. SENSITIVITY CONTROL: the separating input is a remainder that contains a separator without starting with one, which requires from to already end with sep — the project root being the filesystem root, resolve(dirname(missionDir)) === \"/\". Applied and read: eight probe scenarios and all 11 applicable conformance cases identical.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001StringLiteral\u0001\"\"\u0001\"^\"\u0001const parts = (base ? abs.slice(base.length).replace(new RegExp(\"^\" + sep.replace(\"\\\\\", \"\\\\\\\\\")), \"\") : abs).split(sep);"
  },
  {
   "key": "377|87|377|91|StringLiteral|\"\"",
   "line": 377,
   "mutator": "StringLiteral",
   "filing": "equivalent",
   "evidence": "Empties the search value, so on POSIX the regex source becomes ^\\\\/ (backslash-slash) instead of ^/ and the leading separator survives the strip. The walk absorbs it: parts[0] is then \"\", and the loop first statement if (!want) continue exists for exactly such empty segments (cur was already set to base, and the loop starts at index 0). SENSITIVITY CONTROL: separating them needs an empty leading segment to CARRY meaning, which it cannot here. Applied and read: eight probe scenarios and all 11 applicable conformance cases identical.",
   "argument": "Empties the search value, so on POSIX the regex source becomes ^\\\\/ (backslash-slash) instead of ^/ and the leading separator survives the strip. The walk absorbs it: parts[0] is then \"\", and the loop first statement if (!want) continue exists for exactly such empty segments (cur was already set to base, and the loop starts at index 0). SENSITIVITY CONTROL: separating them needs an empty leading segment to CARRY meaning, which it cannot here. Applied and read: eight probe scenarios and all 11 applicable conformance cases identical.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001StringLiteral\u0001\"\"\u0001\"\\\\\"\u0001const parts = (base ? abs.slice(base.length).replace(new RegExp(\"^\" + sep.replace(\"\\\\\", \"\\\\\\\\\")), \"\") : abs).split(sep);"
  },
  {
   "key": "377|93|377|99|StringLiteral|\"\"",
   "line": 377,
   "mutator": "StringLiteral",
   "filing": "equivalent",
   "evidence": "Empties the replacement value. On POSIX the search value does not occur in sep (\"/\"), so the call is a no-op in both versions and the regex is ^/ either way — unobservable by construction on this platform. SENSITIVITY CONTROL: on Windows the shipped build needs the doubling to escape \\ for the RegExp while the mutant yields a bare ^ whose replacement is a no-op — and even there the un-stripped leading \\ only produces an empty parts[0] that if (!want) continue absorbs. Applied and read: identical on eight probe scenarios and all 11 applicable conformance cases.",
   "argument": "Empties the replacement value. On POSIX the search value does not occur in sep (\"/\"), so the call is a no-op in both versions and the regex is ^/ either way — unobservable by construction on this platform. SENSITIVITY CONTROL: on Windows the shipped build needs the doubling to escape \\ for the RegExp while the mutant yields a bare ^ whose replacement is a no-op — and even there the un-stripped leading \\ only produces an empty parts[0] that if (!want) continue absorbs. Applied and read: identical on eight probe scenarios and all 11 applicable conformance cases.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001StringLiteral\u0001\"\"\u0001\"\\\\\\\\\"\u0001const parts = (base ? abs.slice(base.length).replace(new RegExp(\"^\" + sep.replace(\"\\\\\", \"\\\\\\\\\")), \"\") : abs).split(sep);"
  },
  {
   "key": "377|103|377|105|StringLiteral|\"Stryker was here!\"",
   "line": 377,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Splices a literal into the head of the bounded remainder. With the bound engaged the walk matches no first segment and returns null for every in-project pointer. On a case-insensitive volume spellingViaRealpath silently re-derives most of that loss (resolvePointer consults it exactly on null), which is why plain case divergence still refuses — but that rung compares with toLowerCase(), which leaves U+017F and U+1E9E untouched. MISSION: code/sguard.ts on disk, row `| rule-a | applied | file:code/ſguard.ts#guardFields |`. Shipped build refuses; mutant ACCEPTS, exit 0 — the RWD-2026-0031/0035 false green re-opened. Applied and read: spelling-conformance fails unicode-fold-long-s and unicode-fold-capital-sharp-s on APFS, but both cases require unicodeFold and runward CI had no macOS leg until 2026-08-27, so on every runner the project used they were skipped and nothing fired. Self-gate exit 0.",
   "argument": "Splices a literal into the head of the bounded remainder. With the bound engaged the walk matches no first segment and returns null for every in-project pointer. On a case-insensitive volume spellingViaRealpath silently re-derives most of that loss (resolvePointer consults it exactly on null), which is why plain case divergence still refuses — but that rung compares with toLowerCase(), which leaves U+017F and U+1E9E untouched. MISSION: code/sguard.ts on disk, row `| rule-a | applied | file:code/ſguard.ts#guardFields |`. Shipped build refuses; mutant ACCEPTS, exit 0 — the RWD-2026-0031/0035 false green re-opened. Applied and read: spelling-conformance fails unicode-fold-long-s and unicode-fold-capital-sharp-s on APFS, but both cases require unicodeFold and runward CI had no macOS leg until 2026-08-27, so on every runner the project used they were skipped and nothing fired. Self-gate exit 0.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const parts = (base ? abs.slice(base.length).replace(new RegExp(\"^\" + sep.replace(\"\\\\\", \"\\\\\\\\\")), \"\") : abs).split(sep);"
  },
  {
   "key": "382|86|382|100|ArithmeticOperator|parts[0] - sep",
   "line": 382,
   "mutator": "ArithmeticOperator",
   "filing": "equivalent",
   "evidence": "Lives in the Windows drive-letter arm, dead on the platform the pass runs on: abs comes from resolve(), so on POSIX it starts with /, parts[0] is invariantly \"\", the parts[0] === \"\" test always takes the sep branch and the drive-letter regex is never evaluated. SENSITIVITY CONTROL: the separating input is a Windows path C:\\proj\\code\\guard.ts, where parts[0] is \"C:\" and this arm decides between the drive ROOT C:\\ and the drive-RELATIVE C: the windows-latest leg filed on 2026-08-17. It cannot occur here: mutation-ratchet.yml is runs-on: ubuntu-latest for all three jobs. Verified by applying the most destructive member of the group (parts[0] - sep, i.e. cur = NaN, which would throw in readdirSync and answer UNCHECKABLE): all eight probe scenarios identical to the shipped build.",
   "argument": "Lives in the Windows drive-letter arm, dead on the platform the pass runs on: abs comes from resolve(), so on POSIX it starts with /, parts[0] is invariantly \"\", the parts[0] === \"\" test always takes the sep branch and the drive-letter regex is never evaluated. SENSITIVITY CONTROL: the separating input is a Windows path C:\\proj\\code\\guard.ts, where parts[0] is \"C:\" and this arm decides between the drive ROOT C:\\ and the drive-RELATIVE C: the windows-latest leg filed on 2026-08-17. It cannot occur here: mutation-ratchet.yml is runs-on: ubuntu-latest for all three jobs. Verified by applying the most destructive member of the group (parts[0] - sep, i.e. cur = NaN, which would throw in readdirSync and answer UNCHECKABLE): all eight probe scenarios identical to the shipped build.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001ArithmeticOperator\u0001parts[0] - sep\u0001parts[0] + sep\u0001let cur = base ? base : (parts[0] === \"\" ? sep : (/^[A-Za-z]:$/.test(parts[0]) ? parts[0] + sep : parts[0]));"
  },
  {
   "key": "382|30|382|45|ConditionalExpression|true",
   "line": 382,
   "mutator": "ConditionalExpression",
   "filing": "equivalent",
   "evidence": "Forces a condition already invariantly true on the platform the pass runs on: abs comes from resolve(), so on POSIX it starts with / and parts[0] is always \"\". SENSITIVITY CONTROL: the separating input is a Windows path C:\\proj\\code\\guard.ts, where parts[0] is \"C:\" and the mutant would pick \\ over the drive root C:\\ — the drive-RELATIVE walk the windows-latest leg filed on 2026-08-17. It cannot occur here: mutation-ratchet.yml is runs-on: ubuntu-latest for all three jobs. Applied and read: eight probe scenarios and all 11 applicable conformance cases identical.",
   "argument": "Forces a condition already invariantly true on the platform the pass runs on: abs comes from resolve(), so on POSIX it starts with / and parts[0] is always \"\". SENSITIVITY CONTROL: the separating input is a Windows path C:\\proj\\code\\guard.ts, where parts[0] is \"C:\" and the mutant would pick \\ over the drive root C:\\ — the drive-RELATIVE walk the windows-latest leg filed on 2026-08-17. It cannot occur here: mutation-ratchet.yml is runs-on: ubuntu-latest for all three jobs. Applied and read: eight probe scenarios and all 11 applicable conformance cases identical.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001ConditionalExpression\u0001true\u0001parts[0] === \"\"\u0001let cur = base ? base : (parts[0] === \"\" ? sep : (/^[A-Za-z]:$/.test(parts[0]) ? parts[0] + sep : parts[0]));"
  },
  {
   "key": "382|55|382|68|Regex|/[A-Za-z]:$/",
   "line": 382,
   "mutator": "Regex",
   "filing": "equivalent",
   "evidence": "Lives in the Windows drive-letter arm, dead on the platform the pass runs on: abs comes from resolve(), so on POSIX it starts with /, parts[0] is invariantly \"\", the parts[0] === \"\" test always takes the sep branch and the drive-letter regex is never evaluated. SENSITIVITY CONTROL: the separating input is a Windows path C:\\proj\\code\\guard.ts, where parts[0] is \"C:\" and this arm decides between the drive ROOT C:\\ and the drive-RELATIVE C: the windows-latest leg filed on 2026-08-17. It cannot occur here: mutation-ratchet.yml is runs-on: ubuntu-latest for all three jobs. Verified by applying the most destructive member of the group (parts[0] - sep, i.e. cur = NaN, which would throw in readdirSync and answer UNCHECKABLE): all eight probe scenarios identical to the shipped build.",
   "argument": "Lives in the Windows drive-letter arm, dead on the platform the pass runs on: abs comes from resolve(), so on POSIX it starts with /, parts[0] is invariantly \"\", the parts[0] === \"\" test always takes the sep branch and the drive-letter regex is never evaluated. SENSITIVITY CONTROL: the separating input is a Windows path C:\\proj\\code\\guard.ts, where parts[0] is \"C:\" and this arm decides between the drive ROOT C:\\ and the drive-RELATIVE C: the windows-latest leg filed on 2026-08-17. It cannot occur here: mutation-ratchet.yml is runs-on: ubuntu-latest for all three jobs. Verified by applying the most destructive member of the group (parts[0] - sep, i.e. cur = NaN, which would throw in readdirSync and answer UNCHECKABLE): all eight probe scenarios identical to the shipped build.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001Regex\u0001/[A-Za-z]:$/\u0001/^[A-Za-z]:$/\u0001let cur = base ? base : (parts[0] === \"\" ? sep : (/^[A-Za-z]:$/.test(parts[0]) ? parts[0] + sep : parts[0]));"
  },
  {
   "key": "382|55|382|68|Regex|/^[A-Za-z]:/",
   "line": 382,
   "mutator": "Regex",
   "filing": "equivalent",
   "evidence": "Lives in the Windows drive-letter arm, dead on the platform the pass runs on: abs comes from resolve(), so on POSIX it starts with /, parts[0] is invariantly \"\", the parts[0] === \"\" test always takes the sep branch and the drive-letter regex is never evaluated. SENSITIVITY CONTROL: the separating input is a Windows path C:\\proj\\code\\guard.ts, where parts[0] is \"C:\" and this arm decides between the drive ROOT C:\\ and the drive-RELATIVE C: the windows-latest leg filed on 2026-08-17. It cannot occur here: mutation-ratchet.yml is runs-on: ubuntu-latest for all three jobs. Verified by applying the most destructive member of the group (parts[0] - sep, i.e. cur = NaN, which would throw in readdirSync and answer UNCHECKABLE): all eight probe scenarios identical to the shipped build.",
   "argument": "Lives in the Windows drive-letter arm, dead on the platform the pass runs on: abs comes from resolve(), so on POSIX it starts with /, parts[0] is invariantly \"\", the parts[0] === \"\" test always takes the sep branch and the drive-letter regex is never evaluated. SENSITIVITY CONTROL: the separating input is a Windows path C:\\proj\\code\\guard.ts, where parts[0] is \"C:\" and this arm decides between the drive ROOT C:\\ and the drive-RELATIVE C: the windows-latest leg filed on 2026-08-17. It cannot occur here: mutation-ratchet.yml is runs-on: ubuntu-latest for all three jobs. Verified by applying the most destructive member of the group (parts[0] - sep, i.e. cur = NaN, which would throw in readdirSync and answer UNCHECKABLE): all eight probe scenarios identical to the shipped build.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001Regex\u0001/^[A-Za-z]:/\u0001/^[A-Za-z]:$/\u0001let cur = base ? base : (parts[0] === \"\" ? sep : (/^[A-Za-z]:$/.test(parts[0]) ? parts[0] + sep : parts[0]));"
  },
  {
   "key": "382|55|382|68|Regex|/^[^A-Za-z]:$/",
   "line": 382,
   "mutator": "Regex",
   "filing": "equivalent",
   "evidence": "Lives in the Windows drive-letter arm, dead on the platform the pass runs on: abs comes from resolve(), so on POSIX it starts with /, parts[0] is invariantly \"\", the parts[0] === \"\" test always takes the sep branch and the drive-letter regex is never evaluated. SENSITIVITY CONTROL: the separating input is a Windows path C:\\proj\\code\\guard.ts, where parts[0] is \"C:\" and this arm decides between the drive ROOT C:\\ and the drive-RELATIVE C: the windows-latest leg filed on 2026-08-17. It cannot occur here: mutation-ratchet.yml is runs-on: ubuntu-latest for all three jobs. Verified by applying the most destructive member of the group (parts[0] - sep, i.e. cur = NaN, which would throw in readdirSync and answer UNCHECKABLE): all eight probe scenarios identical to the shipped build.",
   "argument": "Lives in the Windows drive-letter arm, dead on the platform the pass runs on: abs comes from resolve(), so on POSIX it starts with /, parts[0] is invariantly \"\", the parts[0] === \"\" test always takes the sep branch and the drive-letter regex is never evaluated. SENSITIVITY CONTROL: the separating input is a Windows path C:\\proj\\code\\guard.ts, where parts[0] is \"C:\" and this arm decides between the drive ROOT C:\\ and the drive-RELATIVE C: the windows-latest leg filed on 2026-08-17. It cannot occur here: mutation-ratchet.yml is runs-on: ubuntu-latest for all three jobs. Verified by applying the most destructive member of the group (parts[0] - sep, i.e. cur = NaN, which would throw in readdirSync and answer UNCHECKABLE): all eight probe scenarios identical to the shipped build.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001Regex\u0001/^[^A-Za-z]:$/\u0001/^[A-Za-z]:$/\u0001let cur = base ? base : (parts[0] === \"\" ? sep : (/^[A-Za-z]:$/.test(parts[0]) ? parts[0] + sep : parts[0]));"
  },
  {
   "key": "388|32|388|48|EqualityOperator|i <= parts.length",
   "line": 388,
   "mutator": "EqualityOperator",
   "filing": "equivalent",
   "evidence": "Adds one iteration past the end. parts[parts.length] is undefined and the body first statement is if (!want) continue, so the extra pass does nothing and the loop exits. SENSITIVITY CONTROL: the nearby variant that WOULD separate them is the same loop without that guard — entries.includes(undefined) is false and caseFold(undefined) throws on .normalize. The guard is not incidental: it also absorbs the empty leading segment of a POSIX absolute path and any // in a written pointer, so no refactor that keeps this loop meaningful removes it. Applied and read: eight probe scenarios and all 11 applicable conformance cases identical.",
   "argument": "Adds one iteration past the end. parts[parts.length] is undefined and the body first statement is if (!want) continue, so the extra pass does nothing and the loop exits. SENSITIVITY CONTROL: the nearby variant that WOULD separate them is the same loop without that guard — entries.includes(undefined) is false and caseFold(undefined) throws on .normalize. The guard is not incidental: it also absorbs the empty leading segment of a POSIX absolute path and any // in a written pointer, so no refactor that keeps this loop meaningful removes it. Applied and read: eight probe scenarios and all 11 applicable conformance cases identical.",
   "stableKey": "evidence\u0001onDiskSpelling\u0001EqualityOperator\u0001i <= parts.length\u0001i < parts.length\u0001for (let i = base ? 0 : 1; i < parts.length; i++) {"
  }
 ]
}
