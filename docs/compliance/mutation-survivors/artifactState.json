{
 "function": "artifactState",
 "probeMissions": [
  "consolidated-2026-09-04"
 ],
 "verdicts": [
  {
   "key": "139|13|139|35|ConditionalExpression|false",
   "line": 139,
   "mutator": "ConditionalExpression",
   "filing": "equivalent",
   "evidence": "Le retour anticip\u00e9 'untouched' sur liste vide devient inatteignable, mais la chute donne le m\u00eame r\u00e9sultat : [].some(...) est false par d\u00e9finition du langage, donc hasFilled=false et le m\u00eame litt\u00e9ral 'untouched' est retourn\u00e9 ; pour toute liste non vide la condition \u00e9tait d\u00e9j\u00e0 fausse. Aucun effet de bord r\u00e9ordonn\u00e9 (la lecture du template pr\u00e9c\u00e8de le test dans les deux formes). CONTR\u00d4LE DE SENSIBILIT\u00c9 : une divergence exigerait que [].some retourne vrai (impossible, s\u00e9mantique ECMAScript) ou que les deux litt\u00e9raux 'untouched' diff\u00e8rent (m\u00eame cha\u00eene). MESUR\u00c9 sur les trois r\u00e9gimes du r\u00e9pertoire : fxc (z\u00e9ro .md \u2014 le chemin m\u00eame que le mutant court-circuite), scaf (template seul), ex (contrats remplis) \u2014 \u00e9tats identiques untouched/untouched/filled sur les deux builds ; batterie byte-identique. \u00c9quivalent au sens fort.",
   "argument": "Le retour anticip\u00e9 'untouched' sur liste vide devient inatteignable, mais la chute donne le m\u00eame r\u00e9sultat : [].some(...) est false par d\u00e9finition du langage, donc hasFilled=false et le m\u00eame litt\u00e9ral 'untouched' est retourn\u00e9 ; pour toute liste non vide la condition \u00e9tait d\u00e9j\u00e0 fausse. Aucun effet de bord r\u00e9ordonn\u00e9 (la lecture du template pr\u00e9c\u00e8de le test dans les deux formes). CONTR\u00d4LE DE SENSIBILIT\u00c9 : une divergence exigerait que [].some retourne vrai (impossible, s\u00e9mantique ECMAScript) ou que les deux litt\u00e9raux 'untouched' diff\u00e8rent (m\u00eame cha\u00eene). MESUR\u00c9 sur les trois r\u00e9gimes du r\u00e9pertoire : fxc (z\u00e9ro .md \u2014 le chemin m\u00eame que le mutant court-circuite), scaf (template seul), ex (contrats remplis) \u2014 \u00e9tats identiques untouched/untouched/filled sur les deux builds ; batterie byte-identique. \u00c9quivalent au sens fort.",
   "stableKey": "mission\u0001artifactState\u0001ConditionalExpression\u0001false\u0001contracts.length === 0\u0001if (contracts.length === 0)"
  },
  {
   "key": "149|44|149|46|ArrayDeclaration|[\"Stryker was here\"]",
   "line": 149,
   "mutator": "ArrayDeclaration",
   "filing": "equivalent",
   "evidence": "Occurrence 1 : le plancher placeholders de la branche templateKey. Le repli ne joue que si match est null (z\u00e9ro placeholder) ; .length passe de 0 \u00e0 1, tous deux < 3, la garde de divergence prend la main identiquement. CONTR\u00d4LE DE SENSIBILIT\u00c9 : divergence seulement si le seuil tombait dans (0,1] ou si le contenu du tableau \u00e9tait lu \u2014 .length est la seule consultation et 3 est le plancher partag\u00e9 check/pack (RWD-2026-0061). MESUR\u00c9 sur le chemin match-null de cette branche : b-x (decision-matrix, 0 placeholder, template+1 octet, templateKey) -> in-progress/below-floor identique des deux c\u00f4t\u00e9s ; fxws idem ; batterie compl\u00e8te byte-identique \u00e0 la baseline.",
   "argument": "Occurrence 1 : le plancher placeholders de la branche templateKey. Le repli ne joue que si match est null (z\u00e9ro placeholder) ; .length passe de 0 \u00e0 1, tous deux < 3, la garde de divergence prend la main identiquement. CONTR\u00d4LE DE SENSIBILIT\u00c9 : divergence seulement si le seuil tombait dans (0,1] ou si le contenu du tableau \u00e9tait lu \u2014 .length est la seule consultation et 3 est le plancher partag\u00e9 check/pack (RWD-2026-0061). MESUR\u00c9 sur le chemin match-null de cette branche : b-x (decision-matrix, 0 placeholder, template+1 octet, templateKey) -> in-progress/below-floor identique des deux c\u00f4t\u00e9s ; fxws idem ; batterie compl\u00e8te byte-identique \u00e0 la baseline.",
   "stableKey": "mission\u0001artifactState\u0001ArrayDeclaration\u0001[\"Stryker was here\"]\u0001[]\u0001if ((content.match(PLACEHOLDER) || []).length >= 3)"
  },
  {
   "key": "156|30|156|92|MethodExpression|s.split(\"\\n\").map(l => l.trim())",
   "line": 156,
   "mutator": "MethodExpression",
   "filing": "equivalent",
   "evidence": "Supprimer le filtre garde les lignes vides (apr\u00e8s trim) dans lines(). C\u00f4t\u00e9 template : '' entre dans templateLines. C\u00f4t\u00e9 contenu : chaque ligne vide ajout\u00e9e est alors absorb\u00e9e par templateLines.has('') \u2014 donc added est identique \u2014 \u00c0 CONDITION que le template contienne au moins une ligne vide. MESUR\u00c9 : les 15 templates de mission livr\u00e9s ont tous \u22656 lignes vides (min decision-matrix=6, max reference-stack=32) ; lines() n'est appel\u00e9e que sur ces templates et le contenu d'artefacts templateKey, jamais ailleurs. Une ligne vide p\u00e8se par ailleurs 0 mot (addedWords inchang\u00e9). CONTR\u00d4LE DE SENSIBILIT\u00c9 : l'entr\u00e9e voisine divergente est un template SANS aucune ligne vide face \u00e0 un contenu qui en ajoute (added.length gonflerait) \u2014 impossible avec l'artefact livr\u00e9, TEMPLATES pointe dans le paquet install\u00e9. Contr\u00f4le mesur\u00e9 : b-blank2l (2 vraies lignes + 3 lignes vides intercal\u00e9es) -> in-progress/below-floor identique des deux c\u00f4t\u00e9s ; batterie byte-identique.",
   "argument": "Supprimer le filtre garde les lignes vides (apr\u00e8s trim) dans lines(). C\u00f4t\u00e9 template : '' entre dans templateLines. C\u00f4t\u00e9 contenu : chaque ligne vide ajout\u00e9e est alors absorb\u00e9e par templateLines.has('') \u2014 donc added est identique \u2014 \u00c0 CONDITION que le template contienne au moins une ligne vide. MESUR\u00c9 : les 15 templates de mission livr\u00e9s ont tous \u22656 lignes vides (min decision-matrix=6, max reference-stack=32) ; lines() n'est appel\u00e9e que sur ces templates et le contenu d'artefacts templateKey, jamais ailleurs. Une ligne vide p\u00e8se par ailleurs 0 mot (addedWords inchang\u00e9). CONTR\u00d4LE DE SENSIBILIT\u00c9 : l'entr\u00e9e voisine divergente est un template SANS aucune ligne vide face \u00e0 un contenu qui en ajoute (added.length gonflerait) \u2014 impossible avec l'artefact livr\u00e9, TEMPLATES pointe dans le paquet install\u00e9. Contr\u00f4le mesur\u00e9 : b-blank2l (2 vraies lignes + 3 lignes vides intercal\u00e9es) -> in-progress/below-floor identique des deux c\u00f4t\u00e9s ; batterie byte-identique.",
   "stableKey": "mission\u0001artifactState\u0001MethodExpression\u0001s.split(\"\\n\").map(l => l.trim())\u0001s.split(\"\\n\").map((l) => l.trim()).filter((l) => l.length > 0)\u0001const lines = (s) => s.split(\"\\n\").map((l) => l.trim()).filter((l) => l.length > 0);"
  },
  {
   "key": "156|55|156|63|MethodExpression|l",
   "line": 156,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "Sans trim, la comparaison contenu/template se fait sur lignes BRUTES : une ligne qui n'a chang\u00e9 que d'espaces compte comme 'nouvelle'. RECETTE : mission exemple verte ; remplacer runward/decision-matrix.md par le template auquel on ajoute UN ESPACE FINAL sur 3 lignes riches en mots (fixture fxws \u2014 z\u00e9ro contenu nouveau, 175 mots 'ajout\u00e9s' au sens du mutant). MESUR\u00c9 avant/apr\u00e8s : livr\u00e9 \u2014 '\u25d0 Decision matrix \u2014 started, but too close to the template to count as filled', '1 deliverable(s) not filled', EXIT 1 ; mut\u00e9 \u2014 '\u2713 Decision matrix', 'Current gate all gates passed', 'All expected deliverables are filled', EXIT 0. DOUBLE SURFACE sur la m\u00eame mission : compliance iso-42001 -> OSCAL runward-gate-verdict 'gaps (exit 1)' -> 'clean (exit 0)' sur les 10 composants. C'est exactement le faux vert que la garde de divergence existe pour emp\u00eacher (un diff blanc passe la porte), et le mutant est unilat\u00e9ral : il ne peut que promouvoir des \u00e9tats, jamais en r\u00e9trograder. Rien ne le voit, V\u00c9RIFI\u00c9 en rejouant les 7 jambes : toutes PASS \u2014 chaque fixture du filet diverge par du vrai contenu (les vertes restent vertes, les rouges le sont pour d'autres causes), le cas smoke.js:451 (\u00e9dition d'un octet) ajoute une ligne 'x' vue identiquement par les deux formes ; aucune fixture ne diverge par blancs seuls.",
   "argument": "Sans trim, la comparaison contenu/template se fait sur lignes BRUTES : une ligne qui n'a chang\u00e9 que d'espaces compte comme 'nouvelle'. RECETTE : mission exemple verte ; remplacer runward/decision-matrix.md par le template auquel on ajoute UN ESPACE FINAL sur 3 lignes riches en mots (fixture fxws \u2014 z\u00e9ro contenu nouveau, 175 mots 'ajout\u00e9s' au sens du mutant). MESUR\u00c9 avant/apr\u00e8s : livr\u00e9 \u2014 '\u25d0 Decision matrix \u2014 started, but too close to the template to count as filled', '1 deliverable(s) not filled', EXIT 1 ; mut\u00e9 \u2014 '\u2713 Decision matrix', 'Current gate all gates passed', 'All expected deliverables are filled', EXIT 0. DOUBLE SURFACE sur la m\u00eame mission : compliance iso-42001 -> OSCAL runward-gate-verdict 'gaps (exit 1)' -> 'clean (exit 0)' sur les 10 composants. C'est exactement le faux vert que la garde de divergence existe pour emp\u00eacher (un diff blanc passe la porte), et le mutant est unilat\u00e9ral : il ne peut que promouvoir des \u00e9tats, jamais en r\u00e9trograder. Rien ne le voit, V\u00c9RIFI\u00c9 en rejouant les 7 jambes : toutes PASS \u2014 chaque fixture du filet diverge par du vrai contenu (les vertes restent vertes, les rouges le sont pour d'autres causes), le cas smoke.js:451 (\u00e9dition d'un octet) ajoute une ligne 'x' vue identiquement par les deux formes ; aucune fixture ne diverge par blancs seuls.",
   "stableKey": "mission\u0001artifactState\u0001MethodExpression\u0001l\u0001l.trim()\u0001const lines = (s) => s.split(\"\\n\").map((l) => l.trim()).filter((l) => l.length > 0);"
  },
  {
   "key": "156|79|156|91|ConditionalExpression|true",
   "line": 156,
   "mutator": "ConditionalExpression",
   "filing": "equivalent",
   "evidence": "filter(() => true) est s\u00e9mantiquement le filtre supprim\u00e9 : m\u00eames lignes vides (trim\u00e9es) conserv\u00e9es que pour le mutant MethodExpression de la m\u00eame ligne. M\u00eame argument, m\u00eames mesures : '' est absorb\u00e9e par templateLines car les 15 templates livr\u00e9s portent tous \u22656 lignes vides (mesur\u00e9), et une ligne vide ajoute 0 mot. CONTR\u00d4LE DE SENSIBILIT\u00c9 : divergence seulement face \u00e0 un template sans aucune ligne vide \u2014 inexistant dans le paquet. Contr\u00f4le mesur\u00e9 b-blank2l identique (in-progress/below-floor des deux c\u00f4t\u00e9s), boundary b-3l20w/b-3l19w/b-2l25w identiques (filled/in-progress/in-progress), batterie byte-identique.",
   "argument": "filter(() => true) est s\u00e9mantiquement le filtre supprim\u00e9 : m\u00eames lignes vides (trim\u00e9es) conserv\u00e9es que pour le mutant MethodExpression de la m\u00eame ligne. M\u00eame argument, m\u00eames mesures : '' est absorb\u00e9e par templateLines car les 15 templates livr\u00e9s portent tous \u22656 lignes vides (mesur\u00e9), et une ligne vide ajoute 0 mot. CONTR\u00d4LE DE SENSIBILIT\u00c9 : divergence seulement face \u00e0 un template sans aucune ligne vide \u2014 inexistant dans le paquet. Contr\u00f4le mesur\u00e9 b-blank2l identique (in-progress/below-floor des deux c\u00f4t\u00e9s), boundary b-3l20w/b-3l19w/b-2l25w identiques (filled/in-progress/in-progress), batterie byte-identique.",
   "stableKey": "mission\u0001artifactState\u0001ConditionalExpression\u0001true\u0001l.length > 0\u0001const lines = (s) => s.split(\"\\n\").map((l) => l.trim()).filter((l) => l.length > 0);"
  },
  {
   "key": "156|79|156|91|EqualityOperator|l.length >= 0",
   "line": 156,
   "mutator": "EqualityOperator",
   "filing": "equivalent",
   "evidence": "length >= 0 est une tautologie sur toute cha\u00eene : pr\u00e9dicat toujours vrai, donc troisi\u00e8me forme du m\u00eame mutant 'filtre inerte' que les deux pr\u00e9c\u00e9dents sur cette ligne. M\u00eame absorption de '' par templateLines (les 15 templates livr\u00e9s ont \u22656 lignes vides, mesur\u00e9), m\u00eame poids nul en mots. CONTR\u00d4LE DE SENSIBILIT\u00c9 : l'entr\u00e9e qui s\u00e9parerait > 0 de >= 0 est une ligne de longueur exactement 0 \u2014 c'est pr\u00e9cis\u00e9ment la ligne vide absorb\u00e9e ; il faudrait un template sans ligne vide pour l'observer, inexistant dans l'artefact livr\u00e9. Contr\u00f4les mesur\u00e9s identiques (b-blank2l, boundary, batterie byte-identique \u00e0 la baseline).",
   "argument": "length >= 0 est une tautologie sur toute cha\u00eene : pr\u00e9dicat toujours vrai, donc troisi\u00e8me forme du m\u00eame mutant 'filtre inerte' que les deux pr\u00e9c\u00e9dents sur cette ligne. M\u00eame absorption de '' par templateLines (les 15 templates livr\u00e9s ont \u22656 lignes vides, mesur\u00e9), m\u00eame poids nul en mots. CONTR\u00d4LE DE SENSIBILIT\u00c9 : l'entr\u00e9e qui s\u00e9parerait > 0 de >= 0 est une ligne de longueur exactement 0 \u2014 c'est pr\u00e9cis\u00e9ment la ligne vide absorb\u00e9e ; il faudrait un template sans ligne vide pour l'observer, inexistant dans l'artefact livr\u00e9. Contr\u00f4les mesur\u00e9s identiques (b-blank2l, boundary, batterie byte-identique \u00e0 la baseline).",
   "stableKey": "mission\u0001artifactState\u0001EqualityOperator\u0001l.length >= 0\u0001l.length > 0\u0001const lines = (s) => s.split(\"\\n\").map((l) => l.trim()).filter((l) => l.length > 0);"
  },
  {
   "key": "159|55|159|85|MethodExpression|l.split(/\\s+/)",
   "line": 159,
   "mutator": "MethodExpression",
   "filing": "equivalent",
   "evidence": "Les \u00e9l\u00e9ments de added sortent de lines(), donc sont trim\u00e9s et non vides PAR CONSTRUCTION (le mutant s'applique seul ; la ligne lines() garde son trim et son filtre). Or split(/\\s+/) sur une cha\u00eene qui ne commence ni ne finit par un blanc ne produit aucun jeton vide (ECMAScript : un jeton vide n'appara\u00eet qu'en bord de s\u00e9parateur) \u2014 filter(Boolean) est redondant sur ce domaine. CONTR\u00d4LE DE SENSIBILIT\u00c9 : l'entr\u00e9e voisine divergente est une ligne de added avec blanc de t\u00eate ou de queue, ou vide \u2014 impossible, lines() trim puis filtre juste au-dessus ; il faudrait le mutant no-trim (instruit s\u00e9par\u00e9ment comme hole) appliqu\u00e9 EN M\u00caME TEMPS, or les mutants s'appliquent un \u00e0 un. MESUR\u00c9 : boundary sensibles au compte de mots exacts b-3l20w (20 mots -> filled), b-3l19w (19 -> in-progress), b-2l25w identiques des deux c\u00f4t\u00e9s ; batterie byte-identique.",
   "argument": "Les \u00e9l\u00e9ments de added sortent de lines(), donc sont trim\u00e9s et non vides PAR CONSTRUCTION (le mutant s'applique seul ; la ligne lines() garde son trim et son filtre). Or split(/\\s+/) sur une cha\u00eene qui ne commence ni ne finit par un blanc ne produit aucun jeton vide (ECMAScript : un jeton vide n'appara\u00eet qu'en bord de s\u00e9parateur) \u2014 filter(Boolean) est redondant sur ce domaine. CONTR\u00d4LE DE SENSIBILIT\u00c9 : l'entr\u00e9e voisine divergente est une ligne de added avec blanc de t\u00eate ou de queue, ou vide \u2014 impossible, lines() trim puis filtre juste au-dessus ; il faudrait le mutant no-trim (instruit s\u00e9par\u00e9ment comme hole) appliqu\u00e9 EN M\u00caME TEMPS, or les mutants s'appliquent un \u00e0 un. MESUR\u00c9 : boundary sensibles au compte de mots exacts b-3l20w (20 mots -> filled), b-3l19w (19 -> in-progress), b-2l25w identiques des deux c\u00f4t\u00e9s ; batterie byte-identique.",
   "stableKey": "mission\u0001artifactState\u0001MethodExpression\u0001l.split(/\\s+/)\u0001l.split(/\\s+/).filter(Boolean)\u0001const addedWords = added.reduce((n, l) => n + l.split(/\\s+/).filter(Boolean).length, 0);"
  },
  {
   "key": "159|63|159|68|Regex|/\\s/",
   "line": 159,
   "mutator": "Regex",
   "filing": "equivalent",
   "evidence": "/\\s/ au lieu de /\\s+/ ne diff\u00e8re que sur les blancs CONS\u00c9CUTIFS : chaque blanc suppl\u00e9mentaire produit un jeton vide de plus \u2014 que le filter(Boolean), conserv\u00e9 par ce mutant, \u00e9limine. Le multiset des jetons non vides est identique, donc addedWords aussi. CONTR\u00d4LE DE SENSIBILIT\u00c9 mesur\u00e9 expr\u00e8s : b-dbl.md (template decision-matrix + 3 lignes de 21 mots s\u00e9par\u00e9s par DOUBLES espaces, pile le terrain o\u00f9 les deux regex divergent en jetons bruts) -> filled/null identique sur build livr\u00e9 et build mut\u00e9. La divergence exigerait l'absence du filter(Boolean) \u2014 c'est l'autre mutant de la m\u00eame ligne, appliqu\u00e9 s\u00e9par\u00e9ment ; chacun des deux est le garde-fou de l'autre. Batterie et boundary (b-3l20w/b-3l19w/b-2l25w) byte-identiques.",
   "argument": "/\\s/ au lieu de /\\s+/ ne diff\u00e8re que sur les blancs CONS\u00c9CUTIFS : chaque blanc suppl\u00e9mentaire produit un jeton vide de plus \u2014 que le filter(Boolean), conserv\u00e9 par ce mutant, \u00e9limine. Le multiset des jetons non vides est identique, donc addedWords aussi. CONTR\u00d4LE DE SENSIBILIT\u00c9 mesur\u00e9 expr\u00e8s : b-dbl.md (template decision-matrix + 3 lignes de 21 mots s\u00e9par\u00e9s par DOUBLES espaces, pile le terrain o\u00f9 les deux regex divergent en jetons bruts) -> filled/null identique sur build livr\u00e9 et build mut\u00e9. La divergence exigerait l'absence du filter(Boolean) \u2014 c'est l'autre mutant de la m\u00eame ligne, appliqu\u00e9 s\u00e9par\u00e9ment ; chacun des deux est le garde-fou de l'autre. Batterie et boundary (b-3l20w/b-3l19w/b-2l25w) byte-identiques.",
   "stableKey": "mission\u0001artifactState\u0001Regex\u0001/\\s/\u0001/\\s+/\u0001const addedWords = added.reduce((n, l) => n + l.split(/\\s+/).filter(Boolean).length, 0);"
  },
  {
   "key": "164|40|164|42|ArrayDeclaration|[\"Stryker was here\"]",
   "line": 164,
   "mutator": "ArrayDeclaration",
   "filing": "equivalent",
   "evidence": "Occurrence 2 : le test placeholders du chemin SANS templateKey \u2014 chemin vivant, c'est celui que le pack compliance emprunte (govState passe {label, relPath} sans templateKey ; adr/ et contracts/ retournent avant). M\u00eame structure que les deux autres repli-Stryker : le tableau ne sert que si match est null (z\u00e9ro placeholder), .length passe de 0 \u00e0 1, tous deux < 3, et le retour est 'filled' dans les deux cas. CONTR\u00d4LE DE SENSIBILIT\u00c9 : divergence seulement si le seuil \u00e9tait \u22641 ou si le contenu du tableau \u00e9tait consult\u00e9 \u2014 ni l'un ni l'autre. MESUR\u00c9 sur le chemin exact : b-noph.md (prose sans aucun placeholder, lu sans templateKey \u2014 la forme d'appel de compliance) -> filled/null identique ; ex threat-model sans templateKey -> filled identique ; batterie byte-identique.",
   "argument": "Occurrence 2 : le test placeholders du chemin SANS templateKey \u2014 chemin vivant, c'est celui que le pack compliance emprunte (govState passe {label, relPath} sans templateKey ; adr/ et contracts/ retournent avant). M\u00eame structure que les deux autres repli-Stryker : le tableau ne sert que si match est null (z\u00e9ro placeholder), .length passe de 0 \u00e0 1, tous deux < 3, et le retour est 'filled' dans les deux cas. CONTR\u00d4LE DE SENSIBILIT\u00c9 : divergence seulement si le seuil \u00e9tait \u22641 ou si le contenu du tableau \u00e9tait consult\u00e9 \u2014 ni l'un ni l'autre. MESUR\u00c9 sur le chemin exact : b-noph.md (prose sans aucun placeholder, lu sans templateKey \u2014 la forme d'appel de compliance) -> filled/null identique ; ex threat-model sans templateKey -> filled identique ; batterie byte-identique.",
   "stableKey": "mission\u0001artifactState\u0001ArrayDeclaration\u0001[\"Stryker was here\"]\u0001[]\u0001if ((content.match(PLACEHOLDER) || []).length >= 3)\u0001#2"
  },
  {
   "key": "690|27|690|84|MethodExpression|readdirSync(path).filter(f => f.endsWith(\".md\"))",
   "line": 690,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR \u2014 filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference \u2014 payload or rendered text \u2014 on any of the three probe missions. Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions \u2014 `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR \u2014 filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference \u2014 payload or rendered text \u2014 on any of the three probe missions. Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions \u2014 `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001artifactState\u0001MethodExpression\u0001readdirSync(path).filter(f => f.endsWith(\".md\"))\u0001readdirSync(path).filter((f) => f.endsWith(\".md\")).sort()\u0001const contracts = readdirSync(path).filter((f) => f.endsWith(\".md\")).sort();"
  },
  {
   "key": "690|27|690|77|MethodExpression|readdirSync(path)",
   "line": 690,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR \u2014 filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference \u2014 payload or rendered text \u2014 on any of the three probe missions. Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions \u2014 `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR \u2014 filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference \u2014 payload or rendered text \u2014 on any of the three probe missions. Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions \u2014 `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001artifactState\u0001MethodExpression\u0001readdirSync(path)\u0001readdirSync(path).filter((f) => f.endsWith(\".md\"))\u0001const contracts = readdirSync(path).filter((f) => f.endsWith(\".md\")).sort();"
  },
  {
   "key": "690|70|690|75|StringLiteral|\"\"",
   "line": 690,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR \u2014 filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference \u2014 payload or rendered text \u2014 on any of the three probe missions. Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions \u2014 `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR \u2014 filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference \u2014 payload or rendered text \u2014 on any of the three probe missions. Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions \u2014 `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001artifactState\u0001StringLiteral\u0001\"\"\u0001\".md\"\u0001const contracts = readdirSync(path).filter((f) => f.endsWith(\".md\")).sort();"
  },
  {
   "key": "703|39|703|126|LogicalOperator|armed || contracts.some(f => readFileSync(join(path, f), \"utf8\").trim() !== template)",
   "line": 703,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR \u2014 filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference \u2014 payload or rendered text \u2014 on any of the three probe missions. Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions \u2014 `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR \u2014 filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference \u2014 payload or rendered text \u2014 on any of the three probe missions. Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions \u2014 `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001artifactState\u0001LogicalOperator\u0001armed || contracts.some(f => readFileSync(join(path, f), \"utf8\").trim() !== template)\u0001armed && contracts.some((f) => readFileSync(join(path, f), \"utf8\").trim() !== template)\u0001return hasFilled ? \"filled\" : armed && contracts.some((f) => readFileSync(join(path, f), \"utf8\").trim() !== template) ? \"in-progress\" : \"untouched\";"
  },
  {
   "key": "703|48|703|126|MethodExpression|contracts.every(f => readFileSync(join(path, f), \"utf8\").trim() !== template)",
   "line": 703,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR \u2014 filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference \u2014 payload or rendered text \u2014 on any of the three probe missions. Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions \u2014 `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR \u2014 filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference \u2014 payload or rendered text \u2014 on any of the three probe missions. Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions \u2014 `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001artifactState\u0001MethodExpression\u0001contracts.every(f => readFileSync(join(path, f), \"utf8\").trim() !== template)\u0001contracts.some((f) => readFileSync(join(path, f), \"utf8\").trim() !== template)\u0001return hasFilled ? \"filled\" : armed && contracts.some((f) => readFileSync(join(path, f), \"utf8\").trim() !== template) ? \"in-progress\" : \"untouched\";"
  },
  {
   "key": "703|70|703|125|ConditionalExpression|true",
   "line": 703,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR \u2014 filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference \u2014 payload or rendered text \u2014 on any of the three probe missions. Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions \u2014 `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR \u2014 filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference \u2014 payload or rendered text \u2014 on any of the three probe missions. Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions \u2014 `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001artifactState\u0001ConditionalExpression\u0001true\u0001readFileSync(join(path, f), \"utf8\").trim() !== template\u0001return hasFilled ? \"filled\" : armed && contracts.some((f) => readFileSync(join(path, f), \"utf8\").trim() !== template) ? \"in-progress\" : \"untouched\";"
  },
  {
   "key": "703|70|703|112|MethodExpression|readFileSync(join(path, f), \"utf8\")",
   "line": 703,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR \u2014 filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference \u2014 payload or rendered text \u2014 on any of the three probe missions. Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions \u2014 `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR \u2014 filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference \u2014 payload or rendered text \u2014 on any of the three probe missions. Residual variant beyond the pinned family tests; no probe mission or net leg reaches the mutated branch with distinguishing data. Probe suite (2026-09-04, committed recipe): three `init --example` missions \u2014 `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "mission\u0001artifactState\u0001MethodExpression\u0001readFileSync(join(path, f), \"utf8\")\u0001readFileSync(join(path, f), \"utf8\").trim()\u0001return hasFilled ? \"filled\" : armed && contracts.some((f) => readFileSync(join(path, f), \"utf8\").trim() !== template) ? \"in-progress\" : \"untouched\";"
  }
 ]
}
