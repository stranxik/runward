{
 "function": "textOutsideManifest",
 "probeMissions": [
  "BASE, common to all of them: `node dist/cli.js init --yes --example --path <dir>` on runward 0.36.0, which is green under `check --strict` (exit 0). Every mission below is that tree with edits to runward/floor.md only, so the baseline difference is exactly the trap. Runner: `node scripts/mutation-probe-strict.mjs --function textOutsideManifest --mission <dir> --ledger reports/mutation/probe-mission-<X>.jsonl`.",
  "tom-A (kills 367|42|367|71|Regex|/#{1,6}\\s+Rule conformance/i). Insert one line of prose immediately above the `## Rule conformance` heading: `Each rule is accounted for in the \"## Rule conformance\" table below.` — the heading text appears inline, not at column 0. Replace the `state-event-sourcing` row's evidence with `file:runward/floor.md#state-event-sourcing`, a pointer into the manifest at the row's own slug. Baseline exit 1, one conformance violation.",
  "tom-B (kills 367|42|367|71|Regex|/^#{1,6}\\sRule conformance/i). Write the heading with two spaces: `##  Rule conformance`. `readManifest` still parses every row (`\\s+`). Same circular row as tom-A. Baseline exit 1, one violation.",
  "tom-C (kills 372|13|372|24|Regex|/#{1,6}\\s/). Insert between the heading and the table: `> Status vocabulary: applied / deviated / n/a — rationale in ADR # 2.` The `# 2` is a `#` followed by whitespace, but not at column 0. Same circular row. Baseline exit 1, one violation.",
  "tom-D (kills 372|13|372|24|Regex|/^#{1,6}\\S/) — the only mission whose baseline is GREEN. Demote the section that follows the table from `## 2. Proof against the success criterion` to `# 2. …` (level 1) and add under it `The interaction log is append-only, keyed by request ID, and never mutated in place.`. Point the `state-event-sourcing` row at that sentence: `file:runward/floor.md#\"The interaction log is append-only, keyed by request ID\"`. Baseline exit 0; the mutant makes an honest mission red.",
  "tom-E (kills 376|65|376|69|StringLiteral|\"\"). Add, below the table, a hyphenated compound wrapped over two lines: `The persistence adapter follows the reference stack's state-event` / `-sourcing conventions end to end.` Same circular row (`#state-event-sourcing`). Baseline exit 1; joining the lines with nothing manufactures the symbol.",
  "tom-F — the control board, and it must stay unchanged under every mutant (it does: 0/11 observable). Five pointer shapes at once: `state-event-sourcing` → another manifest (`file:runward/architecture.md#\"Rule conformance\"`, accepted); `hexa-architecture` → the rule corpus (`file:runward/rules/hexa-architecture.md`, refused as `the rule's own text`); `hexa-adapter-pattern` → a line inside a fenced code block of the manifest itself (`file:runward/floor.md#\"npm run triage -- --replay 2026-05\"`, accepted); `provider-no-crash-missing-env` → ordinary source, untouched (accepted); `tools-scope-atomicity` → its own slug in the manifest (refused). Baseline exit 1, exactly two violations, 4965 bytes.",
  "tom-G — the fence probe. Put a fenced illustration of the format above the real table (```markdown / `## Rule conformance` / a header row / ```), keep the real `## Rule conformance` below it, and use the circular row. NOT a mutant mission: it is GREEN at baseline (exit 0) with the pristine build. See `sideFindings`.",
  "tom-H — the catch probe. tom-A's tree with `chmod 000 runward/floor.md`. Pristine and mutated alike: 0 bytes of stdout, exit 1, stderr `✗ EACCES: permission denied, open '/tmp/tom-H/runward/floor.md'`. The unreadable-deliverable case is stopped upstream of this function.",
  "tom-I — a second, sharper witness for line 376 and a stress case for the `start === -1` branch. Two-space heading (`##  Rule conformance`) plus two adjacent prose lines `The keyword classifier ships in two tiers: alpha` / `beta hardening lands in the next release.`, and the row cites `file:runward/floor.md#alphabeta` — a symbol that appears NOWHERE in the document. Baseline exit 1; mutants 5 and 11 each flip it to exit 0.",
  "All missions start from init --yes --example into an empty directory (green under check --strict, exit 0) and edit runward/architecture.md only. The Evidence cell of its hexa-architecture row is replaced by a self-citation file:architecture.md#SYMBOL, which is what makes circularEvidence call textOutsideManifest. Every pristine verdict was verified BEFORE probing, and in every red mission the conformance gap count is exactly 1 — this function's message and nothing else.",
  "m1-circular: SYMBOL = hexa-architecture, no other edit. The slug exists only in column 1 of the table. Pristine exit 1, conformance 1. The control for 'the universal green key is refused'. 0 of 17 observable here: a plain document does not exercise the fence or heading edges.",
  "m2-fence-in-section: m1 plus, between the heading and its table, a fence indented by TWO spaces whose body is a '## A heading that lives inside the fence' line and a pointer. readManifest fence-skips it, so all six rows are still read. Pristine exit 1, conformance 1.",
  "m3-fence-in-section-col0: identical to m2 but the fence delimiters sit at column 0. Pristine exit 1, conformance 1. The discriminator that isolates the caret-\\S variant to indented fences.",
  "m4-inline-backticks: m1 plus one prose line that MENTIONS a fence opener inline, immediately above the heading, with no leading whitespace so the pristine caret-\\s* test does not fire. Pristine exit 1, conformance 1.",
  "m5-two-space-head: m1 with the heading rewritten with two spaces after the hashes. readManifest uses \\s+ and still finds the table. Pristine exit 1, conformance 1.",
  "m6-hash-in-section: m1 plus a prose line containing '# 3' and '# 4' between the heading and the table. Pristine exit 1, conformance 1.",
  "m7-boundary: SYMBOL = guardFieldsLegacy, plus two prose lines above the heading, the first ending in 'guardFields' and the second starting with 'Legacy'. The symbol thus exists in the file only inside the excluded table and, in prose, only ACROSS the newline. Pristine exit 1, conformance 1.",
  "m8-stryker: SYMBOL = Stryker, appearing nowhere else in the deliverable. Pristine exit 1, conformance 1.",
  "m9-terminating-heading: SYMBOL = TriageBoundaryProof, and the heading that ENDS the conformance section carries it. Pristine exit 0, conformance 0.",
  "m10-unanchored-title: SYMBOL = OpenQuestionsProof; the terminating heading is rewritten to MENTION the Rule conformance table mid-sentence. Pristine exit 0, conformance 0.",
  "m11-level1-terminator: SYMBOL = GuardProofNote; a LEVEL-1 section is inserted immediately after the table. Pristine exit 0, conformance 0.",
  "m12-fenced-illustrations: two symbols cited; headings at depth 3, 4, 5 and 6 wrap two illustrations, each a fenced block containing a full 'Rule conformance' heading and a table row, one with column-0 delimiters and one indented. Pristine exit 0, conformance 0 (fenced text is kept, so both symbols are found). Built to catch the FALSE RED direction.",
  "m13-eof-section: m1 with the whole conformance block cut out and re-appended at the end of the file, so the excluded section runs to EOF. Pristine exit 1, conformance 1.",
  "m14-fence-stress: SYMBOL = TildeFencedProof, inside a tilde fence; plus a fence line whose info string is itself heading-like, and a heading line immediately after an opening fence. Pristine exit 0, conformance 0. Built to attack the delimiter-line flag by making delimiter lines maximally heading-like.",
  "m15-eof-green: SYMBOL = EofBoundaryProof, section at EOF, cited symbol on the last kept line, file written with NO trailing newline. Pristine exit 0, conformance 0.",
  "m16-only-section and m17-section-only: the deliverable cut down toward the conformance section alone, the last leaving `keep` empty so the function returns the empty string. Pristine exit 1, conformance 1. The emptiest inputs this function can be given.",
  "INSTRUMENTS: mutation-probe.mjs (gateArgs confirmed to carry --strict) once per mission, 17 mutants each. On top of that a 6-instrument battery per mutant over all 17 missions — check --strict --json, clear text, --freeze compared on the byte length AND sha256 of the lock it writes into a fresh copy with SOURCE_DATE_EPOCH pinned, --attest, --sarif, --vsa — so 102 observations per mutant. Self-gate: 0 of 17.",
  "NET FIRST, as the revised brief requires. mutation-testcmd.sh on the pristine build: exit 0, 709/709, 32.8 s. Then under each of the six mutants: all six exit 0, 709 pass, identical test COUNT across all seven runs, which is how the agent knows no file was silently dropped under load. Plus the four non-unit nets pristine and per mutant, 28 runs, all exit 0. So unlike the onDiskSpelling story, NONE of these six is killed on macOS. Load was 14-19 during the runs, which biases toward spurious FAILURES, i.e. toward wrongly clearing a mutant, and nothing failed, so the bias did not operate.",
  "m-fold: the example mission with code/src/core/domain/ϴguard.ts on disk (U+03F4 GREEK CAPITAL THETA SYMBOL) cited as ϑguard.ts (U+03D1 GREEK THETA SYMBOL). Both names created from Node, never through shell argv, so no Unicode round-trip. Verified on the volume first: reading ϑguard.ts opens the on-disk ϴguard.ts, so the pointer resolves here and would not on a case-sensitive runner. Pristine: exit 1, gaps, conformance 1. One mission exercising all three functions at once.",
  "m-fold-theta, the control that proves m-fold is not an accident: same on-disk U+03F4 name, pointer written with U+03B8 (ordinary theta). Under the caseFold mutant it STILL refuses, byte-identical, because plain toLowerCase() collapses U+03F4 onto U+03B8 and the realpath rung re-derives the verdict. It is the U+03D1 spelling that gets past both rungs. Choosing the wrong control here is precisely how one concludes harmless for a live false green.",
  "m-tom: a fenced block whose CLOSING fence sits on the line immediately above the Rule conformance heading, with no blank line between them (ordinary markdown, GFM renders it identically), plus prose inside the section, and a self-citation. Pristine: exit 1, gaps, conformance 1.",
  "m-outside: a repository root containing proj/ and shared/ϴguard.ts, the row citing ../shared/ϑguard.ts. resolvePointer admits it through the repoRootAbove branch, the spelling diverges, and the remedy cannot be expressed relative to the project, so the no-remedy message renders. Pristine: exit 1, conformance 1.",
  "m-dotdot: what the remedy the '..' mutants would prescribe actually does. Pristine: exit 1, 'resolves to <parent> — outside the project this mission audits (ADR-0019)'. The RWD-2026-0034 dead end verbatim.",
  "DIRECT BATTERY, 20 cases through the module's own exports, one JSON line per case, diffed pristine against each mutant. Every state under test asserted to EXIST before anything is asserted about it: the symlinked-root shape checks lstat().isSymbolicLink(), that realpath resolves to the real directory, that the WRITTEN root does not contain the canonical spelling, and that the CANONICAL root does. Sensitivity controls that must NOT move and do not: sentinels to null, root-itself to null, '../x.ts' to null, '../..' to null, GUARD.TS to code/guard.ts, sigma to code/ςx.ts, ss to code/ßy.ts, exact spelling to SPELLING_VERIFIED. Battery run 7 times."
 ],
 "verdicts": [
  {
   "key": "524|15|524|17|StringLiteral|\"Stryker was here!\"",
   "line": 524,
   "mutator": "StringLiteral",
   "filing": "equivalent",
   "evidence": "Applied by scripts/mutation-probe-strict.mjs on nine missions (the self-gate plus /tmp/tom-A … /tmp/tom-I): every run returned a byte-identical `check --json --strict` payload — e.g. tom-B exit 1 / 4713 bytes, tom-D exit 0 / 4375 bytes, tom-F exit 1 / 4965 bytes, tom-H exit 1 / 0 bytes. Ledgers: reports/mutation/probe-mission-{A..I}.jsonl. Also applied by hand on tom-B (scratchpad/apply.mjs): `mutants 1: status 1 · exit 1 conformance 1 · 4713 bytes`, character-for-character the pristine line. The same harness DOES see this function's return value change — it caught mutants #5 and #11 as exit-code flips on the same missions — so the null result is a measurement, not a blind spot.",
   "argument": "`raw` is assigned unconditionally as the first statement of the `try`. The only path that skips that assignment is a throw inside `readFileSync`, and the `catch` returns a literal without ever reading `raw`. No read of `raw` can therefore observe its initialiser: the initialiser is dead on every path, for every file and every mission, independently of any reachability argument about the caller.",
   "stableKey": "evidence\u0001textOutsideManifest\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001let raw = \"\";"
  },
  {
   "key": "528|11|530|6|BlockStatement|{}",
   "line": 528,
   "mutator": "BlockStatement",
   "filing": "equivalent",
   "evidence": "Identical `check --json --strict` payload on all nine missions, including /tmp/tom-H, which was built for this mutant: the deliverable runward/floor.md is `chmod 000`. Observed there (pristine AND mutated, identically): the gate never emits JSON at all — 0 bytes on stdout, process exit 1, and on stderr `✗ EACCES: permission denied, open '/tmp/tom-H/runward/floor.md'`. The unreadable-file case is stopped upstream and never reaches this catch. Hand-applied on tom-B: `mutants 2: status 1 · exit 1 conformance 1 · 4713 bytes`, identical to pristine.",
   "argument": "Two independent reasons, either sufficient. (1) The `catch` cannot be entered on any call this code can receive. `textOutsideManifest` is called only from `circularEvidence`, only when `abs === self`, i.e. on the deliverable whose rows `evidenceReport` is iterating — and `evidenceReport` obtained those rows from an UNGUARDED `readFileSync(join(missionDir, deliverable), \"utf8\")` a few statements earlier, in the same process, on the same inode. A file that cannot be read there yields no rows, no pointer and no call; a file that can be read there is still readable microseconds later. So `readFileSync(abs)` inside this function cannot throw. This is a CALLER-COUPLED equivalence: it holds because of where the function is called from, not because of what the function does. It would become live the day `textOutsideManifest` is called on a path the caller has not already read. (2) Even if the catch WERE entered, emptying it changes nothing: `raw` keeps its `\"\"` initialiser, `\"\".split(\"\\n\")` is `[\"\"]`, `findIndex` returns -1, and the pristine `start === -1` branch returns `[\"\"].join(\"\\n\")` — the empty string, which is exactly what the pristine `catch { return \"\"; }` returns. The mutant reaches the same value by a longer route.",
   "stableKey": "evidence\u0001textOutsideManifest\u0001BlockStatement\u0001{}\u0001{ return \"\"; }\u0001catch {"
  },
  {
   "key": "529|16|529|18|StringLiteral|\"Stryker was here!\"",
   "line": 529,
   "mutator": "StringLiteral",
   "filing": "equivalent",
   "evidence": "Identical payload on all nine missions, including /tmp/tom-H (deliverable `chmod 000`), where pristine and mutant alike produce 0 bytes of stdout and `✗ EACCES: permission denied, open '/tmp/tom-H/runward/floor.md'` — the gate stops before any row is parsed, so this catch is not the code that handles an unreadable deliverable. Hand-applied on tom-B: `mutants 3: status 1 · exit 1 conformance 1 · 4713 bytes`, identical to pristine. Ledgers: reports/mutation/probe-mission-{A..I}.jsonl.",
   "argument": "The `catch` cannot be entered on any call this code can receive. `textOutsideManifest` is called only from `circularEvidence`, only when `abs === self`, i.e. on the deliverable whose rows `evidenceReport` is iterating — and `evidenceReport` obtained those rows from an UNGUARDED `readFileSync(join(missionDir, deliverable), \"utf8\")` a few statements earlier, in the same process, on the same inode. A file that cannot be read there yields no rows, no pointer and no call; a file that can be read there is still readable microseconds later. So `readFileSync(abs)` inside this function cannot throw. This is a CALLER-COUPLED equivalence: it holds because of where the function is called from, not because of what the function does. It would become live the day `textOutsideManifest` is called on a path the caller has not already read. Unlike mutant `363|11|365|6|BlockStatement|{}`, this one has NO second line of defence: if the catch were ever entered it would return `\"Stryker was here!\"`, and `outside.includes(symbol)` would then green any pointer whose symbol is a substring of that string (`#\"was here\"`, `#Stryker`). The filing therefore rests entirely on the unreachability above, and it is the one verdict in this set that a refactor of the caller would invalidate.",
   "stableKey": "evidence\u0001textOutsideManifest\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001return \"\";"
  },
  {
   "line": 535,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "FALSE GREEN. Dropping the caret makes the fence test 'contains a fence opener anywhere', so one sentence that MENTIONS a fence opens one; every following line, including the real conformance heading, is recorded as fenced, the section is never excluded, and the slug is found in the very row that claims it. m4-inline-backticks: pristine exit 1/gaps/conformance 1; mutant exit 0/clean/conformance 0. This mutant is IDENTICAL on m2, m3, m12 and m14 (documents whose only fence lines are real delimiters), which is why a mission with an inline mention had to be built. Self-gate identical. Battery: 6 of 102, all six instruments on m4 including the freeze lock bytes and sha256.",
   "caughtBy": null,
   "mission": "m4-inline-backticks",
   "key": "535|13|535|28|Regex|/\\s*(```|~~~)/",
   "stableKey": "evidence\u0001textOutsideManifest\u0001Regex\u0001/\\s*(```|~~~)/\u0001/^\\s*(```|~~~)/\u0001if (/^\\s*(```|~~~)/.test(line)) {"
  },
  {
   "line": 535,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "FALSE GREEN in BOTH directions. The mutant lets a non-space prefix precede the fence opener while refusing leading whitespace. m4-inline-backticks: pristine exit 1/gaps/conformance 1, mutant exit 0/clean/conformance 0 — an inline mention opens a fence. m2-fence-in-section (indented fence): pristine exit 1, mutant exit 0 — the indented fence stops being one. Discriminator measured: on m3 (same illustration, column-0 fence) this mutant is IDENTICAL, exit 1 both sides, while the other five fence mutants all flip there; the difference is exactly the indentation. On m12 it also produces a FALSE RED: pristine exit 0/clean, mutant exit 1 with 1 gap on the symbol living in the indented illustration. Self-gate identical. Battery: 18 of 102.",
   "caughtBy": null,
   "mission": "m4-inline-backticks (also m2, m12)",
   "key": "535|13|535|28|Regex|/^\\S*(```|~~~)/",
   "stableKey": "evidence\u0001textOutsideManifest\u0001Regex\u0001/^\\S*(```|~~~)/\u0001/^\\s*(```|~~~)/\u0001if (/^\\s*(```|~~~)/.test(line)) {"
  },
  {
   "line": 536,
   "mutator": "BooleanLiteral",
   "filing": "equivalent",
   "evidence": "The value written is the fenced flag of the fence DELIMITER line itself. Battery: IDENTICAL on all 102 observations, including four missions built to make delimiter lines load-bearing — m2 and m3 (a fenced heading inside the conformance section), m14 (tilde delimiters, a delimiter whose info string is heading-like, and a heading line immediately after an opening fence) and m12 (a column-0 and an indented illustration, each containing a full conformance heading). Argument: fenced[i] is read only by heading(i) = !fenced[i] && /^#{1,6}\\s/.test(lines[i]); this literal is written only for lines matching the fence test, whose first non-blank characters are backticks or tildes, so a hash is never at column 0 for exactly those lines and the second conjunct is false whatever the flag says. The push count is unchanged, so no index shifts. The same battery DOES see this function move — 14 of the 17 mutants here move on it — and on m2 and m14 its immediate neighbours one line above and one line below BOTH flip the exit code in both directions, so the null reading is a measurement, not a blind spot in the fixture.",
   "caughtBy": null,
   "mission": "battery of 17 missions, no movement anywhere",
   "argument": "fenced[i] is read only by heading(i) = !fenced[i] && /^#{1,6}\\s/.test(lines[i]); this literal is written only for lines matching the fence test, whose first non-blank characters are backticks or tildes, so a hash is never at column 0 for exactly those lines and the second conjunct is false whatever the flag says. The push count is unchanged, so no index shifts. The same battery DOES see this function move — 14 of the 17 mutants here move on it — and on m2 and m14 its immediate neighbours one line above and one line below BOTH flip the exit code in both directions, so the null reading is a measurement, not a blind spot in the fixture.",
   "key": "536|25|536|29|BooleanLiteral|false",
   "stableKey": "evidence\u0001textOutsideManifest\u0001BooleanLiteral\u0001false\u0001true\u0001fenced.push(true);"
  },
  {
   "line": 542,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "FALSE GREEN. Unanchored, the test makes any line CONTAINING a hash-then-space a heading, so the walk ends on an ordinary prose line and every row below it — the whole table — is kept in the text circularEvidence searches. A '# 42' in an evidence cell or a note is enough. m6-hash-in-section (a prose line with '# 3' and '# 4' between the heading and the table; readManifest skips it because it does not start with a pipe, so all six rows are still read): pristine exit 1/gaps/conformance 1; mutant exit 0/clean/conformance 0. Self-gate identical. Battery: 6 of 102, all six instruments on m6 including the freeze lock bytes and sha256.",
   "caughtBy": null,
   "mission": "m6-hash-in-section",
   "key": "542|42|542|53|Regex|/#{1,6}\\s/",
   "stableKey": "evidence\u0001textOutsideManifest\u0001Regex\u0001/#{1,6}\\s/\u0001/^#{1,6}\\s/\u0001const heading = (i) => !fenced[i] && /^#{1,6}\\s/.test(lines[i]);"
  },
  {
   "line": 543,
   "mutator": "ArrayDeclaration",
   "filing": "hole",
   "evidence": "FALSE GREEN. Unlike the register's equivalent Stryker cases, this literal is not decoration: `keep` is the haystack of outside.includes(symbol), so seeding it greens every self-citation whose symbol is a substring of the seeded string. m8-stryker (the row cites the symbol Stryker, which appears nowhere else in the deliverable): pristine exit 1/gaps/conformance 1; mutant exit 0/clean/conformance 0. Self-gate identical. Battery: 6 of 102, all six instruments on m8 including the freeze lock bytes and sha256.",
   "caughtBy": null,
   "mission": "m8-stryker",
   "key": "543|18|543|20|ArrayDeclaration|[\"Stryker was here\"]",
   "stableKey": "evidence\u0001textOutsideManifest\u0001ArrayDeclaration\u0001[\"Stryker was here\"]\u0001[]\u0001const keep = [];"
  },
  {
   "line": 545,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "FALSE RED. Unanchored, the title test fires on any heading that MENTIONS the table, so a second section is excluded and the fact it states disappears from the text the self-citation is checked against. m10-unanchored-title (the terminating heading rewritten to mention the Rule conformance table mid-sentence, its prose carrying the cited symbol): pristine exit 0/clean/conformance 0; mutant exit 1/gaps/conformance 1 on that pointer. Self-gate identical. Battery: 6 of 102, all six instruments on m10 including the freeze lock bytes and sha256.",
   "caughtBy": null,
   "mission": "m10-unanchored-title",
   "key": "545|27|545|56|Regex|/#{1,6}\\s+Rule conformance/i",
   "stableKey": "evidence\u0001textOutsideManifest\u0001Regex\u0001/#{1,6}\\s+Rule conformance/i\u0001/^#{1,6}\\s+Rule conformance/i\u0001if (heading(i) && /^#{1,6}\\s+Rule conformance/i.test(lines[i])) {"
  },
  {
   "line": 545,
   "mutator": "Regex",
   "filing": "hole",
   "evidence": "FALSE GREEN. Collapsing \\s+ to \\s means one stray space in the heading stops the section being excluded, and the universal green key works again — while the manifest itself is still parsed, so nothing else in the gate notices. m5-two-space-head (the heading written with two spaces; readManifest uses \\s+ and still finds the table, so all six rows are read): pristine exit 1/gaps/conformance 1; mutant exit 0/clean/conformance 0. Self-gate identical. Battery: 6 of 102, all six instruments on m5 including the freeze lock bytes and sha256.",
   "caughtBy": null,
   "mission": "m5-two-space-head",
   "key": "545|27|545|56|Regex|/^#{1,6}\\sRule conformance/i",
   "stableKey": "evidence\u0001textOutsideManifest\u0001Regex\u0001/^#{1,6}\\sRule conformance/i\u0001/^#{1,6}\\s+Rule conformance/i\u0001if (heading(i) && /^#{1,6}\\s+Rule conformance/i.test(lines[i])) {"
  },
  {
   "line": 547,
   "mutator": "EqualityOperator",
   "filing": "equivalent",
   "evidence": "The inner walk differs from pristine only when the excluded section runs to end-of-file. Pristine leaves the loop at i = lines.length, then the decrement and the outer increment land back on lines.length and the outer loop ends. The mutant walks one further — the heading test is false because the line is undefined — then the decrement and the outer increment land on lines.length+1 and the outer loop ends there. Neither path pushes another line, so keep is identical array-for-array; in every other case the walk stops on a real heading and the two are the same expression. Battery: IDENTICAL on all 102 observations, including the four EOF and edge missions m13, m15 (no trailing newline), m16 and m17 (empty keep). The same battery DOES see this function move: 14 of the 17 mutants here move on it.",
   "caughtBy": null,
   "mission": "battery of 17 missions, no movement anywhere",
   "argument": "The inner walk differs from pristine only when the excluded section runs to end-of-file. Pristine leaves the loop at i = lines.length, then the decrement and the outer increment land back on lines.length and the outer loop ends. The mutant walks one further — the heading test is false because the line is undefined — then the decrement and the outer increment land on lines.length+1 and the outer loop ends there. Neither path pushes another line, so keep is identical array-for-array; in every other case the walk stops on a real heading and the two are the same expression. Battery: IDENTICAL on all 102 observations, including the four EOF and edge missions m13, m15 (no trailing newline), m16 and m17 (empty keep). The same battery DOES see this function move: 14 of the 17 mutants here move on it.",
   "key": "547|20|547|36|EqualityOperator|i <= lines.length",
   "stableKey": "evidence\u0001textOutsideManifest\u0001EqualityOperator\u0001i <= lines.length\u0001i < lines.length\u0001while (i < lines.length && !heading(i))"
  },
  {
   "line": 549,
   "mutator": "UpdateOperator",
   "filing": "hole",
   "evidence": "FALSE RED. An increment instead of a decrement means the outer loop's own increment skips PAST the heading that terminated the section, so that heading and the line after it are dropped from the kept text — two lines of the deliverable silently unavailable as evidence. m9-terminating-heading (the heading that ends the conformance section carries the cited symbol): pristine exit 0/clean/conformance 0; mutant exit 1/gaps/conformance 1 on that pointer. Self-gate identical. Battery: 6 of 102, all six instruments on m9 including the freeze lock bytes and sha256.",
   "caughtBy": null,
   "mission": "m9-terminating-heading",
   "key": "549|13|549|16|UpdateOperator|i++",
   "stableKey": "evidence\u0001textOutsideManifest\u0001UpdateOperator\u0001i++\u0001i--\u0001i--; // the loop's own i++ lands on the next heading"
  },
  {
   "line": 576,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "FALSE GREEN. Joining with the empty string welds every line to the next and manufactures symbols that no line contains. It is a one-way weakening: concatenation can only ADD matches to outside.includes(symbol), never remove one, so it can only ever turn a refusal into a pass. m7-boundary (two prose lines above the table, the first ending in 'guardFields' and the second starting with 'Legacy', so the cited symbol guardFieldsLegacy exists in the file only inside the excluded table and, in prose, only across the line break): pristine exit 1/gaps/conformance 1; mutant exit 0/clean/conformance 0. Self-gate identical. Battery: 6 of 102, all six instruments on m7 including the freeze lock bytes and sha256.",
   "caughtBy": null,
   "mission": "m7-boundary",
   "key": "576|22|576|26|StringLiteral|\"\"",
   "stableKey": "evidence\u0001textOutsideManifest\u0001StringLiteral\u0001\"\"\u0001\"\\n\"\u0001return keep.join(\"\\n\");"
  }
 ]
}
