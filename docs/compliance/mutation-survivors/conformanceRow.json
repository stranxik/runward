{
 "function": "conformanceRow",
 "probeMissions": [
  "m0: the shipped example untouched. Pristine exit 0, clean. mA (the control): m0 with one row rewritten to a bare self-citation. Pristine exit 1, gaps, 1 conformance gap. Every mission below is mA plus one block inserted immediately ABOVE the Rule conformance heading; only the row line differs.",
  "mB: row with leading AND trailing pipe, the shape the unit fixtures use. Pristine exit 1, 1 gap. The RWD-2026-0032 fix holds.",
  "mC: leading pipe, NO trailing pipe (legal GFM). Pristine exit 1, 1 gap.",
  "mD: NO leading pipe, no trailing pipe (legal GFM). Pristine exit 0, verdict CLEAN. A live defect in the shipped build.",
  "mE: trailing pipe only. Pristine exit 0, CLEAN. Same live defect.",
  "mF, negative control for mD: the same shape with a DIFFERENT slug in cell 1. Pristine exit 1. Proves the clearing in mD/mE comes from that row naming the rule and not from the surrounding heading or the extra lines.",
  "BATTERY of 571 cases in-process, driving the exported evidenceReport over a fixture built exactly like the unit test's: 9 status spellings crossed with 14 pipe shapes; 14 degenerate bar-only lines; the 6 honest forms the test file pins plus pipe-bearing variants of each; inside-section and after-section placements; 16 hand-aimed edges (BOM, NBSP, tab, CR, fullwidth pipe, backslash-escaped pipe, empty cell 2, pipe runs, whitespace, NFD status); and 400 seeded-PRNG fuzz lines. Each case's signature is the JSON of the returned problem strings, with a sha over all 571.",
  "SURFACE SWEEP, 35 rows: the 7 missions times 5 surfaces, each hashed — clear text, --sarif, --attest, --vsa with SOURCE_DATE_EPOCH pinned, and --freeze compared on the BYTES of the lock it writes.",
  "NET SENSITIVITY CONTROLS, to prove the net reaches this function at all and in both directions: forcing the function to return false fails 8 unit tests, forcing it to return true fails 2. So the net is live on both the too-TRUE and the too-FALSE side, and a survivor here is a real survivor, not an unreached line.",
  "base: init --yes --example, green. Every other mission is a copy carrying exactly one deliberate change, and the pristine verdict of each was read BEFORE any mutant was applied.",
  "m-crow-green: a self-citation, plus one line above the heading with a LEADING pipe, NO trailing pipe, and a first cell carrying whitespace. Pristine exit 1, one gap. The shape that separates startsWith from endsWith.",
  "m-crow-red: the same self-citation, and above the heading an honest sentence that merely ENDS with a pipe. Pristine exit 0, clean.",
  "m-crow-prose and m-crow-bare: pipe-free prose, and nothing at all. The controls for the pipe-free region and for the must-stay-red case.",
  "m-spell and m-spell-dir: one pointer mis-cased on its file, and on a DIRECTORY segment, nothing else changed, so the divergence is the ONLY violation. Pristine exit 1 with exactly one gap. m-spell-control: untouched, carried through every capture so 'this mission did not move' is distinguishable from 'nothing moves'.",
  "conformanceRow BATTERY, 4591 markdown blocks read through evidenceReport itself using the unit test's own oracle: 0 problems means the block was KEPT, 1 means the function called it a row. Leads times first cells times separators times statuses times tails; the same shapes behind three fence styles; 237 pipe-free shapes including five pipe LOOK-ALIKE characters, NBSP, U+2028, BOM, 4000-character lines and mixed blocks; plus all 23 cases the unit test pins, asserted to read as that test pins them.",
  "conformanceRow FUZZ, 6000 blocks from a seeded xorshift32 so it is a fixture and not a lottery, 4000 of them guaranteed pipe-free — density in the one region the two guard mutants can act in.",
  "onDiskSpelling DIRECT BATTERY, 329 calls on a purpose-built fixture: mixed-case directories and files, the five Unicode fold rungs, an NFD-named file, a ten-deep path, an empty directory and a 0111 directory. Each target fed in seven shapes — plain, one/two/three trailing separators, a doubled inner separator, an extra leading separator, every segment doubled — plus 14 absolute edge cases. Paths concatenated BY HAND, not with path.join, because join() normalises away exactly the shapes the loop bound is about.",
  "CASE-SENSITIVE REPRODUCTION: a 300 MB case-sensitive APFS sparse image, verified case-sensitive by probe. Every mission copied onto it and re-measured, and the whole unit net re-run with TMPDIR pointing into it, so 'survives on ubuntu-latest' is MEASURED here rather than inferred from process.platform. The volume was probed for normalisation too: a file written NFC is found under its NFD name, so it is case-sensitive but normalisation-insensitive, and that limit is stated wherever it matters."
 ],
 "verdicts": [
  {
   "line": 583,
   "mutator": "StringLiteral",
   "filing": "equivalent",
   "evidence": "Same batteries, same result, measured separately rather than reasoned about together. 4591 blocks, digest 600e11c8 — byte-identical to pristine AND to its neighbour; 6000 fuzz blocks identical; 8 missions times 6 surfaces identical; the four extra nets exit 0; unit net exit 0, 723 pass. Same sensitivity controls moving 237 and 1000. The argument the measurement backs: includes of the empty string is true for every string, so the guard never fires — the same state the neighbour produces by returning false outright, and the two produce IDENTICAL digests, so that is an observation and not an inference.",
   "caughtBy": null,
   "mission": "battery + fuzz + 8 missions",
   "argument": "includes of the empty string is true for every string, so the guard never fires — the same state the neighbour produces by returning false outright, and the two produce IDENTICAL digests, so that is an observation and not an inference.",
   "key": "583|21|583|24|StringLiteral|\"\"",
   "stableKey": "evidence\u0001conformanceRow\u0001StringLiteral\u0001\"\"\u0001\"|\"\u0001if (!t.includes(\"|\"))"
  },
  {
   "line": 583,
   "mutator": "ConditionalExpression",
   "filing": "equivalent",
   "evidence": "Removing the pipe fast-path changes nothing anywhere reachable, and the reach is demonstrated rather than claimed. Structured battery, 4591 blocks: pristine 3131 kept / 1460 dropped, digest 600e11c8; mutant identical, 0 of 4591 differ, and the 23 pinned cases read exactly as the unit test pins them. Deterministic fuzz, 6000 blocks of which 4000 carry no pipe at all — the only region this mutant can act in: identical digest, 0 of 6000 differ. Gate: 8 missions times 6 surfaces (json, text, sarif, attest, vsa, freeze exit plus the BYTES of the lock) every one identical, and a second pristine capture matches the first so 'identical' is a stable reading and not luck. SENSITIVITY CONTROL, same function, same statement, not on any list: making the guard return TRUE instead of false moves 237 of 4591 battery cases — all pipe-free — 3881 of the fuzz's 4000 pipe-free half, and flips a mission from exit 0 clean to exit 1 gaps. The other listed mutant of this function moves 1000 of 4591 and two missions. So the instrument moves, in this mutant's own region, for edits of this mutant's own statement. The argument the measurement backs: with no pipe in the line the split yields a one-element array and the next line's cell-count test returns false regardless. One call site, so this oracle covers every use. Also survives the whole net with fixtures on a case-sensitive volume.",
   "caughtBy": null,
   "mission": "battery + fuzz + 8 missions",
   "argument": "with no pipe in the line the split yields a one-element array and the next line's cell-count test returns false regardless. One call site, so this oracle covers every use. Also survives the whole net with fixtures on a case-sensitive volume.",
   "key": "583|9|583|25|ConditionalExpression|false",
   "stableKey": "evidence\u0001conformanceRow\u0001ConditionalExpression\u0001false\u0001!t.includes(\"|\")\u0001if (!t.includes(\"|\"))"
  },
  {
   "line": 585,
   "mutator": "StringLiteral",
   "filing": "equivalent",
   "evidence": "The FIRST replace's replacement, the one stripping the leading pipe. The replacement text carries no pipe, so the pipe count is unchanged and the split returns exactly the same number of cells; the only thing that changes is cell 0, which this function never reads — it tests the cell COUNT and cell 1. When the row has fewer than three cells the length test short-circuits before cell 1 is touched, so the degenerate bar-only cases cannot diverge either. MEASURED with the same battery and the same sensitivity control: byte-identical to pristine, 0 differing cases, against 30/30/18 moved cases for the three guard mutants; all seven missions byte-identical; the surface sweep byte-identical under cmp; the official probe reports no observable difference where its siblings report OBSERVABLE. IDENTITY: this mutant and the next are indistinguishable by (line, mutator, replacement) and are told apart only by column — 36 is the first replace, 55 the second.",
   "caughtBy": null,
   "mission": "all seven missions; battery; surface sweep; official probe",
   "argument": "The FIRST replace's replacement, the one stripping the leading pipe. The replacement text carries no pipe, so the pipe count is unchanged and the split returns exactly the same number of cells; the only thing that changes is cell 0, which this function never reads — it tests the cell COUNT and cell 1. When the row has fewer than three cells the length test short-circuits before cell 1 is touched, so the degenerate bar-only cases cannot diverge either. MEASURED with the same battery and the same sensitivity control: byte-identical to pristine, 0 differing cases, against 30/30/18 moved cases for the three guard mutants; all seven missions byte-identical; the surface sweep byte-identical under cmp; the official probe reports no observable difference where its siblings report OBSERVABLE. IDENTITY: this mutant and the next are indistinguishable by (line, mutator, replacement) and are told apart only by column — 36 is the first replace, 55 the second.",
   "key": "585|36|585|38|StringLiteral|\"Stryker was here!\"",
   "stableKey": "evidence\u0001conformanceRow\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const cells = t.replace(/^\\|/, \"\").replace(/\\|$/, \"\").split(\"|\").map((c) => c.trim());"
  },
  {
   "line": 585,
   "mutator": "StringLiteral",
   "filing": "equivalent",
   "evidence": "The SECOND replace's replacement, stripping the trailing pipe. Both pristine and mutant CONSUME the trailing pipe — one replaces it with nothing, the other with pipe-free text — so the pipe count and therefore the cell count are identical; only the last cell's content changes, and the last cell is never read. The one case where the last cell IS cell 1 is a two-cell row, and there the count test is already false, which is exactly the case the test file pins and which the always-true control shows the net does guard. MEASURED with the same battery and controls: byte-identical throughout, against the moved cases for the guard mutants; all missions identical; surface sweep byte-identical; probe reports no observable difference. IDENTITY: same triple as the previous verdict, column 55.",
   "caughtBy": null,
   "mission": "all seven missions; battery; surface sweep; official probe",
   "argument": "The SECOND replace's replacement, stripping the trailing pipe. Both pristine and mutant CONSUME the trailing pipe — one replaces it with nothing, the other with pipe-free text — so the pipe count and therefore the cell count are identical; only the last cell's content changes, and the last cell is never read. The one case where the last cell IS cell 1 is a two-cell row, and there the count test is already false, which is exactly the case the test file pins and which the always-true control shows the net does guard. MEASURED with the same battery and controls: byte-identical throughout, against the moved cases for the guard mutants; all missions identical; surface sweep byte-identical; probe reports no observable difference. IDENTITY: same triple as the previous verdict, column 55.",
   "key": "585|55|585|57|StringLiteral|\"Stryker was here!\"",
   "stableKey": "evidence\u0001conformanceRow\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001const cells = t.replace(/^\\|/, \"\").replace(/\\|$/, \"\").split(\"|\").map((c) => c.trim());\u0001#2",
   "ordinal": 2
  },
  {
   "line": 612,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "Observable in BOTH directions, through the gate, on both filesystems. FALSE GREEN on m-crow-green: pristine exit 1 / gaps / one gap; mutant exit 0 / clean / zero. sarif results 1 to 0, attest verdict gaps to clean, vsa moved, and --freeze goes from REFUSING to sealing a 1916-byte lock. RWD-2026-0002's universal green key re-armed a third way: with endsWith, a row carrying its leading pipe but no trailing one, whose first cell has a space, is no longer a row, so it is kept and the slug is found outside the manifest. FALSE RED on m-crow-red: honest prose that merely ends with a pipe goes pristine exit 0 clean to mutant exit 1 gaps — documentary evidence refused, the direction that gets a gate switched off. Battery: 1000 of 4591 flip, 45 dropped-to-kept and 955 kept-to-dropped. Controls m-crow-prose and m-crow-bare do not move, so the two that do are moving on the row test and not on the fixture. Caught nowhere: unit net exit 0 on APFS and on a case-sensitive volume, self-gate unchanged, the four extra nets exit 0. The gate-level flip reproduces identically on the case-sensitive volume, so this hole is filesystem-independent.",
   "caughtBy": null,
   "mission": "m-crow-green and m-crow-red, plus the battery",
   "key": "612|12|612|29|MethodExpression|t.endsWith(\"|\")",
   "stableKey": "evidence\u0001conformanceRow\u0001MethodExpression\u0001t.endsWith(\"|\")\u0001t.startsWith(\"|\")\u0001return t.startsWith(\"|\") || /^\\S+$/.test(cells[0]);"
  }
 ]
}
