{
 "function": "natureSatisfied",
 "probeMissions": [
  "consolidated-2026-09-04"
 ],
 "verdicts": [
  {
   "key": "1320|9|1320|25|ConditionalExpression|false",
   "line": 1320,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 1320, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 1320, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001ConditionalExpression\u0001false\u0001nature === \"adr\"\u0001if (nature === \"adr\")"
  },
  {
   "key": "1320|20|1320|25|StringLiteral|\"\"",
   "line": 1320,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 1320, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 1320, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001StringLiteral\u0001\"\"\u0001\"adr\"\u0001if (nature === \"adr\")"
  },
  {
   "key": "1321|16|1321|112|MethodExpression|pointers.every(p => p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`))",
   "line": 1321,
   "mutator": "MethodExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001MethodExpression\u0001pointers.every(p => p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`))\u0001pointers.some((p) => p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`))\u0001return pointers.some((p) => p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`));"
  },
  {
   "key": "1321|30|1321|111|ArrowFunction|() => undefined",
   "line": 1321,
   "mutator": "ArrowFunction",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 1321, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 1321, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001ArrowFunction\u0001() => undefined\u0001(p) => p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`)\u0001return pointers.some((p) => p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`));"
  },
  {
   "key": "1321|37|1321|111|ConditionalExpression|true",
   "line": 1321,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 1321, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 1321, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001ConditionalExpression\u0001true\u0001p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`)\u0001return pointers.some((p) => p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`));"
  },
  {
   "key": "1321|37|1321|111|ConditionalExpression|false",
   "line": 1321,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 1321, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 1321, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001ConditionalExpression\u0001false\u0001p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`)\u0001return pointers.some((p) => p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`));"
  },
  {
   "key": "1321|37|1321|111|LogicalOperator|p.kind === \"adr\" && !!p.adrId || adrIdExists(missionDir, `ADR-${p.adrId}`)",
   "line": 1321,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001LogicalOperator\u0001p.kind === \"adr\" && !!p.adrId || adrIdExists(missionDir, `ADR-${p.adrId}`)\u0001p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`)\u0001return pointers.some((p) => p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`));"
  },
  {
   "key": "1321|37|1321|66|ConditionalExpression|true",
   "line": 1321,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001ConditionalExpression\u0001true\u0001p.kind === \"adr\" && !!p.adrId\u0001return pointers.some((p) => p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`));"
  },
  {
   "key": "1321|37|1321|66|LogicalOperator|p.kind === \"adr\" || !!p.adrId",
   "line": 1321,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001LogicalOperator\u0001p.kind === \"adr\" || !!p.adrId\u0001p.kind === \"adr\" && !!p.adrId\u0001return pointers.some((p) => p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`));"
  },
  {
   "key": "1321|37|1321|53|ConditionalExpression|true",
   "line": 1321,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001ConditionalExpression\u0001true\u0001p.kind === \"adr\"\u0001return pointers.some((p) => p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`));"
  },
  {
   "key": "1321|37|1321|53|EqualityOperator|p.kind !== \"adr\"",
   "line": 1321,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 1321, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 1321, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001EqualityOperator\u0001p.kind !== \"adr\"\u0001p.kind === \"adr\"\u0001return pointers.some((p) => p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`));"
  },
  {
   "key": "1321|48|1321|53|StringLiteral|\"\"",
   "line": 1321,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 1321, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 1321, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001StringLiteral\u0001\"\"\u0001\"adr\"\u0001return pointers.some((p) => p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`));"
  },
  {
   "key": "1321|57|1321|66|BooleanLiteral|!p.adrId",
   "line": 1321,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 1321, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 1321, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001BooleanLiteral\u0001!p.adrId\u0001!!p.adrId\u0001return pointers.some((p) => p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`));"
  },
  {
   "key": "1321|58|1321|66|BooleanLiteral|p.adrId",
   "line": 1321,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 1321, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 1321, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001BooleanLiteral\u0001p.adrId\u0001!p.adrId\u0001return pointers.some((p) => p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`));"
  },
  {
   "key": "1321|94|1321|110|StringLiteral|``",
   "line": 1321,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001StringLiteral\u0001``\u0001`ADR-${p.adrId}`\u0001return pointers.some((p) => p.kind === \"adr\" && !!p.adrId && adrIdExists(missionDir, `ADR-${p.adrId}`));"
  },
  {
   "key": "1324|13|1324|20|ConditionalExpression|false",
   "line": 1324,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001ConditionalExpression\u0001false\u0001!p.path\u0001if (!p.path)"
  },
  {
   "key": "1327|13|1327|40|ConditionalExpression|false",
   "line": 1327,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001ConditionalExpression\u0001false\u0001!abs || !isRegularFile(abs)\u0001if (!abs || !isRegularFile(abs))"
  },
  {
   "key": "1327|13|1327|40|LogicalOperator|!abs && !isRegularFile(abs)",
   "line": 1327,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001LogicalOperator\u0001!abs && !isRegularFile(abs)\u0001!abs || !isRegularFile(abs)\u0001if (!abs || !isRegularFile(abs))"
  },
  {
   "key": "1333|15|1335|10|BlockStatement|{}",
   "line": 1333,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001BlockStatement\u0001{}\u0001{ continue; }\u0001catch {"
  },
  {
   "key": "1336|13|1336|31|ConditionalExpression|true",
   "line": 1336,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001ConditionalExpression\u0001true\u0001nature === \"junit\"\u0001if (nature === \"junit\" && isJUnitReport(content))"
  },
  {
   "key": "1338|13|1338|57|ConditionalExpression|false",
   "line": 1338,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001ConditionalExpression\u0001false\u0001nature === \"sarif\" && isSarifReport(content)\u0001if (nature === \"sarif\" && isSarifReport(content))"
  },
  {
   "key": "1338|13|1338|57|LogicalOperator|nature === \"sarif\" || isSarifReport(content)",
   "line": 1338,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001LogicalOperator\u0001nature === \"sarif\" || isSarifReport(content)\u0001nature === \"sarif\" && isSarifReport(content)\u0001if (nature === \"sarif\" && isSarifReport(content))"
  },
  {
   "key": "1338|13|1338|31|ConditionalExpression|true",
   "line": 1338,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001ConditionalExpression\u0001true\u0001nature === \"sarif\"\u0001if (nature === \"sarif\" && isSarifReport(content))"
  },
  {
   "key": "1338|13|1338|31|EqualityOperator|nature !== \"sarif\"",
   "line": 1338,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001EqualityOperator\u0001nature !== \"sarif\"\u0001nature === \"sarif\"\u0001if (nature === \"sarif\" && isSarifReport(content))"
  },
  {
   "key": "1338|24|1338|31|StringLiteral|\"\"",
   "line": 1338,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001StringLiteral\u0001\"\"\u0001\"sarif\"\u0001if (nature === \"sarif\" && isSarifReport(content))"
  },
  {
   "key": "1339|20|1339|24|BooleanLiteral|false",
   "line": 1339,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001BooleanLiteral\u0001false\u0001true\u0001return true;"
  },
  {
   "key": "1340|13|1340|59|ConditionalExpression|false",
   "line": 1340,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001ConditionalExpression\u0001false\u0001nature === \"eslint\" && isEslintReport(content)\u0001if (nature === \"eslint\" && isEslintReport(content))"
  },
  {
   "key": "1340|13|1340|59|LogicalOperator|nature === \"eslint\" || isEslintReport(content)",
   "line": 1340,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "Hole with an exact recipe, measured 2026-09-04: applied at line 1340, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "Hole with an exact recipe, measured 2026-09-04: applied at line 1340, the machine payload of `check --strict --json` on the `nu` probe mission DIFFERS from the pristine build — the observable exists and nothing kills the mutant today (survived the CI unit pass and the 1,312-trial whole-net pass). The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001LogicalOperator\u0001nature === \"eslint\" || isEslintReport(content)\u0001nature === \"eslint\" && isEslintReport(content)\u0001if (nature === \"eslint\" && isEslintReport(content))"
  },
  {
   "key": "1340|13|1340|32|ConditionalExpression|true",
   "line": 1340,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001ConditionalExpression\u0001true\u0001nature === \"eslint\"\u0001if (nature === \"eslint\" && isEslintReport(content))"
  },
  {
   "key": "1340|13|1340|32|EqualityOperator|nature !== \"eslint\"",
   "line": 1340,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001EqualityOperator\u0001nature !== \"eslint\"\u0001nature === \"eslint\"\u0001if (nature === \"eslint\" && isEslintReport(content))"
  },
  {
   "key": "1340|24|1340|32|StringLiteral|\"\"",
   "line": 1340,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001StringLiteral\u0001\"\"\u0001\"eslint\"\u0001if (nature === \"eslint\" && isEslintReport(content))"
  },
  {
   "key": "1341|20|1341|24|BooleanLiteral|false",
   "line": 1341,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001BooleanLiteral\u0001false\u0001true\u0001return true;\u0001#2"
  },
  {
   "key": "1342|13|1342|91|ConditionalExpression|false",
   "line": 1342,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001ConditionalExpression\u0001false\u0001nature === \"coverage\" && (isLcovReport(content) || isCoberturaReport(content))\u0001if (nature === \"coverage\" && (isLcovReport(content) || isCoberturaReport(content)))"
  },
  {
   "key": "1342|13|1342|91|LogicalOperator|nature === \"coverage\" || isLcovReport(content) || isCoberturaReport(content)",
   "line": 1342,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001LogicalOperator\u0001nature === \"coverage\" || isLcovReport(content) || isCoberturaReport(content)\u0001nature === \"coverage\" && (isLcovReport(content) || isCoberturaReport(content))\u0001if (nature === \"coverage\" && (isLcovReport(content) || isCoberturaReport(content)))"
  },
  {
   "key": "1342|13|1342|34|ConditionalExpression|true",
   "line": 1342,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001ConditionalExpression\u0001true\u0001nature === \"coverage\"\u0001if (nature === \"coverage\" && (isLcovReport(content) || isCoberturaReport(content)))"
  },
  {
   "key": "1342|13|1342|34|EqualityOperator|nature !== \"coverage\"",
   "line": 1342,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001EqualityOperator\u0001nature !== \"coverage\"\u0001nature === \"coverage\"\u0001if (nature === \"coverage\" && (isLcovReport(content) || isCoberturaReport(content)))"
  },
  {
   "key": "1342|24|1342|34|StringLiteral|\"\"",
   "line": 1342,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001StringLiteral\u0001\"\"\u0001\"coverage\"\u0001if (nature === \"coverage\" && (isLcovReport(content) || isCoberturaReport(content)))"
  },
  {
   "key": "1342|39|1342|90|ConditionalExpression|true",
   "line": 1342,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001ConditionalExpression\u0001true\u0001isLcovReport(content) || isCoberturaReport(content)\u0001if (nature === \"coverage\" && (isLcovReport(content) || isCoberturaReport(content)))"
  },
  {
   "key": "1342|39|1342|90|LogicalOperator|isLcovReport(content) && isCoberturaReport(content)",
   "line": 1342,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001LogicalOperator\u0001isLcovReport(content) && isCoberturaReport(content)\u0001isLcovReport(content) || isCoberturaReport(content)\u0001if (nature === \"coverage\" && (isLcovReport(content) || isCoberturaReport(content)))"
  },
  {
   "key": "1343|20|1343|24|BooleanLiteral|false",
   "line": 1343,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001BooleanLiteral\u0001false\u0001true\u0001return true;\u0001#3"
  },
  {
   "key": "1344|13|1344|58|ConditionalExpression|false",
   "line": 1344,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001ConditionalExpression\u0001false\u0001nature === \"sbom\" && isCycloneDxSbom(content)\u0001if (nature === \"sbom\" && isCycloneDxSbom(content))"
  },
  {
   "key": "1344|13|1344|58|LogicalOperator|nature === \"sbom\" || isCycloneDxSbom(content)",
   "line": 1344,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001LogicalOperator\u0001nature === \"sbom\" || isCycloneDxSbom(content)\u0001nature === \"sbom\" && isCycloneDxSbom(content)\u0001if (nature === \"sbom\" && isCycloneDxSbom(content))"
  },
  {
   "key": "1344|13|1344|30|ConditionalExpression|true",
   "line": 1344,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001ConditionalExpression\u0001true\u0001nature === \"sbom\"\u0001if (nature === \"sbom\" && isCycloneDxSbom(content))"
  },
  {
   "key": "1344|13|1344|30|EqualityOperator|nature !== \"sbom\"",
   "line": 1344,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001EqualityOperator\u0001nature !== \"sbom\"\u0001nature === \"sbom\"\u0001if (nature === \"sbom\" && isCycloneDxSbom(content))"
  },
  {
   "key": "1344|24|1344|30|StringLiteral|\"\"",
   "line": 1344,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001StringLiteral\u0001\"\"\u0001\"sbom\"\u0001if (nature === \"sbom\" && isCycloneDxSbom(content))"
  },
  {
   "key": "1345|20|1345|24|BooleanLiteral|false",
   "line": 1345,
   "mutator": "BooleanLiteral",
   "filing": "hole",
   "evidence": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "argument": "COULD NOT CLEAR — filed as a hole because no measurement decided it, not because one condemned it (the isRegularFile precedent). Survived the CI chunked unit pass, survived the whole-net pass (wholenet-verdicts.jsonl, 2026-09-03/04), and produced no observable difference — payload or rendered text — on any of the three probe missions. The nature adapters' branches need a mission whose manifest requires that exact nature with a report in the matching state; neither the probe missions nor the whole net's missions carry one. Probe suite (2026-09-04, committed recipe): three `init --example` missions — `nu` (untouched), `optin` (scaffold-lock structureContract:true), `declencheur` (optin plus an invalid `**Date**` header field in framing.md). For every mutant below, applied by exact offset into dist, `check --strict --json` AND the rendered `check --strict` text were compared byte-for-byte against the pristine build on all three.",
   "stableKey": "evidence\u0001natureSatisfied\u0001BooleanLiteral\u0001false\u0001true\u0001return true;\u0001#4"
  }
 ]
}
