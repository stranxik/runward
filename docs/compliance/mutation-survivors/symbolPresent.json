{
  "function": "symbolPresent",
  "probeMissions": [
    "m1 — THE WIDE MISSION. `runward init --example` (request-triage) copied, then the Evidence cell of 22 `applied` rows rewritten so every shape of the pointer grammar meets the parsers at once. Two new evidence files: `code/b.ts` (declares `companion`, `twoCharSym`, `guardFieldsLegacy` but NOT `guardFields`; carries the lines `the exact sentence`, `the exact ; sentence`, `the sealed promiseX`, `marker axb here`, `one char q here`) and `code/test/pointers.test.ts` (declares `the guard fails closed`, `spaced name`, `l'invariant tient`, `guard`). Cells: `file:code/b.ts#companion file:code/deleted-a.ts` (space-separated) · `file:code/b.ts#companion,file:code/deleted-b.ts` (comma, no space) · `file:code/b.ts#\"the exact sentence\"; file:code/deleted-c.ts` · `file:code/b.ts#\"the exact ; sentence\"` (a `;` INSIDE the quoted symbol, which must not cut a segment) · `file:code/b.ts#guardFields` (ADR-0051: a substring of a real identifier, must be REFUSED) · `test:code/test/pointers.test.ts::'l'invariant tient'` (delimiting apostrophe) · `file:code/b.ts#` (empty `#`) · `file:code/b.ts#q` (one-character symbol) · `file:code/b.ts#a.b` (dotted, absent) · `file:code/b.ts#companion; ; file:code/b.ts#twoCharSym` (empty middle segment) · `file:code/b.ts#\"unclosed sym file:code/deleted-e.ts` (quote never closed) · `file:code/b.ts#companion<U+2028>; file:code/deleted-f.ts` · `file:code/b.ts#companion<CR> file:code/deleted-g.ts` · `  file:code/b.ts#companion  ` (surrounding whitespace) · `file:code/b.ts#companion , file:code/b.ts#\"the sealed promise\"` · `,file:code/b.ts#companion` (leading comma) · `file:code/b.ts#companion;file:code/deleted-h.ts` · `adr:0001 file:code/deleted-i.ts` · `test:…::  the guard fails closed` · `test:…::the \"guard\" path` · `file:code/deleted-k.ts<U+2028> a trailing note` · `file:code/b.ts#\"a, b\" file:code/deleted-l.ts;;`. Baseline: exit 1, 15 conformance violations, evidence typed 22 / prose 1.",
    "m2 — MINIMAL, ONE DEFECT (a dotted symbol that is absent). The shipped reference mission plus `code/b.ts`, with exactly one cell changed: execution-topology.md `topology-sovereignty-by-data-class` = `file:code/b.ts#a.b`, where `code/b.ts` carries `marker axb here` and never the text `a.b`. Baseline: exit 1, ONE violation (`symbol \"a.b\" not found in the file`). Purpose: turn a message-level difference into an EXIT-CODE difference.",
    "m3 — MINIMAL, ONE CELL (an invisible line terminator between a dead pointer and prose). Same construction, the single cell being `file:code/deleted-j.ts<U+2028> a trailing note`, the cited file never created. Baseline: exit 0, ZERO violations — the shipped code drops that pointer in silence and the row still reads as evidence. Purpose: catch a mutant that makes the gate see a pointer the shipped code cannot.",
    "m4 — MINIMAL, ONE CELL (a symbol that is not a valid regular expression body). Same construction, the single cell being `file:code/b.ts#[companion`. Baseline: exit 1, ONE ordinary violation (`symbol \"[companion\" not found in the file`). Purpose: separate 'the gate refuses' from 'the gate renders no verdict at all'.",
    "m5 — MINIMAL, ONE CELL, GREEN in baseline (an honest quoted sentence). Same construction, the single cell being `file:code/b.ts#\"the sealed promise\"`, where `code/b.ts` carries the line `the sealed promiseX` — so the sentence IS present as text, followed by a word character. Baseline: exit 0, zero violations. Purpose: catch a mutant that turns an honest green RED.",
    "m6 — PROSE BEFORE THE POINTER. Ten cells in which every pointer is preceded by prose, a quote opener, a comma run or a `;`, since that is the configuration in which a mis-cut chunk shows: `the guard is at file:code/deleted-m.ts and also file:code/b.ts#companion` · `see file:code/b.ts#companion , then file:code/deleted-n.ts` · `x file:code/deleted-o.ts` · `\"quoted opener file:code/deleted-p.ts` · `file:code/b.ts#companion; prose in the second segment file:code/deleted-q.ts` · `  ,  file:code/deleted-r.ts ,, file:code/b.ts#twoCharSym` · `file:code/b.ts#\"a b\" file:code/deleted-s.ts` · `test:…::spaced name file:code/deleted-t.ts` · `adr:0001 and file:code/b.ts#companion, file:code/deleted-u.ts` · `;file:code/deleted-v.ts;`. Baseline: exit 1, 11 violations.",
    "self-gate — runward's own mission, `node dist/cli.js check --strict --json` with no `--path`. Baseline exit 0, verdict clean. Recorded for completeness; per the standing warning a 0/N result here is not innocence, since runward's own manifest only writes the robust pointer forms."
  ],
  "verdicts": [
    {
      "key": "23|9|23|37|Regex|/[A-Za-z_$][A-Za-z0-9_$]*$/",
      "line": 23,
      "mutator": "Regex",
      "filing": "hole",
      "evidence": "FALSE GREEN, verified by exit code, and three further observations in three other shapes. (1) m2, whose only defect is the cell `file:code/b.ts#a.b`: `check --strict --json` went from **exit 1 to exit 0** and the violation `Topology · topology-sovereignty-by-data-class · typed pointer file:code/b.ts#a.b — symbol \"a.b\" not found in the file (moved or renamed? update the pointer)` disappeared. The gate stopped refusing a pointer that names something the file does not contain. (2) m5, whose only cell is the honest `file:code/b.ts#\"the sealed promise\"` over a file carrying `the sealed promiseX`: **exit 0 to exit 1**, with the new violation `typed pointer file:code/b.ts#\"the sealed promise\" — symbol \"the sealed promise\" not found in the file` — an honest pointer turned RED. (3) m1 moved in both directions inside one run: the `#a.b` violation REMOVED and the `#\"the sealed promise\"` violation ADDED, exit staying 1 (so an exit-code-only instrument would have reported nothing). (4) m4, whose only cell is `file:code/b.ts#[companion`: the mutant produced **zero bytes on stdout** and died with `✗ Invalid regular expression: /(?<![\\w$])[companion(?![\\w$])/: Unmatched ')'` — `check --strict --json` rendered no JSON at all, so an agent driving on the ADR-0030 machine contract gets no verdict. Ledgers: reports/mutation/probe-symbolPresent-{m1,m2,m4,m5,m3,m6,selfgate}.jsonl. Mechanism, read off the four observations rather than off the code: dropping the `^` makes the identifier test accept any symbol that merely ENDS in an identifier run, so dotted names, quoted sentences and bracketed text are interpolated into a `RegExp` — where `.` becomes a wildcard, a trailing word character defeats the `(?![\\w$])` boundary, and an unbalanced `[` is a syntax error. Note the blast radius is wider than `check`: `dist/lib/spec-conformance.js` imports `symbolPresent` for `spec-check` (see src/lib/spec-conformance.ts:77), so the same flip lands there too."
    },
    {
      "key": "28|24|28|51|Regex|/\\b(file|test|adr):(\\S.*)/",
      "line": 28,
      "mutator": "Regex",
      "filing": "hole",
      "evidence": "Verified by exit code. On m3, whose only cell is `file:code/deleted-j.ts<U+2028> a trailing note` and whose cited file does not exist, `check --strict --json` went from **exit 0 to exit 1**, gaining `Topology · topology-sovereignty-by-data-class · typed pointer does not resolve: file:code/deleted-j.ts — update it or remove the row`. On m1 the same mutant added `Handover · handover-agents-charter-final · typed pointer does not resolve: file:code/deleted-k.ts` (same cell shape), exit staying 1. Identical payloads on m2, m4, m5, m6 and the self-gate, none of which carries an interior line terminator. Ledgers: reports/mutation/probe-symbolPresent-{m1,m3,m2,m4,m5,m6,selfgate}.jsonl; details captured with scripts/probe-detail.mjs. What the measurement shows is uncomfortable and is the reason this is filed rather than cleared: the mutant is STRICTER than the shipped code. The `$` anchor is exactly what makes an invisible U+2028 (or CR) left in a cell swallow a typed pointer — `.` cannot cross a line terminator, so `(\\S.*)$` never reaches the end and the whole pointer vanishes while the row still reads as evidence and the deleted file is never opened. That is the RWD-2026-0006 family (docs/compliance/known-defects.md:41, 'silent no-op pointers that looked precise and verified nothing'), alive in the shipped 0.36.0 build and measured here on m3's baseline: exit 0 over a cell citing a file that does not exist. Nothing in the suite pins the anchor in either direction, so a change to this constant — in either direction — is unobserved."
    }
  ]
}
