{
 "function": "renderIso42001Readiness",
 "probeMissions": [],
 "verdicts": [
  {
   "key": "147|15|147|17|ArrayDeclaration|[\"Stryker was here\"]",
   "line": 147,
   "mutator": "ArrayDeclaration",
   "filing": "display-only",
   "evidence": "Same class and same measurement as the eleven `L.push(\"\")` injection twins (reference argument on the occurrence-16 one), with the position named because it is the strongest one available: this literal is the accumulator's SEED, so it lands on line 1 and pushes the H1 `# ISO/IEC 42001 — assessment-readiness draft` to line 2. Measured on a fresh mission and on examples/request-triage through the real CLI: one line is added, no claim changes, and the whole smoke ISO block still passes — including `compMd.includes(\"assessment-readiness draft\")`, which reads the title as a substring and does not care that it is no longer the first line. Filed with its siblings rather than as a defect because it alters no count, status, caveat or scope; it is nevertheless the sharpest illustration of the structural note recorded there — nothing in the net pins even the first byte of this document.",
   "argument": "Same class and same measurement as the eleven `L.push(\"\")` injection twins (reference argument on the occurrence-16 one), with the position named because it is the strongest one available: this literal is the accumulator's SEED, so it lands on line 1 and pushes the H1 `# ISO/IEC 42001 — assessment-readiness draft` to line 2. Measured on a fresh mission and on examples/request-triage through the real CLI: one line is added, no claim changes, and the whole smoke ISO block still passes — including `compMd.includes(\"assessment-readiness draft\")`, which reads the title as a substring and does not care that it is no longer the first line. Filed with its siblings rather than as a defect because it alters no count, status, caveat or scope; it is nevertheless the sharpest illustration of the structural note recorded there — nothing in the net pins even the first byte of this document.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001ArrayDeclaration\u0001[\"Stryker was here\"]\u0001[]\u0001const L = [];"
  },
  {
   "key": "150|13|150|43|ConditionalExpression|true",
   "line": 150,
   "mutator": "ConditionalExpression",
   "filing": "equivalent",
   "evidence": "`counts` is written at exactly one site (dist/lib/compliance.js:151) and READ at exactly one site (line 178), where it is read by three literal keys: `counts.applied`, `counts.deviated`, `counts[\"n/a\"]`. The guard decides only whether a status OUTSIDE those three gets a key created; under `true` such a row writes `counts[<that status>] = NaN`, a key line 178 never names. SENSITIVITY CONTROL, measured rather than asserted: the input class that exercises the guard at all is a manifest row whose Status column is a word outside the three counted ones, and it is ordinary and reachable — `readManifest` lower-cases the Status cell and validates no vocabulary, so `| rule-four | pending | tbd |` is accepted verbatim. A fixture carrying exactly that row (plus an `n/a` row, so all three counted branches stay live) renders BYTE-IDENTICAL under the mutant, as do a fresh `runward init` mission and examples/request-triage through the real CLI. I do not offer a nearby input where the two differ, because none exists: the guard cannot reach any of the three keys line 178 reads. What would make it matter is a CODE change, not an input — if that sentence ever summed `Object.values(counts)` instead of naming three keys, a `pending` row would flip it. That is a reason to keep the guard; it is not a reason to call this mutant live.",
   "argument": "`counts` is written at exactly one site (dist/lib/compliance.js:151) and READ at exactly one site (line 178), where it is read by three literal keys: `counts.applied`, `counts.deviated`, `counts[\"n/a\"]`. The guard decides only whether a status OUTSIDE those three gets a key created; under `true` such a row writes `counts[<that status>] = NaN`, a key line 178 never names. SENSITIVITY CONTROL, measured rather than asserted: the input class that exercises the guard at all is a manifest row whose Status column is a word outside the three counted ones, and it is ordinary and reachable — `readManifest` lower-cases the Status cell and validates no vocabulary, so `| rule-four | pending | tbd |` is accepted verbatim. A fixture carrying exactly that row (plus an `n/a` row, so all three counted branches stay live) renders BYTE-IDENTICAL under the mutant, as do a fresh `runward init` mission and examples/request-triage through the real CLI. I do not offer a nearby input where the two differ, because none exists: the guard cannot reach any of the three keys line 178 reads. What would make it matter is a CODE change, not an input — if that sentence ever summed `Object.values(counts)` instead of naming three keys, a `pending` row would flip it. That is a reason to keep the guard; it is not a reason to call this mutant live.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001ConditionalExpression\u0001true\u0001counts[r.status] !== undefined\u0001if (counts[r.status] !== undefined)"
  },
  {
   "key": "150|13|150|43|ConditionalExpression|false",
   "line": 150,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "Measured on examples/request-triage through the real CLI: `**23 applied - 0 deviated - 13 n/a** across 36 accounted rule(s)` becomes `**0 applied - 0 deviated - 0 n/a** across 36 accounted rule(s)`. RECIPE: `cp -r examples/request-triage /tmp/m && runward compliance iso-42001 -p /tmp/m`, then read section 2 of runward/compliance/iso-42001-readiness.md. That sentence is the pack's own summary of what feeds the Statement of Applicability's implementation-status column (ISO/IEC 42001 6.1.3); after the mutation it reports zero implemented controls on a mission whose conformance table, three lines below and unchanged, lists 23 `applied` rows — the document contradicts itself and understates the mission to an assessor. Nothing catches it: no assertion in test/unit/compliance.test.js or test/smoke.js reads any count in the ISO draft (measured: applied, real CLI re-run, all sixteen pass). Invisible on a fresh mission, where the true counts are 0/0/0 anyway; it appears exactly on the missions that have done work.",
   "argument": "Measured on examples/request-triage through the real CLI: `**23 applied - 0 deviated - 13 n/a** across 36 accounted rule(s)` becomes `**0 applied - 0 deviated - 0 n/a** across 36 accounted rule(s)`. RECIPE: `cp -r examples/request-triage /tmp/m && runward compliance iso-42001 -p /tmp/m`, then read section 2 of runward/compliance/iso-42001-readiness.md. That sentence is the pack's own summary of what feeds the Statement of Applicability's implementation-status column (ISO/IEC 42001 6.1.3); after the mutation it reports zero implemented controls on a mission whose conformance table, three lines below and unchanged, lists 23 `applied` rows — the document contradicts itself and understates the mission to an assessor. Nothing catches it: no assertion in test/unit/compliance.test.js or test/smoke.js reads any count in the ISO draft (measured: applied, real CLI re-run, all sixteen pass). Invisible on a fresh mission, where the true counts are 0/0/0 anyway; it appears exactly on the missions that have done work.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001ConditionalExpression\u0001false\u0001counts[r.status] !== undefined\u0001if (counts[r.status] !== undefined)"
  },
  {
   "key": "150|13|150|43|EqualityOperator|counts[r.status] === undefined",
   "line": 150,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "Observably the same defect as the `-> false` twin on this line, measured the same way and byte-identical to it: on examples/request-triage the summary reads `**0 applied - 0 deviated - 0 n/a** across 36 accounted rule(s)` against 23 `applied` rows in the table below. The inversion counts only the statuses the object does NOT hold, so the three named keys stay at zero forever while stray NaN keys accumulate for words like `pending` — and those three keys are the only ones line 178 reads. Recipe and net evidence as recorded on the `-> false` twin; nothing in the unit suite or in test/smoke.js catches it.",
   "argument": "Observably the same defect as the `-> false` twin on this line, measured the same way and byte-identical to it: on examples/request-triage the summary reads `**0 applied - 0 deviated - 0 n/a** across 36 accounted rule(s)` against 23 `applied` rows in the table below. The inversion counts only the statuses the object does NOT hold, so the three named keys stay at zero forever while stray NaN keys accumulate for words like `pending` — and those three keys are the only ones line 178 reads. Recipe and net evidence as recorded on the `-> false` twin; nothing in the unit suite or in test/smoke.js catches it.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001EqualityOperator\u0001counts[r.status] === undefined\u0001counts[r.status] !== undefined\u0001if (counts[r.status] !== undefined)"
  },
  {
   "key": "151|13|151|31|UpdateOperator|counts[r.status]--",
   "line": 151,
   "mutator": "UpdateOperator",
   "filing": "hole",
   "evidence": "Measured on examples/request-triage through the real CLI: `**23 applied - 0 deviated - 13 n/a**` becomes `**-23 applied - 0 deviated - -13 n/a**`. Same recipe and same silence as the two guard mutants on the line above (`cp -r examples/request-triage /tmp/m && runward compliance iso-42001 -p /tmp/m`, section 2). This entry differs from them in that the emitted document now carries a NEGATIVE count of implemented controls, which is not merely wrong but impossible for the field it feeds — an ISO 42001 Statement of Applicability implementation-status tally. Nothing in the unit suite or in test/smoke.js reads it (measured, applied and re-run).",
   "argument": "Measured on examples/request-triage through the real CLI: `**23 applied - 0 deviated - 13 n/a**` becomes `**-23 applied - 0 deviated - -13 n/a**`. Same recipe and same silence as the two guard mutants on the line above (`cp -r examples/request-triage /tmp/m && runward compliance iso-42001 -p /tmp/m`, section 2). This entry differs from them in that the emitted document now carries a NEGATIVE count of implemented controls, which is not merely wrong but impossible for the field it feeds — an ISO 42001 Statement of Applicability implementation-status tally. Nothing in the unit suite or in test/smoke.js reads it (measured, applied and re-run).",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001UpdateOperator\u0001counts[r.status]--\u0001counts[r.status]++\u0001counts[r.status]++;"
  },
  {
   "key": "152|12|152|58|StringLiteral|\"\"",
   "line": 152,
   "mutator": "StringLiteral",
   "filing": "defence-in-depth",
   "evidence": "Caught by test/smoke.js, the end-to-end leg. Measured, not reasoned: the mutant was applied to dist/lib/compliance.js and the real `runward compliance iso-42001` run on a fresh mission and on a copy of examples/request-triage; in both, smoke's `compMd.includes(\"assessment-readiness draft\")` (the ADR-0016 compliance block of test/smoke.js) goes from true to false. It survives the mutation measurement because stryker.config.json's commandRunner is scripts/mutation-testcmd.sh, which runs `node --test test/unit/*.test.js` and nothing else; smoke.js is in `npm test` and in CI, outside that command. This is precisely the pass-2 case the register's Method section describes.",
   "argument": "Caught by test/smoke.js, the end-to-end leg. Measured, not reasoned: the mutant was applied to dist/lib/compliance.js and the real `runward compliance iso-42001` run on a fresh mission and on a copy of examples/request-triage; in both, smoke's `compMd.includes(\"assessment-readiness draft\")` (the ADR-0016 compliance block of test/smoke.js) goes from true to false. It survives the mutation measurement because stryker.config.json's commandRunner is scripts/mutation-testcmd.sh, which runs `node --test test/unit/*.test.js` and nothing else; smoke.js is in `npm test` and in CI, outside that command. This is precisely the pass-2 case the register's Method section describes.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"# ISO/IEC 42001 — assessment-readiness draft\"\u0001L.push(\"# ISO/IEC 42001 — assessment-readiness draft\");"
  },
  {
   "key": "153|12|153|14|StringLiteral|\"Stryker was here!\"",
   "line": 153,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 2 of the fresh draft, the blank between the H1 and the draft-framing blockquote.",
   "argument": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 2 of the fresh draft, the blank between the H1 and the draft-framing blockquote.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");"
  },
  {
   "key": "155|12|155|127|StringLiteral|\"\"",
   "line": 155,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Measured: line 4 of the draft — `> deterministically from ratified engineering artifacts (no model call, nothing scraped or run). It populates the` — becomes empty, which also splits the header blockquote in two and leaves the sentence above it ending on `on 2026-01-01,`. The claim is unique in the document: grepping the emitted draft, `model call` and `scraped or run` each occur on exactly one line, that one. What a regulated reader loses is the pack's PROVENANCE — that it was assembled deterministically from artifacts at rest, with no model call and nothing scraped or run — which is the whole of ADR-0016's promise and the difference between this document and one an LLM wrote. RECIPE: any mission; `runward compliance iso-42001`; `grep -c 'no model call' runward/compliance/iso-42001-readiness.md` returns 1 before and 0 after. Nothing catches it: the unit suite's header assertions read `/Draft/`, `/not a compliance claim/` and the `Lens:` stamp, all on other lines (measured: applied, real CLI re-run, all nine unit and all seven smoke ISO assertions pass).",
   "argument": "Measured: line 4 of the draft — `> deterministically from ratified engineering artifacts (no model call, nothing scraped or run). It populates the` — becomes empty, which also splits the header blockquote in two and leaves the sentence above it ending on `on 2026-01-01,`. The claim is unique in the document: grepping the emitted draft, `model call` and `scraped or run` each occur on exactly one line, that one. What a regulated reader loses is the pack's PROVENANCE — that it was assembled deterministically from artifacts at rest, with no model call and nothing scraped or run — which is the whole of ADR-0016's promise and the difference between this document and one an LLM wrote. RECIPE: any mission; `runward compliance iso-42001`; `grep -c 'no model call' runward/compliance/iso-42001-readiness.md` returns 1 before and 0 after. Nothing catches it: the unit suite's header assertions read `/Draft/`, `/not a compliance claim/` and the `Lens:` stamp, all on other lines (measured: applied, real CLI re-run, all nine unit and all seven smoke ISO assertions pass).",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"> deterministically from ratified engineering artifacts (no model call, nothing scraped or run). It populates the\"\u0001L.push(\"> deterministically from ratified engineering artifacts (no model call, nothing scraped or run). It populates the\");"
  },
  {
   "key": "156|12|156|129|StringLiteral|\"\"",
   "line": 156,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Measured: line 5 — `> **technical-evidence layer and its index**; the applicability, risk-acceptance, policy and management sign-off it` — becomes empty; `technical-evidence layer` occurs on exactly one line of the emitted draft, that one. The lost claim is the document's own SCOPE: which layer of an AI management system it populates. Its complement survives — the `## Required from the operator / organization (runward cannot produce this)` section at line 46 and its seven items still tell the reader what runward cannot supply — so the loss is one-sided rather than total; the positive bound is stated nowhere else, and a reader who does not know which layer the pack covers cannot place it in an ISO 42001 assessment. RECIPE: any mission; `runward compliance iso-42001`; `grep -c 'technical-evidence layer' runward/compliance/iso-42001-readiness.md` returns 1 before and 0 after. Nothing catches it (measured: applied, real CLI re-run, all nine unit and all seven smoke ISO assertions pass).",
   "argument": "Measured: line 5 — `> **technical-evidence layer and its index**; the applicability, risk-acceptance, policy and management sign-off it` — becomes empty; `technical-evidence layer` occurs on exactly one line of the emitted draft, that one. The lost claim is the document's own SCOPE: which layer of an AI management system it populates. Its complement survives — the `## Required from the operator / organization (runward cannot produce this)` section at line 46 and its seven items still tell the reader what runward cannot supply — so the loss is one-sided rather than total; the positive bound is stated nowhere else, and a reader who does not know which layer the pack covers cannot place it in an ISO 42001 assessment. RECIPE: any mission; `runward compliance iso-42001`; `grep -c 'technical-evidence layer' runward/compliance/iso-42001-readiness.md` returns 1 before and 0 after. Nothing catches it (measured: applied, real CLI re-run, all nine unit and all seven smoke ISO assertions pass).",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"> **technical-evidence layer and its index**; the applicability, risk-acceptance, policy and management sign-off it\"\u0001L.push(\"> **technical-evidence layer and its index**; the applicability, risk-acceptance, policy and management sign-off it\");"
  },
  {
   "key": "157|12|157|133|StringLiteral|\"\"",
   "line": 157,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Measured: line 6 — `> cannot invent are listed under \"Required from the operator\". This is **supporting evidence**, never certification —` — becomes empty. The claim survives on lines this mutant does not touch, and deliberately not on its immediate neighbour, so the argument is not circular: line 1, the H1 `# ISO/IEC 42001 — assessment-readiness draft`, and line 3, `> **Draft, incomplete — not a compliance claim.**`, both still tell the reader the document is not a certification, and line 7 adds `only an accredited body certifies an AI management system`. The pointer to the operator section is carried by that section's own heading at line 46. Note for the reader who checks: smoke's `compMd.includes(\"Required from the operator\")` still passes here, and it passes on the heading, not on this line. Surface: the human-read markdown; what goes is framing that is stated three more times.",
   "argument": "Measured: line 6 — `> cannot invent are listed under \"Required from the operator\". This is **supporting evidence**, never certification —` — becomes empty. The claim survives on lines this mutant does not touch, and deliberately not on its immediate neighbour, so the argument is not circular: line 1, the H1 `# ISO/IEC 42001 — assessment-readiness draft`, and line 3, `> **Draft, incomplete — not a compliance claim.**`, both still tell the reader the document is not a certification, and line 7 adds `only an accredited body certifies an AI management system`. The pointer to the operator section is carried by that section's own heading at line 46. Note for the reader who checks: smoke's `compMd.includes(\"Required from the operator\")` still passes here, and it passes on the heading, not on this line. Surface: the human-read markdown; what goes is framing that is stated three more times.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"> cannot invent are listed under \\\"Required from the operator\\\". This is **supporting evidence**, never certification —\"\u0001L.push(\"> cannot invent are listed under \\\"Required from the operator\\\". This is **supporting evidence**, never certification —\");"
  },
  {
   "key": "158|12|158|129|StringLiteral|\"\"",
   "line": 158,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Measured: line 7 — `> only an accredited body certifies an AI management system. Verify the current ISO/IEC 42001 text before an audit.` — becomes empty. Both halves survive elsewhere, on lines this mutant does not touch: the certification point on line 1 (`assessment-readiness draft`), line 3 (`not a compliance claim`) and line 6 (`**supporting evidence**, never certification`); the verify-the-text instruction on line 58, `_Regime mapping is dated engineering framing, not legal advice; ISO Annex A control counts/templates are behind the paywalled standard — confirm against the purchased text._`. This filing and its twin on line 6 lean on each other's neighbours but not on each other — lines 1 and 3 anchor both, and neither mutant touches them. Surface: the human-read markdown.",
   "argument": "Measured: line 7 — `> only an accredited body certifies an AI management system. Verify the current ISO/IEC 42001 text before an audit.` — becomes empty. Both halves survive elsewhere, on lines this mutant does not touch: the certification point on line 1 (`assessment-readiness draft`), line 3 (`not a compliance claim`) and line 6 (`**supporting evidence**, never certification`); the verify-the-text instruction on line 58, `_Regime mapping is dated engineering framing, not legal advice; ISO Annex A control counts/templates are behind the paywalled standard — confirm against the purchased text._`. This filing and its twin on line 6 lean on each other's neighbours but not on each other — lines 1 and 3 anchor both, and neither mutant touches them. Surface: the human-read markdown.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"> only an accredited body certifies an AI management system. Verify the current ISO/IEC 42001 text before an audit.\"\u0001L.push(\"> only an accredited body certifies an AI management system. Verify the current ISO/IEC 42001 text before an audit.\");"
  },
  {
   "key": "160|12|160|14|StringLiteral|\"Stryker was here!\"",
   "line": 160,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 9 of the fresh draft, the blank between the header blockquote and the declared-non-scope blockquote.",
   "argument": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 9 of the fresh draft, the blank between the header blockquote and the declared-non-scope blockquote.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#2"
  },
  {
   "key": "163|12|163|70|StringLiteral|\"\"",
   "line": 163,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "The prefix goes and the constant stays: measured, line 10 becomes `A green row proves a decision was traced to resolving, non-empty ...` instead of `> **Declared non-scope of every green row (ADR-0040).** A green row proves ...`. GATE_NON_SCOPE is concatenated unchanged, which is why test/unit/compliance.test.js's `MARK = \"It never proves the evidence truly implements\"` assertion still passes — and that test states the project's own position on this exact question: the assertion is on a fragment of the text 'because what must ship is the sentence a reader sees, not an identifier a bundler could rename'. The sentence a reader sees ships in full. What goes is the label, the ADR-0040 reference and the blockquote marker; the quantifier the label carries ('every green row') is restated by the constant's own opening words, `A green row proves...`, which are generic over green rows. Surface: the human-read markdown.",
   "argument": "The prefix goes and the constant stays: measured, line 10 becomes `A green row proves a decision was traced to resolving, non-empty ...` instead of `> **Declared non-scope of every green row (ADR-0040).** A green row proves ...`. GATE_NON_SCOPE is concatenated unchanged, which is why test/unit/compliance.test.js's `MARK = \"It never proves the evidence truly implements\"` assertion still passes — and that test states the project's own position on this exact question: the assertion is on a fragment of the text 'because what must ship is the sentence a reader sees, not an identifier a bundler could rename'. The sentence a reader sees ships in full. What goes is the label, the ADR-0040 reference and the blockquote marker; the quantifier the label carries ('every green row') is restated by the constant's own opening words, `A green row proves...`, which are generic over green rows. Surface: the human-read markdown.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"> **Declared non-scope of every green row (ADR-0040).** \"\u0001L.push(\"> **Declared non-scope of every green row (ADR-0040).** \" + GATE_NON_SCOPE);"
  },
  {
   "key": "164|12|164|14|StringLiteral|\"Stryker was here!\"",
   "line": 164,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 11 of the fresh draft, the blank between the declared-non-scope blockquote and the `## 1.` heading.",
   "argument": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 11 of the fresh draft, the blank between the declared-non-scope blockquote and the `## 1.` heading.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#3"
  },
  {
   "key": "165|12|165|66|StringLiteral|\"\"",
   "line": 165,
   "mutator": "StringLiteral",
   "filing": "defence-in-depth",
   "evidence": "Caught by test/smoke.js. Measured: mutant applied to dist/lib/compliance.js, real `runward compliance iso-42001` run on a fresh mission and on a copy of examples/request-triage; in both, smoke's `compMd.includes(\"Agentic-risk coverage (OWASP ASI\")` goes from true to false. Survives the mutation measurement for the reason recorded on the title mutant of this function: stryker.config.json's test command is `node --test test/unit/*.test.js` only, and smoke.js sits outside it, in `npm test` and in CI.",
   "argument": "Caught by test/smoke.js. Measured: mutant applied to dist/lib/compliance.js, real `runward compliance iso-42001` run on a fresh mission and on a copy of examples/request-triage; in both, smoke's `compMd.includes(\"Agentic-risk coverage (OWASP ASI\")` goes from true to false. Survives the mutation measurement for the reason recorded on the title mutant of this function: stryker.config.json's test command is `node --test test/unit/*.test.js` only, and smoke.js sits outside it, in `npm test` and in CI.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"## 1. Agentic-risk coverage (OWASP ASI → your rules)\"\u0001L.push(\"## 1. Agentic-risk coverage (OWASP ASI → your rules)\");"
  },
  {
   "key": "166|12|166|14|StringLiteral|\"Stryker was here!\"",
   "line": 166,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 13 of the fresh draft, the blank between the `## 1.` heading and its lede sentence.",
   "argument": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 13 of the fresh draft, the blank between the `## 1.` heading and its lede sentence.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#4"
  },
  {
   "key": "168|12|168|14|StringLiteral|\"Stryker was here!\"",
   "line": 168,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 15 of the fresh draft, the blank between the section-1 lede and the coverage table's header row. Noted because a reader will ask, and flagged as spec-derived rather than measured (there is no markdown renderer in this tree): by the GFM spec a table cannot interrupt a paragraph, so with that separator filled the coverage block would display as pipe-delimited text rather than a table. That is a display consequence, which is the filing; the ten rows are emitted verbatim and unchanged either way.",
   "argument": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 15 of the fresh draft, the blank between the section-1 lede and the coverage table's header row. Noted because a reader will ask, and flagged as spec-derived rather than measured (there is no markdown renderer in this tree): by the GFM spec a table cannot interrupt a paragraph, so with that separator filled the coverage block would display as pipe-delimited text rather than a table. That is a display consequence, which is the filing; the ten rows are emitted verbatim and unchanged either way.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#5"
  },
  {
   "key": "169|12|169|50|StringLiteral|\"\"",
   "line": 169,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Measured on both missions: exactly one line changes — line 16 of the fresh draft, `| ASI | Risk | Rules addressing it |`, goes empty; the delimiter row and all ten data rows are untouched. No claim is lost: every ASI id, its OWASP label and its rule list are still emitted verbatim, and the same coverage is carried in machine form by the OSCAL component-definition's asi-01..asi-10 implemented-requirements, which renderOscal builds from the same inputs and which this mutant does not touch. What changes is the table's column labels. Noted, spec-derived rather than measured: without a header row a GFM table does not render as a table, so the section would display as pipe-delimited text — a display consequence, which is the filing. Nothing keys on it: smoke's `/ASI0[1-9] \\|/` reads the data rows and still passes (measured, applied and re-run).",
   "argument": "Measured on both missions: exactly one line changes — line 16 of the fresh draft, `| ASI | Risk | Rules addressing it |`, goes empty; the delimiter row and all ten data rows are untouched. No claim is lost: every ASI id, its OWASP label and its rule list are still emitted verbatim, and the same coverage is carried in machine form by the OSCAL component-definition's asi-01..asi-10 implemented-requirements, which renderOscal builds from the same inputs and which this mutant does not touch. What changes is the table's column labels. Noted, spec-derived rather than measured: without a header row a GFM table does not render as a table, so the section would display as pipe-delimited text — a display consequence, which is the filing. Nothing keys on it: smoke's `/ASI0[1-9] \\|/` reads the data rows and still passes (measured, applied and re-run).",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"| ASI | Risk | Rules addressing it |\"\u0001L.push(\"| ASI | Risk | Rules addressing it |\");"
  },
  {
   "key": "170|12|170|27|StringLiteral|\"\"",
   "line": 170,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Measured on both missions: exactly one line changes — the GFM delimiter row `|---|---|---|` goes empty, and the header row and all ten data rows are untouched. This is the line that makes the block a table at all, so the display consequence is the whole of the mutant: every ASI id, label and rule list is still emitted verbatim, in order, and the machine form of the same coverage (renderOscal's asi-01..asi-10 implemented-requirements) is untouched. Nothing keys on it — smoke's `/ASI0[1-9] \\|/` reads the data rows and still passes (measured, applied and re-run), and no unit assertion reads the ISO coverage table.",
   "argument": "Measured on both missions: exactly one line changes — the GFM delimiter row `|---|---|---|` goes empty, and the header row and all ten data rows are untouched. This is the line that makes the block a table at all, so the display consequence is the whole of the mutant: every ASI id, label and rule list is still emitted verbatim, in order, and the machine form of the same coverage (renderOscal's asi-01..asi-10 implemented-requirements) is untouched. Nothing keys on it — smoke's `/ASI0[1-9] \\|/` reads the data rows and still passes (measured, applied and re-run), and no unit assertion reads the ISO coverage table.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"|---|---|---|\"\u0001L.push(\"|---|---|---|\");"
  },
  {
   "key": "171|47|174|6|BlockStatement|{}",
   "line": 171,
   "mutator": "BlockStatement",
   "filing": "defence-in-depth",
   "evidence": "Caught by test/smoke.js. Measured: with the loop body emptied the ten coverage rows disappear (fresh draft 60 lines -> 50; example 101 -> 91) and smoke's `/ASI0[1-9] \\|/` goes from true to false on both missions. Survives the mutation measurement because stryker.config.json's test command is `node --test test/unit/*.test.js` only; smoke.js is in `npm test` and in CI, outside it.",
   "argument": "Caught by test/smoke.js. Measured: with the loop body emptied the ten coverage rows disappear (fresh draft 60 lines -> 50; example 101 -> 91) and smoke's `/ASI0[1-9] \\|/` goes from true to false on both missions. Survives the mutation measurement because stryker.config.json's test command is `node --test test/unit/*.test.js` only; smoke.js is in `npm test` and in CI, outside it.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001BlockStatement\u0001{}\u0001{ const slugs = inputs.asiCoverage.get(id) ?? []; L.push(`| ${id} | ${ASI_LABELS[id]} | ${slugs.length ? slugs.map((s) => `\\`${s}\\``).join(\", \") : \"**no rule mapped — gap to assess**\"} |`); }\u0001for (const id of Object.keys(ASI_LABELS)) {"
  },
  {
   "key": "172|23|172|55|LogicalOperator|inputs.asiCoverage.get(id) && []",
   "line": 172,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "FALSE GAP ON EVERY ROW. `inputs.asiCoverage.get(id) && []` yields `[]` for every id — an array, empty or not, is truthy — so `slugs.length` is 0 always and every cell takes the fallback. Measured on examples/request-triage through the real CLI: all ten coverage rows change, e.g. `| ASI01 | Agent Goal Hijack | `frontier-deterministic-boundary`, `hexa-move-deterministic-out`, `security-prompt-injection` |` becomes `| ASI01 | Agent Goal Hijack | **no rule mapped — gap to assess** |`. RECIPE: `cp -r examples/request-triage /tmp/m && runward compliance iso-42001 -p /tmp/m`; section 1 then declares an unassessed agentic-security gap on all ten OWASP ASI categories of a mission that maps rules to all ten. This section is the ISO 42001 6.1.2/6.1.3 feed — a reader acts on it by opening ten risk-assessment gaps that do not exist, and by concluding that the engineering corpus addresses nothing. Nothing catches it: smoke's `/ASI0[1-9] \\|/` matches the row prefix, not the cell, so it still passes (measured, applied and re-run), and no unit assertion reads the ISO coverage cells.",
   "argument": "FALSE GAP ON EVERY ROW. `inputs.asiCoverage.get(id) && []` yields `[]` for every id — an array, empty or not, is truthy — so `slugs.length` is 0 always and every cell takes the fallback. Measured on examples/request-triage through the real CLI: all ten coverage rows change, e.g. `| ASI01 | Agent Goal Hijack | `frontier-deterministic-boundary`, `hexa-move-deterministic-out`, `security-prompt-injection` |` becomes `| ASI01 | Agent Goal Hijack | **no rule mapped — gap to assess** |`. RECIPE: `cp -r examples/request-triage /tmp/m && runward compliance iso-42001 -p /tmp/m`; section 1 then declares an unassessed agentic-security gap on all ten OWASP ASI categories of a mission that maps rules to all ten. This section is the ISO 42001 6.1.2/6.1.3 feed — a reader acts on it by opening ten risk-assessment gaps that do not exist, and by concluding that the engineering corpus addresses nothing. Nothing catches it: smoke's `/ASI0[1-9] \\|/` matches the row prefix, not the cell, so it still passes (measured, applied and re-run), and no unit assertion reads the ISO coverage cells.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001LogicalOperator\u0001inputs.asiCoverage.get(id) && []\u0001inputs.asiCoverage.get(id) ?? []\u0001const slugs = inputs.asiCoverage.get(id) ?? [];"
  },
  {
   "key": "172|53|172|55|ArrayDeclaration|[\"Stryker was here\"]",
   "line": 172,
   "mutator": "ArrayDeclaration",
   "filing": "equivalent",
   "evidence": "The mutant seeds the fallback of `inputs.asiCoverage.get(id) ?? []` with a non-empty array, so it is observable only when the Map has no entry for an id the loop visits. SENSITIVITY CONTROL — the nearby input where the two DO differ, measured: calling renderIso42001Readiness with a hand-built inputs object whose asiCoverage Map holds only `ASI01` produces, under the mutant, nine rows reading ``| ASI02 | Tool Misuse & Exploitation | `Stryker was here` |`` where the baseline reads `**no rule mapped — gap to assess**` — an invented rule where the pack should declare a gap. WHY IT CANNOT OCCUR in the shipped product: the only caller is complianceCommand, which always builds its inputs with gatherComplianceInputs, and that function's first act is `for (const id of Object.keys(ASI_LABELS)) asiCoverage.set(id, [])` — over the same ASI_LABELS object whose `Object.keys` this loop iterates. Every id the loop visits therefore has an entry, and the `??` is a type-level courtesy for `Map.get`'s signature, never a runtime path. Measured identical on a fresh `runward init` mission and on examples/request-triage through the real CLI, and on three synthetic fixtures. The exception is stated plainly: a third-party caller who hand-builds a short Map, as some tests in this repo do for renderOscal, would see the difference; no path inside the package does.",
   "argument": "The mutant seeds the fallback of `inputs.asiCoverage.get(id) ?? []` with a non-empty array, so it is observable only when the Map has no entry for an id the loop visits. SENSITIVITY CONTROL — the nearby input where the two DO differ, measured: calling renderIso42001Readiness with a hand-built inputs object whose asiCoverage Map holds only `ASI01` produces, under the mutant, nine rows reading ``| ASI02 | Tool Misuse & Exploitation | `Stryker was here` |`` where the baseline reads `**no rule mapped — gap to assess**` — an invented rule where the pack should declare a gap. WHY IT CANNOT OCCUR in the shipped product: the only caller is complianceCommand, which always builds its inputs with gatherComplianceInputs, and that function's first act is `for (const id of Object.keys(ASI_LABELS)) asiCoverage.set(id, [])` — over the same ASI_LABELS object whose `Object.keys` this loop iterates. Every id the loop visits therefore has an entry, and the `??` is a type-level courtesy for `Map.get`'s signature, never a runtime path. Measured identical on a fresh `runward init` mission and on examples/request-triage through the real CLI, and on three synthetic fixtures. The exception is stated plainly: a third-party caller who hand-builds a short Map, as some tests in this repo do for renderOscal, would see the difference; no path inside the package does.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001ArrayDeclaration\u0001[\"Stryker was here\"]\u0001[]\u0001const slugs = inputs.asiCoverage.get(id) ?? [];"
  },
  {
   "key": "173|16|173|146|StringLiteral|``",
   "line": 173,
   "mutator": "StringLiteral",
   "filing": "defence-in-depth",
   "evidence": "Caught by test/smoke.js. Measured: the ten coverage rows become ten empty lines and smoke's `/ASI0[1-9] \\|/` goes from true to false on a fresh mission and on a copy of examples/request-triage. Same reason for surviving the measurement as its siblings on this table: stryker.config.json runs `node --test test/unit/*.test.js` only, and smoke.js is outside that command though inside `npm test` and CI.",
   "argument": "Caught by test/smoke.js. Measured: the ten coverage rows become ten empty lines and smoke's `/ASI0[1-9] \\|/` goes from true to false on a fresh mission and on a copy of examples/request-triage. Same reason for surviving the measurement as its siblings on this table: stryker.config.json runs `node --test test/unit/*.test.js` only, and smoke.js is outside that command though inside `npm test` and CI.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001``\u0001`| ${id} | ${ASI_LABELS[id]} | ${slugs.length ? slugs.map((s) => `\\`${s}\\``).join(\", \") : \"**no rule mapped — gap to assess**\"} |`\u0001L.push(`| ${id} | ${ASI_LABELS[id]} | ${slugs.length ? slugs.map((s) => `\\`${s}\\``).join(\", \") : \"**no rule mapped — gap to assess**\"} |`);"
  },
  {
   "key": "173|74|173|91|ArrowFunction|() => undefined",
   "line": 173,
   "mutator": "ArrowFunction",
   "filing": "hole",
   "evidence": "`slugs.map(() => undefined)` makes every mapped rule name disappear while the commas that separated them remain. Measured on examples/request-triage through the real CLI: ``| ASI01 | Agent Goal Hijack | `frontier-deterministic-boundary`, `hexa-move-deterministic-out`, `security-prompt-injection` |`` becomes `| ASI01 | Agent Goal Hijack | , ,  |`, and ASI02's seven rules become `, , , , , ,  |`. RECIPE: `cp -r examples/request-triage /tmp/m && runward compliance iso-42001 -p /tmp/m`; section 1's third column names not one rule. The document still asserts that each risk IS addressed — it does not fall back to `**no rule mapped — gap to assess**` — while naming no evidence for the assertion, which is the worse of the two failures: an assessor cannot follow a single control back to a rule. Nothing catches it, including `assert.ok(!md.includes(\"undefined\"))` in test/unit/compliance.test.js, because Array.prototype.join renders undefined as the empty string (measured: applied, real CLI re-run, all nine unit and all seven smoke ISO assertions pass).",
   "argument": "`slugs.map(() => undefined)` makes every mapped rule name disappear while the commas that separated them remain. Measured on examples/request-triage through the real CLI: ``| ASI01 | Agent Goal Hijack | `frontier-deterministic-boundary`, `hexa-move-deterministic-out`, `security-prompt-injection` |`` becomes `| ASI01 | Agent Goal Hijack | , ,  |`, and ASI02's seven rules become `, , , , , ,  |`. RECIPE: `cp -r examples/request-triage /tmp/m && runward compliance iso-42001 -p /tmp/m`; section 1's third column names not one rule. The document still asserts that each risk IS addressed — it does not fall back to `**no rule mapped — gap to assess**` — while naming no evidence for the assertion, which is the worse of the two failures: an assessor cannot follow a single control back to a rule. Nothing catches it, including `assert.ok(!md.includes(\"undefined\"))` in test/unit/compliance.test.js, because Array.prototype.join renders undefined as the empty string (measured: applied, real CLI re-run, all nine unit and all seven smoke ISO assertions pass).",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001ArrowFunction\u0001() => undefined\u0001(s) => `\\`${s}\\``\u0001L.push(`| ${id} | ${ASI_LABELS[id]} | ${slugs.length ? slugs.map((s) => `\\`${s}\\``).join(\", \") : \"**no rule mapped — gap to assess**\"} |`);"
  },
  {
   "key": "173|81|173|91|StringLiteral|``",
   "line": 173,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Same defect and byte-identical output to the `(s) => undefined` mutant on this line, measured on the same missions: the backtick-wrapped slug becomes an empty template, so ``| ASI01 | Agent Goal Hijack | `frontier-deterministic-boundary`, ... |`` becomes `| ASI01 | Agent Goal Hijack | , ,  |`. Recipe, consequence and net evidence as recorded there; separate operator, same hole.",
   "argument": "Same defect and byte-identical output to the `(s) => undefined` mutant on this line, measured on the same missions: the backtick-wrapped slug becomes an empty template, so ``| ASI01 | Agent Goal Hijack | `frontier-deterministic-boundary`, ... |`` becomes `| ASI01 | Agent Goal Hijack | , ,  |`. Recipe, consequence and net evidence as recorded there; separate operator, same hole.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001``\u0001`\\`${s}\\``\u0001L.push(`| ${id} | ${ASI_LABELS[id]} | ${slugs.length ? slugs.map((s) => `\\`${s}\\``).join(\", \") : \"**no rule mapped — gap to assess**\"} |`);"
  },
  {
   "key": "173|98|173|102|StringLiteral|\"\"",
   "line": 173,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Measured on examples/request-triage: one line class changes and only the separator — the cell `` `frontier-deterministic-boundary`, `hexa-move-deterministic-out`, `security-prompt-injection` `` becomes the same three backtick-wrapped slugs run together with no `, ` between them. Every rule slug is still present, in the same cell, in the same order, for the same ASI id; nothing is added, removed or renamed. That is the line between this mutant and its two siblings on the same statement, which DELETE the slugs and are filed holes. Rows with a single mapped rule are byte-identical, so it is observable only where an ASI has two or more rules. Surface: the human-read markdown; the machine form of the same coverage is renderOscal's component-definition, untouched.",
   "argument": "Measured on examples/request-triage: one line class changes and only the separator — the cell `` `frontier-deterministic-boundary`, `hexa-move-deterministic-out`, `security-prompt-injection` `` becomes the same three backtick-wrapped slugs run together with no `, ` between them. Every rule slug is still present, in the same cell, in the same order, for the same ASI id; nothing is added, removed or renamed. That is the line between this mutant and its two siblings on the same statement, which DELETE the slugs and are filed holes. Rows with a single mapped rule are byte-identical, so it is observable only where an ASI has two or more rules. Surface: the human-read markdown; the machine form of the same coverage is renderOscal's component-definition, untouched.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\", \"\u0001L.push(`| ${id} | ${ASI_LABELS[id]} | ${slugs.length ? slugs.map((s) => `\\`${s}\\``).join(\", \") : \"**no rule mapped — gap to assess**\"} |`);"
  },
  {
   "key": "173|106|173|142|StringLiteral|\"\"",
   "line": 173,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "The fallback string is the pack's declaration that an OWASP ASI category has NO rule mapped and is therefore a gap to assess; the mutant empties it, so the cell renders blank and the gap stops being declared. Measured on a synthetic mission whose corpus maps only ASI01 and ASI02: eight rows go from `| ASI03 | Identity & Privilege Abuse | **no rule mapped — gap to assess** |` to `| ASI03 | Identity & Privilege Abuse |  |`. NOT reachable on the shipped corpus — measured IDENTICAL on a fresh `runward init` mission and on examples/request-triage through the real CLI, both of which map all ten categories — which is exactly why nothing catches it and why it is filed rather than dismissed: the missions where it bites are the ones with an incomplete corpus, i.e. a brownfield repo run through `runward characterize`, or any operator who prunes rules. RECIPE: a mission whose rules/*.md front-matter `asi:` lists leave one id unmapped; `runward compliance iso-42001`; that id's row in section 1 is blank instead of declaring the gap. The CLI's own summary still prints `ASI coverage N/10 categories mapped to a rule`, so the terminal keeps telling the truth while the artefact that leaves the building stops.",
   "argument": "The fallback string is the pack's declaration that an OWASP ASI category has NO rule mapped and is therefore a gap to assess; the mutant empties it, so the cell renders blank and the gap stops being declared. Measured on a synthetic mission whose corpus maps only ASI01 and ASI02: eight rows go from `| ASI03 | Identity & Privilege Abuse | **no rule mapped — gap to assess** |` to `| ASI03 | Identity & Privilege Abuse |  |`. NOT reachable on the shipped corpus — measured IDENTICAL on a fresh `runward init` mission and on examples/request-triage through the real CLI, both of which map all ten categories — which is exactly why nothing catches it and why it is filed rather than dismissed: the missions where it bites are the ones with an incomplete corpus, i.e. a brownfield repo run through `runward characterize`, or any operator who prunes rules. RECIPE: a mission whose rules/*.md front-matter `asi:` lists leave one id unmapped; `runward compliance iso-42001`; that id's row in section 1 is blank instead of declaring the gap. The CLI's own summary still prints `ASI coverage N/10 categories mapped to a rule`, so the terminal keeps telling the truth while the artefact that leaves the building stops.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"**no rule mapped — gap to assess**\"\u0001L.push(`| ${id} | ${ASI_LABELS[id]} | ${slugs.length ? slugs.map((s) => `\\`${s}\\``).join(\", \") : \"**no rule mapped — gap to assess**\"} |`);"
  },
  {
   "key": "175|12|175|14|StringLiteral|\"Stryker was here!\"",
   "line": 175,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 28 of the fresh draft, the blank between the last coverage row and the `## 2.` heading.",
   "argument": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 28 of the fresh draft, the blank between the last coverage row and the `## 2.` heading.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#6"
  },
  {
   "key": "176|12|176|68|StringLiteral|\"\"",
   "line": 176,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Measured on both missions: exactly one line changes — `## 2. Control-implementation status (rule conformance)` goes empty. No claim moves with it: the section's lede, the line immediately below, still opens `Feeds the Statement of Applicability's implementation-status + evidence columns (6.1.3). From your mission's manifests: ...`, so the block self-identifies, and its counts and rows are untouched. What is lost is the heading that separates it from section 1; a reader skimming headings alone would find the conformance table under `## 1. Agentic-risk coverage`. Nothing keys on it — no unit assertion and none of smoke's seven ISO assertions reads it (measured, applied and re-run). Surface: the human-read markdown.",
   "argument": "Measured on both missions: exactly one line changes — `## 2. Control-implementation status (rule conformance)` goes empty. No claim moves with it: the section's lede, the line immediately below, still opens `Feeds the Statement of Applicability's implementation-status + evidence columns (6.1.3). From your mission's manifests: ...`, so the block self-identifies, and its counts and rows are untouched. What is lost is the heading that separates it from section 1; a reader skimming headings alone would find the conformance table under `## 1. Agentic-risk coverage`. Nothing keys on it — no unit assertion and none of smoke's seven ISO assertions reads it (measured, applied and re-run). Surface: the human-read markdown.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"## 2. Control-implementation status (rule conformance)\"\u0001L.push(\"## 2. Control-implementation status (rule conformance)\");"
  },
  {
   "key": "177|12|177|14|StringLiteral|\"Stryker was here!\"",
   "line": 177,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 30 of the fresh draft, the blank between the `## 2.` heading and its lede sentence.",
   "argument": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 30 of the fresh draft, the blank between the `## 2.` heading and its lede sentence.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#7"
  },
  {
   "key": "178|12|178|288|StringLiteral|``",
   "line": 178,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "The mutated template is the ONLY place this document states its control-implementation counts. Measured on examples/request-triage through the real CLI: `Feeds the Statement of Applicability's implementation-status + evidence columns (6.1.3). From your mission's manifests: **23 applied - 0 deviated - 13 n/a** across 36 accounted rule(s).` becomes an empty line. RECIPE: `cp -r examples/request-triage /tmp/m && runward compliance iso-42001 -p /tmp/m`; section 2 of the draft then opens on a bare table with no summary and no ISO clause reference. Two things go at once: the counts a reader acts on, and the mapping of the section onto ISO/IEC 42001 6.1.3, which is what makes the table an SoA feed rather than a list. It is the counterpart of the counting-guard mutants above — those falsify the numbers, this deletes them — and nothing catches either: no unit or smoke assertion reads any count in this document (measured, applied and re-run).",
   "argument": "The mutated template is the ONLY place this document states its control-implementation counts. Measured on examples/request-triage through the real CLI: `Feeds the Statement of Applicability's implementation-status + evidence columns (6.1.3). From your mission's manifests: **23 applied - 0 deviated - 13 n/a** across 36 accounted rule(s).` becomes an empty line. RECIPE: `cp -r examples/request-triage /tmp/m && runward compliance iso-42001 -p /tmp/m`; section 2 of the draft then opens on a bare table with no summary and no ISO clause reference. Two things go at once: the counts a reader acts on, and the mapping of the section onto ISO/IEC 42001 6.1.3, which is what makes the table an SoA feed rather than a list. It is the counterpart of the counting-guard mutants above — those falsify the numbers, this deletes them — and nothing catches either: no unit or smoke assertion reads any count in this document (measured, applied and re-run).",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001``\u0001`Feeds the Statement of Applicability's implementation-status + evidence columns (${cl.controlSelection}). From your mission's manifests: **${counts.applied} applied · ${counts.deviated} deviated · ${counts[\"n/a\"]} n/a** across ${inputs.conformance.length} accounted rule(s).`\u0001L.push(`Feeds the Statement of Applicability's implementation-status + evidence columns (${cl.controlSelection}). From your mission's manifests: **${counts.applied} applied · ${counts.deviated} deviated · ${counts[\"n/a\"]} n/a** across ${inputs.conformance.length} accounted rule(s).`);"
  },
  {
   "key": "179|12|179|14|StringLiteral|\"Stryker was here!\"",
   "line": 179,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 32 of the fresh draft, the blank between the section-2 lede and the conformance block. The same GFM note recorded on the occurrence-5 twin applies on missions that render the conformance TABLE rather than the no-manifest sentence.",
   "argument": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 32 of the fresh draft, the blank between the section-2 lede and the conformance block. The same GFM note recorded on the occurrence-5 twin applies on missions that render the conformance TABLE rather than the no-manifest sentence.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#8"
  },
  {
   "key": "180|9|180|40|ConditionalExpression|true",
   "line": 180,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "FALSE 'NOTHING FILLED' ON A FILLED MISSION. `if (true)` always takes the empty branch, so the conformance table is replaced by the note that no manifest was found. Measured on examples/request-triage through the real CLI: the draft goes from 101 lines to 64, the 36-row table disappears, and section 2 reads ``_No filled `Rule conformance` manifest found yet — fill the architect/floor/govern deliverables (see `runward check --strict`)._`` immediately under a sentence still saying `**23 applied - 0 deviated - 13 n/a** across 36 accounted rule(s)`. RECIPE: `cp -r examples/request-triage /tmp/m && runward compliance iso-42001 -p /tmp/m`. The evidence column an ISO 42001 assessor reads — the file: and test: pointers behind every applied rule — is gone from the pack, and the pack tells the assessor the operator did no conformance work. MEASURED, NOT REASONED, and the measurement corrected me: I predicted smoke's `/topology-(port-placement-mapped|sovereignty-by-data-class|trace-export-decision|usage-registry-present)/` assertion on the example mission would kill this, and it does not — those rule slugs also appear in the section-1 coverage table, so the regex still matches with the whole conformance table deleted. Nothing catches it.",
   "argument": "FALSE 'NOTHING FILLED' ON A FILLED MISSION. `if (true)` always takes the empty branch, so the conformance table is replaced by the note that no manifest was found. Measured on examples/request-triage through the real CLI: the draft goes from 101 lines to 64, the 36-row table disappears, and section 2 reads ``_No filled `Rule conformance` manifest found yet — fill the architect/floor/govern deliverables (see `runward check --strict`)._`` immediately under a sentence still saying `**23 applied - 0 deviated - 13 n/a** across 36 accounted rule(s)`. RECIPE: `cp -r examples/request-triage /tmp/m && runward compliance iso-42001 -p /tmp/m`. The evidence column an ISO 42001 assessor reads — the file: and test: pointers behind every applied rule — is gone from the pack, and the pack tells the assessor the operator did no conformance work. MEASURED, NOT REASONED, and the measurement corrected me: I predicted smoke's `/topology-(port-placement-mapped|sovereignty-by-data-class|trace-export-decision|usage-registry-present)/` assertion on the example mission would kill this, and it does not — those rule slugs also appear in the section-1 coverage table, so the regex still matches with the whole conformance table deleted. Nothing catches it.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001ConditionalExpression\u0001true\u0001inputs.conformance.length === 0\u0001if (inputs.conformance.length === 0) {"
  },
  {
   "key": "180|9|180|40|ConditionalExpression|false",
   "line": 180,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "The mirror of its `-> true` twin: `if (false)` never takes the empty branch, so a mission with no conformance rows gets a table header and delimiter with no rows instead of the sentence explaining why. Measured on a fresh `runward init` mission through the real CLI: ``_No filled `Rule conformance` manifest found yet — fill the architect/floor/govern deliverables (see `runward check --strict`)._`` is replaced by `| Rule | Status | Evidence | Phase |` and `|---|---|---|---|` and nothing else. RECIPE: `runward init && runward compliance iso-42001`; section 2. What goes is a caveat the reader acts on — the pack's own statement that section 2 is empty because no manifest has been filled, and the named remediation (`runward check --strict`, the architect/floor/govern deliverables). The count sentence above still says `0 applied - 0 deviated - 0 n/a across 0 accounted rule(s)`, so nothing FALSE is asserted; what is asserted is less, on precisely the mission state in which this document is most often opened for the first time. Filed hole rather than display-only because the lost line is a caveat and a remediation rather than chrome, and because both siblings on the same predicate are holes. Nothing catches it (measured: applied, real CLI re-run, all seven smoke ISO assertions pass).",
   "argument": "The mirror of its `-> true` twin: `if (false)` never takes the empty branch, so a mission with no conformance rows gets a table header and delimiter with no rows instead of the sentence explaining why. Measured on a fresh `runward init` mission through the real CLI: ``_No filled `Rule conformance` manifest found yet — fill the architect/floor/govern deliverables (see `runward check --strict`)._`` is replaced by `| Rule | Status | Evidence | Phase |` and `|---|---|---|---|` and nothing else. RECIPE: `runward init && runward compliance iso-42001`; section 2. What goes is a caveat the reader acts on — the pack's own statement that section 2 is empty because no manifest has been filled, and the named remediation (`runward check --strict`, the architect/floor/govern deliverables). The count sentence above still says `0 applied - 0 deviated - 0 n/a across 0 accounted rule(s)`, so nothing FALSE is asserted; what is asserted is less, on precisely the mission state in which this document is most often opened for the first time. Filed hole rather than display-only because the lost line is a caveat and a remediation rather than chrome, and because both siblings on the same predicate are holes. Nothing catches it (measured: applied, real CLI re-run, all seven smoke ISO assertions pass).",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001ConditionalExpression\u0001false\u0001inputs.conformance.length === 0\u0001if (inputs.conformance.length === 0) {"
  },
  {
   "key": "180|9|180|40|EqualityOperator|inputs.conformance.length !== 0",
   "line": 180,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "`!==` inverts the predicate, so it is both of its siblings at once, and both directions were measured. On examples/request-triage it behaves as the `-> true` twin: 101 lines to 64, the 36-row conformance table replaced by ``_No filled `Rule conformance` manifest found yet..._`` under a sentence still reporting 36 accounted rules. On a fresh `runward init` mission it behaves as the `-> false` twin: the note replaced by an empty two-line table. Recipes and consequences as recorded on those two entries; nothing in the unit suite or in test/smoke.js catches either direction (measured, applied and re-run through the real CLI).",
   "argument": "`!==` inverts the predicate, so it is both of its siblings at once, and both directions were measured. On examples/request-triage it behaves as the `-> true` twin: 101 lines to 64, the 36-row conformance table replaced by ``_No filled `Rule conformance` manifest found yet..._`` under a sentence still reporting 36 accounted rules. On a fresh `runward init` mission it behaves as the `-> false` twin: the note replaced by an empty two-line table. Recipes and consequences as recorded on those two entries; nothing in the unit suite or in test/smoke.js catches either direction (measured, applied and re-run through the real CLI).",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001EqualityOperator\u0001inputs.conformance.length !== 0\u0001inputs.conformance.length === 0\u0001if (inputs.conformance.length === 0) {"
  },
  {
   "key": "181|16|181|146|StringLiteral|\"\"",
   "line": 181,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Only reachable on the empty branch. Verified through the real CLI (`runward init`, then `runward compliance iso-42001`): section 2 loses the sentence _No filled `Rule conformance` manifest found yet — fill the architect/floor/govern deliverables (see `runward check --strict`)._ and renders blank. The fact that sentence states survives verbatim one line above, in a line the mutant does not touch: **0 applied · 0 deviated · 0 n/a** across 0 accounted rule(s). No count, status, caveat or scope moves; what is lost is the remediation pointer. Surface: section 2 of `runward/compliance/iso-42001-readiness.md`. Nothing reads it: the mutation net is `node --test test/unit/*.test.js` (scripts/mutation-testcmd.sh), the three unit tests that call this renderer assert only the ADR-0040 non-scope fragment, /Draft/, the date, \"not a compliance claim\", the lens stamp, the section-1 clause refs and the A.6.2.8 operator item, and `test/smoke.js` replayed against this mutant does not notice either.",
   "argument": "Only reachable on the empty branch. Verified through the real CLI (`runward init`, then `runward compliance iso-42001`): section 2 loses the sentence _No filled `Rule conformance` manifest found yet — fill the architect/floor/govern deliverables (see `runward check --strict`)._ and renders blank. The fact that sentence states survives verbatim one line above, in a line the mutant does not touch: **0 applied · 0 deviated · 0 n/a** across 0 accounted rule(s). No count, status, caveat or scope moves; what is lost is the remediation pointer. Surface: section 2 of `runward/compliance/iso-42001-readiness.md`. Nothing reads it: the mutation net is `node --test test/unit/*.test.js` (scripts/mutation-testcmd.sh), the three unit tests that call this renderer assert only the ADR-0040 non-scope fragment, /Draft/, the date, \"not a compliance claim\", the lens stamp, the section-1 clause refs and the A.6.2.8 operator item, and `test/smoke.js` replayed against this mutant does not notice either.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"_No filled `Rule conformance` manifest found yet — fill the architect/floor/govern deliverables (see `runward check --strict`)._\"\u0001L.push(\"_No filled `Rule conformance` manifest found yet — fill the architect/floor/govern deliverables (see `runward check --strict`)._\");"
  },
  {
   "key": "183|10|188|6|BlockStatement|{}",
   "line": 183,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "Deletes the whole Statement-of-Applicability evidence table while the count sentence above it is untouched. Recipe: `cp -R examples/request-triage/runward .` then `runward compliance iso-42001` — the packaged reference mission. Before: `**23 applied · 0 deviated · 13 n/a** across 36 accounted rule(s).` followed by 36 rows carrying each rule, its status, its typed evidence pointer and its phase. After: the same count sentence, then nothing — section 2 asserts 36 accounted rules and shows none of them. A pack that states counts with no rows behind them is the RWD-2026-0058 shape: the claim leaves the building, the evidence that qualifies it does not. Nothing notices: not the unit net (the golden fixture renders eu-ai-act, never this function), and not `test/smoke.js` — its `topology-*` assertion, which reads as if it pinned the manifest rows, is in fact satisfied by the section-1 ASI coverage table built from `rules/` (verified: replayed against this mutant, it still passes).",
   "argument": "Deletes the whole Statement-of-Applicability evidence table while the count sentence above it is untouched. Recipe: `cp -R examples/request-triage/runward .` then `runward compliance iso-42001` — the packaged reference mission. Before: `**23 applied · 0 deviated · 13 n/a** across 36 accounted rule(s).` followed by 36 rows carrying each rule, its status, its typed evidence pointer and its phase. After: the same count sentence, then nothing — section 2 asserts 36 accounted rules and shows none of them. A pack that states counts with no rows behind them is the RWD-2026-0058 shape: the claim leaves the building, the evidence that qualifies it does not. Nothing notices: not the unit net (the golden fixture renders eu-ai-act, never this function), and not `test/smoke.js` — its `topology-*` assertion, which reads as if it pinned the manifest rows, is in fact satisfied by the section-1 ASI coverage table built from `rules/` (verified: replayed against this mutant, it still passes).",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001BlockStatement\u0001{}\u0001{ L.push(\"| Rule | Status | Evidence | Phase |\"); L.push(\"|---|---|---|---|\"); for (const r of inputs.conformance) L.push(`| \\`${r.rule}\\` | ${r.status} | ${r.evidence || \"—\"} | ${r.source} |`); }\u0001else {"
  },
  {
   "key": "184|16|184|54|StringLiteral|\"\"",
   "line": 184,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Removes the header row of the section-2 evidence table; every data row survives with its rule, status, evidence pointer and phase intact, so no count, status, caveat or scope moves. The cost is rendering: without a header row the block is no longer a GFM table and the rows read as one run-on paragraph. Surface: the section-2 table of `runward/compliance/iso-42001-readiness.md`. No test and no machine contract reads it — the OSCAL component-definition is assembled by `renderOscal` from the same inputs, not parsed out of this markdown, and `test/smoke.js` replayed against this mutant does not notice. Same argument for the `|---|---|---|---|` delimiter sibling on the next line and for the two ADR-table siblings in section 3.",
   "argument": "Removes the header row of the section-2 evidence table; every data row survives with its rule, status, evidence pointer and phase intact, so no count, status, caveat or scope moves. The cost is rendering: without a header row the block is no longer a GFM table and the rows read as one run-on paragraph. Surface: the section-2 table of `runward/compliance/iso-42001-readiness.md`. No test and no machine contract reads it — the OSCAL component-definition is assembled by `renderOscal` from the same inputs, not parsed out of this markdown, and `test/smoke.js` replayed against this mutant does not notice. Same argument for the `|---|---|---|---|` delimiter sibling on the next line and for the two ADR-table siblings in section 3.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"| Rule | Status | Evidence | Phase |\"\u0001L.push(\"| Rule | Status | Evidence | Phase |\");"
  },
  {
   "key": "185|16|185|35|StringLiteral|\"\"",
   "line": 185,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Same as the `| Rule | Status | Evidence | Phase |` header sibling above: the delimiter row is table chrome, every data row survives verbatim, and the only cost is that the block stops rendering as a GFM table. Verified over six input shapes; `test/smoke.js` replayed against it does not notice.",
   "argument": "Same as the `| Rule | Status | Evidence | Phase |` header sibling above: the delimiter row is table chrome, every data row survives verbatim, and the only cost is that the block stops rendering as a GFM table. Verified over six input shapes; `test/smoke.js` replayed against it does not notice.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"|---|---|---|---|\"\u0001L.push(\"|---|---|---|---|\");"
  },
  {
   "key": "187|20|187|90|StringLiteral|``",
   "line": 187,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Every row of the section-2 evidence table becomes an empty line, leaving the header, the delimiter and the count sentence in place: an empty Statement-of-Applicability table under `**23 applied · 0 deviated · 13 n/a** across 36 accounted rule(s).` Recipe: the packaged `examples/request-triage` mission, `runward compliance iso-42001`. This is worse than the block-deletion sibling because the table chrome remains, so the document reads as a table that legitimately has no entries rather than as a section that failed to render. Nothing notices (unit net does not walk this function; `test/smoke.js` replayed against it passes, for the reason recorded on the block-deletion sibling).",
   "argument": "Every row of the section-2 evidence table becomes an empty line, leaving the header, the delimiter and the count sentence in place: an empty Statement-of-Applicability table under `**23 applied · 0 deviated · 13 n/a** across 36 accounted rule(s).` Recipe: the packaged `examples/request-triage` mission, `runward compliance iso-42001`. This is worse than the block-deletion sibling because the table chrome remains, so the document reads as a table that legitimately has no entries rather than as a section that failed to render. Nothing notices (unit net does not walk this function; `test/smoke.js` replayed against it passes, for the reason recorded on the block-deletion sibling).",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001``\u0001`| \\`${r.rule}\\` | ${r.status} | ${r.evidence || \"—\"} | ${r.source} |`\u0001L.push(`| \\`${r.rule}\\` | ${r.status} | ${r.evidence || \"—\"} | ${r.source} |`);"
  },
  {
   "key": "187|55|187|72|ConditionalExpression|true",
   "line": 187,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "The Evidence column of every conformance row becomes the literal `true`. Verified on the packaged `examples/request-triage` mission: the row for `contracts-governance` keeps \"applied\" and its \"Architect\" phase, and its pointer file:code/src/core/ports/model-provider.port.ts#TriageModelPort … is replaced by `true`, for all 36 rows. Section 2 declares it feeds the Statement of Applicability implementation-status AND evidence columns; the mutant keeps the status and destroys the evidence, with no marker that anything is missing. Nothing notices, in the unit net or in `test/smoke.js`.",
   "argument": "The Evidence column of every conformance row becomes the literal `true`. Verified on the packaged `examples/request-triage` mission: the row for `contracts-governance` keeps \"applied\" and its \"Architect\" phase, and its pointer file:code/src/core/ports/model-provider.port.ts#TriageModelPort … is replaced by `true`, for all 36 rows. Section 2 declares it feeds the Statement of Applicability implementation-status AND evidence columns; the mutant keeps the status and destroys the evidence, with no marker that anything is missing. Nothing notices, in the unit net or in `test/smoke.js`.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001ConditionalExpression\u0001true\u0001r.evidence || \"—\"\u0001L.push(`| \\`${r.rule}\\` | ${r.status} | ${r.evidence || \"—\"} | ${r.source} |`);"
  },
  {
   "key": "187|55|187|72|ConditionalExpression|false",
   "line": 187,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "Same as the `true` sibling on this line, with `false` substituted for every evidence pointer instead. Verified on the packaged `examples/request-triage` mission.",
   "argument": "Same as the `true` sibling on this line, with `false` substituted for every evidence pointer instead. Verified on the packaged `examples/request-triage` mission.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001ConditionalExpression\u0001false\u0001r.evidence || \"—\"\u0001L.push(`| \\`${r.rule}\\` | ${r.status} | ${r.evidence || \"—\"} | ${r.source} |`);"
  },
  {
   "key": "187|55|187|72|LogicalOperator|r.evidence && \"—\"",
   "line": 187,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "Inverts the evidence column: `r.evidence && \"—\"` returns the em dash whenever evidence EXISTS, and the empty string when it does not. Verified through the real CLI on the packaged `examples/request-triage` mission: all 36 rows keep their status and report `—` — no evidence recorded — where the mission carries typed pointers such as `file:code/src/core/domain/guard.ts#guardFields`. The pack then states that 23 controls are `applied` on no evidence at all, which is precisely the reading `runward check --strict` refuses to produce. Nothing notices, in the unit net or in `test/smoke.js`.",
   "argument": "Inverts the evidence column: `r.evidence && \"—\"` returns the em dash whenever evidence EXISTS, and the empty string when it does not. Verified through the real CLI on the packaged `examples/request-triage` mission: all 36 rows keep their status and report `—` — no evidence recorded — where the mission carries typed pointers such as `file:code/src/core/domain/guard.ts#guardFields`. The pack then states that 23 controls are `applied` on no evidence at all, which is precisely the reading `runward check --strict` refuses to produce. Nothing notices, in the unit net or in `test/smoke.js`.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001LogicalOperator\u0001r.evidence && \"—\"\u0001r.evidence || \"—\"\u0001L.push(`| \\`${r.rule}\\` | ${r.status} | ${r.evidence || \"—\"} | ${r.source} |`);"
  },
  {
   "key": "187|69|187|72|StringLiteral|\"\"",
   "line": 187,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Only distinguishable when a manifest row has an empty Evidence cell, which is a real shape (a `| rule | status |  |` row parses to `evidence: \"\"`). The cell then renders blank instead of `—`; both read as an absent pointer and the Status column, which carries the claim, is untouched. Sensitivity control: on the packaged reference mission and on every row that has a pointer, the two builds are byte-identical — this mutant can only ever change the empty-cell filler. Surface: the Evidence cell of the section-2 table. No test and no machine contract reads it.",
   "argument": "Only distinguishable when a manifest row has an empty Evidence cell, which is a real shape (a `| rule | status |  |` row parses to `evidence: \"\"`). The cell then renders blank instead of `—`; both read as an absent pointer and the Status column, which carries the claim, is untouched. Sensitivity control: on the packaged reference mission and on every row that has a pointer, the two builds are byte-identical — this mutant can only ever change the empty-cell filler. Surface: the Evidence cell of the section-2 table. No test and no machine contract reads it.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"—\"\u0001L.push(`| \\`${r.rule}\\` | ${r.status} | ${r.evidence || \"—\"} | ${r.source} |`);"
  },
  {
   "key": "189|12|189|14|StringLiteral|\"Stryker was here!\"",
   "line": 189,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "The mutant replaces a blank separator line with arbitrary text; it changes no count, status, caveat or scope, and the document's blank lines are read by nothing — not by the three unit tests that call this renderer, not by `test/smoke.js`, and not by the OSCAL, which is assembled from the same inputs by `renderOscal` rather than parsed out of this markdown. Surface: the block separator between the section-2 table and the section-3 heading in `runward/compliance/iso-42001-readiness.md`. Honest caveat: these separators are markdown block boundaries, so an arbitrary replacement corrupts rendering — here the injected line falls directly under the last table row and a GFM parser absorbs it as a further row. That is a formatting defect, not a claim defect: `Stryker was here!` cannot be mistaken for a finding. Same argument for the six sibling blank-line mutants in this function.",
   "argument": "The mutant replaces a blank separator line with arbitrary text; it changes no count, status, caveat or scope, and the document's blank lines are read by nothing — not by the three unit tests that call this renderer, not by `test/smoke.js`, and not by the OSCAL, which is assembled from the same inputs by `renderOscal` rather than parsed out of this markdown. Surface: the block separator between the section-2 table and the section-3 heading in `runward/compliance/iso-42001-readiness.md`. Honest caveat: these separators are markdown block boundaries, so an arbitrary replacement corrupts rendering — here the injected line falls directly under the last table row and a GFM parser absorbs it as a further row. That is a formatting defect, not a claim defect: `Stryker was here!` cannot be mistaken for a finding. Same argument for the six sibling blank-line mutants in this function.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#9"
  },
  {
   "key": "190|12|190|50|StringLiteral|\"\"",
   "line": 190,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Removes a navigational heading. The section it names is still fully self-describing: the line immediately below is `The \"key design choices, alternatives, and re-evaluation triggers\" an ISO 42001 auditor expects (records under Annex A control groups).` followed by the ADR table or the explicit no-ADR sentence. No count, status, caveat or scope moves. Surface: the section-3 heading of `runward/compliance/iso-42001-readiness.md`; no test and no machine contract reads it, and `test/smoke.js` (which asserts on the section-1 heading and on `Required from the operator`) does not.",
   "argument": "Removes a navigational heading. The section it names is still fully self-describing: the line immediately below is `The \"key design choices, alternatives, and re-evaluation triggers\" an ISO 42001 auditor expects (records under Annex A control groups).` followed by the ADR table or the explicit no-ADR sentence. No count, status, caveat or scope moves. Surface: the section-3 heading of `runward/compliance/iso-42001-readiness.md`; no test and no machine contract reads it, and `test/smoke.js` (which asserts on the section-1 heading and on `Required from the operator`) does not.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"## 3. Design decisions (ADR journal)\"\u0001L.push(\"## 3. Design decisions (ADR journal)\");"
  },
  {
   "key": "191|12|191|14|StringLiteral|\"Stryker was here!\"",
   "line": 191,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Blank separator between the section-3 heading and its lede. Same argument as the first blank-line sibling in this function.",
   "argument": "Blank separator between the section-3 heading and its lede. Same argument as the first blank-line sibling in this function.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#10"
  },
  {
   "key": "192|12|192|161|StringLiteral|``",
   "line": 192,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "This is the only line in section 3 that maps the ADR journal onto ISO/IEC 42001, and the clause value it interpolates (`${cl.annexControls}` → `Annex A`) is versioned lens data under ADR-0022 rather than a hardcoded string. With the mutant, section 3 renders as a heading and a table of design decisions with no statement of which control groups they feed — evidence with no destination, in a document whose entire purpose is to say where each block of evidence files. No statement becomes false; a mapping silently stops being carried for one of the document's four sections. The unit test that exists for exactly this (`readiness drafts: the extracted mapping renders`) checks the section-1 clause refs and the A.6.2.8 operator item and does not look at this line; verified surviving over six input shapes and through the CLI.",
   "argument": "This is the only line in section 3 that maps the ADR journal onto ISO/IEC 42001, and the clause value it interpolates (`${cl.annexControls}` → `Annex A`) is versioned lens data under ADR-0022 rather than a hardcoded string. With the mutant, section 3 renders as a heading and a table of design decisions with no statement of which control groups they feed — evidence with no destination, in a document whose entire purpose is to say where each block of evidence files. No statement becomes false; a mapping silently stops being carried for one of the document's four sections. The unit test that exists for exactly this (`readiness drafts: the extracted mapping renders`) checks the section-1 clause refs and the A.6.2.8 operator item and does not look at this line; verified surviving over six input shapes and through the CLI.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001``\u0001`The \"key design choices, alternatives, and re-evaluation triggers\" an ISO 42001 auditor expects (records under ${cl.annexControls} control groups).`\u0001L.push(`The \"key design choices, alternatives, and re-evaluation triggers\" an ISO 42001 auditor expects (records under ${cl.annexControls} control groups).`);"
  },
  {
   "key": "193|12|193|14|StringLiteral|\"Stryker was here!\"",
   "line": 193,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Blank separator between the section-3 lede and the ADR table. Same argument as the first blank-line sibling in this function.",
   "argument": "Blank separator between the section-3 lede and the ADR table. Same argument as the first blank-line sibling in this function.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#11"
  },
  {
   "key": "194|9|194|33|ConditionalExpression|true",
   "line": 194,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "Forces the no-ADR branch always. Verified through the real CLI on the packaged `examples/request-triage` mission, which carries three accepted ADRs: the table rows naming ADR-0001, ADR-0002 and ADR-0003 with status \"accepted\" are replaced by the sentence _No ratified ADR found in `runward/adr/`._ The pack states, to an assessor, that a mission with a ratified decision journal has none. Nothing notices: the unit net never walks this function and `test/smoke.js` replayed against this mutant does not notice.",
   "argument": "Forces the no-ADR branch always. Verified through the real CLI on the packaged `examples/request-triage` mission, which carries three accepted ADRs: the table rows naming ADR-0001, ADR-0002 and ADR-0003 with status \"accepted\" are replaced by the sentence _No ratified ADR found in `runward/adr/`._ The pack states, to an assessor, that a mission with a ratified decision journal has none. Nothing notices: the unit net never walks this function and `test/smoke.js` replayed against this mutant does not notice.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001ConditionalExpression\u0001true\u0001inputs.adrs.length === 0\u0001if (inputs.adrs.length === 0) {"
  },
  {
   "key": "194|9|194|33|ConditionalExpression|false",
   "line": 194,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "Forces the else branch always, so a mission with no ratified ADR gets the table chrome and no rows instead of the explicit sentence. Verified through the real CLI on a fresh `runward init` (whose `adr/` holds only the template and the README, so `inputs.adrs` is empty): the sentence _No ratified ADR found in `runward/adr/`._ is replaced by `| ADR | Status |` / `|---|---|` and nothing under them. Nothing false is asserted, but the document's only statement of absence for section 3 is gone — replaced by a table an assessor may read as a rendering failure rather than as an empty journal. Section 3 has no count line to fall back on, which is what separates this from the section-2 empty-branch sibling.",
   "argument": "Forces the else branch always, so a mission with no ratified ADR gets the table chrome and no rows instead of the explicit sentence. Verified through the real CLI on a fresh `runward init` (whose `adr/` holds only the template and the README, so `inputs.adrs` is empty): the sentence _No ratified ADR found in `runward/adr/`._ is replaced by `| ADR | Status |` / `|---|---|` and nothing under them. Nothing false is asserted, but the document's only statement of absence for section 3 is gone — replaced by a table an assessor may read as a rendering failure rather than as an empty journal. Section 3 has no count line to fall back on, which is what separates this from the section-2 empty-branch sibling.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001ConditionalExpression\u0001false\u0001inputs.adrs.length === 0\u0001if (inputs.adrs.length === 0) {"
  },
  {
   "key": "194|9|194|33|EqualityOperator|inputs.adrs.length !== 0",
   "line": 194,
   "mutator": "EqualityOperator",
   "filing": "hole",
   "evidence": "Inverts the branch, so it is both of the two siblings at once: a mission with ratified ADRs prints _No ratified ADR found in `runward/adr/`._ and a mission with none prints a table header with no rows under it. Both directions verified through the real CLI — the packaged `examples/request-triage` mission and a fresh `runward init`.",
   "argument": "Inverts the branch, so it is both of the two siblings at once: a mission with ratified ADRs prints _No ratified ADR found in `runward/adr/`._ and a mission with none prints a table header with no rows under it. Both directions verified through the real CLI — the packaged `examples/request-triage` mission and a fresh `runward init`.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001EqualityOperator\u0001inputs.adrs.length !== 0\u0001inputs.adrs.length === 0\u0001if (inputs.adrs.length === 0) {"
  },
  {
   "key": "194|35|196|6|BlockStatement|{}",
   "line": 194,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "Empties the no-ADR branch, so a mission with no ratified ADR renders section 3 as a lede followed directly by section 4. Verified through the real CLI on a fresh `runward init`. Same substance as the sibling that empties the sentence itself: section 3 has no count line, so this statement is the document's only account of what the ADR journal holds, and its removal leaves a silent gap where an assessor expects either records or an explicit `none`.",
   "argument": "Empties the no-ADR branch, so a mission with no ratified ADR renders section 3 as a lede followed directly by section 4. Verified through the real CLI on a fresh `runward init`. Same substance as the sibling that empties the sentence itself: section 3 has no count line, so this statement is the document's only account of what the ADR journal holds, and its removal leaves a silent gap where an assessor expects either records or an explicit `none`.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001BlockStatement\u0001{}\u0001{ L.push(\"_No ratified ADR found in `runward/adr/`._\"); }\u0001if (inputs.adrs.length === 0) {"
  },
  {
   "key": "195|16|195|60|StringLiteral|\"\"",
   "line": 195,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Same as the block-emptying sibling on this branch, reached by blanking the string instead: on a fresh `runward init` the sentence _No ratified ADR found in `runward/adr/`._ becomes an empty line and section 3 states nothing at all about the ADR journal. Verified through the real CLI.",
   "argument": "Same as the block-emptying sibling on this branch, reached by blanking the string instead: on a fresh `runward init` the sentence _No ratified ADR found in `runward/adr/`._ becomes an empty line and section 3 states nothing at all about the ADR journal. Verified through the real CLI.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"_No ratified ADR found in `runward/adr/`._\"\u0001L.push(\"_No ratified ADR found in `runward/adr/`._\");"
  },
  {
   "key": "197|10|202|6|BlockStatement|{}",
   "line": 197,
   "mutator": "BlockStatement",
   "filing": "hole",
   "evidence": "Deletes the ADR table on every mission that has ADRs. Verified through the real CLI on the packaged `examples/request-triage` mission: the three accepted ADRs and their statuses disappear and section 3 runs from its lede straight to section 4. The lede still tells the assessor these are the records ISO expects; the records are gone, with no statement that there are none. Nothing notices, in the unit net or in `test/smoke.js`.",
   "argument": "Deletes the ADR table on every mission that has ADRs. Verified through the real CLI on the packaged `examples/request-triage` mission: the three accepted ADRs and their statuses disappear and section 3 runs from its lede straight to section 4. The lede still tells the assessor these are the records ISO expects; the records are gone, with no statement that there are none. Nothing notices, in the unit net or in `test/smoke.js`.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001BlockStatement\u0001{}\u0001{ L.push(\"| ADR | Status |\"); L.push(\"|---|---|\"); for (const a of inputs.adrs) L.push(`| ${a.title} (\\`${a.file}\\`) | ${a.status || \"—\"} |`); }\u0001else {"
  },
  {
   "key": "198|16|198|34|StringLiteral|\"\"",
   "line": 198,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Header row of the section-3 ADR table; the rows survive with title, filename and status. Same argument as the section-2 table-chrome siblings: no count, status, caveat or scope moves, the only cost is that the block stops rendering as a GFM table, and no test or machine contract reads it.",
   "argument": "Header row of the section-3 ADR table; the rows survive with title, filename and status. Same argument as the section-2 table-chrome siblings: no count, status, caveat or scope moves, the only cost is that the block stops rendering as a GFM table, and no test or machine contract reads it.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"| ADR | Status |\"\u0001L.push(\"| ADR | Status |\");"
  },
  {
   "key": "199|16|199|27|StringLiteral|\"\"",
   "line": 199,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Delimiter row of the section-3 ADR table. Same argument as the section-2 table-chrome siblings.",
   "argument": "Delimiter row of the section-3 ADR table. Same argument as the section-2 table-chrome siblings.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"|---|---|\"\u0001L.push(\"|---|---|\");"
  },
  {
   "key": "201|20|201|73|StringLiteral|``",
   "line": 201,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Every ADR row becomes an empty line while the header and delimiter remain: an empty design-decision journal presented as a table, on a mission that has one. Verified on the packaged `examples/request-triage` mission, whose three accepted ADRs vanish. Same reading as the section-2 row-emptying sibling — the surviving chrome makes the absence look legitimate rather than broken.",
   "argument": "Every ADR row becomes an empty line while the header and delimiter remain: an empty design-decision journal presented as a table, on a mission that has one. Verified on the packaged `examples/request-triage` mission, whose three accepted ADRs vanish. Same reading as the section-2 row-emptying sibling — the surviving chrome makes the absence look legitimate rather than broken.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001``\u0001`| ${a.title} (\\`${a.file}\\`) | ${a.status || \"—\"} |`\u0001L.push(`| ${a.title} (\\`${a.file}\\`) | ${a.status || \"—\"} |`);"
  },
  {
   "key": "201|54|201|69|ConditionalExpression|true",
   "line": 201,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "The Status column of every ADR becomes the literal `true`. Verified on the packaged `examples/request-triage` mission: the three rows that read \"accepted\" read `true` instead, title and filename untouched. The status word is what tells an assessor whether a decision is in force, superseded or merely proposed; the mutant destroys it for every row and substitutes a token that is not a status at all. Nothing notices, in the unit net or in `test/smoke.js`.",
   "argument": "The Status column of every ADR becomes the literal `true`. Verified on the packaged `examples/request-triage` mission: the three rows that read \"accepted\" read `true` instead, title and filename untouched. The status word is what tells an assessor whether a decision is in force, superseded or merely proposed; the mutant destroys it for every row and substitutes a token that is not a status at all. Nothing notices, in the unit net or in `test/smoke.js`.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001ConditionalExpression\u0001true\u0001a.status || \"—\"\u0001L.push(`| ${a.title} (\\`${a.file}\\`) | ${a.status || \"—\"} |`);"
  },
  {
   "key": "201|54|201|69|ConditionalExpression|false",
   "line": 201,
   "mutator": "ConditionalExpression",
   "filing": "hole",
   "evidence": "Same as the `true` sibling on this line, with `false` substituted for every ADR status. Verified on the packaged `examples/request-triage` mission.",
   "argument": "Same as the `true` sibling on this line, with `false` substituted for every ADR status. Verified on the packaged `examples/request-triage` mission.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001ConditionalExpression\u0001false\u0001a.status || \"—\"\u0001L.push(`| ${a.title} (\\`${a.file}\\`) | ${a.status || \"—\"} |`);"
  },
  {
   "key": "201|54|201|69|LogicalOperator|a.status && \"—\"",
   "line": 201,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "Inverts the status column: `a.status && \"—\"` yields the em dash for every ADR that HAS a status. Verified through the real CLI on the packaged `examples/request-triage` mission — all three ADRs, `accepted` in the mission's own files, are reported with no status. A journal of decisions whose statuses all read `—` tells an assessor that none of them is ratified, which is the opposite of the mission's state. Nothing notices, in the unit net or in `test/smoke.js`.",
   "argument": "Inverts the status column: `a.status && \"—\"` yields the em dash for every ADR that HAS a status. Verified through the real CLI on the packaged `examples/request-triage` mission — all three ADRs, `accepted` in the mission's own files, are reported with no status. A journal of decisions whose statuses all read `—` tells an assessor that none of them is ratified, which is the opposite of the mission's state. Nothing notices, in the unit net or in `test/smoke.js`.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001LogicalOperator\u0001a.status && \"—\"\u0001a.status || \"—\"\u0001L.push(`| ${a.title} (\\`${a.file}\\`) | ${a.status || \"—\"} |`);"
  },
  {
   "key": "201|66|201|69|StringLiteral|\"\"",
   "line": 201,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Only distinguishable when an ADR carries no `**Status**:` line, a real shape (`readAdrs` keeps such a file and sets `status: \"\"`). The Status cell then renders blank instead of `—`; both read as no status recorded, and the ADR's title and filename are untouched. Sensitivity control: on the packaged reference mission, every ADR has a status and the two builds are byte-identical — this mutant can only ever change the empty-cell filler. Surface: the Status cell of the section-3 table; nothing reads it.",
   "argument": "Only distinguishable when an ADR carries no `**Status**:` line, a real shape (`readAdrs` keeps such a file and sets `status: \"\"`). The Status cell then renders blank instead of `—`; both read as no status recorded, and the ADR's title and filename are untouched. Sensitivity control: on the packaged reference mission, every ADR has a status and the two builds are byte-identical — this mutant can only ever change the empty-cell filler. Surface: the Status cell of the section-3 table; nothing reads it.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"—\"\u0001L.push(`| ${a.title} (\\`${a.file}\\`) | ${a.status || \"—\"} |`);"
  },
  {
   "key": "203|12|203|14|StringLiteral|\"Stryker was here!\"",
   "line": 203,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Blank separator between the ADR table and the section-4 heading. Same argument as the first blank-line sibling in this function, including the caveat that the injected line falls under the last table row and would be absorbed as a further row.",
   "argument": "Blank separator between the ADR table and the section-4 heading. Same argument as the first blank-line sibling in this function, including the caveat that the injected line falls under the last table row and would be absorbed as a further row.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#12"
  },
  {
   "key": "204|12|204|51|StringLiteral|\"\"",
   "line": 204,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Removes a navigational heading. The two bullets beneath it are self-labelled and carry their own clause reference (`- Threat model (feeds risk assessment 6.1.2): …`), so no count, status, caveat or scope moves; the visible cost is that they sit under the section-3 heading and the document's numbering jumps from 3 to the operator section. Surface: the section-4 heading of `runward/compliance/iso-42001-readiness.md`; no test and no machine contract reads it. This is filed differently from the `## Required from the operator …` heading below, which carries a scope claim in its own words and is cross-referenced from the document header.",
   "argument": "Removes a navigational heading. The two bullets beneath it are self-labelled and carry their own clause reference (`- Threat model (feeds risk assessment 6.1.2): …`), so no count, status, caveat or scope moves; the visible cost is that they sit under the section-3 heading and the document's numbering jumps from 3 to the operator section. Surface: the section-4 heading of `runward/compliance/iso-42001-readiness.md`; no test and no machine contract reads it. This is filed differently from the `## Required from the operator …` heading below, which carries a scope claim in its own words and is cross-referenced from the document header.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"## 4. Risk & impact inputs (presence)\"\u0001L.push(\"## 4. Risk & impact inputs (presence)\");"
  },
  {
   "key": "205|12|205|14|StringLiteral|\"Stryker was here!\"",
   "line": 205,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Blank separator between the section-4 heading and its first bullet. Same argument as the first blank-line sibling in this function.",
   "argument": "Blank separator between the section-4 heading and its first bullet. Same argument as the first blank-line sibling in this function.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#13"
  },
  {
   "key": "206|12|206|173|StringLiteral|``",
   "line": 206,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Deletes the threat-model line outright, in both of its branches. Section 4 is titled `Risk & impact inputs (presence)` and has exactly two bullets; after the mutant it has one, and the document says nothing at all about the artifact that feeds the ISO risk assessment (6.1.2) — neither that it is filled nor that it is not counted. Verified over six input shapes and through the real CLI: on a fresh `runward init` the line `- Threat model (feeds risk assessment 6.1.2): **not counted** (raw template)` disappears, so the mission whose threat model is still the shipped template is no longer flagged as such anywhere in the pack. Nothing notices, in the unit net or in `test/smoke.js`.",
   "argument": "Deletes the threat-model line outright, in both of its branches. Section 4 is titled `Risk & impact inputs (presence)` and has exactly two bullets; after the mutant it has one, and the document says nothing at all about the artifact that feeds the ISO risk assessment (6.1.2) — neither that it is filled nor that it is not counted. Verified over six input shapes and through the real CLI: on a fresh `runward init` the line `- Threat model (feeds risk assessment 6.1.2): **not counted** (raw template)` disappears, so the mission whose threat model is still the shipped template is no longer flagged as such anywhere in the pack. Nothing notices, in the unit net or in `test/smoke.js`.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001``\u0001`- Threat model (feeds risk assessment ${cl.riskAssessment}): ${inputs.threatModel ? \"**filled**\" : `**not counted** (${inputs.threatModelState ?? \"missing\"})`}`\u0001L.push(`- Threat model (feeds risk assessment ${cl.riskAssessment}): ${inputs.threatModel ? \"**filled**\" : `**not counted** (${inputs.threatModelState ?? \"missing\"})`}`);"
  },
  {
   "key": "206|97|206|109|StringLiteral|\"\"",
   "line": 206,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Empties the positive status token: on a mission whose threat model is filled, the line renders `- Threat model (feeds risk assessment 6.1.2): ` with nothing after the colon. Verified through the real CLI on the packaged `examples/request-triage` mission. The direction is conservative — it can only remove a green, never manufacture one — but a presence section that prints a governance item with no state is a status a reader cannot act on, and it will send an assessor hunting for an artifact that exists. Nothing notices.",
   "argument": "Empties the positive status token: on a mission whose threat model is filled, the line renders `- Threat model (feeds risk assessment 6.1.2): ` with nothing after the colon. Verified through the real CLI on the packaged `examples/request-triage` mission. The direction is conservative — it can only remove a green, never manufacture one — but a presence section that prints a governance item with no state is a status a reader cannot act on, and it will send an assessor hunting for an artifact that exists. Nothing notices.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"**filled**\"\u0001L.push(`- Threat model (feeds risk assessment ${cl.riskAssessment}): ${inputs.threatModel ? \"**filled**\" : `**not counted** (${inputs.threatModelState ?? \"missing\"})`}`);"
  },
  {
   "key": "206|112|206|171|StringLiteral|``",
   "line": 206,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Empties the negative branch: a mission whose threat model is missing or still a raw template renders `- Threat model (feeds risk assessment 6.1.2): ` with nothing after the colon. Verified through the real CLI on a fresh `runward init`, where the true line is `**not counted** (raw template)`. This is the dangerous direction and the exact RWD-2026-0058 shape: the caveat that qualifies a governance input is dropped on the way out, and a blank beside a bullet reads as nothing to report rather than as not counted. Nothing notices.",
   "argument": "Empties the negative branch: a mission whose threat model is missing or still a raw template renders `- Threat model (feeds risk assessment 6.1.2): ` with nothing after the colon. Verified through the real CLI on a fresh `runward init`, where the true line is `**not counted** (raw template)`. This is the dangerous direction and the exact RWD-2026-0058 shape: the caveat that qualifies a governance input is dropped on the way out, and a blank beside a bullet reads as nothing to report rather than as not counted. Nothing notices.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001``\u0001`**not counted** (${inputs.threatModelState ?? \"missing\"})`\u0001L.push(`- Threat model (feeds risk assessment ${cl.riskAssessment}): ${inputs.threatModel ? \"**filled**\" : `**not counted** (${inputs.threatModelState ?? \"missing\"})`}`);"
  },
  {
   "key": "206|132|206|168|LogicalOperator|inputs.threatModelState && \"missing\"",
   "line": 206,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "`??` guards a missing value; `&&` makes the whole expression return `\"missing\"` for EVERY non-empty state, so the reason a governance file is not counted is overwritten. Verified through the real CLI on a fresh `runward init`: `- Threat model (feeds risk assessment 6.1.2): **not counted** (raw template)` becomes `… **not counted** (missing)`. The pack then tells an assessor that a file which exists on disk is absent, and it contradicts `runward check` about the same file — `govState` exists precisely so the two surfaces use one vocabulary, and its comment says two vocabularies for one fact is how a reader concludes the surfaces disagree. `below floor` collapses to `missing` the same way. Note that the unit suite has an assertion that would catch one output of this mutant (`no lens field may render as undefined`), and it survives only because `gatherComplianceInputs` never supplies the shape that triggers it.",
   "argument": "`??` guards a missing value; `&&` makes the whole expression return `\"missing\"` for EVERY non-empty state, so the reason a governance file is not counted is overwritten. Verified through the real CLI on a fresh `runward init`: `- Threat model (feeds risk assessment 6.1.2): **not counted** (raw template)` becomes `… **not counted** (missing)`. The pack then tells an assessor that a file which exists on disk is absent, and it contradicts `runward check` about the same file — `govState` exists precisely so the two surfaces use one vocabulary, and its comment says two vocabularies for one fact is how a reader concludes the surfaces disagree. `below floor` collapses to `missing` the same way. Note that the unit suite has an assertion that would catch one output of this mutant (`no lens field may render as undefined`), and it survives only because `gatherComplianceInputs` never supplies the shape that triggers it.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001LogicalOperator\u0001inputs.threatModelState && \"missing\"\u0001inputs.threatModelState ?? \"missing\"\u0001L.push(`- Threat model (feeds risk assessment ${cl.riskAssessment}): ${inputs.threatModel ? \"**filled**\" : `**not counted** (${inputs.threatModelState ?? \"missing\"})`}`);"
  },
  {
   "key": "206|159|206|168|StringLiteral|\"\"",
   "line": 206,
   "mutator": "StringLiteral",
   "filing": "defence-in-depth",
   "evidence": "Unreachable from any runward command. The only producer of these inputs is `gatherComplianceInputs`, which always sets `threatModelState` from `govState`, and `govState` returns one of `missing`, `raw template`, `below floor` or an `ArtifactState` word (`missing` | `untouched` | `in-progress` | `filled`) — never undefined and never empty — so the `?? \"missing\"` fallback cannot fire and the two builds are byte-identical on all six probed input shapes, including a fresh `runward init` and the packaged reference mission. It is filed here rather than `equivalent` because the input is not impossible, only unproduced: `ComplianceInputs.threatModelState` is optional in the published type and `dist/lib/compliance.js` is importable through the package's `\"./*\"` export, so a caller building inputs by hand would get `**not counted** ()` instead of `**not counted** (missing)`. No leg of the net catches this mutant — it is unreached, not caught.",
   "argument": "Unreachable from any runward command. The only producer of these inputs is `gatherComplianceInputs`, which always sets `threatModelState` from `govState`, and `govState` returns one of `missing`, `raw template`, `below floor` or an `ArtifactState` word (`missing` | `untouched` | `in-progress` | `filled`) — never undefined and never empty — so the `?? \"missing\"` fallback cannot fire and the two builds are byte-identical on all six probed input shapes, including a fresh `runward init` and the packaged reference mission. It is filed here rather than `equivalent` because the input is not impossible, only unproduced: `ComplianceInputs.threatModelState` is optional in the published type and `dist/lib/compliance.js` is importable through the package's `\"./*\"` export, so a caller building inputs by hand would get `**not counted** ()` instead of `**not counted** (missing)`. No leg of the net catches this mutant — it is unreached, not caught.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"missing\"\u0001L.push(`- Threat model (feeds risk assessment ${cl.riskAssessment}): ${inputs.threatModel ? \"**filled**\" : `**not counted** (${inputs.threatModelState ?? \"missing\"})`}`);"
  },
  {
   "key": "207|12|207|166|StringLiteral|``",
   "line": 207,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Deletes the evaluation-rubric line outright, in both branches. Same substance as the threat-model sibling above: section 4 is a presence section with exactly two entries and the mutant silently removes one, so the pack says nothing about the artifact that feeds impact/validation analysis. Verified over six input shapes and through the real CLI on a fresh `runward init` and on the packaged `examples/request-triage` mission (where the true line is `**filled**`).",
   "argument": "Deletes the evaluation-rubric line outright, in both branches. Same substance as the threat-model sibling above: section 4 is a presence section with exactly two entries and the mutant silently removes one, so the pack says nothing about the artifact that feeds impact/validation analysis. Verified over six input shapes and through the real CLI on a fresh `runward init` and on the packaged `examples/request-triage` mission (where the true line is `**filled**`).",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001``\u0001`- Evaluation rubric (feeds impact/validation analysis): ${inputs.evalRubric ? \"**filled**\" : `**not counted** (${inputs.evalRubricState ?? \"missing\"})`}`\u0001L.push(`- Evaluation rubric (feeds impact/validation analysis): ${inputs.evalRubric ? \"**filled**\" : `**not counted** (${inputs.evalRubricState ?? \"missing\"})`}`);"
  },
  {
   "key": "207|91|207|103|StringLiteral|\"\"",
   "line": 207,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Empties the positive status token for the evaluation rubric: on the packaged `examples/request-triage` mission the line renders `- Evaluation rubric (feeds impact/validation analysis): ` with nothing after the colon. Verified through the real CLI. Same argument as the threat-model `**filled**` sibling.",
   "argument": "Empties the positive status token for the evaluation rubric: on the packaged `examples/request-triage` mission the line renders `- Evaluation rubric (feeds impact/validation analysis): ` with nothing after the colon. Verified through the real CLI. Same argument as the threat-model `**filled**` sibling.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"**filled**\"\u0001L.push(`- Evaluation rubric (feeds impact/validation analysis): ${inputs.evalRubric ? \"**filled**\" : `**not counted** (${inputs.evalRubricState ?? \"missing\"})`}`);"
  },
  {
   "key": "207|106|207|164|StringLiteral|``",
   "line": 207,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Empties the negative branch for the evaluation rubric: on a fresh `runward init` the true line `- Evaluation rubric (feeds impact/validation analysis): **not counted** (raw template)` renders with nothing after the colon. Verified through the real CLI. Same argument as the threat-model sibling — the caveat that qualifies a governance input is dropped, in the direction that flatters the mission.",
   "argument": "Empties the negative branch for the evaluation rubric: on a fresh `runward init` the true line `- Evaluation rubric (feeds impact/validation analysis): **not counted** (raw template)` renders with nothing after the colon. Verified through the real CLI. Same argument as the threat-model sibling — the caveat that qualifies a governance input is dropped, in the direction that flatters the mission.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001``\u0001`**not counted** (${inputs.evalRubricState ?? \"missing\"})`\u0001L.push(`- Evaluation rubric (feeds impact/validation analysis): ${inputs.evalRubric ? \"**filled**\" : `**not counted** (${inputs.evalRubricState ?? \"missing\"})`}`);"
  },
  {
   "key": "207|126|207|161|LogicalOperator|inputs.evalRubricState && \"missing\"",
   "line": 207,
   "mutator": "LogicalOperator",
   "filing": "hole",
   "evidence": "Same as the threat-model `??` sibling, on the evaluation rubric. Verified through the real CLI on a fresh `runward init`: `**not counted** (raw template)` becomes `**not counted** (missing)`, so a file that exists as an unfilled template is reported absent, and the pack and `runward check` describe the same file in different words.",
   "argument": "Same as the threat-model `??` sibling, on the evaluation rubric. Verified through the real CLI on a fresh `runward init`: `**not counted** (raw template)` becomes `**not counted** (missing)`, so a file that exists as an unfilled template is reported absent, and the pack and `runward check` describe the same file in different words.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001LogicalOperator\u0001inputs.evalRubricState && \"missing\"\u0001inputs.evalRubricState ?? \"missing\"\u0001L.push(`- Evaluation rubric (feeds impact/validation analysis): ${inputs.evalRubric ? \"**filled**\" : `**not counted** (${inputs.evalRubricState ?? \"missing\"})`}`);"
  },
  {
   "key": "207|152|207|161|StringLiteral|\"\"",
   "line": 207,
   "mutator": "StringLiteral",
   "filing": "defence-in-depth",
   "evidence": "Same as the threat-model `\"missing\"` sibling: `evalRubricState` is always set by `gatherComplianceInputs` via `govState`, which cannot return undefined or an empty string, so the `??` fallback never fires and the two builds are byte-identical on all six probed input shapes. Filed here rather than `equivalent` for the same reason — the field is optional in the published `ComplianceInputs` type and the module is importable through the package's `\"./*\"` export, so a hand-built inputs object would render `**not counted** ()`. No leg of the net catches it.",
   "argument": "Same as the threat-model `\"missing\"` sibling: `evalRubricState` is always set by `gatherComplianceInputs` via `govState`, which cannot return undefined or an empty string, so the `??` fallback never fires and the two builds are byte-identical on all six probed input shapes. Filed here rather than `equivalent` for the same reason — the field is optional in the published `ComplianceInputs` type and the module is importable through the package's `\"./*\"` export, so a hand-built inputs object would render `**not counted** ()`. No leg of the net catches it.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"missing\"\u0001L.push(`- Evaluation rubric (feeds impact/validation analysis): ${inputs.evalRubric ? \"**filled**\" : `**not counted** (${inputs.evalRubricState ?? \"missing\"})`}`);"
  },
  {
   "key": "208|12|208|14|StringLiteral|\"Stryker was here!\"",
   "line": 208,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Blank separator between the evaluation-rubric bullet and the operator-required heading. Same argument as the first blank-line sibling in this function; here the injected line would be read as a lazy continuation of the preceding list item rather than as a table row.",
   "argument": "Blank separator between the evaluation-rubric bullet and the operator-required heading. Same argument as the first blank-line sibling in this function; here the injected line would be read as a lazy continuation of the preceding list item rather than as a table row.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#14"
  },
  {
   "key": "209|12|209|88|StringLiteral|\"\"",
   "line": 209,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Removes the heading that draws the document's central line — between what runward assembled from the mission's artifacts and what only the operator can supply. Two concrete consequences, both verified through the real CLI on a fresh `runward init`. First, the operator-required list is absorbed into `## 4. Risk & impact inputs (presence)`, so a reader navigating by heading attributes the AI policy, the risk methodology and the management-review minutes to the evidence layer runward produced. Second, the document header still says `the applicability, risk-acceptance, policy and management sign-off it cannot invent are listed under \"Required from the operator\"` — the pack now points at a section it does not contain. The compensating prose line `These sections are managerial, legal or organizational — no tool can assemble them from engineering artifacts:` does survive directly above the list, which is the strongest argument against this filing; it is a sentence, not a boundary, and the header's cross-reference is dangling either way. `test/smoke.js` asserts `Required from the operator` but is satisfied by that header sentence — replayed against this mutant, it does not notice.",
   "argument": "Removes the heading that draws the document's central line — between what runward assembled from the mission's artifacts and what only the operator can supply. Two concrete consequences, both verified through the real CLI on a fresh `runward init`. First, the operator-required list is absorbed into `## 4. Risk & impact inputs (presence)`, so a reader navigating by heading attributes the AI policy, the risk methodology and the management-review minutes to the evidence layer runward produced. Second, the document header still says `the applicability, risk-acceptance, policy and management sign-off it cannot invent are listed under \"Required from the operator\"` — the pack now points at a section it does not contain. The compensating prose line `These sections are managerial, legal or organizational — no tool can assemble them from engineering artifacts:` does survive directly above the list, which is the strongest argument against this filing; it is a sentence, not a boundary, and the header's cross-reference is dangling either way. `test/smoke.js` asserts `Required from the operator` but is satisfied by that header sentence — replayed against this mutant, it does not notice.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"## Required from the operator / organization (runward cannot produce this)\"\u0001L.push(\"## Required from the operator / organization (runward cannot produce this)\");"
  },
  {
   "key": "210|12|210|14|StringLiteral|\"Stryker was here!\"",
   "line": 210,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Blank separator between the operator-required heading and its lede. Same argument as the first blank-line sibling in this function.",
   "argument": "Blank separator between the operator-required heading and its lede. Same argument as the first blank-line sibling in this function.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#15"
  },
  {
   "key": "211|12|211|124|StringLiteral|\"\"",
   "line": 211,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Measured on a fresh `runward init` mission (60-line draft) and on examples/request-triage (101-line draft), applying the mutant to dist/lib/compliance.js and re-running the real CLI: exactly one line changes — line 48 of the fresh draft goes empty. No claim is lost, because the sentence's claim is already made by the heading immediately above it, line 46, `## Required from the operator / organization (runward cannot produce this)`, which this mutant does not touch; the seven operator-required items rendered from `lens.operatorRequired` are unchanged, so test/unit/compliance.test.js's `/\\*\\*Runtime AI event logs\\*\\* \\(A\\.6\\.2\\.8\\)/` still holds and smoke's `compMd.includes(\"Required from the operator\")` still passes (it passes on the heading). Surface: runward/compliance/iso-42001-readiness.md, read by a human; the machine layer of the pack is the OSCAL component-definition built by renderOscal, untouched here.",
   "argument": "Measured on a fresh `runward init` mission (60-line draft) and on examples/request-triage (101-line draft), applying the mutant to dist/lib/compliance.js and re-running the real CLI: exactly one line changes — line 48 of the fresh draft goes empty. No claim is lost, because the sentence's claim is already made by the heading immediately above it, line 46, `## Required from the operator / organization (runward cannot produce this)`, which this mutant does not touch; the seven operator-required items rendered from `lens.operatorRequired` are unchanged, so test/unit/compliance.test.js's `/\\*\\*Runtime AI event logs\\*\\* \\(A\\.6\\.2\\.8\\)/` still holds and smoke's `compMd.includes(\"Required from the operator\")` still passes (it passes on the heading). Surface: runward/compliance/iso-42001-readiness.md, read by a human; the machine layer of the pack is the OSCAL component-definition built by renderOscal, untouched here.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"These sections are managerial, legal or organizational — no tool can assemble them from engineering artifacts:\"\u0001L.push(\"These sections are managerial, legal or organizational — no tool can assemble them from engineering artifacts:\");"
  },
  {
   "key": "212|12|212|14|StringLiteral|\"Stryker was here!\"",
   "line": 212,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "REFERENCE ARGUMENT for the twelve injection-position survivors in this function (the eleven `L.push(\"\")` twins and the `const L = []` seed). Measured on a fresh `runward init` mission and on examples/request-triage, mutant applied to dist/lib/compliance.js and the real `runward compliance iso-42001` re-run: exactly one line changes — the blank separator at line 49 of the 60-line fresh draft, between `These sections are managerial...:` and the operator-required list, now carries the literal. Every claim in the document is byte-identical: the ASI coverage cells, the `**N applied - N deviated - N n/a** across N accounted rule(s)` sentence, the conformance rows, the ADR rows, the governance-presence lines, the declared non-scope and the certification caveats. The mutant ADDS a line; it changes none. A line that asserts nothing about the mission changes no status, count, caveat or scope, which is the test this register applies. Surface: the human-read markdown draft — no test and no machine contract reads its blank lines, and renderOscal's component-definition is untouched. STRUCTURAL NOTE, made once for all twelve: they survive for one reason, that the module's only byte-for-byte fixture (test/unit/oscal-golden.test.js) renders eu-ai-act and pins the OSCAL JSON, so nothing pins a single byte of the ISO 42001 markdown. If this class is to be closed, the fix is one golden fixture for this draft, not twelve defect entries.",
   "argument": "REFERENCE ARGUMENT for the twelve injection-position survivors in this function (the eleven `L.push(\"\")` twins and the `const L = []` seed). Measured on a fresh `runward init` mission and on examples/request-triage, mutant applied to dist/lib/compliance.js and the real `runward compliance iso-42001` re-run: exactly one line changes — the blank separator at line 49 of the 60-line fresh draft, between `These sections are managerial...:` and the operator-required list, now carries the literal. Every claim in the document is byte-identical: the ASI coverage cells, the `**N applied - N deviated - N n/a** across N accounted rule(s)` sentence, the conformance rows, the ADR rows, the governance-presence lines, the declared non-scope and the certification caveats. The mutant ADDS a line; it changes none. A line that asserts nothing about the mission changes no status, count, caveat or scope, which is the test this register applies. Surface: the human-read markdown draft — no test and no machine contract reads its blank lines, and renderOscal's component-definition is untouched. STRUCTURAL NOTE, made once for all twelve: they survive for one reason, that the module's only byte-for-byte fixture (test/unit/oscal-golden.test.js) renders eu-ai-act and pins the OSCAL JSON, so nothing pins a single byte of the ISO 42001 markdown. If this class is to be closed, the fix is one golden fixture for this draft, not twelve defect entries.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#16"
  },
  {
   "key": "215|12|215|14|StringLiteral|\"Stryker was here!\"",
   "line": 215,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 57 of the fresh draft, the blank between the last operator-required item and the `_Regime mapping is dated engineering framing..._` disclaimer.",
   "argument": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 57 of the fresh draft, the blank between the last operator-required item and the `_Regime mapping is dated engineering framing..._` disclaimer.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#17"
  },
  {
   "key": "216|12|216|101|StringLiteral|``",
   "line": 216,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Measured: `runward compliance iso-42001` emits line 58 of the fresh draft as `_Regime mapping is dated engineering framing, not legal advice; ISO Annex A control counts/templates are behind the paywalled standard — confirm against the purchased text._`; with the mutant that line is empty and nothing else changes (both missions). RECIPE: any mission — `runward init && runward compliance iso-42001`, then `grep -c 'legal advice' runward/compliance/iso-42001-readiness.md` returns 1 before and 0 after. The document loses the ONLY sentence in which it says it is not legal advice (grep of the emitted draft: `legal advice`, `dated engineering framing`, `paywalled` and `purchased text` each occur on exactly one line, that one), and with it the ADR-0022 caveat that the lens is dated, versioned data rather than an authority. Nothing catches it: no assertion in test/unit/compliance.test.js reads `lens.disclaimerTail` for the ISO draft — it pins the lens STAMP (`Lens: ISO/IEC 42001 (mapping version 2023)`), which is line 8 — and none of the seven ISO assertions in test/smoke.js reads it (measured: applied, real CLI re-run, all pass). On an artefact handed to a regulated buyer a missing not-legal-advice disclaimer is a defect, not a wording preference.",
   "argument": "Measured: `runward compliance iso-42001` emits line 58 of the fresh draft as `_Regime mapping is dated engineering framing, not legal advice; ISO Annex A control counts/templates are behind the paywalled standard — confirm against the purchased text._`; with the mutant that line is empty and nothing else changes (both missions). RECIPE: any mission — `runward init && runward compliance iso-42001`, then `grep -c 'legal advice' runward/compliance/iso-42001-readiness.md` returns 1 before and 0 after. The document loses the ONLY sentence in which it says it is not legal advice (grep of the emitted draft: `legal advice`, `dated engineering framing`, `paywalled` and `purchased text` each occur on exactly one line, that one), and with it the ADR-0022 caveat that the lens is dated, versioned data rather than an authority. Nothing catches it: no assertion in test/unit/compliance.test.js reads `lens.disclaimerTail` for the ISO draft — it pins the lens STAMP (`Lens: ISO/IEC 42001 (mapping version 2023)`), which is line 8 — and none of the seven ISO assertions in test/smoke.js reads it (measured: applied, real CLI re-run, all pass). On an artefact handed to a regulated buyer a missing not-legal-advice disclaimer is a defect, not a wording preference.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001``\u0001`_Regime mapping is dated engineering framing, not legal advice; ${lens.disclaimerTail}_`\u0001L.push(`_Regime mapping is dated engineering framing, not legal advice; ${lens.disclaimerTail}_`);"
  },
  {
   "key": "217|12|217|14|StringLiteral|\"Stryker was here!\"",
   "line": 217,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 59 of the fresh draft, the blank after the disclaimer — the document's last line before its terminating newline.",
   "argument": "Same construction and same measurement as the occurrence-16 twin (the reference argument for this class): the mutant is applied to dist/lib/compliance.js and the real `runward compliance iso-42001` is re-run on a fresh `runward init` mission and on a copy of examples/request-triage. Exactly one line changes and it is a blank separator; no count, status, caveat or scope in the document changes, and every unit and smoke assertion on this draft still passes. Here the injection lands on line 59 of the fresh draft, the blank after the disclaimer — the document's last line before its terminating newline.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"Stryker was here!\"\u0001\"\"\u0001L.push(\"\");\u0001#18"
  },
  {
   "key": "218|19|218|23|StringLiteral|\"\"",
   "line": 218,
   "mutator": "StringLiteral",
   "filing": "hole",
   "evidence": "Measured on examples/request-triage through the real CLI: the emitted draft collapses from 101 lines to 2 — the whole document becomes a single line. Every boundary between one claim and the next disappears: `| ASI10 | Rogue Agents | ... |## 2. Control-implementation status (rule conformance)Feeds the Statement of Applicability's...` runs together, so a table cell, a section heading and a count sentence are no longer separable by a reader or by any line-oriented tool. RECIPE: any mission; `runward compliance iso-42001`; `wc -l runward/compliance/iso-42001-readiness.md` reports 1 instead of 101. Nothing catches it, and the reason belongs in the register: every assertion guarding this document — nine in test/unit/compliance.test.js, seven in test/smoke.js — is a substring or a regex containing no newline, so all sixteen still pass on a document that has ceased to be a document (measured: applied, real CLI re-run, no assertion breaks).",
   "argument": "Measured on examples/request-triage through the real CLI: the emitted draft collapses from 101 lines to 2 — the whole document becomes a single line. Every boundary between one claim and the next disappears: `| ASI10 | Rogue Agents | ... |## 2. Control-implementation status (rule conformance)Feeds the Statement of Applicability's...` runs together, so a table cell, a section heading and a count sentence are no longer separable by a reader or by any line-oriented tool. RECIPE: any mission; `runward compliance iso-42001`; `wc -l runward/compliance/iso-42001-readiness.md` reports 1 instead of 101. Nothing catches it, and the reason belongs in the register: every assertion guarding this document — nine in test/unit/compliance.test.js, seven in test/smoke.js — is a substring or a regex containing no newline, so all sixteen still pass on a document that has ceased to be a document (measured: applied, real CLI re-run, no assertion breaks).",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"\\n\"\u0001return L.join(\"\\n\") + \"\\n\";"
  },
  {
   "key": "218|27|218|31|StringLiteral|\"\"",
   "line": 218,
   "mutator": "StringLiteral",
   "filing": "display-only",
   "evidence": "Measured: the emitted file loses its final newline and nothing else — the fresh draft's last claim line is unchanged, and the file simply ends one byte earlier. No consumer branches on it: the draft is read by a human; the machine layer of the pack is the OSCAL component-definition produced by renderOscal (untouched); the repo's byte-for-byte guard, test/unit/oscal-golden.test.js, pins that JSON and not this markdown; and the document stays deterministic — two successive `runward compliance iso-42001` runs still produce identical bytes. Surface: the POSIX trailing newline of runward/compliance/iso-42001-readiness.md.",
   "argument": "Measured: the emitted file loses its final newline and nothing else — the fresh draft's last claim line is unchanged, and the file simply ends one byte earlier. No consumer branches on it: the draft is read by a human; the machine layer of the pack is the OSCAL component-definition produced by renderOscal (untouched); the repo's byte-for-byte guard, test/unit/oscal-golden.test.js, pins that JSON and not this markdown; and the document stays deterministic — two successive `runward compliance iso-42001` runs still produce identical bytes. Surface: the POSIX trailing newline of runward/compliance/iso-42001-readiness.md.",
   "stableKey": "compliance\u0001renderIso42001Readiness\u0001StringLiteral\u0001\"\"\u0001\"\\n\"\u0001return L.join(\"\\n\") + \"\\n\";\u0001#2"
  }
 ]
}
