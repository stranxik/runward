{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx --yes runward check --strict 2>&1 || true",
            "timeout": 120,
            "statusMessage": "runward gate (advisory) — hard gate is CI"
          }
        ]
      }
    ]
  }
}
