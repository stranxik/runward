{
  "version": 1,
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "bash": "npx --yes runward check --strict 2>&1 || true",
        "powershell": "npx --yes runward check --strict; exit 0",
        "timeoutSec": 120
      }
    ]
  }
}
